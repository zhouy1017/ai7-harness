# AI7 V2 UI/UX decision queue

Status: **61-question base, Issue #5 9-question feature delta, Issue #8 Batches 1–5 with all 18 decisions resolved, and response-presentation 7-question delta complete**

Question 16 originally expanded the interview to 59 questions and resolved the Default Execution Rule envelope. Question 35 expanded it to 60, and Question 54 exposed a new Detached Manuscript Reader authority branch, producing the current 61-question tree. Choices enter this file only when an answer exposes a material unresolved branch or a recorded decision needs a durable pointer.

Question 25/59 resolved Rewind as an append-only attempt branch from an eligible business milestone, with material change routed through Plan Revision and Redo. See [ADR 0002](./adr/0002-append-only-run-rewind.md).

Question 32/59 resolved committed Apply reversal as a newly prepared, approved, and receipted reverse Effect against current authority. See [ADR 0003](./adr/0003-reverse-committed-apply-with-a-new-effect.md).

Question 35/60 resolved evidence-assurance efficiency through policy-bounded Quick, default Standard, and Strict progressive levels. See [ADR 0004](./adr/0004-use-tiered-progressive-evidence-assurance.md).

Question 40/60 resolved the target-house Signoff mismatch by projecting the exact stated-next-use record as a user-facing labeled Milestone Version, with separate internal checkpoint/signoff records. See [ADR 0005](./adr/0005-project-signoff-as-a-user-facing-milestone-version.md).

Question 42/60 resolved the document-representation boundary: DOCX is the ordinary user's primary editable format, revision-pinned Markdown is the internal Agent Exchange Projection and explicit fallback export, and PDF is an optional fixed-layout export. Markdown never becomes manuscript authority and agent-authored changes remain Proposals. See [ADR 0006](./adr/0006-use-purpose-specific-document-representations.md).

Question 43/60 removed the proposed external-delivery branch from V1: the product stops at exact local export and its Effect Receipt, with no direct channel integration, handoff record, recipient tracking, or delivery-confirmation state. No ADR was required because a later release can add a separate external Effect flow without changing V1 local-export semantics.

Question 44/60 replaced the user-facing public-release candidate/permission workflow with the target-house `发稿版本` designation. One explicit exact-version interaction creates the familiar designation and a separate internal Public Release Permission; ordinary local export stays unaffected and no publication Effect is implied. See [ADR 0007](./adr/0007-use-publication-version-as-public-release-permission-projection.md).

Question 45/60 accepted an affected-Book Recovery Workspace that compares durable journal recovery, relevant checkpoint/milestone state, and verified Recovery Snapshots before restoring only as a new descendant. Multiple Books remain separate attention items and history is never silently overwritten. No ADR was created because the interaction directly projects the already accepted manuscript-history and recovery architecture.

Question 46/60 accepted a strictly bounded Editing Protection Mode for temporary Edit Journal/service durability failure: short process-local typing continuity, exact at-risk disclosure, blocked consequential navigation/mutation, automatic retry, and Protective Read-only State at the safety limit. Provider/network unavailability alone does not enter this mode. No ADR was required because the accepted recovery architecture and frozen persistence-failure semantics already require a bounded buffer or safe stop.

Question 47/60 accepted an explicit `授权并在联网后开始` path. It creates one exact authorized Run in Connectivity Wait State and starts after Reconnect Preflight only when every material plan/provider/source/outbound/Run Budget Ceiling/Credential Reference boundary remains unchanged and live readiness is valid; drafts never self-start and waiting remains cancellable. A connection/credential blocker under an unchanged boundary preserves the authorization, while material drift requires Plan Revision. See [ADR 0008](./adr/0008-authorize-exact-runs-for-deferred-connectivity-start.md).

Question 48/60 accepted one quiet, optional, once-only Contextual Feedback Prompt at the originating Proposal Decision, Review Decision, or clear Task Outcome. Existing business dispositions are already Quality Signals, so the prompt asks for contextual reasons rather than duplicate scoring; alternatives remain unselected, `AI7 的猜测` is explicit, dismissal infers nothing, and ordinary feedback stays out of Global Attention. No ADR was required because prompt placement and interruption level are reversible UI choices that directly project the accepted non-blocking Quality Signal boundary.

Question 49/60 accepted quiet Learning Material candidate identification and an explicit, unselected Book-first eligibility review only when a real editor decision is needed. Current Book reuse is recommended; named Series and House reuse require separate consequence-led choices, while exclusion and deferral remain first class. Inclusion grants only eligibility for later signals/candidates, never memory activation, Run source access, model training, factual status, or publication authority. See [ADR 0009](./adr/0009-use-explicit-book-first-learning-eligibility.md).

Question 50/60 accepted a searchable Book-grouped Learning Audit entry with an object-centered bidirectional influence chain and layered remediation preview. Future, running and immutable historical consequences remain distinct; re-inclusion appends history, and batch remediation is restricted to one explicit common scope/disposition. No ADR was required because this is a reversible information organization over the already accepted Learning Audit and remediation contract.

Question 51/60 accepted on-demand Model Service setup: first launch remains local-first, while the first model-dependent Task shows one contextual blocker and returns from role-first Settings to the exact preserved Task draft. No ADR was required because this directly projects the accepted local/provider independence and keeps onboarding timing reversible.

Question 52/60 accepted Settings-centered channel/data-location disclosure with exception-led notices and no arbitrary V1 data-root picker. No ADR was required because portable/installer locations and Protected Secret Store separation are fixed architecture; the decision only selects their reversible user presentation. A future custom/network root or cross-channel migration protocol would reopen the storage authority and require ADR consideration.

Question 53/60 accepted Windows-first Limited Shortcut Remapping for navigation/search/view commands plus IME-safe command suppression. The owner clarified that every action needs a discoverable pointer/keyboard entry, not permanent always-on display: secondary actions may use labeled disclosures or deeper menus, while currently actionable safety and named-authority bars remain direct. No ADR was required because this is reversible input/presentation configuration under fixed IME/accessibility guarantees.

Question 54/60 accepted separate standard/compact workbench density, independently adjustable manuscript reading typography, accessible resizable regions, importance-based view hiding/closing, and a required Detached Manuscript Reader. Density/layout preferences are reversible and need no ADR. The detached window creates a new hard-to-reverse read-only-versus-concurrent-edit synchronization choice, so the interview total increases from 60 to 61 and Question 55 addresses that boundary before any multi-window ADR is recorded.

Question 55/61 rejected both a read-only detached Reader and concurrent editable mirrors. The accepted Detached Manuscript Window receives the one active editable manuscript subpage through an IME- and durability-guarded Manuscript Surface Transfer; the workbench shows a noninteractive placeholder and the service enforces one Active Manuscript Surface Binding. This hard-to-reverse multi-Renderer lifecycle/input boundary is recorded in ADR 0010.

Question 56/61 accepted `跟随系统` as the default Application Theme Preference with manual `浅色`/`深色` overrides, coherent behavior across every AI7 window, semantic color roles and unconditional Windows high-contrast/forced-colors priority. No ADR was created because this is reversible presentation configuration and changes no product authority or document state.

> Platform-scope integration (2026-08-25): ADR 0028 now makes Windows and macOS one product. Earlier Windows-first answers remain Windows adapter decisions, not cross-platform defaults. The same objects, journeys, state meanings, authority and negative guarantees apply on macOS; menus/shortcuts, dialogs, notifications, accessibility behavior, paths, Protected Secret Store, distribution presentation and security prompts adapt natively. This integration creates no new non-E2E validation gate.

Question 57/61 accepted one cross-surface Semantic State Presentation Grammar while retaining every exact domain-state name and negative authority. Authoritative Completion Styling is reserved for a supporting AI7 record, classified outcome evidence or verified Effect Receipt. No ADR was created because this is an evolvable presentation system constrained by already-fixed domain semantics.

Question 58/61 accepted consequence-first, two-layer Chinese microcopy: the primary layer names object, exact state, consequence, retained-safe boundary and one safe next action; sanitized Provider/Harness/support detail expands secondarily. No ADR was created because copy rules are reversible presentation policy over fixed authority and privacy constraints.

Question 59/61 accepted the V1 retain/reshape/drop mapping and retention of all fourteen journey IDs. `migration-from-v1.md` and `journeys.md` now make the boundary explicit. No ADR was created because this is provenance/migration documentation over already accepted decisions, not a new hard-to-reverse product boundary. `J-01` successful-import evidence naming and `J-13` Series membership management remain the two seams for Question 60.

Question 60/61 closed both journey seams. `J-01` ends in a durable user-facing Manuscript Import Record only after exact import records/revision persist. `J-13` uses `书系 > 成员与共享范围`, a four-part Series Membership Impact Preview, exact add/remove commands and append-only change records without changing frozen Runs or granting implicit source/learning/provider/mutation authority. No ADR was created because these interactions project already-fixed import, Series and per-Run scope semantics and remain evolvable presentation/command organization.

Question 61/61 accepted documentation-only candidate completion. This queue is closed for the current interview. Future feature requirements reopen only the affected decision branch as an explicit delta; they do not authorize implementation or silently rewrite the completed history. No ADR was created because session/document lifecycle is reversible workflow metadata rather than product architecture.

## Issue #5 feature delta

Feature-delta Question 1/9 accepted Word-like Proposal review semantics without Word geometry: every independently reviewable change keeps an exact-range or exact-range-set anchor and persistent Manuscript-anchored Proposal Card identity; inline marks and compact margin anchors remain beside the Manuscript, only the active or nearby card expands, and large/structural work preserves the same identity through the existing virtualized navigator and Dedicated Work Workspace. No ADR was created because this selects a reversible presentation/scaling pattern over already accepted Proposal identity, long-manuscript and authority boundaries.

Feature-delta Question 2/9 accepted one Proposal Change Item per semantically independent editorial intent, with separate disposition even when adjacent items share a visual card group. Only a named Atomic Proposal Change Group may be indivisible, and only when partial acceptance would create an explicitly explained internal inconsistency; proximity, shared paragraph, Task, Run or model output is insufficient. This hard-to-reverse Proposal-decision granularity and its narrow exception are recorded in [ADR 0011](./adr/0011-use-proposal-change-items-and-explicit-atomic-groups.md).

Feature-delta Question 3/9 accepted a stable four-region Manuscript-anchored Proposal Card: `修改内容` for exact current-to-proposed content, `修改理由` for concise AI7 rationale, `依据与核查` for independently expandable source/evidence/verification records, and `你的处理` for disposition/edit controls plus the editor's optional reason. No ADR was created because the separation is a reversible information hierarchy over already distinct Proposal, evidence and decision records.

Feature-delta Question 4/9 accepted one `将以上工序保存为可复用工序` entry followed by a Reusable Procedure Classification Preview that produces exactly one Default Execution Rule, Task Skill Candidate, Workflow Profile Draft or Developer Capability Proposal. The common entry reduces terminology burden but creates no common persisted type; code-bearing needs may recommend Plugin evaluation only through the developer proposal path. No ADR was created because the underlying object and authority boundaries were already accepted and the unified entry/classification presentation is reversible.

Feature-delta Question 5/9 accepted capture from one completed Run or an explicit ordered set of completed user-visible editorial steps, with add/remove/reorder before save. The extraction preview retains reusable purpose, business structure, parameter slots and declared abstract requirements while excluding manuscript/Book/source instance content, credentials, concrete provider/model bindings, factual conclusions, decisions, approvals, receipts, hidden Harness activity and unsuccessful instance steps by default. Local provenance stays separate, and no ambient activity is captured silently. This hard-to-reverse data and authority boundary is recorded in [ADR 0012](./adr/0012-extract-reusable-structure-without-instance-authority.md).

Feature-delta Question 6/9 accepted two low-burden Task Skill save actions followed by separate authority: both create an immutable local-user candidate version; `保存并送交检查` enters AI7-owned admission and independent provider-free checking; and a successful version remains disabled until the editor reviews its Authority Ceiling and separately enables it. The authoring Run cannot validate, install, enable, approve, promote or activate its output, and repair creates a new immutable version. No ADR was created because this is the UI projection of the already accepted Task Skill admission and authority lifecycle rather than a new architecture decision.

Feature-delta Question 7/9 accepted one global Automation Center projection with typed sections, versions grouped under stable identities, exact per-version links to related work/deliveries, and manual version/entry deletion. New unpinned Task Skill use defaults to the newest enabled compatible version and freezes that exact version before authorization; existing Runs, exact rules and Workflow Instances never upgrade silently. Only a never-admitted wholly unreferenced candidate/draft may be deleted permanently; referenced or authority-bearing versions leave a non-executable Historical Version Stub and preserve all linked authoritative records, while active pins block removal. This convenience-versus-lineage decision is recorded in [ADR 0013](./adr/0013-use-latest-eligible-new-version-and-preserve-historical-pins.md).

Feature-delta Question 8/9 accepted instance-wide default discoverability for enabled local-user Task Skills through Automation Center, intent-based recommendation and manual selection. Recommendation Applicability may narrow where a skill is proactively suggested by Book, Series, Editorial Deliverable type or Workflow phase, but compatible manual selection remains possible with a mismatch explanation. Reuse carries only parameterized procedure structure, defaults every new Run Source Scope to the current Book and requires separate selection/authorization for Series, Cross-project or House Editorial Memory sources. No ADR was created because this is a reversible catalog/recommendation presentation over the already accepted separation among enablement, recommendation and per-Run source authority.

Feature-delta Question 9/9 accepted independent completion paths for the remaining classified results. Workflow Profile drafts publish as new immutable versions, and a separate action designates a version only for new Workflow Instances while existing-instance migration remains explicit. Default Execution Rule drafts require exact-envelope review before enablement and can react only to future user-submitted deterministic matches. Developer Capability Proposals are saved as non-executing developer handoffs and cannot generate, install or enable Plugins. All later edits create new versions. No ADR was created because these interactions project already accepted type, pinning, rule and developer-capability boundaries without changing their architectural ownership.

The feature-delta decision tree is closed. The two owner-requested features are fully mapped into requirements, information architecture, interaction behavior, visual direction, J-05/J-15, bilingual terms and ADRs 0011–0013 where the three-part threshold was met. This remains a candidate design package and grants no implementation authority.

## Issue #8 missing-design completion

The repository-only omission audit reopened and has now closed narrowly identified branches after the historical 61-question interview and Issue #5 feature delta. This explicit candidate delta does not rewrite the earlier answers, grant `main` acceptance, or authorize implementation. The completed dependency order lives in [`MISSING-DESIGN-DECISION-MAP.md`](./MISSING-DESIGN-DECISION-MAP.md).

### Batch 1/5 — Book creation and manuscript import targeting

Decision 1.1 accepted `Book = 图书` as the preferred Simplified Chinese domain/product label. `稿件` remains Manuscript, `书库` remains Book Library, and Book identity means the source/privacy/mutation authority rather than a filesystem project, workspace, physical-publication status, or Manuscript alias. Root Editorial context and the bilingual index now own this term; candidate labels use `图书` consistently. No ADR was created because this standardizes missing language without changing the already accepted Book authority boundary.

Decision 1.2 accepted explicit Manuscript Import Target selection. Global entry leaves `新建图书` and eligible exact existing Books unselected; a current Book may appear only as `建议目标`. Context, recency, filename, path, source location, or folder never chooses a target. No ADR was created because target-selection presentation is reversible while the underlying Book authority boundary is already fixed.

Decision 1.3 accepted a non-authoritative import-bound Book Creation Draft for the no-Book/new-Book manuscript-import path. When its required identity and preflight are valid, AI7 immediately guides the editor to Review Before Import and the final conversion action; it does not first commit an empty Book. The final action converts the draft into the authoritative Book while importing its first Manuscript in the same all-or-nothing business transaction. Cancellation or failure before commit leaves no authoritative Book shell. No ADR was created because this extends the already accepted atomic import rule rather than creating a new authority type. Batch 2 separately records the derived need for an explicit standalone Book-creation variant when the editor intentionally wants an empty Book.

Decision 1.4 accepted an editor-confirmed working Book title as the only required creation metadata, with optional internal reference and richer bibliographic metadata available later. AI7 may locally extract candidate data during preflight, but it displays and permits editing of Book Title Suggestions only after `新建图书` is selected. Non-empty DOCX title metadata supplies the primary editable suggestion, filename stem is its fallback, and bounded title-bearing early content may supply separately source-labeled alternatives. No content candidate silently displaces the primary/fallback rule; every candidate remains editable, and no model, Provider, network, silent first-heading adoption, or automatic suggestion creates Book metadata. No ADR was created because this is an evolvable local drafting and disclosure rule.

Decision 1.5 accepted one consequence-first Review Before Import for the new-Book path. It names the exact target/title, original-file identity/provenance, fidelity/degradation state, proposed primary Manuscript and initial branch/revision, exact Workflow Profile/Instance, effective Book Editorial Dimension Set, created records, and named non-effects. `新建图书并导入稿件`—or the exact degraded variant—then atomically creates the Book/stable identity, Book dimension set, Manuscript/branch/revision, pinned Workflow Instance, source/provenance/fidelity records, applicable Import Degradation Decision, and Manuscript Import Record. It creates no Series membership, learning eligibility, Provider permission/transmission, Publication Version, release, or delivery fact. No ADR was created because the exact transaction is the already accepted atomic import boundary made complete and visible.

At the Batch 1 checkpoint, only the new-Book dependency was closed; Batch 2 was then assigned eligible existing-Book relationships, duplicate identity/choices, reimport base/comparison, source-only treatment, and staged-import interruption/restart behavior without weakening the accepted Batch 1 boundary.

### Batch 2/5 — Existing-Book relationships, matching, reimport and interruption

Decision 2.1 accepted at most one primary Manuscript per Book. A deliberately empty Book may receive its first Manuscript or source-only material; a populated Book may reimport its sole primary Manuscript or accept source-only material; a distinct intended work requires another Book. Target and relationship remain separate unselected decisions, and source-only completion creates a Source Version plus Source Import Record with no Manuscript/Workflow mutation or `稿件已导入` wording. Standalone Book creation intentionally creates only Book identity and dimensions after its own review, while an import-bound draft cannot commit an empty shell. This cardinality and authority trade-off is recorded in root [ADR 0029](../adr/0029-keep-one-primary-manuscript-per-book.md).

Decision 2.2 accepted layered local exact matching without automatic deduplication. AI7 distinguishes exact immutable original-file identity associated with a prior Source Version, exact parsed content/structure under another name, same-name/different-content Filename Collision and fuzzy similarity; only the exact classes are Exact Import Matches, and common source-family/lineage membership is insufficient by itself. Matches expose exact Books, Source Versions and records plus unselected open/reimport/continue choices, but never select a target/relationship, infer lineage, share a Book-owned Source Version across Books, or expose the matching algorithm as an editorial choice. No ADR was created because this is reversible deterministic disclosure over fixed authority.

Decision 2.3 accepted three-way reimport only under verified Book-owned Source Version lineage and an explicitly labeled conservative two-way comparison when lineage is unconfirmed. Dirty journal state first receives its already-required safety checkpoint; exact unambiguous structure alone maps automatically. Changed content atomically produces a current-Book Source Version, descendant Manuscript Revision and Manuscript Reimport Record; a completed no-change comparison persists its Source Version/evidence and `未发现稿件变化` record without an empty revision. This provenance/history trade-off is recorded in root [ADR 0030](../adr/0030-compare-reimports-without-inventing-source-lineage.md).

Decision 2.4 accepted restart-safe persistence of one complete verified non-authoritative Staged Import Snapshot under the Agent Data Root. Restart offers unselected continuation/abandonment and revalidates source, target, relationship, lineage, fidelity and consequential review without auto-resume or commit. A complete snapshot may continue after original-file access loss; incomplete staging requires exact picker reselection, parse failure retains payload-free diagnostics, explicit cancellation deletes staged material, and atomic-boundary uncertainty fails closed as `导入提交结果待确认`. This continuity-versus-local-retention trade-off is recorded in root [ADR 0031](../adr/0031-persist-verified-import-staging-for-explicit-recovery.md).

Batch 2 closes the existing-Book import/interruption dependency. The estimate remains 18 decisions in five batches: 9 are resolved and 9 remain. Batch 3 covers authoritative journal-newer working-state pins, budget exhaustion, and restart behavior for interrupted active Runs; it does not reopen import staging or the one-primary-Manuscript rule.

### Batch 3/5 — Exact `Task Input / 任务输入`, budget boundary, and interrupted Run continuation

Decision 3.1 accepted materializing acknowledged journal-newer working text through a Manuscript Checkpoint with purpose `Task Input / 任务输入` before Plan Preview or any Run Authorization. Every attached prior-revision pin and pending manuscript target/range/source/evidence reference exact-resolves on the resulting revision into a new task-bound pin while the original pin and provenance remain immutable; changed or ambiguous references require reselection/removal and block planning. Once resolved, that revision supplies the exact Task Intent, range, source, evidence, Plan Envelope, and Run pins, and later edits never retarget them. This is a low-ceremony local prerequisite, not a new checkpoint type, Milestone Version, or Signoff. Checkpoint failure preserves the Task draft and journaled edits but blocks authorization. The hard-to-reverse revision-authority choice is recorded in root [ADR 0032](../adr/0032-materialize-task-input-before-exact-run-pinning.md).

Decision 3.2 accepted an exact Run Budget Ceiling state whose default is `unset`, displayed as `未设置任务预算上限`. The default means AI7 imposes no product-side per-Run monetary/usage stop; it never means free or unlimited Provider service, and provider account quotas/spending controls remain the default spend boundary. Editors may opt into an explicit ceiling. Reaching one ends the current Run with a terminal partial Task Outcome, preserves actual work/evidence/usage/Effects/receipts, and requires Plan Revision plus a newly authorized linked Redo Run to change the ceiling. A Provider Account Limit is a separate remediable service blocker whose unchanged Run still requires explicit Resume after remediation. This boundary is recorded in root [ADR 0033](../adr/0033-default-run-budget-ceiling-to-unset.md).

Decision 3.3 accepted explicit Resume after ordinary interruption. Safe restart/service reconciliation settles the existing authorized Run as `任务已中断 · 可续行` without provider dispatch. `续行` triggers lightweight revalidation and, when unchanged, opens a new Harness Execution Span in the same Run; material drift routes to Plan Revision/Redo and ambiguous Effects remain `结果待确认`. The separately authorized `授权并在联网后开始` path remains the narrow automatic-dispatch exception after unchanged Reconnect Preflight. This restart authority trade-off is recorded in root [ADR 0034](../adr/0034-require-explicit-resume-after-interruption.md).

Batch 3 closes the authoritative working-state and Run-lifecycle dependency. The estimate remains 18 decisions in five batches: 12 are resolved and 6 remain. Batch 4 covers the remaining Source Version acquisition paths, Series Knowledge authoring/promotion, and persistent Series Retrieval Exclusions without reopening Batch 3's exact pins, budget state, or continuation rules.

### Batch 4/5 — Source Version acquisition, Series Knowledge, and retrieval exclusions

Decision 4.1 accepted three explicit durable Source Version acquisition paths: a supported local file, exact editor-pasted/entered material, and a retention-permitted fully retrieved external research snapshot. File-specific `作为来源材料导入` and pasted/entered-and-research `保存为来源材料` start with an unselected exact existing Book or explicit source-bound Book Creation Draft and a consequence-first review of retained content and provenance. Existing-Book acquisition creates a target-owned Source Version and Source Acquisition Record; a file keeps Source Import Record as its specialization. If no Book exists, the draft plus Review Before Book Creation may atomically create a zero-Manuscript Book and its first Source Version. Search snippets, incomplete/failed retrievals, model answers, attachments and mere Task use do not auto-retain a Source Version, although separate Task/evidence records may persist. Same-Book exact identity reuse is explicit; cross-Book acquisition creates new target-owned authority. Initial research retrieval is not canonical Exact Fetch. This retention/authority boundary is recorded in root [ADR 0035](../adr/0035-require-explicit-book-targeted-source-acquisition.md).

Decision 4.2 accepted two Series Knowledge Candidate entry paths: exact editor authorship and exact provenance from a member-Book Manuscript Revision, Source Version or reviewed evidence. Every candidate remains non-authoritative until `书系知识纳入审阅` presents the exact Series, proposed new or existing Series Knowledge Item, content, provenance, conflicts, current revision and future reuse scope. A conflict must be edited and re-reviewed, explicitly preserved, or cancelled; preservation records rather than resolves it. Explicit `纳入书系知识` creates the item with its first immutable revision or appends one immutable Series Knowledge Revision and a Series Knowledge Promotion Decision while the original Book/source remains source of record. Membership, accepted Proposal, Milestone Version, Learning Eligibility and model output never auto-promote. Promotion does not itself create Run Source Scope, authorize or perform retrieval, permit Provider transmission, decide factual or learning status, mutate a manuscript, or authorize publication; the revision becomes eligible only for later exact Series-scoped selection and pinning. This governed-sharing boundary is recorded in root [ADR 0036](../adr/0036-promote-series-knowledge-through-explicit-review.md).

Decision 4.3 accepted append-only Series Retrieval Exclusion revisions for an exact member Book, Source Version, Series Knowledge Item or stable knowledge class. An item target covers its current and future revisions; a class target also covers later matching items. The impact preview separates future reads, queued/authorized/active Runs, completed history and unaffected authority. Once committed, the exclusion immediately blocks every later affected Series read; an affected Run stops for Plan Revision plus renewed Run Authorization or cancellation, and same-binding Resume, Retry, fallback and substitution are unavailable. Original Plan/authorization/binding, already-fetched evidence, Session/history and completed results remain immutable; already-sent Provider data cannot be recalled, while completed affected results gain a historical marker. Superseding/ending does not restore authority or auto-resume. The exclusion is Series-path-specific and does not decide Learning Eligibility or separately authorized Cross-project access. This current-restriction/history trade-off is recorded in root [ADR 0037](../adr/0037-enforce-versioned-series-retrieval-exclusions-immediately.md).

At the Batch 4 checkpoint, 15 of 18 decisions were resolved and Batch 5 remained the final frontier. The following section records its completed Delivery Package, native export-collision and maintenance decisions without reopening the owner-settled local-export/manual exact `发稿版本` boundary.

### Batch 5/5 — Delivery Package, native local-export conflict, and maintenance

Decision 5.1 accepted one versioned destination- and format-independent Delivery Package content manifest bound unconditionally to one exact Editorial Deliverable Revision, optionally identified by an exact Milestone Version, plus its stated purpose, included Editorial Artifacts, applicable Gate/Signoff references, exclusions and limitations. Formats, filenames, paths, fidelity disposition, Effect Approval and per-file receipts belong to separate Local Export Preparations. Repeated export or format/path change never mutates the package. See root [ADR 0038](../adr/0038-separate-delivery-package-identity-from-local-export.md).

Decision 5.2 accepted the current platform's native save/copy conflict workflow for existing local targets. Native alternative-name/path, cancellation, and replace/overwrite equivalents normalize into exact AI7 consequences without a second AI7 modal. Create/replace resolves the final target before exact Effect Approval; cancellation records no attempted Effect; drift reopens native handling; ambiguous outcome reconciles before retry; apply-to-all covers only enumerated collisions. See root [ADR 0039](../adr/0039-delegate-local-export-collisions-to-native-os-workflows.md).

Decision 5.3 accepted stable Maintenance Cases with immutable revisions for Correction, Errata, Supersession, Withdrawal, Reissue and Archive bound to exact Publication Version/Deliverable revision. Correction uses Proposal/Apply and a new revision, Errata stays an Editorial Artifact, Supersession/Reissue needs a separately designated newer Publication Version, and Withdrawal/Archive remains internal AI7 status without external recall/takedown proof. Prior versions, permissions, packages, exports and receipts remain immutable. See root [ADR 0040](../adr/0040-preserve-post-designation-maintenance-as-versioned-cases.md).

Batch 5 closes the 18-decision five-batch omission audit. V1 remains local-export-only with independent manual exact `发稿版本`; no accepted answer adds external sending, publisher/platform integration, delivery proof, recall, takedown, or automatic publication.

## Response-presentation feature delta

Response Question 1/7 defined `QA` as user-facing Interactive Editorial Dialogue: contextual question answering bound to one exact Book and active manuscript, deliverable, source, or other work object. It is not software QA, a generic chat root, or a Harness Session transcript.

Response Question 2/7 assigned every task type one explicit Response Presentation Mode with exactly `Waiting Only` and `Interactive Stream`. Waiting Only is the default. Writing, rewriting, factual verification, research, Proposal generation, automation, export, and all ordinary/background Runs retain it; only Interactive Editorial Dialogue is allowlisted for Interactive Stream. Users and runtime heuristics do not reclassify individual Runs.

Response Question 3/7 resolved the request for visible chain-of-thought-like progress through `Live Reasoning Summary`: a transient user-facing account of approach, checks, evidence comparison, and uncertainty that never claims raw chain of thought, system prompts, Provider reasoning tokens, or tool parameters and automatically hides before formal answer text begins.

Response Question 4/7 defined `Interactive Answer Stream`: prose arrives as complete semantic fragments, lists/tables as complete items/rows, and citations only after exact source binding. Proposal content, factual conclusions, structured authoritative records, and executable actions wait for complete valid objects; unfinished answers remain explicitly labeled.

Response Question 5/7 made streaming foreground-only. Leaving the dialogue hides summary and answer content behind `等待回答`, produces no fragment notification or focus theft, and changes no Provider execution, pause, cancellation, or priority. Returning restores complete received fragments and resumes live presentation or shows the settled answer.

Response Question 6/7 defined stop/interruption behavior. AI7 preserves only complete semantic fragments as an Incomplete Dialogue Answer, discards malformed tails, hides the reasoning summary, and provides explicit `继续回答`/`重新回答` new attempts. It never silently retries, continues, changes Provider, or falls back, and incomplete content gains no final, factual, Proposal, learning, or executable authority.

Response Question 7/7 placed completed and incomplete answers in recoverable Dialogue Answer History under the exact Book/work-object/Task context across navigation and restart. It is a non-authoritative join through exact Execution Bindings/Spans to messages and attempt history owned only by the Harness Session Ledger, not a third ledger or Task Ledger transcript copy. Live Reasoning Summary is excluded from visible history. Later dialogue turns may use prior answers without widening source or Provider-egress scope, and an explicit named action is required to create a governed Proposal, Task Intent Draft, or other object from selected answer content; the draft does not itself create the `Task Input / 任务输入` Manuscript Checkpoint purpose.

The response-presentation decision tree is closed. The hard-to-reverse default/exception, foreground/background, persistence, attempt, and authority tradeoff is recorded in [ADR 0014](./adr/0014-wait-by-default-and-stream-only-interactive-dialogue.md). The delta is mapped into D-084, requirements, information architecture, interaction behavior, visual direction, migration semantics, J-16, bilingual terms, and this queue. It remains candidate design and grants no implementation authority.
