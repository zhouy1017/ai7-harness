# Provider-free source-checkout build and launch

This is the operational routing document for the provider-free J-01 local DOCX import workspace, complete J-02 long-manuscript module, cohesive J-08 interrupted-manuscript recovery module, J-12 Book/Revision, Product Data Location, and Model Service credential slice, J-15 fixed native carrier and authority-sidecar lifecycle, J-03 provider-denied Task authorization record, and J-04 covered baseline manuscript analysis over the local deterministic model route. The governing contract remains [`docs/agents/source-checkout-buildability.md`](../agents/source-checkout-buildability.md); this file records their concrete implementation.

## Declared host matrix

| Host identity | Architecture | Exact toolchain | Host-native prerequisite and detection |
| --- | --- | --- | --- |
| Windows 11 24H2+, NT build 26100+ | x64 | Node 24.18.1; pnpm 11.24.0; Electron 43.4.1; TypeScript 6.0.3 | `%SystemRoot%\\System32\\tar.exe`; `doctor` requires that exact canonical path |
| macOS 15+, Darwin 24+ | arm64 | Node 24.18.1; pnpm 11.24.0; Electron 43.4.1; TypeScript 6.0.3 | `/usr/bin/ditto`; `doctor` requires that exact canonical path |
| GitHub Actions `windows-2025` | x64 | same pins | truthfully labelled Windows Server 2025 CI exception, enabled only by the workflow marker |

The archive adapters are OS tools only. Their bytes are neither copied into `dist/` nor loaded by AI7. Windows extraction uses fixed `tar.exe -xf <verified-zip> -C <canonical-staging-root>` arguments; macOS uses fixed `ditto -x -k <verified-zip> <canonical-staging-root>` arguments. No shell, PATH search, variable flags, or arbitrary archive is involved.

## Exact command surface

```text
pnpm run doctor
pnpm run bootstrap
pnpm run check
pnpm test
pnpm run test:service
pnpm run build
pnpm run start-built -- --data-root <absolute-path>
pnpm run start-built -- --data-root <absolute-path> --trusted-operational-scope developer-live [--run-budget-ceiling <n>] [--provider-cache-root <absolute-path>]
node tools/enroll-dev-credential.mjs --slot opencode-go --from-file <absolute-path>
node tools/enroll-dev-credential.mjs --slot opencode-go --check
pnpm run e2e -- --journey J-01
pnpm run e2e -- --journey J-02
pnpm run e2e -- --journey J-08
pnpm run e2e -- --journey J-12
pnpm run e2e -- --journey J-15
pnpm run e2e -- --journey J-03
pnpm run e2e -- --journey J-04
pnpm run e2e -- --journey J-05
pnpm run e2e -- --journey J-06
pnpm run e2e -- --journey J-07
pnpm run e2e -- --journey J-09
pnpm run e2e:all
pnpm --silent run e2e:diagnose -- --journey <J-01|J-02|J-08|J-12|J-15|J-03|J-04|J-05|J-06|J-07|J-09>
pnpm run e2e:debug -- --journey <J-01|J-02|J-08|J-12|J-15|J-03|J-04|J-05|J-06|J-07|J-09>
pnpm run e2e:repeat -- --journey <J-01|J-02|J-08|J-12|J-15|J-03|J-04|J-05|J-06|J-07|J-09> --times <n>
```

`check` runs the exact TypeScript check that `build` performs, with full diagnostics for the developer host; `test` runs the vitest unit suites under `tests/unit`; `test:service` runs the vitest service-integration suites under `tests/service`, which open the real service stores on a disposable Agent Data Root outside this checkout and need no Electron, no Provider, and no network; its baseline-analysis suite drives the real single-slot scheduler, harness, egress gate, and `ai7-local-deterministic` adapter over the hand-written synthetic fixtures under `tests/fixtures/model/`. All three are Local Verification Ladder layers under ADR 0062: developer-run surfaces that never become hosted gates and never appear in the workflow.

`e2e:debug` runs one admitted Journey with the controller-only `AI7_E2E_LOCAL_DEBUG` switch: the runner's stdout/stderr, the product process output that Playwright logs for the launched browser (`playwright-browser.log`), the complete failure error with its `cause` chain and stack, and a screenshot of every open renderer page taken before the runner's cleanup are written only under ignored `test-results/e2e/<journey>/<timestamp>/`. `e2e:repeat` runs the same Journey up to `n` times through that mode, stops at the first failure, and deletes the artifacts of passing runs. Both refuse to run under `CI`, `GITHUB_ACTIONS`, or the Windows CI marker, never forward the switch to the product process, and leave `e2e`, `e2e:all`, and `e2e:diagnose` output byte-identical (ADR 0062).

The pnpm SRI-bearing `packageManager` declaration is the pnpm selection authority. `doctor` reports the OS/CPU, exact runtime pins, canonical native adapter, official npm registry, host Node distribution identity/URL/SHA-256, lock/config hashes, and host Electron artifact identity/URL/SHA-256. The Node runtime executing the command must itself be exactly 24.18.1; CI selects it with the full-SHA-pinned `actions/setup-node` action and the repository records the official distribution digest.

`bootstrap` performs the host check before changing generated state. It then proves generated dependency, store, cache, and runtime roots are exact canonical children of this checkout; rejects redirected junctions or symlinks; removes only the exact checkout-local `node_modules`; and runs a frozen pnpm install. After that install it verifies the installed closure before acquiring the Electron runtime: every package directory the virtual store resolved for this host must be non-empty and no `node_modules` symlink may dangle. An incomplete closure fails the command with one payload-safe marker that names only the incomplete and inspected counts, never a package name or path; the recovery is to remove `node_modules` and re-run `bootstrap`. A complete install produces the same output as before. The child has a closed PATH and fresh task-local pnpm/npm user, global, structured, and auth config files. The official unauthenticated npm registry is fixed. Recognized identity/path/output and preload overrides fail in `doctor`; Node's environment-proxy, extra-CA, disabled-TLS-verification, and SSL certificate selectors also fail before parent-process Electron acquisition. Other ambient proxy variables are not enabled for Node fetch and are not forwarded to acquisition children.

The hosted runner explicitly supplies `NPM_CONFIG_PREFIX` as an empty string for every exact root command, neutralizing the runner image's otherwise ambient npm prefix before pnpm starts. `doctor` treats only that exact empty value as non-influential and rejects every nonempty value; bootstrap children never inherit the selector.

The Electron runtime is not an npm dependency. Bootstrap downloads or reuses only the host artifact declared in [`config/dependency-artifacts.json`](../../config/dependency-artifacts.json), verifies its SHA-256, extracts to a new checkout-local staging directory with the fixed OS adapter, and proves Electron 43.4.1, embedded Node 24.18.1, ABI 148, `node:sqlite`, FTS5, and the archive-root `LICENSE` and `LICENSES.chromium.html` carriers. Only then does it rename the staged carrier into the generated `.runtime/` owner. A prior runtime never satisfies the command without fresh verified extraction.

`build` enters through a fixed dynamic-import controller, so implementation parse, resolution, static-import, and execution failures stay behind the same closed output boundary. The implementation rejects ambient acquisition selectors before dynamically importing esbuild, type-checks the one root package, validates any existing `dist/` is the exact canonical generated target, replaces that output, and writes the production-shaped carrier without source maps. A TypeScript failure emits only bounded root-relative file/line/column/error-code tuples, a bounded config/error-code tuple, a fixed omitted-count tuple, or a fixed TypeScript unclassified marker; every other build exception emits only `BUILD/unclassified`. Compiler prose, exception messages, source excerpts, absolute paths, and payload-shaped text are discarded. No signing, installer, updater, provider, or release mechanics are involved.

`start-built` accepts the data-root option and the trusted launch form after at most one pnpm literal separator.

The trusted launch form (ADR 0065, ADR 0067) is argv and nothing else. `--trusted-operational-scope` selects `development-ci` (the default, and what every Journey and every CI run uses) or `developer-live`; `--run-budget-ceiling <n>` and `--provider-cache-root <absolute-path>` are accepted only beside `developer-live`. The launcher, Electron main, and the service each parse the same three arguments with the same parser, so an unknown scope, a malformed ceiling, or a relative cache root fails closed wherever it is first seen. No environment variable and no product setting selects a scope, `fixture-recording` and `ordinary-production` are not selectable from the source checkout, and the product-data `runtimeForm` stays `source-checkout`. A `developer-live` launch is human-attended by construction: it refuses to combine with a Journey environment or any Journey control.

Under `developer-live` the built product binds the immutable Provider Processing v5 selected by active-policy-set v5: route `opencode-go`, model `deepseek-v4-flash`, one technical Session per Analysis Unit, the three declared suboperations (cross-unit reduction, assurance sampling and the Run Report reflection), and a required non-`unset` Run Budget Ceiling whose default is 30,000 tokens per frozen Coverage Manifest unit, resolved once the manifest freezes and before the plan is, with the explicit `--run-budget-ceiling` overriding it; the ceiling is evaluated before every dispatch. Only ADR 0043-admitted Public SampleBooks are transmittable — in this slice, exact `sample1`. The service captures the native `fetch` before installing network denial and arms a single-host allowance for exactly the policy's endpoint host and port; every other host, port, and primitive stays denied, and the global `fetch` is denied even for that host. The Provider Result Cache and the Provider Test Ledger live under `--provider-cache-root`, which defaults to the checkout's sibling `../ai7-harness-provider-cache/` and must lie outside every checkout: an identical request replays without transmitting, and a repeated test item id is refused, so the same live test runs at most once. Nothing in the cache or the ledger is logged, uploaded, or committed.

`tools/enroll-dev-credential.mjs` is the only reader of the Owner's development credential file. `--from-file <absolute-path>` writes the value into the OS Protected Secret Store under the fixed `opencode-go` Credential Reference and prints nothing on either stream; a failure is an exit code, because any diagnostic would sit one process away from the value. `--check` prints exactly `present` or `absent`. It refuses under CI, under a Journey launch, and for any other slot. Never read, print, copy, or commit the credential file itself. Before Electron exists, it loads the built shared data-root owner, creates and canonicalizes the external root plus its exact `shell` child, and places `--user-data-dir=<canonical-shell>` before the built main entry. Main independently requires Chromium's early switch and Electron's `userData` path to resolve to that same canonical directory before setting the path or opening authority. The launcher binds the main process to its exact parent PID and remains attached. The fixed payload-free `AI7_READY` line is forwarded only after canonical Agent Data Root setup, the single-instance lock, SQLite authority, awaited dormant six-service Cordis composition, renderer load, and first paint. Pre-readiness failure tears down owned processes and exits nonzero. Parent loss terminates main and service leases. A clean existing data root is preserved; pre-existing linked or redirected shell/store/object paths fail closed.

Database open preserves the canonical v1-through-v14 migration history and admits only exact known predecessor or final v15 structure. V11 adds one strict `model_service_connections` projection to the existing SQLite authority. Migration and validation share its one canonical `CREATE TABLE` definition; exact-schema validation enforces the SQL, `STRICT` form, columns, keys, checks, unique constraint, and exact object set before the fixed row-binding truth check. It can hold only the fixed Main Editorial connection identity, role, DeepSeek provider/model IDs, adapter/configuration revisions, empty approved fallback chain, credential slot, editor-chosen connection name, stable opaque Credential Reference, sanitized operation state, and timestamps. It has no secret, endpoint, prompt, payload, Provider request, session, Run, export, Effect, artifact, or authority field. After exact v11 validation, v12 adds exactly two strict native-artifact relations: one immutable installation relation for the fixed declarative carrier and one exact-Book enablement relation. V13 adds two strict append-only sidecar relations owned by that same store: the fixed `ai7.editorial-workspace-profile.authority` Revision 1 and Revision 2 definitions, plus exact-Book revision pins. Every writable SQLite connection enables and verifies recursive triggers, so conflict replacement cannot bypass the sidecar update/delete guards. Migration validates the native carrier first, materializes both definitions for an installed carrier, and pins every existing enabled Book to Revision 1 without changing its native enablement time. A fresh enablement pins only Revision 2; an explicit later review of a migrated Book appends Revision 2 while retaining Revision 1. Pin times must be canonical instants, while causal order comes from the fixed revision append path rather than assuming a monotonic host clock. Absence of native enablement and pins is durable disabled. V14 adds ten strict immutable relations for the one provider-denied Task authorization record: Task Intent, Task Input checkpoint, Manuscript and Artifact pins, Run Source Scope, Provider Resolution and Execution plans, Plan Envelope, Run Authorization, and a terminal non-dispatched Run record. Canonical JSON, SHA-256 links, exact sample1 lineage, Revision 2 carrier pin, source evidence versus readable scope, fixed missing-credential DeepSeek binding, and Provider Processing v1 denial are revalidated at startup. The shared bounded checkpoint owner serializes Task Input and reimport work per branch; a clean journal reuses its current exact Revision instead of creating an empty one. V15 adds thirteen strict append-only relations for the one covered baseline manuscript analysis Task kind, each guarded by update/delete triggers: Task Intent, Task Input checkpoint, the frozen plan records (Coverage Manifest, Manuscript and Artifact pins, Run Source Scope, Provider Resolution and Execution plans, Plan Envelope), Run Authorization, Run record with append-only state transitions, the single execution attempt, its Execution Binding and Harness Execution Spans, and the immutable Result Set with its revisions and per-unit results. A v14 store migrates forward without rewriting any J-03 row, and the J-03 tables keep their exact v14 definitions. All earlier Book/import/editor/recovery migrations and semantic validation remain in force; unknown versions, mutated definitions, illegal pin histories, conflict replacement, and direct update/delete attempts fail closed.

The existing recovery and editor authority remains unchanged. Checkpoint reconstruction and restore are paged or set-based inside SQLite; snapshot restore streams verified bounded batches into the hidden stage and only the final revalidating transaction can expose the descendant. Snapshot bytes use one versioned digest-bound JSONL area beneath the existing external Agent Data Root; bounded line decoding, hidden partial formation, file sync, separate byte/manifest hashing, independent stream verification, no-clobber promotion, SQLite visibility last, and per-entry recognized orphan cleanup fail closed. Applicable snapshots are verified newest-first in bounded order so a damaged newer object cannot hide an older verified one. DOCX parsing writes at most 256 buffered rows per synchronous short transaction to an inert same-database ingest relation, and a separate short authority transaction either promotes the complete validated snapshot or exposes none of it. Startup removes inert ingest rows before import recovery; neither relation nor the recovery-object area is a second store, recovery authority, or commit owner.

`e2e` accepts only `J-01`, `J-02`, `J-08`, `J-12`, `J-15`, `J-03`, `J-04`, `J-05`, `J-06`, `J-07`, or `J-09`. Each runner reports only its current controller-owned closed stage on failure. `e2e:gate` accepts no arguments and invokes the ADR 0075 pull-request subset in J-08 → J-12 → J-15 → J-03 → J-04 order under its own `GATE_COMPLETION/` markers, reproducing the hosted pull-request occurrence; it is never Local completion. `e2e:all` accepts no arguments and invokes those same runners in fresh controller processes in J-01 → J-02 → J-08 → J-12 → J-15 → J-03 → J-04 → J-05 → J-06 → J-07 → J-09 order, captures and discards bounded child streams, stops on the first failure, and emits only its own fixed markers — on a failure, a second marker names the admitted location and error class the controller classified. When the failed Journey printed a readiness trace (Issue #518; J-01 does), a third, `LOCAL_COMPLETION/<journey>/readiness/<fields>`, carries it once the controller has validated its content-free shape. For the launch in flight, the trace gives when it launched, the last startup step main reached (main names each step on stderr as `AI7_STARTUP/<step>` under `AI7_E2E_JOURNEY`; stdout stays the readiness handshake), `AI7_READY`, a startup failure and the exit, in milliseconds since the launch. J-01 reads them from Playwright's `browser` debug log, the only place Playwright reports a launch's lines: before Playwright loads, it points that log at a file in its disposable run root, with each line's time. Every other line the product printed is only counted. `e2e:gate`, `e2e:debug` and `e2e:repeat` relay the same trace under their own prefixes. Only a pass from every admitted Journey produces `LOCAL_COMPLETION/all/pass`. An operator signal uses the controller channel to request bounded cleanup of the active Journey's browser, loopback, and exact disposable runtime root before emitting `LOCAL_COMPLETION/<journey>/interrupted` and exiting nonzero. Each runner also handles a direct foreground-process-group SIGINT/SIGTERM and treats controller-channel loss as parent-lease termination, prevents later resource acquisition, interrupts active browser ownership, then unwinds through its single `finally` cleanup owner; a bounded runner-side hard stop remains only as final lost-channel containment. A child signal without a recorded controller interruption remains product/framework `fail`, not `interrupted`. `e2e:diagnose` accepts exactly one admitted Journey, invokes the ordinary runner with no product-facing diagnostic argument or environment variable, captures bounded child streams, discards arbitrary output, and emits only a controller-owned Journey/stage/fixed-error-class tuple marked `not-completion`. The supported `--silent` pnpm invocation prevents invalid command arguments from being reflected by the package manager. Neither command changes scenario admission, subject topology, fixtures, egress, runtime roots, or cleanup.

J-01 preflights exact `SampleBooks/sample1.docx` as a regular non-symlink file of 29,550 bytes with SHA-256 `b8a3dbde0aa8a1ec7265f9ae3fe47877759e7947c5ab69682cd0a8f424a8d483`. J-02 streams a deterministic exact 10,000,000-character/50,000-block clean Chinese DOCX into the disposable external run root and removes it at cleanup. J-08 generates only small clearly public synthetic DOCX inputs under that same disposable boundary and removes them with every recovery object and SQLite fact at cleanup. J-12 additionally generates two runtime-only synthetic credential values, never outputs them, exercises the real current-host OS Protected Secret Store, verifies that lifecycle only through nonsecret ready/missing product projections and one stable opaque reference across first save, restart, replacement, removal, and final restart, and scans product-data bytes for raw UTF-8/UTF-16 secrets plus raw and common text encodings of their SHA-256 digests. The setup Node process never reads a raw OS-store value. J-15 creates only two empty Books. Through product-visible behavior it proves fresh direct Revision 2 pinning, then closes every product process before its controller writes an exact disposable schema-v12 predecessor with a canonical future-skewed enablement time. Production startup performs the v12→v13 migration, exposes the migrated Book at Revision 1 with Revision 2 offered, appends Revision 2 only after the existing exact-Book action without assuming the wall clock advanced, retains both revision-ordered pins across restart, and leaves the second Book disabled and unpinned. The predecessor write is scenario setup only: the controller does not query database contents or use them as a result oracle. J-15 uses no DOCX, credential, Provider, screenshot, trace, video, or retained evidence. J-03 imports exact sample1 through the real UI, installs/enables Revision 2, creates and removes one runtime-only synthetic credential through the supported product setup path, performs acknowledged edits, prepares and records one immutable provider-denied authorization, restarts, and verifies that later edits did not retarget or dispatch it. It proves applicable J-14 IME/focus/reflow/forced-colors behavior, zero loopback/provider/session/scheduler activity, post-close secret-byte absence, and complete disposable cleanup without using the database as a scenario oracle. Signal interruption only attempts confirmed closure of the current or acquiring browser. The Journey body's `finally` is the sole idempotent owner of full credential, metadata, direct OS-store, loopback, and root cleanup; the controller waits for any earlier interrupt closure, while a synchronous final-cleanup flag makes a later interrupt return without competing for those resources. Product cleanup treats durable `missing` for the same valid reference as already successful; otherwise its failure-only direct fallback runs the exact pinned Electron executable in Node mode, accepts only that opaque reference over stdin, emits no stdout or stderr, and treats deletion as successful only when the native adapter confirms it. If product cleanup and relaunch both fail before the reference reaches the controller, full cleanup must first confirm that the acquired browser closed and only then may read the exact nonsecret `model_service_connections` row from the synthetic Agent Data Root to recover that reference. A rejected close preserves browser ownership and the disposable root, rejects full cleanup, and prevents SQLite recovery, direct OS-store deletion, loopback cleanup, and root deletion from racing a potentially live product. Zero rows or no database precedes any possible native write; an ambiguous structure fails closed. J-04 imports exact sample1 through the real UI, installs/enables Revision 2, and creates and removes one runtime-only synthetic credential through the same setup path with J-03's credential-cleanup ownership, then starts the baseline manuscript analysis on the clean imported Revision: it observes the derived Coverage Manifest and its unit accounting, authorizes and dispatches the Run through the real single-slot scheduler, composed harness, egress gate, and `ai7-local-deterministic` adapter bound by the launch control J-04 shares only with J-09 to the hand-written synthetic fixture `sample1-baseline-one-unit-failure`, waits for the immutable Result Set Revision, verifies the four independently labeled axes, the one adapter-failure gap, the unresolved cross-unit conflicts, `回到稿件范围` navigation into the editor, restart persistence, and `stale` freshness after one acknowledged edit while every other axis and the revision identity stay unchanged, and proves zero loopback requests and no execution-facing renderer API; from Issue #48 it additionally relaunches the product bound to the `sample1-baseline-transient-retry` fixture to prove one in-envelope `safe-retry` Plan Adaptation and one Plan Revision on a re-prepared `重新分析所选范围` Task whose stale version is refused with `plan-revision-required` until `重新确认计划` yields version 2. Each admitted runner installs the built Node egress guard before third-party controller imports, preflights its disposable Agent Data Root and shell with the same built owner, and launches the same exact Electron carrier directly with that shell selected before the main entry. Playwright control travels only over inherited Chromium `--remote-debugging-pipe` file descriptors: no debug port, inspector, WebSocket endpoint, TCP listener, or dev server exists.

The main-owned picker hook supplies the selected result but cannot expose a path to the renderer or skip local streaming parse, server-derived review, atomic import, SQLite paging/indexing, bounded 32-block projections, or durable journal flow. J-01 retains identity-bearing restart, review invalidation/rebinding, source-path loss and reselection, prepared-attempt reconciliation, completion paint/acknowledgement, uncertain-outcome containment, and durable abandonment-cleanup cases. Its source-only cases keep target, relationship, and same-Book reuse separately unselected; prove a source-bound zero-Manuscript new Book, explicit same-Book Source Version reuse with new provenance and Source Import Record, existing-empty and existing-populated cross-Book results, preservation of existing Manuscript/Revision/Workflow identities, reviewed restart, recovered after-commit completion, and ambiguous outcome containment with no retry/cancel/commit action. Source completion is exactly `来源材料已导入` and exposes direct Source Version and Source Import Record inspection, including retained file/content/structure boundary, provenance acquisition time, and named non-effects. J-02 additionally drives outline/position navigation, CJK search and Search Return Position, reviewed frozen atomic replacement, editing during cooperative service work, cancellation, Milestone Version, restart/reopen, durable undo/redo, and applicable J-14 keyboard/composition/focus/zoom/forced-colors behavior. J-08 proves clean versus interrupted lifetimes, startup priority ahead of import recovery, exact unselected candidate identities, two or three eligible choices according to verified snapshot availability, permanent readonly viewing, durable deferral and typed return routing through a lower-priority import, service-enforced branch guards, unrelated prior-work access, newest-to-older snapshot fallback and invisible orphan cleanup, selection consequence disclosure, all-or-none descendant restoration, old-history frontier enforcement, persistent recovered-state review, later-Milestone clearing, CDP-confirmed Chromium cancellation, and direct J-14 recovery-screen behavior. J-12 proves one Workbench per exact Book, role-first Model Service settings, visible launch-policy integrity, actual Tab reachability and focus visibility, real OS-store save/no-redisplay/restart/same-reference replace/remove/restart, truthful Product Data Location separation, fixed verified development-ci active-set v4 selection of Provider Processing v1 zero transmission, separate External Export v1, no Public Release Permission, applicable J-14 behavior, and zero sentinel requests. Controllers do not use product database contents as scenario proof; J-12's sole database read is the bounded cleanup-only exact nonsecret metadata fallback described above and occurs only after confirmed browser closure. Controllers read no manuscript payload and do not retain, upload, or log any secret, manuscript text, screenshot, trace, video, artifact, or SampleBook derivative; disposable runtime state is deleted after successful cleanup, while an unconfirmed close deliberately preserves ownership and the root for fail-closed containment.

The macOS source-checkout host contract is macOS 15+ arm64, matching the selected V1 CPU/OS support profile. Source checkout truthfully reports `source-checkout`; it does not claim the separately selected direct-download Developer ID-signed/hardened/notarized DMG. Packaged macOS product data belongs in the OS-resolved per-user Application Support location for `io.github.zhouy1017.ai7` with no silent fallback, but this source-checkout route performs no package, signing, notarization, upload, or release action.

## J-03 authorization boundary

For v14 specifically, startup validation binds the exact native carrier and Revision 2 sidecar digests, an independently identified Book/primary Manuscript/Task Input Revision/digest readable scope, unreadable Source Version lineage evidence, the fixed missing-credential DeepSeek binding, and Provider Processing v1 denial. Preparation is unavailable until all current prerequisites exist, while an already frozen record reopens from its immutable rows. J-03 proves the Task action is unavailable before those prerequisites and that a renderer-supplied foreign Book identifier cannot override the Main-owned current-Book route. Its prepared product projection and UI expose the same exact carrier and scope identities. Cancellation checkpoints prevent further credential, edit, preparation, or authorization mutations after an interrupt; bounded browser disconnect and protected-store cleanup must succeed before disposable state is deleted.

## J-04 covered baseline analysis

The baseline manuscript analysis is one exact-versioned covered Task kind over the J-03 authority: it reuses the Task Input checkpoint owner, the Revision 2 artifact pin, the fixed missing-credential DeepSeek binding, and the verified development-ci Provider Processing v1 denial. Preparation derives a deterministic Coverage Manifest from the pinned Revision (title/heading blocks start a structural section; each section is split greedily into units of at most 1,200 graphemes with one overlap block carried from the previous unit; own ranges partition every block exactly once) and freezes it with the Provider Resolution plan, Execution plan, and Plan Envelope. The Plan Envelope carries the prompt-contract digest and the behavior-composition digest so a later attempt with a changed prompt or harness composition cannot reuse it.

Main accepts `--j04-picker-path <sample1.docx>` and `--j04-model-adapter <fixture identity>` only under `AI7_E2E_JOURNEY=J-04` — the adapter control also under `AI7_E2E_JOURNEY=J-09` (Issue #424), with the service child told which of the two it serves — mutually exclusive with the other Journey controls. The service loads the named fixture from `tests/fixtures/model/` before the store opens and binds the in-process `ai7-local-deterministic` route to it; the route pins the fixture identity, its lineage digest, the route, and the model name into the plan, the Execution Binding, and the Result Set Revision. Without the control the Run Authorization is still recorded, but the Run settles `blocked-before-dispatch` with its three visible reasons and creates no Session, payload, or network activity. The remote DeepSeek adapter exists as an assembled-request/parsed-response owner only; its transport is denied under Provider Processing v1 and the egress gate refuses every remote route.

Connectivity Wait (Issue #502) is schema revision 30, on service protocol version 43. It widens `analysis_run_states` with two Run states — `awaiting-connectivity`, a Run authorized by `联网后开始任务` that waits until Reconnect Preflight admits it, and `cancelled`, a waiting Run the editor cancelled before it ever dispatched — rebuilt once, shape-detected, with every row copied byte for byte; nothing else moves. Whether the device is online is a local reading, never a probe: an interface carrying an address that is neither internal nor link-local (`src/service/connectivity.ts`). Reconnect Preflight (`src/service/reconnect-preflight.ts`) runs when the service starts, every fifteen seconds while it runs, after a credential changes, and from the drawer every two seconds while it shows a waiting Run: it leaves the Run waiting for the network, for a missing credential or for the slot, blocks it naming what moved when the plan it bound no longer stands, and otherwise admits it through the execution owner, which accepts a waiting Run only on that word. Since service protocol version 58 (Issue #536) the blocked state's canonical detail also keeps why, `cause`: `plan-moved`, or `launch` when the execution owner refused the admission, and a record without it reads as `launch`. A Run blocked because its plan moved reads `需要重新确认计划` in the drawer, ②A and 待我处理, where it sits with the plan decisions, and the drawer's `重新准备` prepares the same goal and range as a new Task and opens its plan. Any other block keeps `派发前已阻止`. Since service protocol version 60 (Issue #539), 待我处理 checks what a waiting Run waits for only while one waits, and a check that fails reads as waiting for the connection; a waiting Run the next Reconnect Preflight will admit is `analysis-waiting-admission` — 正在排队, with 「已经联网：AI7 先核对计划，没有变化就开始；现在什么都没有运行。」 — never `analysis-queued`. No schema revision moves. Main also accepts `--j04-connectivity-path <absolute path>` under `AI7_E2E_JOURNEY=J-04`, beside the model adapter rather than exclusive of it: the service reads that file's word, `offline`, as the device's connectivity at each reading — so one launch can go offline and come back — and treats the deterministic route as one that reaches its model over the network. Absent, or saying anything else, the reading is online.

Default execution rules (Issue #421) are schema revision 31, on service protocol version 44. It widens `analysis_run_authorizations.origin` to admit `default-execution-rule` — rebuilt once, shape-detected, every row copied byte for byte — and adds three append-only relations owned by `src/service/default-execution-rules.ts`: `default_execution_rules` (one per Book and task pattern), `default_execution_rule_versions` (what each version binds: the viewed plan's material inputs except the range and the predecessor) and `default_execution_rule_states` (in force or turned off). A rule is never a standing authorization: 快速开始 prepares the Task exactly as 先看计划 does and authorizes it exactly as 开始任务 would, the authorization naming the rule version, and stops at the plan — recording nothing — when offline, when the slot is busy, when the rule changed or was turned off, when what it binds differs from the plan, or under developer-live, whose Provider Processing forbids a matching rule.

取消任务 (Issue #422) is schema revision 32, on service protocol version 45. It widens `analysis_run_states` with `cancelling` — 正在取消, a Run the editor cancelled while it was under way — and `analysis_task_outcomes.classification` with `cancelled`, each rebuilt once, shape-detected, with every row copied byte for byte; the Run states widen from revision 29's text or revision 31's in one step. `cancelBaselineAnalysisRun` records `cancelling` at once; the execution owner stops the Run at the next unit boundary — the unit in flight finishes and nothing after it is sent — and records `cancelled` with a partial Result Set Revision whose units it never reached are `not-attempted`, and a Task Outcome `cancelled` whose Run Report names the cross-unit reduction, the sample and the reflection as not run. A Run cancelled before it began its units, and one no execution holds any more (AI7 closed while it ran), end `cancelled` with no revision. Main also accepts `--j10-picker-path <absolute path>`, the J-04 model adapter and `--j10-unit-hold-path <absolute path>` under `AI7_E2E_JOURNEY=J-10`, the last beside the adapter: once a unit's turn is back, the owner lets it settle only while fewer units have settled than the number that file names, so the Journey can hold a unit in flight, cancel, and release it. Absent, or naming no number, the file holds nothing, and every wait gives up after two minutes or at an interruption.

暂停 and 续行 (Issue #422, S76b) are schema revision 33, on service protocol version 46. It widens `analysis_run_states` with `pausing` (正在暂停), `paused` (已暂停) and `resumable` (任务已中断 · 可续行) — rebuilt once, shape-detected, with every row copied byte for byte, from revision 29's, 31's or 32's text in one step — and adds one append-only relation owned by `src/service/analysis/run-checkpoints.ts`, `analysis_unit_checkpoints`: for each baseline Run and unit, the unit's settled record, closed or a gap, with its observation, as one canonical record and its digest, written the moment the unit settles. `pauseBaselineAnalysisRun` records `pausing` at once; the execution owner stops at the next unit boundary — the unit in flight finishes and is kept — and records `paused`, freeing the slot. `resumeBaselineAnalysisRun` revalidates first — the plan unchanged, every kept unit reading back to its digest, the slot free, the route's credential present when it sends to a model service, and the device online when the route reaches its model over the network — and then admits the same Run again under its own authorization: the same attempt and execution binding, whose digest is recomputed and must match, its units' outcomes, observations and usage rebuilt from what was kept, and only the units not yet kept submitted. AI7 closing under a baseline Run leaves it `resumable`: a turn that came back whole settles and is kept, one cut off is not, and nothing is sent until `续行`. At start the service reconciles what its predecessor left before it reads a request: `admitted` or `executing` becomes `resumable`, `pausing` becomes `paused`, and a Run left `cancelling` is finished by the execution owner, sending nothing. `取消任务` on a paused or resumable Run forms the partial revision of what it kept — once the slot is free, if another Run holds it — or ends with no revision when it kept none. A Review Run's category Tasks still end `interrupted` when AI7 closes under them. J-10's unit hold counts a continued Run's kept units among those settled.

更新计划 (Issue #419, S73) is schema revision 34, on service protocol version 47. It widens `analysis_plan_revisions.trigger_kind` with `plan-edit` — rebuilt once, shape-detected from revision 33's text, with every row copied byte for byte; a store before revision 17 has no Plan Revisions yet and its migration creates them in the current text — and adds no relation. `editBaselineAnalysisPlan` takes the Task Intent, the envelope digest of the version the editor was reading and the full set of what the plan leaves out. That set can only hold what a baseline analysis Run honours: `removedSteps` ⊆ `assurance-sampling` (核对与抽检) and `disallowedAdaptations` ⊆ `safe-retry`. In one transaction it records a Plan Revision, with trigger `plan-edit`, actor `editor`, the material inputs unchanged, and one `edited` diff line per changed item, and writes the next plan version. That version's execution plan names what it leaves out as `editorEdits`, present only on an edited plan, so every other plan stays byte-identical, and its envelope's Plan Boundary Split leaves a withdrawn adaptation out. It is refused, recording nothing, when the Task has been authorized, a key-content change is pending, the editor read another version, the set names something else, nothing changed, or any component other than the execution plan and the envelope would come out different — as when the launch's route or fixture moved since the plan froze. 重新确认计划 carries a version's edits into the version it writes. The Run reads `editorEdits` back, checks the envelope's adaptation classes against what they withdraw — by class, not by the split's words — draws no assurance sample when 核对与抽检 is left out, and makes no safe retry the plan withholds, saying so in the unit's gap.

改计划重做 (Issue #422, S76c) is service protocol version 48, with no schema revision. `prepareBaselineAnalysis` takes an optional `redoOf`, the run record id of the Run the new Task redoes: the Book's latest Task's Run, cancelled after it began executing, or the preparation is refused with `ANALYSIS_REDO_STALE`, recording nothing. What that Run kept it formed into the Book's latest revision, so the redo must be `sync-current` over it — allowed although the manuscript has not moved, since its gaps are what the redo reads; a Run that kept nothing must be redone as the Task it was, the same mode over the same range, or the first baseline again. Any other request, or one that reconfirms, is refused with `ANALYSIS_REDO_INVALID`. The Task Intent's canonical JSON records `redoOfRunRecordId`, a key no earlier record holds, so every other intent stays byte-identical, and the projection answers `taskIntent.redoOf` with that Run and its Task. The new Task's first plan version carries the redone Task's last edits. A first baseline whose Run was cancelled, failed, interrupted or blocked before it formed a revision leaves `actions.canPrepare` true, so ②A offers it again. The execution owner's `carriesStoppedRun` answers, recording nothing, whether this launch can still carry a stopped Run under the Execution Binding it persisted — the check `续行` and a stopped Run's cancellation make when they re-admit it. When it cannot, `续行` is refused with that reason, and the stopped Run's `取消影响摘要` and `改计划重做摘要` say that what it read cannot become a revision, since its cancellation then settles without one, and the redo is the Task it was. A plan whose material inputs moved while the Run was stopped now sends `续行`'s refusal to 改计划重做, the newly authorized Redo Run of CONT-016.

Clarification Requests (Issue #422, S76d) are schema revision 35, on service protocol version 49. `analysis_run_states` widens to `awaiting-clarification` — rebuilt once, shape-detected from revision 34's text or any earlier one, with every row copied byte for byte — and two append-only relations, owned by `analysis/clarifications.ts`, keep what a Run asked and what the editor answered: `analysis_clarification_requests`, one per Run and unit, and `analysis_clarification_answers`, one per request. `editBaselineAnalysisPlan` takes an optional `askFirstAdaptations` ⊆ `safe-retry`, never together with the same adaptation withheld. The execution plan's `editorEdits` and the envelope's split name it only when it holds something — the split then lists it under `askFirst` and says where the editor may be asked — so every earlier record reads back byte for byte. A Run under such a plan records a request instead of its safe retry, with the failure and what the first attempt cost; the unit stays unsettled while the others are read, an answer found at a unit boundary is applied there, and once nothing else can be read the Run records `awaiting-clarification` and frees the slot, the last read of the ledger and that record made with nothing awaited between them. `answerBaselineAnalysisClarification` records the answer — `retry` or `record-gap`, a note of at most 500 characters or none, bound to the request's digest — and is refused for another Task's question, one already answered, or a Run that ended or is stopping. A Run waiting for it goes on at once, or once the slot is free — and one AI7 closed on before that goes on at the next launch, from the startup reconciliation: `retry` records the plan's declared safe-retry adaptation, naming the answer, and makes the retry as the unit's second attempt; `record-gap` settles the unit as an `adapter-failure` gap in the answer's words. A Run cancelled while it waits settles the waiting unit as a gap too. The deterministic adapter now counts a unit's attempts across the Run's executions, from the spans it recorded.

The editor's Run Budget Ceiling (Issue #51, S16a) needs no schema revision; it is service protocol version 50. `editBaselineAnalysisPlan` takes an optional `runBudgetCeiling` — `{ kind: 'tokens', maxTotalTokens }`, a whole count from 1 to twelve digits, or `null` for none — and the execution plan's `editorEdits` names it only when set, so every earlier record reads back byte for byte. Outside developer-live the version the edit yields freezes it into its Provider Resolution Plan, the one field that component may move under an edit, and the drift re-derivation reads the version's own edit, so the editor's ceiling is never a drift of the plan; under developer-live the launch sets the ceiling, and an edit naming one is refused. The Run is held to it before every dispatch, the safe retry and an answered retry included. A ceiling that stops a range, the reduction or the sample ends the Run `interrupted`; a spent ceiling also ends it at an open question or when AI7 stops under it, while the editor's pause only lapses, so a Run with no request left completes. Its Task Outcome names the stop — `stop: { reason: 'run-budget-ceiling-reached', maxTotalTokens, usedTokens, unitsSettled, unitsTotal }` — which `taskOutcome.stop` projects and `redoOf` accepts, as it accepts a Run cancelled after it began. The token counts are the ones each turn reports; on the deterministic route they are the fixture's. A stop keeps what it spent on a reduction or a sample by stage (`carriedUsage.stages`, Issue #541): `续行` counts it toward the ceiling and into the Run's usage and Run Report by stage, so the report reconciles with the stop. A stop recorded before the stages were kept counts toward the ceiling only. Since service protocol version 59, 待我处理 gives a Run the launch's ceiling stopped under developer-live the next step `redo` (`改计划重做`) and says the ceiling was the launch's.

A Provider Account Limit (Issue #51, S16b) is service protocol version 51, and needs no schema revision either. A reading range, the reduction or a sampling turn the model service refuses on the account's limit no longer ends a Run of a kind that keeps its progress. The Run records `resumable` with `stopReason: 'provider-account-limit'`, the refusal's `condition` as AI7 classified it, and the refused `unitOrdinal` (absent for the reduction or a sample). The refused range stays unsettled, and nothing more is sent. `accountLimitOf` reads that stop back, so the Run reads `模型服务账户限额` rather than `任务已中断 · 可续行`. `TaskPlanRunControlProjection.accountLimit` carries it to the drawer, and 待我处理 holds it as `analysis-account-limit` with the next step `resolve-model-service`. `续行` is S76b's, unchanged: the same Run and attempt, a new span, and the unit's next turn — which the deterministic adapter serves as its next attempt, from the spans the Run recorded. A kind that keeps no progress still ends the Run `interrupted`, as before. `sample1-baseline-account-limit` layers over the happy fixture, refusing unit 4's first turn with status 402.

A dispatched Run executes inside one single-slot scheduler attempt: the Credential Broker checks slot readiness by presence only and releases no value; the harness composes the pinned DSH loop with the complete `ai7:baseline-analysis-contract` system prompt section; every `llm/stream` payload passes the egress gate before the adapter sees it; each unit reply is parsed strictly against the unit-result schema, and an invalid, failed, or interrupted unit becomes a gap rather than a silent omission. The reducers close section reduction, cross-unit contradiction/continuity, and book synthesis over the closed units; conflicts are reported and never resolved. The Result Set Revision is append-only, links the Task Outcome, and exposes four independent axes (coverage, reducer closure, exact-revision freshness, semantic/evidence assurance) with no aggregate flag. Freshness is recomputed on every read by a local deterministic comparison against the current working state, so one acknowledged edit marks the revision `stale` without changing it. Once a revision exists, the exact three Analysis Update Controls (`同步到当前稿件`, `重新分析所选范围`, `重新分析全书`) each start a new user-initiated Task through the same prepare → Plan Preview → authorize → execute path with a content-exact, position-independent reuse plan, submit only the recomputed units, and append an immutable successor Result Set Revision whose lineage, predecessor link, and four recomputed axes the Analysis Result Revision History keeps reachable, read-only, and bound to each revision's original pin across restart. The renderer exposes only `inspectBaselineAnalysis`, `prepareBaselineAnalysis`, and `authorizeBaselineAnalysis`; no Effect, Enrollment, Apply, export, or learning surface exists, and `回到稿件范围` opens the ordinary editor window at the referenced block.

## J-05 Editorial Marks

Main accepts `--j05-picker-path <composed.docx>` only under `AI7_E2E_JOURNEY=J-05`, mutually exclusive with the other Journey controls; the Journey composes its manuscript from exact `sample1` at run time. Editorial Marks live in five relations that schema revision 22 adds and `src/service/editorial-marks.ts` owns: `editorial_marks` holds each mark's immutable pin — Revision, journal position, block digest, grapheme range and exact text — beside the live range that follows every durable change of the block, and `proposal_change_items`, `proposal_item_decisions`, `proposal_decision_reasons` and `editorial_mark_replies` are append-only ledgers guarded by triggers. A window projection carries the marks standing in its blocks, so a range is always read against the text it was followed through. The selection menu's 文字处理 commands run in the window that owns the editor (`runEditorClipboardCommand`), because the page holds no clipboard permission.

The Whole-manuscript Position Rail (Issue #409) is one read, `getManuscriptRail`, owned by `BoundedManuscriptStore.getRail`: chapter ticks from `manuscript_outline`, the open 修改建议 / 批注 / 备注 from `editorial_marks`, and the block runs the Book's latest Result Set Revision left unread, each placed as a proportion of the manuscript's characters through the offset index, so its cost follows the chapters and marks (at most 400 and 2,000, with a sparse flag past either) and never the manuscript's length, and it carries no manuscript text. `src/renderer/position-rail.ts` only draws behind the range input the surface already had; the renderer reads it again 1.2 s after typing rests and whenever the marks change.

审阅 (Issue #417) is schema revision 24. It rebuilds the three kind-coupled analysis relations (`analysis_task_intents`, `analysis_result_sets`, `analysis_result_set_revisions`) exactly as revision 20 did, so that a Book holds one Result Set per Review Category beside its baseline and factual ones — the family `editorial-review/<categoryId>` is admitted by the shape of its kind identity, never by a list, because a category is configuration (`src/service/review/category-configuration.ts`) — and adds seven append-only relations that `src/service/review/review-runs.ts` owns: `review_runs`, `review_run_authorizations`, `review_run_category_events`, `review_findings`, `review_finding_dispositions`, `quality_signals` and `review_reports`. A Review Run records the editor's one approval; `ReviewRunDriver` (`src/service/review/review-run-driver.ts`) authorizes and dispatches each category's own Task only when its turn comes, through the one execution owner, and writes each category's findings and the marks they become in one transaction. Main accepts `--j04-model-adapter sample1-review-authored` like any other fixture identity; that authored fixture layers over `sample1-baseline-transient-retry` and is regenerated, after a clause of one of its three categories changes, with `AI7_REGENERATE_REVIEW_FIXTURE=1 pnpm exec vitest run tests/service/review-fixture-generator.test.ts`.

AI7 Apply for a Change Suggestion is `src/service/manuscript-apply.ts` over schema revision 23's five append-only relations — `manuscript_effect_intents`, `manuscript_effect_targets`, `manuscript_effect_approvals`, `manuscript_effect_dispatches` and `manuscript_effect_receipts`. One interaction writes the Proposal Decision, the Effect Intent with its exact targets, the Effect Approval, the dispatch and the Effect Receipt in the one transaction that writes the text (`BoundedManuscriptStore.commitExactReplacements`, journaled as a `replacement` command and made a history boundary), so an Apply is committed with its receipt or not at all, the renderer's `clientEffectId` is the idempotency key that makes a repeated request answer with the receipt already held, and `getManuscriptApplyOutcome` has only two answers. A receipt binds identities, digests, times and the manuscript state before and after — Revision, journal sequence and working digest — and no manuscript text. Main accepts `--j05-apply-control lose-first-acknowledgement` only under `AI7_E2E_JOURNEY=J-05`: the first Apply commits and its acknowledgement is withheld from the renderer once.

## J-06 稿件冲突

Main accepts `--j06-picker-path <composed.docx>` only under `AI7_E2E_JOURNEY=J-06`, mutually exclusive with the other Journey controls; the Journey composes its manuscript from exact `sample1` at run time. 稿件冲突 is `src/renderer/proposal-conflict.ts`, a Dedicated Work Workspace that `src/renderer/index.ts` only routes to and back, with every word it adds in `src/renderer/proposal-conflict-labels.ts`; the Mark Card's `解决冲突…`, the §2 line and the conflict states live in `src/renderer/editorial-marks.ts` and `src/renderer/editorial-mark-labels.ts`. The three renderer members — `inspectProposalConflict`, `saveProposalConflictDraft` and `resolveProposalConflict`, each naming `{ manuscriptId, branchId, markId }` — travel over `ai7:j06:` channels gated exactly as the mark commands are: the window's manuscript capability within the route's Book, and the two that record serialized with every other effect. The units the three texts are compared in are `src/shared/conflict-units.ts`, which the service and the renderer both run.

稿件冲突 (Issue #57, ADR 0085) is schema revision 26. It adds three STRICT, append-only relations that `src/service/proposal-conflicts.ts` owns, each refusing UPDATE and DELETE through its triggers and carrying its canonical JSON and digest: `proposal_conflict_drafts` (each save of a Resolution Draft — every unit with its words and resolution, and the draft's text — bound to the basis digest of the texts, range and block it was made on), `proposal_conflict_deferrals` (each 暂不处理) and `proposal_conflict_outcomes` (the one outcome that resolves a conflict: 保留当前稿件 with the rejection it recorded, none for a reversal, or 保存为新提案版本 with the new 修改建议 and its draft). A revision-24 or revision-25 store gains the three empty relations and moves nothing else. The conflict itself is read, never stored: a drifted open 修改建议 is a suggestion conflict and a drifted applied one a reversal conflict, and a new version is an ordinary open 修改建议 on the current words — `converted_from_mark_id` names the suggestion it replaces, which is retired as `converted`, while a reversal's Correction Proposal leaves the Apply applied. It moved the service protocol to version 38.

Import retention (Issue #410, ADR 0086) is schema revision 27, owned by `src/service/import-retention.ts`. The DOCX parser is identity `ai7-docx-fflate-saxes/2`: it reads a text box (`w:txbxContent` under `wps:txbx` or `v:textbox`, read once through markup compatibility's first `mc:Choice`) instead of refusing the file, counts every field as one item of 域 except a `HYPERLINK` field, which like a `w:hyperlink` element is a link and one inline-style item retained with the file, gives every `w:p` a 0-based index in document order that each block carries, and reports ten classes — nine rows and the 预计往返 card — where a class kept with the file reads `retained` (`完整保留（随文件保留）`), notes and fields are `降级导入`, and only a degraded or unsupported class asks for the Import Degradation Decision; the eight-row report of `/1` is frozen so every review recorded under it still rebuilds under its own identity. The revision rebuilds `import_fidelity_categories` to ten positions and the `retained` status with every row copied byte for byte, and adds `import_fidelity_choices` (a review's `retain` or `merge` for its text boxes), `staged_import_block_sources` and `staged_import_text_box_paragraphs` (staged with the snapshot, so the choice is made at review without a re-parse) and `manuscript_block_sources` (written at commit for r1 and a changed reimport's revision: which source paragraph, or which text-box paragraph, every block came from and its digest there); the choice and the mapping refuse UPDATE and DELETE. A revision-24 to -26 store gains the four relations and the widened table and moves nothing else; revisions committed before 27 carry no mapping, and a draft staged under `/1` re-stages through the parser-drift path when it is continued or committed. `prepareNewBookReview` carries `textBoxDisposition` (`retain`, the default, or `merge`) on service protocol version 40, and the import review digest is revision 6.

Imported marks (Issue #411) are schema revision 28, owned by `src/service/imported-marks.ts`. The DOCX parser is identity `ai7-docx-fflate-saxes/3`: it reads a file as it reads with every revision rejected — `w:delText` inside `w:del` and text inside `w:moveFrom` present, text inside `w:ins` or `w:moveTo` absent, text both inserted and deleted in neither — skips formatting, table and numbering revisions (`w:rPrChange`, `w:pPrChange`, `w:sectPrChange` and their kin) instead of refusing the file, and reads `word/comments.xml` and `word/commentsExtended.xml`; a file without comments or revisions reads exactly as `/2` read it, and the ten-class report of `/2` is frozen so every review recorded under it still rebuilds under its own identity. `src/service/docx-marks.ts` turns what the parser saw into marks pinned in the parse's own blocks: a comment is a 批注 clipped to its first paragraph, with its replies folded into its body and a done comment resolved; a deletion, an insertion, or a deletion and an insertion by the same author at the same time with no text between them are one 修改建议 each; a whole paragraph inserted or deleted, a split, a join and a move are describing 批注; a table row or cell inserted or deleted as a whole (`w:trPr/w:ins`, `w:trPr/w:del`, `w:tcPr/w:cellIns`, `w:tcPr/w:cellDel`) is read as a revision of every paragraph it holds, until the row or cell closes — an inserted one's words leave the rejected reading and are described where they would stand, a deleted one's stay and are described on themselves; a revision that changes no text of either reading — a cell merge, a move's or custom XML's range markers — and comments and revisions in text boxes, notes, headers and footers stay with the file — the parser notices one in a note, header or footer part as it streams by, so the row says it is kept rather than that none was found. Under `/3` 批注与修订 reads `完整保留`, counted as the marks it becomes, with the fixed detail, and asks for no decision; a reimport, which made no mark until S63 (below), stated it `不支持导入` and asked for the decision. The revision rebuilds `proposal_change_items` with the `insert` kind — no current text, a proposal written at a point — with every row copied byte for byte and both ledger triggers re-armed, and adds `staged_import_marks`, staged with the snapshot and compared on every re-parse. The new-Book and first-manuscript commit creates every staged mark on r1 in its own transaction, all or none: a mark that does not stand where it was staged refuses the import. A revision-24 to -27 store gains the one relation and the widened ledger and moves nothing else; a draft staged under `/2` re-stages through the parser-drift path when it is continued or committed. A Source Version keeps the reading its parser made, so a Book whose Source Version an earlier parser read cannot take the same file again, as a reimport or as source material: the choice refuses it as `SOURCE_VERSION_PARSER_CHANGED` in the editor's words, and the way on is a new Book (Issue #532; development stores are disposable, ADR 0079 §1b). 「发现相同内容」 compares content and structure digests whichever parser read each side. The mark anchor carries a pending insertion's words beside an applied deletion's and the card each item's change type, on service protocol version 41.

The chapter-level Reimport Comparison (Issue #412, S63) is schema revision 36, on service protocol version 54. `src/service/reimport-groups.ts` gathers a comparison's block mappings into rows: the anchors are the longest run of exact mappings, unchanged or moved, whose current positions rise with their new ones, and each run of changed mappings between two anchors is one row, from the checkpoint revision's paragraphs to the new file's, named by the heading it stands under when the revision has one. A moved paragraph outside that run keeps its identity and stands in no row. A row is resolved by one verb its shape admits, never preselected: 拆分 (one paragraph to several), 改写与新增 (any new paragraphs), 删除 (any current ones) or 并入 (several to one). The verb writes the per-block identity resolutions the commit already read: an edit row keeps its own identity, an insert row claims the row's delete rows in order, and the rest are new or retired; 删除 retires every current identity and makes every new paragraph new. `src/service/reimport-group-ledger.ts` owns five relations: `manuscript_reimport_group_sets` (a comparison's grouping, `ai7.reimport-groups/1`, and its counts), `manuscript_reimport_groups`, `manuscript_reimport_group_members`, `manuscript_reimport_group_resolutions` (each row's verb) and `manuscript_reimport_mark_outcomes`. A review prepared before revision 36 has no group set and is prepared again when it is opened. The resolution digest is `/3`, which binds each resolved row's verb after the mappings. At commit, a mark on a changed row's current paragraphs follows its pinned words when they stand exactly once among the row's new paragraphs, and otherwise is detached and listed — kind, words and former position — in the Manuscript Reimport Record; a mark on an anchored paragraph is untouched. The new file's comments and tracked changes become marks as a new import's do, all or none, except one that already stands — same author, kind, words, body and place — so 批注与修订 reads `完整保留` on a reimport too. `getReimportMappingPage` returns ten rows a page with at most three excerpts of 400 graphemes a side; `resolveReimportMapping` takes `{ groupId, verb }`; the identity-candidate page is gone; the review's comparison counts rows, unresolved rows and exact paragraphs; and the record carries its rows with their verbs and the mark outcomes. A committed reimport lands in the manuscript with its result and the marks that could not follow, not in the Book Work Overview.

Production Documents (Issue #415, S66a) are schema revision 37, on service protocol version 55. A document is a Book's Editorial Deliverable of one house type, and `src/service/production-document-types.ts` holds the V1 baseline as configuration, `ai7.production-document-types/1`: 新闻稿, 宣传文章, 评论文章, 发布会材料 and 营销要点. Records pin the configuration's version and digest. The document lives in the bounded block store as a `manuscripts` row with role `production-document` and its own branch, revisions, blocks, journal and marks, so every editing path serves it unchanged. `manuscripts` is rebuilt at 37: the role check is widened and `book_id UNIQUE` gives way to a partial unique index, `manuscripts_one_primary_per_book`, with every row copied byte for byte. The validator accepts the rebuilt text below 37 too, beside the index, as a store an earlier build planted after this one would hold it. `src/service/production-document-ledger.ts` owns three append-only relations: `production_documents` (the type, and the source-only Source Version the document was made from, at most one document per type of a Book), `production_document_versions` (`版本 N`, each naming its revision) and `production_document_type_decisions` (`本书不做` and `恢复`, in order). `从来源材料创建` reads the retained file again with the current parser and writes the document's r1 under newly minted block identities. `保存为版本` is the block store's checkpoint with the purpose `Document Version / 文档版本`, and the version is recorded in the transaction that makes the revision. The Book overview, its history, the library's state, the position rail's unread ranges, recent work and the remembered entry position read the primary Manuscript only, and a milestone refuses a document. The 交付物 projection carries the documents, and the three commands are `createProductionDocument`, `decideProductionDocumentType` and `saveProductionDocumentVersion`.

How a document's origin material was read (Issue #547) is schema revision 42, on service protocol version 63. `src/service/production-document-origins.ts` owns one append-only relation, `production_document_origin_readings`: one row per document, recorded in the transaction that makes it, naming the Source Version, the reading — every tracked change rejected, the comments left out — and how many of the material's 批注与修订 the document did not carry. It carries its canonical record and digest, verified on every read. Each document's `origin` carries the count as `marksNotCarried`, so its card and its `这份文档的材料` say it for as long as the document exists, not only in the notice that opens it once. A document made before revision 42 has no row and reads `null`: nothing guesses what its material held.

维护事项 (Issue #426, S68a) are schema revision 43, on service protocol version 64. `src/service/maintenance-cases.ts` owns three append-only relations. `maintenance_cases` binds one case to one exact 发稿版本 and the Manuscript Revision it froze, with its classification (更正, 勘误, 替代, 撤回, 再版 or 归档) and a per-Book ordinal. `maintenance_errata_versions` holds each 勘误 version's words. `maintenance_case_revisions` holds every step in order, each chained to the digest of the one before it: the case recorded, a 修改建议 linked, a later designation linked, a 勘误 version saved, or a conclusion. Each carries its status: 未解决, 等待另设发稿版本 or 已完成（AI7 内记录）. 更正 and 勘误 start 未解决, 替代 and 再版 wait for a later designation — until one is linked they may be concluded 仍未解决, never 已完成 — and 撤回 and 归档 are recorded as internal states at once, with `仅在 AI7 内记录；不代表已撤稿、下架、召回、通知接收方或删除外部文件`. Every step names the revision the editor read and is refused against another, and a case already complete takes none. A link grants the case nothing: 更正 links a 修改建议 of the manuscript made after the designation, applied through its own path, and a later designation made through `设为发稿版本`. Each designation in 交付物's read carries at most five case summaries, the newest, with a count and whether a 撤回 or an 归档 holds it; `listMaintenanceCases` reads the older ones twenty at a time before the oldest shown, so every case can be opened, and `inspectMaintenanceCase` reads one case whole: its newest sixty revisions, a 勘误 version linked by its number, the newest 勘误's words, and at most twenty 修改建议 and thirty later designations to link, so its answer stays one frame. A 撤回 current 发稿版本 holds no 图书交付包 condition until another is designated, and designating the same milestone, scope and basis again makes a new designation rather than repeating the withdrawn one. Nothing a case records moves a package, an export or a receipt. The commands are `recordMaintenanceCase`, `appendMaintenanceCaseRevision` and `saveMaintenanceErrata`; the completion words are only `维护事项已记录` and `维护事项结论已记录`. Since service protocol version 65 (Issue #426, S68b) 待我处理 reads every case still waiting on the editor from the same owner. It lists a case that is not complete, whose designation no 归档 has closed, under 等待你的决定 as `维护事项待处理`, or `维护事项待处理 · 等待另设发稿版本` while 替代 or 再版 waits for a linked designation, whatever interim 仍未解决 it recorded. The item names the case's own next step and never blocks. Opening it renders 交付物 with that case open in place: among the listed cases, or drawn below them when it is older than the five 交付物 lists.

A Book's people (Issue #431, S83) are schema revision 44, on service protocol version 66. `src/service/book-people.ts` owns one append-only relation, `book_people_versions`. Each row is one version of a Book's 作者, 责编 and 相关人: the names in the editor's order, each 相关人 a role and a name, and the version and digest of the house's role list it read. That list is configuration, `ai7.book-people-roles/1`: 校对, 美编, 译者, 外审专家, 作者经纪, 营销 and 其他. Each version is chained to the digest of the one before it and carries its canonical record and digest, verified on every read under the list it pinned: every list a release ships stays known, so a later list never makes an earlier version unreadable, and a version pinned to a list this build never shipped still reads, its roles named by their ids. A name is 1–40 characters once NFC-normalized and trimmed, holds no 「、」, comma, semicolon or line break, and is not repeated in its list; a Book holds at most ten 作者, ten 责编 and thirty 相关人. `updateBookPeople` names the version the editor read and is refused against another. A new set answers `人员已保存`; the same set again, or no one before anyone was saved, records nothing and answers `人员没有变化`. The people are attribution dimensions, never accounts, and nothing reads them to decide what anyone may do. The Book Work Overview and every Book summary carry them. `listBooks` takes an optional `filter` — `all`, `title`, `author` or `editor`, and 1–40 characters of words with no line break, which 查找图书 holds to — and keeps the Books whose title, or whose newest version's 作者 or 责编, hold the words, ignoring ASCII case; `all` reads all three. A found list pages as the whole list does. A revision-43 store gains the relation and moves nothing else. J-11 needs no launch control of its own.

The 任务 panel (Issue #423, S77a) is service protocol version 67, with no schema revision. `inspectBookTasks` reads the route's Book's Tasks as 待我处理 reads them — `attentionReadings` of the baseline ledger and of the Review Run ledger, scoped to that Book — and `composeBookTasks` in `src/service/global-attention.ts` places each in `等你处理` (待我处理's two counted groups), `进行中` (its 运行中与已暂停) or `最近完成`, with three states of the panel's own: a baseline Task prepared and not started, a Review Run prepared and not started, and a Task the editor cancelled — one cancelled while it waited to start too, whose Run no Task Outcome names. 最近完成 has no age limit and lists ten; each finished Task names the result `查看结果` opens, the revision an analysis formed or the Review Run itself. A read: it records, claims and terminalizes nothing. The renderer sends a card's and 查看结果's ways to another page, the drawer's 查看运行 and 发起全书任务 through the surface on screen's own leave step first, so the manuscript's local edits are written and its position taken before it is left or a Task Input is frozen, and a refusal keeps it on screen. Main accepts `--j16-picker-path <absolute path>` under `AI7_E2E_JOURNEY=J-16`, and the J-04 model adapter and J-10's `--j10-unit-hold-path` under J-16 as under J-10.

Concurrent Book work (Issue #49, S14) is service protocol version 68, with no schema revision. The execution owner in `src/service/analysis/execution.ts` is the instance's concurrency governor: it executes up to two Runs at once under development-ci (`EXECUTION_RUN_CAPACITY`) and one under developer-live, each with its own attempt, Session, progress, usage and revision. A start made while every place is taken is first checked for what does not depend on a place — this launch has a route and froze the ledger under the same launch, the plan still reads under this launch's route, the Book may be transmitted — and one that fails is blocked before dispatch with the reason at once, never read as waiting. Any other is recorded as always and waits: the Run's last state stays `authorized`, read as `等待运行名额` (V2-UX-CONC-007) by ②A, the Task Drawer, the 任务 panel and 待我处理, whose `取消` ends it before anything is sent. As a place frees the governor takes, in order, a stopped Run whose cancellation waited for one, a Run the editor answered, and then the waiting starts in the order they were made; a start this launch cannot admit is blocked before dispatch with the owner's reason. Nothing starts by itself after a restart (ADR 0034): at start, a Run a stopped service left `authorized` never began, and it is blocked before dispatch with 「AI7 上次关闭时这项任务还在等待运行名额，没有开始……需要时请重新准备并开始。」, for the editor to start again when they choose. A Review Run's categories each take a place, waiting for one while every place is taken. A quick start, a Review Run's approval and `续行` still wait with their reasons rather than queue. Main also accepts J-10's `--j10-unit-hold-path` under `AI7_E2E_JOURNEY=J-09`, beside the J-04 model adapter.

知识库 › 审阅规范文件 (Issue #427, S79a) is schema revision 45, on service protocol version 69. `src/service/review-guidelines.ts` owns one append-only relation, `review_guideline_versions`: each row is a version a house imported of one guideline document the review categories apply, chained to the version before it by that version's digest — version 2 to the built-in document's, as that import recorded it, so a later build that rewords a built-in clause leaves the chain readable — with its numbered clauses, the file it was read from (name, Word or text, digest, bytes) and the issuer `本社`. AI7's built-in first versions stay configuration and are never stored. Only a document whose categories hand its clauses to the model through the Editorial Review Contract takes a house version; 线索条款 (情节逻辑, model-free leads) and 事实核查契约 (事实核查, AI7's fixed factual-review kind) are AI7's fixed statements, which the projection marks `use` and the preview refuses. `previewReviewGuidelineVersion` reads a picked `.docx` (through the manuscript parser's blocks) or UTF-8 `.txt`/`.md`: a paragraph that opens with `1.` or `第1条` begins a clause and the paragraphs after it continue it, numbered 1 to 40 without a gap in half- or full-width digits, each clause at most 300 characters; Word's automatic list numbering is not in the paragraph text, and the refusal says to write the numbers out. The preview is held in the service until `importReviewGuidelineVersion` confirms it. A Review Run prepared afterwards applies each document's latest version and snapshots it, and a Task prepared under an earlier version's prompt contract is never reused: the ledger treats a prepared Task whose plan froze another prompt contract of its kind as a new Task Intent. `inspectReviewGuidelines` reads every Book's Review Runs to say which version each used — a category that formed its findings, never one only prepared, refused or failed — how many findings cite each clause, a finding found again by a later review of its Book counted once, and which Books' latest review is on an older version; it counts the reviews and the Books, and names only the latest five reviews of a version and the first ten Books by title, so the answer stays within one service frame. Under developer-live, a category that applies a house version is unavailable on the 新建审阅 sheet and refused at its turn before anything is authorized or dispatched: the house's clauses would enter the category's prompt, and Provider Processing v5 admits only public or synthetic text (an Owner reading). Main also accepts `--j15-picker-path <absolute path>` under `AI7_E2E_JOURNEY=J-15`.

知识库 › 范例 (Issue #427, S79b) is service protocol version 70, with no schema revision. `inspectExemplars` in `src/service/exemplars.ts` reads a page of the Books with a 发稿版本 — twenty by title, after a `{ title, bookId }` cursor as 书库 pages — each record through its owner, verified as 交付物 and 图书交付包 read it: the Books and every designation from `publication-versions.ts` (`designatedBooks`, `history`), with when a 撤回 came to hold each from `maintenance-cases.ts` (`withdrawnAt`), and every Delivery Record of each house type's document from `production-documents.ts` (`deliveryReadings`). A delivery made while the Book's newest designation then stood unwithdrawn comes in as it is delivered; one made before the first designation, or after a 撤回 held the designation in force (ADR 0040), comes in at the next designation, and waits until there is one. Each document's latest delivery that came in names the exemplar's exact version, the versions delivered before it are counted with the latest ten named, and its Learning Eligibility is `仅本社`, the default, asked of no one. The Book's card names its latest designation by ordinal and time and says when a 撤回 holds it. It is a read over append-only records and writes nothing; the Book's attribution is its current 作者 and 责编 (an Owner reading).

知识库 › 工序与规则's expert 工序 (Issue #427, S79d) is service protocol version 71, with no schema revision. `inspectKnowledgeProcedures` in `src/service/knowledge-procedures.ts` lists the 工序 each review category runs, as the configuration applies now — its title, version, the category it serves, `已启用` or, for a category whose basis does not exist yet, `尚未接通` with a reason said of the house — with how many approved Review Runs applied that version of it, read from their snapshots; and the native artifact in the house's words (editor-surfaces §10), `本社方案 vN` — N the newest AI7 权限侧车 revision the installed 方案 offers — in the lifecycle `EditorialWorkspaceProfileStore.house()` reads as the Book card does (可获取 · 尚未安装, 已安装, or 不可用 · 需要处理 when its retained carrier is missing or altered), with how many Books enabled it. The carrier's identity, version and SHA-256, the 权限侧车's identity and digest, and each 工序's id are the Technical Identity Layer's, under 查看技术详情. It writes nothing.

知识库 › 资料库 (Issue #427, S79c) is service protocol version 72 and schema revision 46, which adds two append-only relations owned by `src/service/library-materials.ts`: `library_materials`, one arrival record per item, and `library_material_decisions`, one chain per item of the editor's attribution and Learning Eligibility decisions, each chained by digest to the one before and the first to the arrival record. `previewLibraryMaterial` reads the file main's picker returned — its format from its content, its size and digest — and keeps nothing; `addLibraryMaterial` copies it whole into `library-objects/sha256/` inside the Agent Data Root, refused when it changed since the preview, and records it with the title and kind the editor gave it; `decideLibraryMaterial` appends one decision, refused when the chain moved since it was read. `previewLibraryMaterial` and `addLibraryMaterial` have main's long request budget, since they read a file of up to 1 GiB whole; the copy is written aside, synced and digested once before it takes its name, and at open the store removes a copy left aside and any kept original whose arrival was never recorded. No answer is read whole: `inspectLibraryMaterials` answers twenty items at a time, newest first, after a `{ recordedAt, materialId }` cursor; `inspectLibraryMaterial` reads one item — the one 待我处理 opens beyond the first page; an arrival and a decision answer with their one item; each item carries its decision count and its latest ten decisions; and 定归属 names its Books from 书库's own `listBooks` pages. A web page is recognized by its opening past leading comments read in one pass, and a Word file by an entry named under `word/`. Nothing is inferred: an item waits for its attribution (one Book or the house; a Series once Series exist) and then its Learning Eligibility, decided under that attribution — changing the attribution sets the eligibility aside — and a Task may list it under 允许参考 once both stand. Until then it is an item of 待我处理's 等待你的决定. The five-layer index is not built here.

②C 评估 (Issue #429, S81a) is service protocol version 73 and schema revision 47, which adds two append-only relations owned by `src/service/evaluation-records.ts`: `evaluation_records`, one per version of a Book's Evaluation Record, bound to the primary manuscript's current revision and snapshotting the Evaluation Profile, and `evaluation_record_entries`, one chain per version of the editor's whole content at each save, the last one `finalized` at 定稿. The built-in profile (`AI7 内置默认`, 第 1 版) scores five items of 20 each; the arithmetic — whole and half points, bands by each item's share, a total that leaves `不评` out with its 满分, and `推荐出版` waiting on every unreviewed `高` risk — is `src/shared/evaluation-scoring.ts`, shared with the page. `inspectEvaluation` and `saveEvaluation` are the route's Book's; a save is refused when the version moved since it was read, is already 定稿 or would record nothing new; `startEvaluation` begins version 1 or, after 定稿, the next from it — its scores, words and each risk's level and statement, never its conclusion, and every risk unreviewed, since a person's review of the old text does not count for the changed one (EVAL-004, EVAL-012) — compared with it item by item. 评估 is reached from the manuscript's `工作` group between 审阅 and 交付物, leaving the manuscript as they do, and from 工作概览's one line. AI7's own 初评, the market block and 审稿意见 come with the next S81 slices.

②A 分析反馈 (Issue #94, S38) is service protocol version 74 and schema revision 48, which adds one append-only relation owned by `src/service/analysis-feedback.ts`: `analysis_feedback_signals`, one Quality Signal per explicit judgment an editor records of one item of a baseline Result Set Revision — its synopsis, or one entry of 人物与名称, 事件, 关系 or 设定 — chained per item, each after the first naming the one it succeeds. A signal binds the revision by its identity and digest, the item by its place and the digest of exactly what it says, and the evidence the item cites, its reading ranges and exact source ranges; it records the judgment, `准确`, `不准确` or `不完整`, an optional reason — one of two or three fitted to the item's kind and the judgment, or `其他` in the editor's own words — and an optional correction. It names the analysis kind the ledger stores the Result Set under, `baseline-manuscript-analysis`, which every read checks. ②A holds its follow-up draws while the editor's focus, or text an input method is still composing, is inside an open card, and the card keeps only committed text as the editor's draft. It names the revision by identity rather than by a foreign key into the analysis ledger, whose relations later revisions may rebuild. The vocabulary is `src/shared/analysis-feedback.ts`, shared with the page. `recordAnalysisFeedback` is refused when the item no longer says what the editor saw, when its latest judgment moved since it was read, when a reason is not one offered for that judgment, or when it would change nothing. `inspectAnalysisFeedback` answers a revision's items with their latest judgments, and the Book's Analysis Quality Metric, `ai7.analysis-quality-metric/1`: each judged item's latest judgment once, over every revision of the Book, by dimension, with the digest of exactly the signals it counted. Silence is no approval: an item nobody judged has no signal and is not counted. A signal or the metric grants no factual truth, Learning Eligibility, policy, artifact change, source scope or Effect, raises no 待我处理 item, and no other Book's signals enter a Book's metric. Main admits `--j04-model-adapter` under `AI7_E2E_JOURNEY=J-11` too, beside `--j11-picker-path`, so J-11's baseline runs on the fixture adapter.

The reason a Proposal Decision is asked for (Issue #61, S26a) is service protocol version 75 and schema revision 49, which adds one append-only relation owned by `src/service/decision-feedback.ts`: `proposal_decision_feedback`, chained per decision — the editor's `不说明`, which records only that no reason was given, and each later change of the reason, a successor that keeps the reason it replaced. The first reason stays where revision 22 keeps it, in `proposal_decision_reasons`. A card's decision now says whether its reason was given, dismissed or neither, how many entries follow it and when it was last changed. `recordProposalDecisionFeedback` is refused when the entries moved since they were read, when there is a reason to dismiss or none to change, when it would change nothing, or when the decision is no longer the mark's current one. The Mark Card asks once, right after a decision recorded without a reason — `拒绝`, `修改后接受` with `为什么这样改` left empty, or `接受并应用`, on the card or from the right-click menu — and leaving the card ends the prompt without a record; an Apply whose acknowledgement was lost is not asked about, since the card reopens from its record; reopening it, or a restart, never asks again, and nothing enters 待我处理.

质量与学习 › 学习准入 (Issue #61, S26b) is service protocol version 76 and schema revision 50, which adds one append-only relation owned by `src/service/learning-eligibility.ts`: `learning_eligibility_decisions`, one chain per Learning Material of a Book, each decision bound to the exact version of the material — its digest — with its choice (`book`, `house`, `excluded` or `deferred`), an optional note, the basis — the policy's identity, mode and version with its plain words, so a later policy adds a basis rather than making earlier decisions unreadable — and the Book's 作者 and 责编 as they stood. The material is identified quietly, never stored, from the records that own it: each 修改建议's current decision that carries the editor's reason or their own wording, each analysis item whose latest judgment says why, and each 审阅 finding the editor last ignored with a reason. The Learning Eligibility Policy is recommendation-only, so every material waits for the editor; a material that changes after its decision is decided again and the decision it had stays on record. `inspectLearningMaterials` answers every Book with material, or one Book, forty materials at a time — Books by title, a Book's materials by kind and then by when each came to be, so a later reason never moves one — after a `{ bookTitle, bookId, orderedAt, materialKey }` cursor, each Book naming how many materials it has in all; `inspectLearningMaterial` reads one material as its card does; `decideLearningMaterial` answers with the one material it decided, and is refused when the material is gone, changed since it was read, its decisions moved, or the decision would change nothing. Every frame checks a material's place by its kind — a 修改建议's decision, an analysis item, or a 审阅 finding's `rvf_` identity. A Book whose material waits is one item of 待我处理's 等待你的决定, `学习准入待处理`, or `学习准入待处理 · 稍后决定` once only deferred material remains. A decision permits later learning signals within its scope and nothing else: no memory, no wider Run sources, no transmission, and no change to the record it came from.

质量与学习 › 反馈历史 (Issue #61, S26c) is service protocol version 77, with no schema revision. `inspectFeedbackHistory` reads, newest first, every 修改建议's current decision with its reason as it stands (given, `不说明`, or none), every analysis item's latest judgment with its reason and correction, and every 审阅 finding's latest `忽略` with its reason. Each comes with the Book it belongs to, its Editorial Dimension where it has one — a 修改建议 a 审阅 category made names that category — and where it opens: the manuscript at the mark, unless the mark's paragraph is gone from the manuscript; ②A on the revision judged, at the item and on its tab; or ②B at the finding. One answer carries at most 300 entries and at most 256 KiB with the Books and people they bring, always the newest, and says when older ones were left out; a reason stands whole up to 200 graphemes, and beyond that as its opening and `…` (Issue #61 review). Each entry is attributed to the version of the Book's 作者 and 责编 in force when it was given, or to the first version saved after it; each Book comes with its current people and every version its entries are attributed to, and the page filters by those. It writes nothing and asks nothing.

设置 › 评估校准与预测 (Issue #430, S82) is service protocol version 78 and schema revision 51, which adds two append-only relations owned by `src/service/evaluation-calibration.ts`: `publication_actuals`, one chain per Book of the 定价与首印 the editor entered — the price in 分 and the first print run in copies, each entry bound to the 发稿版本 current when it was entered and superseding the one before — and `evaluation_preferences`, one chain for the house of its two switches, 校准 and 定价与首印预测, each change recording the thresholds it was made under. AI7 predicts neither price nor print run unless the editor turns the prediction on: `recordPublicationActuals` takes the editor's numbers for the Book's current 发稿版本, naming the one the page listed, and is refused while the Book has none, when a newer 发稿版本 was designated since the page read it (`ACTUALS_MOVED`, Issue #430 review), when the Book's entries moved since they were read, when a number is not a whole positive amount, or when the same numbers already stand for that 发稿版本; a newer 发稿版本 starts 尚未录入 again. `setEvaluationPreferences` refuses to open the prediction until 30 published Books carry actuals, and a prediction already open can always be closed. Calibration starts on and applies only after 10 of the editor's adjustments of AI7's 初评 scores, which do not exist until AI7's 初评 does, so the count is zero and the answer says 初评 is not connected, which is what the page's waiting note follows; it never changes the editor's score or a risk item. The page reads a Chinese input method's full-width digits and point, and its 。, as the number meant. `inspectEvaluationCalibration` answers both switches with their progress and every Book with a 发稿版本 with its entries, and 交付物's `录入定价与首印` line now reads `尚未录入` or `已录入` with the numbers for the current 发稿版本.

书系 (Issue #63, S28a) is service protocol version 79 and schema revision 52. It adds two append-only relations owned by `src/service/series.ts`:
- `series`: the house's Series, each with a name unique however it is spaced or cased, and an optional 说明;
- `series_membership_changes`: one chain per Series and Book, alternating `add` and `remove` by its ordinal. Each record keeps the four-part impact the editor saw, with its digest.

`previewSeriesMembershipChange` answers the Series Membership Impact Preview for one exact Book and Series and records nothing. Its groups are, in order:
- 未来任务: only later explicit Series-scope selection changes;
- 已授权或正在运行: no Task kind can yet name a Series in its scope, so none does;
- 书系知识与学习: the Book's Learning Material, by count, which membership neither activates nor deletes;
- 历史记录: one change record is added.

`changeSeriesMembership` names that preview's digest and recomputes it inside its transaction. It is refused when the membership or a governing record moved (`SERIES_PREVIEW_STALE`), or when the Book already is, or is not, a member.

Membership creates no Task, Run Authorization, source scope, Learning Eligibility, Provider transmission or cross-Book mutation. `inspectSeriesList`, `inspectSeries` and `inspectBookSeries` read the Series and each Book's side of it, every list a page at a time (Issue #63 review): 书系 by name, 50 to a page; a Series' members newest joined first, 50 to a page, so a Book just added heads the first; its change records newest first, 20 to a page; and 加入书系…'s Books — every Book the Series does not hold — by title, 50 to a page, narrowed by `查找书名`. `inspectSeriesMembers`, `inspectSeriesCandidates` and `inspectSeriesHistory` read the pages after the first, each starting after one item of its own list. No page weighs more than 96 KiB, and every page carries its first item. `createSeries` answers the new Series alone and `changeSeriesMembership` the record it made; a Book's side names at most 50 of its Series, with how many in all. 书库's search gains the field `series`. For a member Book, the 书系一致性 category names its Series while it waits for Series Knowledge to reach review; for a Book in no Series it names both conditions, a Series and Series Knowledge in review.

书系知识 (Issue #63, S28b) is service protocol version 80 and schema revision 53. It adds four append-only relations owned by `src/service/series-knowledge.ts`:
- `series_knowledge_candidates`: each candidate's versions;
- `series_knowledge_items`: the items, each with its name and one of eight knowledge classes;
- `series_knowledge_revisions`: their immutable revisions, each keeping its words, authorship or provenance, any conflicts kept, and its use;
- `series_knowledge_promotions`: the promotion decisions.

`proposeSeriesKnowledge` records a candidate for a new or an exact existing item. It is either in the editor's own words, or the exact span of a member Book's working manuscript, verified as a mark's range is: from the window that holds that manuscript's capability, and refused for a Book the Series does not hold.

`inspectSeriesKnowledgeReview` answers 书系知识纳入审阅. Conflicts are found by identity only, and each line names the item or candidate and its version, never its words (Issue #63 review); a review lists at most 50 and says how many there are:
- an item of the same name;
- a revision appended since an existing item was read;
- another open candidate for the same item or name.

`editSeriesKnowledgeCandidate` appends the next version, re-reading an existing item at its current revision. `promoteSeriesKnowledge` names the review's digest. It is refused:
- when the candidate, the item, a conflict or the source Book's membership moved;
- while a conflict is not kept by `保留已披露冲突`;
- without a use.

A promotion creates the item with its first revision, or appends the next revision. It creates no Run Source Scope, performs no retrieval and permits no transmission. The manuscript's selection menu offers a member Book's words to each Series it is in; a Production Document offers nothing of 书系. 移出书系's preview names the items taken from the Book, and the Book's open candidates it holds back until the Book rejoins; 加入书系's says they can be reviewed again.

Nothing of 书系知识 is read whole (Issue #63 review). The Series answer carries the first page of items, by name, each with its current revision only and how many revisions it holds, and the first page of open candidates, oldest proposed first, with the counts. `inspectSeriesKnowledgeItems` reads further pages of items, narrowed to names holding the words `查找条目` gives; `inspectSeriesKnowledgeCandidates` further candidates; and `inspectSeriesKnowledgeRevisions` an item's 历次版本, ten at a time, newest first. Each page is bounded by count and by 64 KiB. A cited passage is shown whole up to 200 graphemes and beyond that as its opening, the record keeping all of it; one cited while changes waited in the journal beyond its revision says so. `proposeSeriesKnowledge` answers with the candidate alone and `promoteSeriesKnowledge` with the item alone, and the page reads the Series again.

数据版本 (Issue #433, S85a) is service protocol version 81 and schema revision 54. It adds one append-only relation owned by `src/service/data-version.ts`: `store_versions`, which records the software version, the Data Version and the schema revision that opened the store, whenever one of them changes. The software version comes from the `package.json` the product ships in, SemVer build metadata included. The built carrier holds no package manifest, so the service entry reads it from the source checkout that contains `dist/` and hands it to the store. A service that stops before it is ready writes `AI7_SERVICE_STOPPED/<code>` to stderr: the refusal's code, never its words (Issue #433 review).

The Data Version (ADR 0079 §1) is `1`, and it is not frozen: Data Version 1 is set at the first packaged release, and until then development stores are disposable. A breaking migration, one older software could not read, would raise it. An additive schema revision stays inside it.

`inspectDataVersion` answers 设置 › 数据与存储's 版本:
- the software version and the Data Version, apart;
- whether the Data Version is frozen;
- the latest software change, and whether it kept the Data Version. Versions order by SemVer precedence, so an older build opening the data again reads `改用较早的软件：从 … 改为 …`, never an update (Issue #433 review);
- the version records, newest first.

A version record rewritten by hand stops the store from opening. Both opening and inspection stream and validate the full chain, retaining only the current record, latest software transition and newest twenty records; an older software transition remains visible even after later schema-only records. Numeric prerelease identifiers are compared exactly, including values beyond JavaScript's safe-integer range. The backup before a breaking upgrade, its rollback, and the package format they share with 导出数据库 come with S85b and S86 (#434).

导出数据库 (Issue #434, S86a) is service protocol version 82 and schema revision 55. `src/service/database-exports.ts` owns three append-only relations: each export's preparation, approval and receipt. They are ledgers like the export ledger's, and every read verifies them.

The package, `ai7.database-package/1`, is a ZIP of:
- `store/ai7.sqlite`, a copy of the store made with SQLite's online backup, 256 pages a step (Issue #434 review). The service answers other requests between two steps, and a stop is honoured at the next. A write made meanwhile lands in the copy too, so the manifest counts what the package holds from the finished copy itself, which stands alone with a rollback journal;
- every other file under the Agent Data Root, except the live store, `export-staging/` and `shell/`;
- `manifest.json`, last: the Data Version, the software version and schema revision, when and why it was made, what it holds, and each member's size and digest.

It is not encrypted (ADR 0079 §1.6). It holds no Model Service credential, because those live in the platform's protected store. fflate writes no ZIP64, so a package over 4 GB or over 65,534 files is refused with the reason. The walk of the Agent Data Root reads one directory at a time, and refuses as soon as the names it holds could not fit, or it has visited more than 65,534 directories. What the manifest says the package holds is counted in the same step as the store's copy is made, so no other request writes in between.

`导出数据库…` opens the system's Save dialog; J-12 answers it with `--j12-save-path`. Choosing the file stages the package in `export-staging/` and records the preparation. `按上述方式导出` writes exactly that package with the export ledger's atomic writer, which now also copies a staged file, and records the receipt. The export runs through External Export Policy v2 as its own target kind, `database-export-package`, and both steps are refused (`EXPORT_POLICY_UNAVAILABLE`) at a launch whose policy was not verified, before anything is staged (Issue #434 review). Neither runs inside a request (Issue #434 review, V2-UX-EXP-011). Each starts the export's one house-wide activity and answers at once. `inspectDatabaseExports` then reads it: the step (`copying`, the bytes of the store's pages copied, then `packing`, `verifying`, `writing` or `committing`) and the bytes read of how many (Issue #434 review). The window holds its controls until an approval's records are read, so no other export starts meanwhile. `cancelDatabaseExport`, 取消导出, stops it until the file is being put in place: a stop asked for while the destination is checked is honoured at the last point before the commit (Issue #434 review). A preparation stopped leaves no package, copy or record. An approval reads the prepared file whole before it records the approval, so a stop there leaves the preparation as it was. A stop while the file is written ends with the receipt `EXPORT_CANCELLED` and the destination as it was. Only one export runs at a time (`DATABASE_EXPORT_BUSY`), and the service stops the one under way at shutdown. A staged package no approval takes is swept when the store next opens, and every entry of the package carries the one fixed archive time, the export's own time being the manifest's `createdAt`. The backups of S86b and S85b will be the same package.

定期自动备份 (Issue #434, S86b) is service protocol version 83 and schema revision 56. `src/service/scheduled-backups.ts` owns three append-only relations: the switch's changes (chained), each backup made (its file's name, size and digest), and each backup removed. The switch is off by default.

While the switch is on, the running service writes the same database package into the fixed backup location beside the Agent Data Root, `<data root>-backups`, once a day:
- the service's background check runs at start, then hourly, and a backup is due when none was made in the day before;
- turning the switch on starts that check at once: the request only records the switch, and the section reads again until the backup is there;
- each backup is written as a `.partial` file and put in place by the create-only publication every export uses, a hard link that fails when the name is taken. So a file that appeared at the name while the package was written is left as it is, and no backup is recorded (`SCHEDULED_BACKUP_EXISTS`). A volume without hard links cannot hold a backup, and the section says the location is unavailable. If the record cannot then be written, the placed file is removed;
- what the record says the backup holds is counted with the store's copy it carries.

Each check first removes what a cut-off check left (`.<uuid>.ai7db.partial` and `.partial.store`), then the backups older than fourteen days, each on its own, and only then writes. So a backup that cannot be written never keeps the space of those whose days passed.

A file is removed only while it is still the one AI7 made:
- its name has the one form `AI7 自动备份 YYYY-MM-DD HH-MM-SS.ai7db`, which the relation checks and every read requires;
- it is a regular file, not a link;
- its size and SHA-256 are the recorded ones.

A file found gone is recorded as `missing`. A file at the name that is another is left where it is and recorded as `changed`. Turning the switch off makes no more backups; those kept stay until their fourteen days pass.

Every read of the three ledgers is a stream, verified row by row, that holds no more than it answers (Issue #434 review). The section lists the twenty newest backups kept and counts the rest. A check reads the backups whose days passed sixteen at a time, oldest first, each turn after the last one read.

At shutdown the service stops a check under way before the store closes: the write stops at its next chunk and removes what it wrote. A backup the check could not make is stated in the section, with its reason by the refusal's code, until one is made or the switch is turned off. No External Export Policy approval is involved: the switch is the decision (ADR 0079 §1.7), and nothing is written anywhere but that location.

导入数据库 and 替换本机全部数据 (Issue #434, S86c) are service protocol version 84 and schema revision 57. `src/service/database-package-reader.ts` reads a package through its ZIP central directory, member by member, and refuses it before anything is taken unless:
- the manifest has its exact shape;
- every member path is relative, inside the Agent Data Root, outside `store/` (but for the store's copy), `shell/` and `export-staging/`, compared without regard to case, and a name Windows writes as named;
- the members are exactly the ZIP's entries, and each one's size and SHA-256 match.

The reader holds every entry to the size its central directory declares (Issue #434 review). Deflated data is inflated 16 KiB of input at a time. An entry that would come to more than it declares, or ends short of it, is damage, refused before any byte past the declared size is kept or handed on. A stored entry's two sizes must be one. A central directory over 16 MiB is refused before it is read.

`导入数据库…` opens the system's Open dialog; J-12 answers it with `--j12-picker-path`. The preview states the file, its origin, its Data Version against this AI7's, what it holds and that every member was verified. A package from a newer Data Version, or of a schema revision this AI7 does not know, is previewed and refused.

`src/service/database-replacement.ts` owns the replacement. The running service cannot replace the files its store holds open, so a replacement is prepared while it runs and applied when the store next opens:
- **Prepare** extracts the package again, verified, into `<data root>-replacing/incoming`, and writes the members it verified to `members.json`. It then writes the data as it is into the backup location as the same package, origin `pre-replace-backup`, named `AI7 替换前备份 <date time>.ai7db` and kept until the editor deletes it. The backup is put in place by the create-only publication every export uses, so a file that appeared at its name is left as it is and nothing is prepared. It writes `intent.json` last, naming the digest of `members.json`.
- **Prepared only while nothing else writes (Issue #434 review):**
  - Preparing is refused (`DATABASE_REPLACEMENT_BUSY`) while a Run executes, waits for a place or finishes its cancellation, a Review Run is driven, a job is under way, or a database export writes.
  - Main refuses it (`DATABASE_REPLACEMENT_UNSAVED`) while any window holds changes not yet saved.
  - Once it waits, the service takes nothing that writes until AI7 starts again: reads, a search, the way to a Book and `取消替换` only. Every other operation is refused as `DATABASE_REPLACEMENT_WAITING`, one added later included, because `replacement-gate.ts` names what is taken.
  - Reconnect Preflight admits and blocks no waiting Run and 定期自动备份 makes no backup meanwhile. So nothing is changed that the replacement would lose, and `取消替换` lets the data be written again.
  - The freeze on admission begins when the replacement (or 回退) is asked for, before its backup is written, not only once it waits (`replacementFrozen`). A preflight already under way checks it again after it has read the credential, and before it admits or blocks each Run (Issue #434 review).
- **Apply**, in `EditorialStore.open`, first verifies `incoming/` again against `members.json`: every member it names, of its size and SHA-256, and nothing else. The list is read only within the bound a package manifest has. When what waits was emptied or changed since, nothing moves: the phase is `refused`, and the replacement is recorded as failed with `failure: 'changed'` (「准备好的文件已不完整或被改动」). Otherwise it moves the data root's entries into `previous/`, moves the package's in, and opens the store. `shell/` and `export-staging/` stay where they are. Each step's phase is written before the step, and each step can be repeated, so an interrupted apply resumes where it stopped.
- **Resumed apply (Issue #434 review):** an apply resumed while it moved the data aside or the package in verifies what waits again first, each member where it now stands: in `incoming/`, or already moved into the Agent Data Root, and once.
  - What waited changed, or its intent no longer reads: `refused.json` is written and the data goes back. That note is read only as a file of at most 64 bytes; one that is not AI7's still records a refusal (`changed`). What came in goes to `discarded/` and the data moved aside comes back, so the data opens as it was.
  - The replacement is recorded as failed with `failure: 'changed'`.
- **Resumed at its first open (Issue #434 review):** an apply resumed at `opening` opens the data only when it is still exactly what was verified. Otherwise the open was interrupted: SQLite leaves its journals beside the store as soon as it opens, so nothing tells an interrupted open's changes from any other. The data goes back as it was, and the replacement is recorded as failed with `failure: 'interrupted'` (「上次启动时打开替换来的数据被中断」).
- **Failure:** a store that will not open is moved into `discarded/`, and the data from `previous/` is moved back.
- **Recording:** `database_replacements` records the replacement in the data that opened: applied, in the data it brought in, or failed, in the data it spared, the failure's reason (`unopenable` or `changed`) in its canonical record only. Only then is the staging place removed. The ledger is read as a stream, verified row by row, holding the twenty newest and counting the rest.

`回退到替换前的数据` prepares the same replacement from the latest replacement's backup, and backs the data up first as well. `现在关闭 AI7` closes the application, unless a window holds changes not yet saved.

The backup before a replacement and the 定期自动备份 check write into the same backup location, with the same `.<uuid>.ai7db.partial` files, so they never write at once. The backup waits for a check under way, and no check starts until it is written. A check's sweep therefore never takes a file the backup is making, and still removes one a cut-off backup left.



Delivery Records (Issue #415, S66b) are schema revision 38, on service protocol version 56. `production_document_deliveries` is one more append-only relation in `src/service/production-document-ledger.ts`: each row is `第 N 次交付` of one document, names one saved version by its revision and digest, and records the recipient, from the house's list (宣传部, 编辑部, 外部媒体, 其他) or in the editor's own words, and an optional note. Its canonical record and digest are verified on every read. `交付` names a saved version, or the current text: bound to the working digest the form read, it is saved as the next version (origin `delivery`) in the transaction that records the delivery, and the recipient and note are checked before anything is saved. A delivery sends nothing. The export of a delivered version goes through the same export ledger under target kind `production-document-version`, with the revision columns left empty and the document's identity and version digest in the canonical record. A Delivery Record reads its file from the newest export of its version approved after it and before the document's next delivery that wrote its file, or else from the newest attempt, so a failed or unconfirmed re-export never hides the file handed over. `交付后有修改` is an edit after a delivery: the document was delivered and its working digest is no delivered version's, so delivering an earlier saved version raises nothing. Production Documents moved out of the 交付物 answer into a read of their own, `inspectProductionDocuments`: with their versions and Delivery Records beside the Manuscript's milestones and designations, the widest answer would not fit one service frame. The documents' commands answer with that read, and `recordProductionDocumentDelivery` is the one new command.

The 图书交付包 (Issue #416, S67a) is schema revision 39, on service protocol version 57. `src/service/book-delivery-packages.ts` owns `book_delivery_package_versions`, append-only: each row is one frozen version of a Book's package, with a stable package identity, its version, the version before it, its purpose, the Publication Version it names, the digest of its content, and the canonical record with its digest. The package reads three things the other owners hold. The current Publication Version comes from `publication-versions.ts` `current`. Each house type's decision and its document's Delivery Records come from `production-documents.ts` `packageReadings`. The Review Runs' states and newest report versions come from `review-runs.ts` `packageReadings`. From them it derives the condition table and the content: the Publication Version, each type's latest delivered version or 本书不做, the latest report of every Run that ran, the fixed exclusions and the limitations. It names the content by its digest. `准备图书交付包` sends that digest back; the service refuses content that moved and refuses while a condition is unmet. The same content and purpose as the newest version records nothing; anything else appends the next version. A running Review Run, or one that ran and has no report, is unmet; a Run never authorized is left out with a limitation, so nothing blocks the package for good. The package is a read of its own, `inspectBookDeliveryPackage`, beside 交付物's and 交付 · 生产文档's; `prepareBookDeliveryPackage` is its one command. It writes no file: the export of a version is S67b's.

The Deliverable Workflow of a Production Document (Issue #415, S66c) is schema revision 40, on service protocol version 61. `src/service/production-document-workflow.ts` owns two append-only relations. `production_document_workflow_instances` pins the workflow profile each document follows, one row per document, recorded with the document; a revision-39 store gains one for every document it holds, activated when that document was made. `production_document_phase_transitions` records every move of every phase, in order, with its prior state, its new one and a skip's or a reopen's reason. Both carry their canonical records and digests, verified on every read. The profile is the built-in 基础书稿编辑流程 2.0.0: seven shared phases (接收与准备, 来源建设, 起草, 审阅与核查, 定稿, 交付, 维护) and no gates, which wait for a profile that defines them (S66d). Only the editor's four commands move a phase: 开始 (未开始 → 进行中), 完成 (进行中 or 已重新打开 → 已完成), 跳过 (未开始, 进行中 or 已重新打开 → 已跳过) and 重新打开 (已完成 or 已跳过 → 已重新打开). The last two take a reason from a set shown unselected, or 自行输入 in the editor's own words. `transitionProductionDocumentPhase` names the count of moves the editor saw, and a move against another count is refused. No Task, Run, model outcome or Effect moves a phase. An open phase reads `等待你处理` only from the document's own facts, never recorded: 审阅与核查 while the document has open 修改建议, and 交付 while its text is no version, no version was delivered, or it moved after a delivery. The lens reads the document again once the editor rests after an edit or a mark change, so what a phase waits on follows the editor's work with no phase moving; a read older than a move never paints over it. 交付物's one read carries each phase's latest move with its reason and how many moves it had, to stay one frame; every earlier move stays recorded.

The export of a 图书交付包 version (Issue #416, S67b) is schema revision 41, on service protocol version 62. `src/service/book-delivery-package-exports.ts` owns two append-only relations. `book_delivery_package_exports` holds each export: the exact package version it wrote, the folder as the dialog returned it, how many files, and the digest of the review it was prepared from. `book_delivery_package_export_files` links each file, in order, to its preparation in the export ledger. Both carry canonical records and digests, verified on every read. A version writes, in order, the Publication Version's revision, each included document's delivered version and each report the package froze, all as DOCX — with 批注 and 修改建议 unless the editor turns one off, and never 备注 — and then `交付包清单.md`. Every file is an ordinary export of `manuscript-export.ts`, with its own preparation, approval and receipt. The 交付包清单 is a target of its own there, `book-delivery-package-version` in Markdown, written from the version's record alone: its purpose, when it was prepared, its content digest, what it holds, each document's Delivery Records, what it leaves out and its limitations. Only preparing and approving write it; reading a preparation verifies the list's digest from its own record, so a later release's words cannot make a recorded export unreadable. `reviewBookDeliveryPackageExport` takes the two switches and lists the files, each with its own Export Fidelity Review and forty at most, and binds them and the switches by one digest. `chooseBookDeliveryPackageExportFolder` asks main's folder dialog, which may make a new folder, and then the service's prepare. That prepare refuses a review that moved and a folder that already holds any of the file names, so an export never replaces a file. It then records one preparation per file and the export that links them. `approveBookDeliveryPackageExport` first checks every file as its approval would — the payload written again, and the destination — and refuses the set whole on any drift, naming the file and writing nothing. It then approves and writes the files one after another. A file that fails, cannot be confirmed or is refused during the writes stops the rest: the written ones stand with their receipts, the rest read `未写入`, and the answer names the file that stopped them and why. An export already approved answers with what it came to and never writes again. Each version's Package Export History lists its approved exports newest first, two of them in one read, with `已导出 N 次`. A folder chosen and never approved is no export. 交付物's export records and a Delivery Record's file never list a package's files, and the package itself never changes. Main accepts `--j07-folder-path <absolute path>` under `AI7_E2E_JOURNEY=J-07` beside `--j07-save-path`, and never with `developer-live`: it answers the folder dialog of that launch once, in place of the platform's own.

DOCX export (Issue #413) is schema revision 29, owned by `src/service/manuscript-export.ts`, on service protocol version 42. `src/service/docx-export.ts` is the pure writer, identity `ai7-docx-export/1`, and the same input always gives it the same bytes. For a revision whose blocks map to a retained original through `manuscript_block_sources`, it walks the original `word/document.xml` counting paragraphs as the parser does: a paragraph that maps to an unedited block carrying no exported mark is restored, stripped to the reading the import took, an edited or marked one is written again from its source paragraph and first-run properties, every other part is copied, and the file's comment parts give way to its own; a revision without a mapping is written fresh from its blocks and every class the file carried reads `无法导出`. Pending 修改建议 become `w:del` and `w:ins` by their author, 批注 become comments with their replies and done, and 备注 become comments by `备注` only when included; highlights never leave. The revision adds three append-only relations with canonical JSON and digests — `export_preparations`, `export_approvals` and `export_receipts`, generic over External Export Policy v2's target kinds — and a revision-28 store gains them and moves nothing else. A review of the current revision first saves a dirty working state as a checkpoint with purpose `Export Input / 导出输入`; a preparation binds exactly the review read and a destination outside the Agent Data Root, bounded at 1,024 code units. Approval writes the payload again and refuses if it or the destination drifted. It stages the file beside its destination under a name bounded to 255 UTF-8 bytes, syncs and verifies it, publishes it under the chosen name, and verifies again. A `create` takes the name with a hard link, which fails if a file appeared there meanwhile; on a volume with no hard links (FAT32 and exFAT report `EISDIR`) a `create` is refused as `EXPORT_CREATE_UNSUPPORTED`, and nothing changes. A `replace` records the size and digest of the file the dialog resolved in the canonical preparation record, checks it at approval and again just before the rename, and refuses a file put there since as `EXPORT_TARGET_CHANGED`. A regenerated paragraph keeps its bookmarks, so a table of contents still links to it. A write that changed nothing is `未能导出` — a failed rename is that even when the untouched file already held the payload — and an unverifiable one, or an approval without a receipt, is `结果待确认` and is never retried. The review, preparation and approval run under the service client's long timeout. The service reviews, prepares and approves only under this launch's verified External Export Policy v2, which the launch policy reports as `currentExportEffectAvailable`.

PDF and Markdown export (Issue #500, S64b) are service protocol version 52, with no schema revision: the export ledger already allows both formats. `reviewManuscriptExport`, `prepareManuscriptExport` and `chooseManuscriptExportDestination` take an optional `format` — `docx`, the default, `pdf` or `markdown` — so each format has its own review, the writer's identity in its input digest, and a destination with its own extension. `src/service/text-export.ts` is the pure writer of both, identities `ai7-pdf-export/1` and `ai7-markdown-export/1`. Neither restores anything from the original: the review names each class the original holds beyond the words `无法导出`, counted as the DOCX review counts them, and each mark it keeps only as words `降级导出`; a text box merged at import is a body paragraph. Markdown writes each block on its own line — a title as `#`, a heading one level deeper, never past six — and escapes every character Markdown or CriticMarkup would read as syntax, keeping a line break as a hard break. 批注 and included 备注 become footnotes, numbered in reading order after their words, with replies; a pending 修改建议 stands in the text as CriticMarkup, its author and date in a footnote. A PDF is printed from an HTML page, since the service has no Chromium and a PDF would not fit a frame: the preparation binds the page's digest, and the receipt the printed file's. Before approving a PDF, main calls `stageManuscriptExport`, which is main-only and never on the renderer's API. It writes the exact page the preparation bound to `<dataRoot>/export-staging/<effect intent>.html`. Main prints that page to A4 with `printToPDF` in a hidden window on its own in-memory session, `ai7-export-print`, network-denied like the product's, with no script, devtools, navigation or new window, loading only from that folder, and writes the PDF beside it, never over a file. Approval requires a file that begins `%PDF-`, or it refuses with `EXPORT_PDF_NOT_PRINTED` and records nothing. It then writes the file to the destination as a DOCX is written, and clears the staging folder.

The 审阅报告 export (Issue #500, S64b part 2; ADR 0079 §3.4) is service protocol version 53, with no schema revision: the export ledger already allows target kind `report`. The export operations take the target `{ kind: 'report', reportId }`, one recorded version of a Review Run's report of the route's Book, read back to the digest it was recorded with. `src/service/report-export.ts` writes it in each format — `ai7-report-docx/1`, `ai7-report-pdf/1` and `ai7-report-markdown/1` — from that version alone: its title, the Run and version it reports, and its four parts in the words `src/shared/report-wording.ts` owns, which 审阅 shows too. The DOCX is a fresh package with a Word table, the PDF is printed from its page as a manuscript's is, and Markdown writes the 概览表 as a pipe table, the one part its review names `降级导出`. The preparation binds the version's digest instead of a revision, so the ledger's revision columns stay empty for it, and its canonical record adds `reportDigest`, a key no earlier record holds. The target projection gains `report`, and the review's `formats` and `formatLine` are the report's own. The options apply to no part of a report, and the card shows none. 交付物's `导出记录` lists a report's exports among the Book's. Main accepts `--j04-save-path <absolute path>` under `AI7_E2E_JOURNEY=J-04` exactly as it accepts J-07's.

## J-07 发稿

Main accepts `--j07-picker-path <composed.docx>` and `--j07-save-path <absolute path>` only under `AI7_E2E_JOURNEY=J-07`, mutually exclusive with the other Journey controls and never with `developer-live`; the Journey composes its manuscript from exact `sample1` at run time, and the save path answers the one Save dialog of that launch in place of the platform's own, once — a second 选择保存位置… opens the real dialog. The Save dialog is the main process's: `showSaveDialog` in the documents folder with the review's file name, filtered to the reviewed format's extension, the platform asking before it replaces a file; the renderer never names a path, and 在文件夹中显示 reveals only a file whose receipt verified it. ⑥ 交付物 is `src/renderer/deliverables.ts`, with every word it adds in `src/renderer/deliverables-labels.ts`; it is reached from the manuscript's `工作` group after 审阅 and from 工作概览's one line, and it reads the service through two renderer members held to the route's Book, `inspectDeliverables()` and `designatePublicationVersion({ milestoneId, scope, basis })`. The milestone form in the 导航 panel sends `saveMilestone` a `purposeKind` — one of the four frozen purposes with no words of its own, or `custom` with the editor's. 设为发稿版本's form judges 发稿范围 and 依据 exactly as the service does, in characters through the shared `publicationText`, never by an HTML `maxLength`, which counts UTF-16 units.

⑥ 发稿 (Issue #414) is schema revision 25. It adds three STRICT, append-only relations that `src/service/publication-versions.ts` owns, each refusing UPDATE and DELETE through its triggers: `publication_versions` (the 发稿版本 as designated — 第 N 次 of its Book, the exact milestone, its revision and that revision's digest, 发稿范围, 依据, actor and time), `public_release_permissions` (the separately identified internal Public Release Permission, one per designation, which ordinary wording never names) and `publication_events` (the `actuals-prompt` and `exemplar-archive` events a designation leaves for S82 and S79, which nothing consumes yet). A revision-24 store gains the three empty relations and moves nothing else. 设为发稿版本 writes the version, its permission and both events in one transaction — or, for an identical repeat of the current designation, nothing — and every 交付物 read verifies what it shows against the digests it was written with. A Milestone Version's purpose is stored as words, a frozen purpose as its own label and 自行输入 as the editor's own, and read back by kind, so `milestone_versions` and its Signoff Record keep their exact shapes; it moved the service protocol to version 35.

## J-09 待我处理

Main accepts `--j09-picker-path <sample1.docx>` only under `AI7_E2E_JOURNEY=J-09`, mutually exclusive with the other picker paths, and — in its second launch — `--j04-model-adapter sample1-baseline-happy` beside it, so the Journey's one completed Run executes exactly as J-04's do; without the adapter its first Run is recorded and blocked before dispatch. 待我处理 (Issue #424, plan slice S78; editor-surfaces §8.1) is `src/renderer/global-attention.ts` — the header's entry with its number, in every window, and the screen it opens in the library state — with every word in `src/renderer/global-attention-labels.ts`; `src/renderer/index.ts` only mounts it, routes an item to its own record in the window that asked, and, before the header leads away from the manuscript or from 稿件冲突, lets that surface settle what it must first. One renderer member, `inspectGlobalAttention()`, takes no input and travels over `ai7:j09:inspect-global-attention` with no Book route; main keeps from its answer only which Recovery Attention States it showed the window, so that opening one from the list claims it for that window — and is refused while another window holds it — and the read itself claims nothing.

The service composes the four groups in `src/service/global-attention.ts` from readings each owner takes of its own records: the uncertain import commits, the pending abandonment cleanups and the pending or deferred Recovery Attention States; every 修改建议 in conflict with the manuscript and not yet resolved, before or after `暂不处理` (`readConflictAttention` in `src/service/proposal-conflicts.ts`); each Book's latest baseline analysis Task and the Task Outcomes of the last seven days from the baseline ledger (`attentionReadings` in `src/service/analysis/baseline-analysis-store.ts`); and each Book's latest Review Run and the recent completed ones from the Review Run ledger (`attentionReadings` in `src/service/review/review-runs.ts`), with the one execution owner's progress and slot handed in by the service entry. Every reading filters in SQL and stops at 500 records, each group lists at most 50 items (最近完成 at most 20) while its total and the count keep every item, and the widest answer fits one frame. It is a read: nothing is written, claimed or terminalized — a Run a stopped service left executing is read as stopped and left as recorded — and it takes no schema revision. It moved the service protocol to version 39.

## Closed product interval

After bootstrap, the E2E controller, Electron main, isolated renderer, and service deny outbound HTTP(S), WebSocket, DNS, and socket primitives appropriate to their process. The product receives no repository token, registry credential, provider credential, debug selector, or ambient payload path. Renderer checks return booleans only; ordinary scenarios emit a fixed failure location, the diagnostic wrapper re-emits only a bounded allowlisted tuple, and neither creates a screenshot, trace, video, artifact, log payload, database assertion, or tracked DOCX.

Generated `node_modules/`, `.pnpm-store/`, `.cache/`, `.runtime/`, `dist/`, E2E temp roots, and Agent Data Roots are reconstructable or disposable outputs. None is a source input. Hosted CI may restore only `.cache/bootstrap/electron` through the exact full-SHA-pinned cache action, keyed by operating system, architecture, and the complete `config/dependency-artifacts.json` hash. A miss follows the same bootstrap path, and bootstrap revalidates the canonical path and declared SHA-256 before use. After successful bootstrap, the verified download may be saved immediately so later build/Journey failure does not prevent a same-pull-request retry from using it; GitHub pull-request cache scoping means cross-pull-request reuse is not assumed. The cache never includes `.pnpm-store`, `node_modules`, `.runtime`, `dist`, Agent Data Roots, databases, product runtime state, logs, credentials, manuscript-shaped data, screenshots, traces, videos, or test artifacts; a hit has no success meaning.
