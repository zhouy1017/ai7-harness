# E2E journeys: what each admitted runner proves

This file records, per admitted Journey, the exact behavior the runner under `e2e/` exercises and asserts. It is one of the two implementation-status records under [ADR 0064](../adr/0064-reweight-repository-development-toward-value-first-delivery.md); the other is root [`PROGRESS.md`](../../PROGRESS.md). The design of every Journey lives in [`docs/ui-ux-v2/journeys.md`](../ui-ux-v2/journeys.md); a design row is never evidence that its branches are implemented. Update this file in the same pull request that changes a runner.

Every runner installs the built Node egress guard before third-party controller imports, preflights its disposable Agent Data Root, launches the exact Electron carrier, and drives it only over inherited Chromium `--remote-debugging-pipe` descriptors: no debug port, inspector, WebSocket endpoint, TCP listener, or dev server exists. No runner reads the product database as a result oracle, and none retains, uploads, or logs a manuscript payload, credential, screenshot, trace, or video.

## J-01 new-Book import

Uses exact [`SampleBooks/sample1.docx`](../../SampleBooks/sample1.docx), 29,550 bytes with SHA-256 `b8a3dbde0aa8a1ec7265f9ae3fe47877759e7947c5ab69682cd0a8f424a8d483`, as its provider-free public input under [ADR 0044](../adr/0044-use-sample1-as-compatibility-and-recording-baseline.md). It verifies the server-derived eight-category fidelity review, all three import-identity finding classes and their initially-unselected target consequences, exact review-v4 rebinding, source-path loss and change recovery, prepared-attempt reconciliation, completion paint and acknowledgement, inconclusive-outcome containment, durable abandonment cleanup, the linked degradation decision and import record, a bounded 32-of-97 manuscript window, and durable journal acknowledgement. Under ADR 0072 §1–2 (#350) it also carries one source-only intake of a minimal PDF the runner writes itself: identified from its bytes as `PDF`, refused as an editable Manuscript with the reason stated, offered only `作为来源材料导入`, reviewed with its digest and byte count and no content or structure digest, committed, and shown on the Source Version record with its format. Its `.txt` converted intake writes twelve paragraphs excerpted from exact `sample1` at run time. Its `.doc` converted intake is the one scenario no admitted or generated input can carry: ADR 0079 §5 left the one legacy binary Word document local-only and no generator for the format exists, so the scenario runs only where a developer keeps that document — `AI7_LOCAL_SAMPLEBOOKS`, or the checkout's own now-ignored `SampleBooks/`; a background test guest holds it under [ADR 0084](../adr/0084-admit-the-legacy-doc-to-the-background-test-guests-for-j-01.md) — and otherwise prints `DISCLOSED_SKIP/J-01/doc-manuscript-local-only-absent`, which the pull-request lane and the nightly both repeat as `…/disclosed-skip/…` beside the Journey's result. It is absent on every CI host by design, so the `.doc` route is covered locally and disclosed everywhere else; every other J-01 scenario is unaffected. Runtime state remains inside the disposable external test data root and is deleted after the journey. The runner also asserts the exact sorted `window.ai7` preload inventory; a new preload method is a synchronized delta here.

## J-02 long-manuscript editing

Generates a deterministic clean 10,000,000-character, 50,000-block Chinese DOCX only under the disposable external E2E root and drives it through the same native picker and production-shaped product. It verifies every renderer window remains at most 32 blocks, exercises navigation, search, and replacement while journal editing remains available, persists a Milestone Version and restart-safe history, and includes the applicable keyboard, real composition-event, visible-focus, 200% reflow, and forced-colors behavior. Since #485 it proves that focus entering the text never pages the window: after the restart, with the pane between its edges, Tab walks into the text, the browser reveals the caret at the window's start, and the window's first paragraph and whole-manuscript position stay what they were (`j14-keyboard-focus-keeps-window`, which names a paged window, a missing precondition and a browser that revealed nothing); the reader's own scroll to the top edge then pages back exactly once and the window stays (`j14-top-edge-pages-back-once`, which names a bounce). Its fine-scroll crossing starts from between the pane's edges, wherever the steps before left the pane. Milestone recovery-object formation keeps the 256-block bound and performs exact hashing, fsync, independent verification, promotion, and SQLite visibility-last commit. The generated DOCX and all runtime state are deleted at cleanup. Its DOCX input stays runtime-generated by design: the subject is scale (a 10,000,000-character, 50,000-block manuscript no admitted Public SampleBook approaches), so #311's rule for composing test manuscripts from admitted content does not apply to it; J-08's inputs were content-bearing and are now composed from exact `sample1` excerpts.

## J-08 interrupted-manuscript recovery

Composes its three small DOCX inputs under the disposable root at run time, from three non-overlapping excerpts of exact [`SampleBooks/sample1.docx`](../../SampleBooks/sample1.docx) — blocks 1–40, 41–70, and 71–95 of its 97 — through `e2e/composed-docx.mjs`, with the three titles authored rather than taken from the source. It composed from a second admitted SampleBook until ADR 0079 §5 narrowed the repository to exact `sample1`; composing from `sample1` does not make any input `sample1`, because the builder writes its own container and the three ranges give the three Books distinct digests. It proves a clean shutdown and reopen creates no Recovery Attention; an interruption after an acknowledged journal edit opens the exact affected Book recovery-first ahead of a pending import; journal reconstruction, checkpoint, and any verified applicable snapshot remain unselected until the user chooses; `仅查看` stays bounded and permanently read-only; and deferral carries an explicit return route while the affected branch remains service-read-only. It proves a corrupt or missing newest snapshot falls back to an older verified one, that no verified snapshot produces exactly two eligible choices plus a separate disclosure, and that mismatch, missing-object, partial, orphan, and structurally incomplete snapshots fail closed. Restoration creates one new descendant without replaying pre-restoration history, and `当前为恢复的工作状态` survives clean restart until a later explicit Milestone clears it. It includes recovery-screen keyboard choice, visible focus, 200% reflow, forced colors, and a controller-owned OS-assigned loopback sentinel proving the renderer fetch is rejected before the sentinel receives any request.

## J-12 Book workbench, data location, and Model Service setup

Creates one public synthetic DOCX, a second empty Book, and runtime-only synthetic credential values inside its disposable root and controller. It proves distinct exact-Book Workbench coexistence, serialized duplicate reuse, background no-focus-steal behavior, offline editing, exact immutable Revision history, restart persistence, and truthful `设置 > 数据与存储`. Its role-first `设置 > 模型服务` path proves four role cards, the one exact Main Editorial DeepSeek binding, real OS-protected save without redisplay, restart, same-reference replacement, removal, removal restart, exact canonical strict nonsecret persistence, data-root absence of raw secrets and common SHA-256-derived encodings, the visibly verified `development-ci` Provider Processing v1 zero-transmission policy, separate External Export v2, no Public Release Permission, and zero configured Harness providers, agents, or sessions. The setup Node process never reads a raw OS-store value. The Journey body's `finally` is the sole idempotent owner of credential, metadata, direct OS-store, loopback, and disposable-root cleanup; a durable `missing` state for the same reference is already-successful cleanup, and the failure-only direct fallback runs the pinned Electron executable in Node mode with the opaque reference over stdin and no output. The runner pins the SQLite `user_version`; a schema revision is a synchronized delta here. Its DOCX stays generated rather than composed from an admitted Public SampleBook, because the Journey's own paragraph text `仅用于本地工作台路由校验` declares its subject: workbench routing, data location, and Model Service setup, none of which reads manuscript content back.

## J-15 native artifact lifecycle

Uses two synthetic empty Books to exercise the fixed repository-bundled `编辑工作区方案` through the production renderer, Main-owned current-Book route, private stdio service, SQLite, and Agent Data Root. Installation retains the unchanged native carrier and materializes the fixed `ai7.editorial-workspace-profile.authority` Revision 1 and Revision 2 definitions while remaining disabled; fresh enablement pins the Book directly to Revision 2. The Journey then closes the product, constructs a disposable exact schema-v12 predecessor with a canonical future-skewed enablement time, and proves production migration pins the existing enabled Book to Revision 1 until the same action explicitly appends Revision 2. Restart retains the append-only history while a later Book remains disabled and unpinned. Revision 2 adds eligibility only for the current Book's primary Manuscript Revision and Source Version; every Task, Plan, Run, Provider, credential, network, Effect, Enrollment, Apply, export, and release authority remains absent. The predecessor constructor drops every post-v12 table; a new schema layer must add its tables to that list.

## J-03 provider-denied Task authorization

Reuses exact `sample1` and the supported setup paths to install and enable the carrier at sidecar Revision 2, create then remove one runtime-only synthetic credential, make an acknowledged Manuscript edit, and prepare one fixed `Task Input / 任务输入` checkpoint. The store keeps the canonical digest-linked Task Intent, Manuscript and Artifact pins, Run Source Scope, Provider Resolution Plan, Execution Plan, Plan Envelope, standard-direct Run Authorization, and terminal `recorded-not-dispatched` Run record; strict append-only validation rejects mutation at startup. The workbench exposes preparation only when the exact lineage, carrier and sidecar, and missing opaque credential prerequisites are present, followed by exactly `记录本次运行授权（不派发）`; the foreground execution boundary answers `前台执行已拒绝 · 未启动` for the recorded-only Run. Later edits, restart, and configuration changes cannot retarget or start the record. The runner pins the SQLite `user_version`.

## J-04 covered baseline manuscript analysis

Imports exact `sample1`, installs and enables Revision 2, creates and removes one runtime-only synthetic credential, then starts the baseline manuscript analysis on the clean imported Revision. It observes the derived Coverage Manifest and its unit accounting, authorizes and dispatches the Run through the real single-slot scheduler, composed harness, Egress Gate, and the `ai7-local-deterministic` adapter bound only by the J-04 launch control `--j04-model-adapter` to the hand-written synthetic fixtures under `tests/fixtures/model/`, waits for the immutable Result Set Revision, verifies the four independently labeled axes, the one adapter-failure gap, the cross-unit conflicts, `回到稿件范围` navigation, restart persistence, and `stale` freshness after one acknowledged edit. It then drives the three Analysis Update Controls, `同步到当前稿件`, `重新分析所选范围`, and `重新分析全书`, asserting the reuse plan counts, the successor revisions (ordinals 2 to 4), lineage, predecessor immutability, the Revision History, read-only opening of an older revision, and restart persistence. Under Issue #48 it relaunches the product bound to the `sample1-baseline-transient-retry` fixture and proves the Plan Envelope as an authority-bearing boundary (ADR 0009): a `重新分析所选范围` Run over unit 5 whose first attempt answers a transient `PROVIDER_ERROR` 503 is retried once in-envelope and recorded as a `safe-retry` Plan Adaptation before the retry is dispatched (the record with its classified reason and first payload digest, the two spans of the unit, usage counting both attempts, the unchanged Execution Binding, the timeline entry `计划内调整 · 单元 5 安全重试 1 次 · <reason>`, and the Overview disclosure in the revision's provenance), while unit 2's non-retry-safe failure stays a first-attempt gap; a second prepared `重新分析所选范围` Task shows the Plan Boundary Split (`运行中可调整` with exactly `safe-retry`, `变化后必须暂停并重新授权` with the fifteen material fields, `需要你参与的位置` as `预计无需中途参与`, and `计划说明，不是运行授权`), then is prepared again with a different range so its version 1 is superseded by a pending Plan Revision whose `查看计划修订` diff names the selected range and the changed reuse-plan counts, refuses authorization of the stale version with `plan-revision-required` and no Run Record, takes the way back — preparing again at the range version 1 froze records a reverting Plan Revision that supersedes the pending one, restores `授权并开始任务` on the version that was never replaced, and writes no second plan version (#281) — drifts once more, yields version 2 on the same Task Intent through `重新确认计划`, and settles revision 6 under the renewed authorization bound to version 2; an acknowledged edit after the checkpoint then changes no plan version, revision, or binding. After the first baseline settles it asserts the Run Liveness Signal ended with the state that produced it — no progress line, no status toast still reading `已进入调度器`, and the Run Record's `上次状态更新` in local time with the exact instant beside it. Since #406 it reaches the analysis as its own destination — the manuscript's `资料与记录` group's `分析`, and `工作概览`'s one-line summary with `打开分析` — and asserts both layers of ②A (V2-UX-ANALYSIS-025, V2-UX-LAYER-008): the seven tabs in the specification's order with `梗概` selected and keyboard selection that survives a re-render, the four sentences under `覆盖范围`, `全书综合`, `与当前稿件` and `可信程度` with their exact readings and one next action each, the contradictions and open questions counted and pointed at 审阅 while their lists sit closed in the technical layer with every pinned identity, `各章` naming the unread range, each update mode's own button opening `先看计划` beside a quick start that stays disabled until a Default Execution Rule exists, and the Run Report opened closed-by-default from the Task Outcome and from every history entry — its decision layer in the editor's words, its technical layer carrying both digests, the instants and the usage per stage. It also asserts the destination's two persistent actions (`打开稿件`, `工作概览`) render enabled, in that order, inside the persistent `.workbench-actions` region and that the region is computed sticky, so the way out survives the longest surface the product renders (V2-UX-LAYER-005, #335). The Journey ends with zero loopback requests. The runner pins the SQLite `user_version` and the stage names in `e2e/controller.mjs`.

## J-05 Editorial Marks and AI7 Apply for a Change Suggestion

Composes a 40-block manuscript from exact `sample1`, imports it as a new Book through the launch control `--j05-picker-path`, and opens the manuscript. With nothing selected, and with a selection that spans two paragraphs, the selection menu names why the 编辑标记 entries cannot act; on a selection inside one paragraph it shows the three groups 文字处理 (剪切 / 复制 / 粘贴 / 粘贴为纯文本), 编辑标记 and AI7 任务 — the last disabled with `任务面接通后可用`, and neither `加入任务范围` nor `在稿件中搜索` present — and answers the arrow, End and Escape keys, returning focus to the text. It then makes the four marks on exact text of one paragraph and asserts each: a 高亮 in the second personal colour with the paragraph's 3 px margin line and no card; a 批注 whose composer quotes the selection and refuses an empty body, whose Mark Card sits below the paragraph aligned to the text column with the source `你`, takes a reply, and leaves the margin line when 标记为已处理; a 备注 that says it is neither exported nor sent to a model and can be rewritten; and a 修改建议 whose creation changes no text, whose card holds the four regions 修改内容 / 修改理由 / 依据与核查 / 你的处理 with nothing preselected and `接受并应用` as its one primary action, and whose replacement shows in place labelled `预览 · 未应用` while the service still returns the original block text. 拒绝 records a Proposal Decision, removes the preview and offers the optional reason chips, of which one is recorded once; 撤回 returns the item to undecided; 修改后接受 with a filled 为什么这样改 (`接受修改后的版本并应用`) records the decision with that reason, asks no second time, and **writes the editor's wording**: the card reads `已应用`, the mark stands on the applied text, the preview is gone, `查看完整凭据` lists the Effect, the Proposal Decision, the Effect Approval, the dispatch, the Effect Receipt and the manuscript state before and after, the service returns the new text with the journal sequence advanced by exactly one, and 撤销 answers `没有可撤销的编辑` because a committed Apply is a history boundary. `接受并应用` on a second suggestion writes its text in one click with the same evidence; `准备撤销本次应用` states what it will write before `确认撤销本次应用`, which writes the original back as one new journal entry and leaves the suggestion open and undecided with its preview again; text typed into the words a suggestion would replace turns it `原文已变` with `接受并应用` and `修改后接受` disabled by name, and 撤销 of that typing makes it exact again. In a second paragraph a highlight is recoloured and converted to a 备注, the 备注 to a 批注, the 批注 to a 修改建议 carrying the comment as its reason, and back to a 批注, each keeping the pinned text and naming its origin, and the last is deleted. Text typed in front of the four marks moves all four with their text before and after the journal acknowledges it; text typed inside the commented range turns that one mark `原文已变` at once and durably, its card quoting the text it was made on and refusing conversion, while its neighbours stay exact; 撤销 makes it exact again. 剪切 and 粘贴为纯文本 run through the window because the page's own clipboard read is refused. The card reflows at 200% and follows the pane; on that narrow window a menu opened at the text column's right edge stays open and inside the window and brings no scrollbar into the pane — the menu floats over the window, and the step fails on a build where it lives inside the pane. Under forced colours, the three system kinds differ by the line under the text and the highlight by an outline. The context-menu key opens a mark's menu on the caret, Enter opens its card, Escape returns to the text. After a restart — launched with `--j05-apply-control lose-first-acknowledgement` — the four marks, the applied suggestion with its receipt, reason and edited text, and the reply are found where they were; a new suggestion is then accepted, main commits the Apply and withholds its acknowledgement, and the surface, hearing nothing, asks the records by the same Effect identity, reports `写入结果已从记录确认`, shows `已应用`, and the journal sequence has advanced by exactly one.

Not proven here: the batch confirmation strip — `确认应用` over a set of 错别字与规范用语 suggestions is one all-or-none Effect proven by `tests/service/manuscript-apply.test.ts`, and its strip belongs to 审阅's results (Issue #417); reversing an Apply that deleted its text, and reversing one whose applied text was changed since, which is a conflict for Issue #57; a mark produced by AI7 or carried in from an imported file on screen — the record family, its source attribution, `查看任务` / `看依据` and the conversions of an AI7 mark are proven by `tests/service/editorial-marks.test.ts`, because no producer exists before Issues #417 and #411; the rail lanes (Issue #409).

## J-14 accessibility obligations

J-14 has no runner of its own. Its keyboard, IME, focus, 200% reflow, and forced-colors obligations are asserted inside the Journeys above where they apply; the Mark surface's are J-05's `j14-marks-*` and `marks-keyboard-menu-*` stages.
