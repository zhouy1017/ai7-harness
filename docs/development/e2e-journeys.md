# E2E journeys: what each admitted runner proves

This file records, per admitted Journey, the exact behavior the runner under `e2e/` exercises and asserts. It is one of the two implementation-status records under [ADR 0064](../adr/0064-reweight-repository-development-toward-value-first-delivery.md); the other is root [`PROGRESS.md`](../../PROGRESS.md). The design of every Journey lives in [`docs/ui-ux-v2/journeys.md`](../ui-ux-v2/journeys.md); a design row is never evidence that its branches are implemented. Update this file in the same pull request that changes a runner.

Every runner installs the built Node egress guard before third-party controller imports, preflights its disposable Agent Data Root, launches the exact Electron carrier, and drives it only over inherited Chromium `--remote-debugging-pipe` descriptors: no debug port, inspector, WebSocket endpoint, TCP listener, or dev server exists. No runner reads the product database as a result oracle, and none retains, uploads, or logs a manuscript payload, credential, screenshot, trace, or video.

## J-01 new-Book import

Uses exact [`SampleBooks/sample1.docx`](../../SampleBooks/sample1.docx), 29,550 bytes with SHA-256 `b8a3dbde0aa8a1ec7265f9ae3fe47877759e7947c5ab69682cd0a8f424a8d483`, as its provider-free public input under [ADR 0044](../adr/0044-use-sample1-as-compatibility-and-recording-baseline.md). It verifies the server-derived fidelity review of [ADR 0086](../adr/0086-classify-ten-import-content-classes-and-retain-them-with-the-source-version.md) — nine rows and the closing 预计往返 card, `sample1`'s 266 inline styles and one section `完整保留（随文件保留）`, so its import asks for no degradation decision — all three import-identity finding classes and their initially-unselected target consequences, exact review-v4 rebinding, source-path loss and change recovery, prepared-attempt reconciliation, completion paint and acknowledgement, inconclusive-outcome containment, durable abandonment cleanup, a clean import record, a bounded 32-of-97 manuscript window, and durable journal acknowledgement. Under ADR 0072 §1–2 (#350) it also carries one source-only intake of a minimal PDF the runner writes itself: identified from its bytes as `PDF`, refused as an editable Manuscript with the reason stated, offered only `作为来源材料导入`, reviewed with its digest and byte count and no content or structure digest, committed, and shown on the Source Version record with its format. Its `.txt` converted intake writes twelve paragraphs excerpted from exact `sample1` at run time. Under ADR 0086 (#410) it composes two more inputs from exact `sample1` at run time through `e2e/composed-docx.mjs`, every word of them the sample's own: one whose field and footnote are the two classes that cannot be retained, which carries the unselected Import Degradation Decision, its acceptance and the linked decision record, and is the degraded reimport's staged file; and one with a text box, imported once with `保留为文本框` preselected (six blocks) and once `并入正文` (eight, the box's two paragraphs after the paragraph that anchors it), each stating its choice on the review and on the import record. Since #532 (PR #549) the summary line of an import that asks for no decision states the choice too: on the offer screen it reads `文本框 · 1 项随文件保留` before any choice and `文本框 · 1 项并入正文` once `并入正文` is chosen, and the review says the same. Under #411 its `imported-marks` scenario composes a third, a file carrying comments and tracked changes (`IMPORTED_MARKS_RECIPE`): a deletion, an insertion, a same-author same-time replacement, a comment with a reply by another author, a comment marked done, a whole paragraph inserted, a move and a formatting change, with neutral author names. Its review states 批注与修订 `完整保留` counted as the seven marks it becomes, asks for no decision and lists `来自文件作者的批注与修改建议 7 条` among the records to create; its record states the converted class; the manuscript reads as the file reads with every revision rejected — eight blocks that are the source's own, nothing applied; seven marks are drawn, every one the file's author's, three 修改建议 and four 批注, the insertion as a point named `待插入「…」` and the done comment resolved; the insertion's card reads `在此插入「…」` with the author as its source, previews its words at the point and offers no 转为批注; and `接受并应用` writes exactly its words into that one block. Under #412 (S63) its reimport scenarios resolve the review's rows — each run of changed paragraphs between two that match exactly — with a verb the row admits, 改写与新增 where it has new paragraphs and otherwise 删除, and check each committed reimport where it now lands: in the manuscript with its result, its record's rows and verbs, and the Book's records one step away. The paged scenario's new file also rewrites fourteen paragraphs in place, merges two and splits one, eighteen rows over two pages of ten. The moved paragraph stands in no row and keeps its identity. 改写与新增 carries the first row's identities in order — current 1–2 to new 1 and 3, so the added paragraph takes 002's and the edited 002 is new — and each rewritten paragraph's in place. 删除 retires the dropped one. 并入 carries the first merged paragraph's. 拆分 carries the split paragraph's to its first part and makes the second new. Cancelling the first row's verb mid-scan leaves the review exactly as it was. The repeated worst case is one row of 260 current and 261 new paragraphs, showing three of each, and the two identical paragraphs are one row that 改写与新增 records as no change with both identities kept. Its `.doc` converted intake is the one scenario no admitted or generated input can carry: ADR 0079 §5 left the one legacy binary Word document local-only and no generator for the format exists, so the scenario runs only where a developer keeps that document — `AI7_LOCAL_SAMPLEBOOKS`, or the checkout's own now-ignored `SampleBooks/`; a background test guest holds it under [ADR 0084](../adr/0084-admit-the-legacy-doc-to-the-background-test-guests-for-j-01.md) — and otherwise prints `DISCLOSED_SKIP/J-01/doc-manuscript-local-only-absent`, which the pull-request lane and the nightly both repeat as `…/disclosed-skip/…` beside the Journey's result. It is absent on every CI host by design, so the `.doc` route is covered locally and disclosed everywhere else; every other J-01 scenario is unaffected. Since #518 (PR #550) J-01 reads each launch's startup from Playwright's `browser` log, which it points at a file in its run root, removed with it (under `e2e:debug`, the debug layer's own log): the first launch that reports the product ready must show the launch, `readiness-signal`, `AI7_READY` and the attached renderer target, and on a failure `e2e:all` prints `LOCAL_COMPLETION/J-01/readiness/…` after the fail marker (the other runners under their own prefixes), naming the launch in flight and the last startup step it reached. Runtime state remains inside the disposable external test data root and is deleted after the journey. The runner also asserts the exact sorted `window.ai7` preload inventory; a new preload method is a synchronized delta here.

## J-02 long-manuscript editing

Generates a deterministic clean 10,000,000-character, 50,000-block Chinese DOCX only under the disposable external E2E root and drives it through the same native picker and production-shaped product. It verifies every renderer window remains at most 32 blocks, exercises navigation, search, and replacement while journal editing remains available, persists a Milestone Version and restart-safe history, and includes the applicable keyboard, real composition-event, visible-focus, 200% reflow, and forced-colors behavior. Since #485 it proves that focus entering the text never pages the window: after the restart, with the pane between its edges, Tab walks into the text, the browser reveals the caret at the window's start, and the window's first paragraph and whole-manuscript position stay what they were (`j14-keyboard-focus-keeps-window`, which names a paged window, a missing precondition and a browser that revealed nothing; since PR #610 (#604) a reveal that did not happen says whether the window had focus, the caret was at the text's start, or the pane moved and came back: `-no-reveal-window-unfocused`, `-no-reveal-caret-not-at-start`, `-no-reveal-moved-back`, otherwise `-no-reveal`); the reader's own scroll to the top edge then pages back exactly once and the window stays (`j14-top-edge-pages-back-once`, which names a bounce). Since PR #586 (#579) its keyboard search step presses ⌘F once, only after the editor reports its composition closed (`data-composing`), the window has focus and focus is in the text, and a failure names the missing precondition (`j14-keyboard-search-focus-composition-open`, `-window-unfocused`, `-editor-unfocused`) or what the press did (`-guard-announced`; since PR #599 (#591) `-key-not-received` or `-modifier-missing`; otherwise `-no-focus-move`). Its fine-scroll crossing starts from between the pane's edges, wherever the steps before left the pane. Since #409 it finds the manuscript opened with the 导航 panel closed beside the right edge's vertical Whole-manuscript Position Rail, opens the panel from the 导航 entry before it uses the outline, the search or the milestone form, and asserts the rail draws one tick per chapter — bounded at 400 however long the manuscript is — says the Book was never analysed and draws no marker, and that the pane's own scrollbar is at rest, shows while the pane scrolls and rests again. Its keyboard window crossing returns the caret to the text without moving the pane and proves PageDown answered with a later window — `j14-keyboard-window-crossing-went-backward` names the opposite — and at 200% the narrow rail stays beside the manuscript while the 导航 panel lies over it. Milestone recovery-object formation keeps the 256-block bound and performs exact hashing, fsync, independent verification, promotion, and SQLite visibility-last commit. Since #414 the milestone takes its purpose from the unselected purpose cards (阶段留档) rather than typed words and is saved with `保存里程碑版本`, and the form's bound pin covers the five cards, none preselected, and 自行输入's own 120-unit field. The generated DOCX and all runtime state are deleted at cleanup. Its DOCX input stays runtime-generated by design: the subject is scale (a 10,000,000-character, 50,000-block manuscript no admitted Public SampleBook approaches), so #311's rule for composing test manuscripts from admitted content does not apply to it; J-08's inputs were content-bearing and are now composed from exact `sample1` excerpts.

## J-08 interrupted-manuscript recovery

Composes its three small DOCX inputs under the disposable root at run time, from three non-overlapping excerpts of exact [`SampleBooks/sample1.docx`](../../SampleBooks/sample1.docx) — blocks 1–40, 41–70, and 71–95 of its 97 — through `e2e/composed-docx.mjs`, with the three titles authored rather than taken from the source. It composed from a second admitted SampleBook until ADR 0079 §5 narrowed the repository to exact `sample1`; composing from `sample1` does not make any input `sample1`, because the builder writes its own container and the three ranges give the three Books distinct digests. It proves a clean shutdown and reopen creates no Recovery Attention; an interruption after an acknowledged journal edit opens the exact affected Book recovery-first ahead of a pending import; journal reconstruction, checkpoint, and any verified applicable snapshot remain unselected until the user chooses; `仅查看` stays bounded and permanently read-only; and deferral carries an explicit return route while the affected branch remains service-read-only. It proves a corrupt or missing newest snapshot falls back to an older verified one, that no verified snapshot produces exactly two eligible choices plus a separate disclosure, and that mismatch, missing-object, partial, orphan, and structurally incomplete snapshots fail closed. Restoration creates one new descendant without replaying pre-restoration history, and `当前为恢复的工作状态` survives clean restart until a later explicit Milestone clears it; since #414 each milestone it saves takes its purpose from the unselected purpose cards (阶段留档). It includes recovery-screen keyboard choice, visible focus, 200% reflow, forced colors, and a controller-owned OS-assigned loopback sentinel proving the renderer fetch is rejected before the sentinel receives any request.

## J-12 Book workbench, data location, and Model Service setup

Creates one public synthetic DOCX, a second empty Book, and runtime-only synthetic credential values inside its disposable root and controller. It proves distinct exact-Book Workbench coexistence, serialized duplicate reuse, background no-focus-steal behavior, offline editing, exact immutable Revision history, restart persistence, and truthful `设置 > 数据与存储`; the milestone that makes its later Revision takes its purpose from the unselected purpose cards since #414, and the read-only historical view offers neither `保存为里程碑版本` nor `保存里程碑版本`. Its role-first `设置 > 模型服务` path proves four role cards, the one exact Main Editorial DeepSeek binding, real OS-protected save without redisplay, restart, same-reference replacement, removal, removal restart, exact canonical strict nonsecret persistence, data-root absence of raw secrets and common SHA-256-derived encodings, the visibly verified `development-ci` Provider Processing v1 zero-transmission policy, separate External Export v2, no Public Release Permission, and zero configured Harness providers, agents, or sessions. The setup Node process never reads a raw OS-store value. The Journey body's `finally` is the sole idempotent owner of credential, metadata, direct OS-store, loopback, and disposable-root cleanup; a durable `missing` state for the same reference is already-successful cleanup, and the failure-only direct fallback runs the pinned Electron executable in Node mode with the opaque reference over stdin and no output. The runner pins the SQLite `user_version`; a schema revision is a synchronized delta here. Its DOCX stays generated rather than composed from an admitted Public SampleBook, because the Journey's own paragraph text `仅用于本地工作台路由校验` declares its subject: workbench routing, data location, and Model Service setup, none of which reads manuscript content back.

## J-15 native artifact lifecycle

Uses two synthetic empty Books to exercise the fixed repository-bundled `编辑工作区方案` through the production renderer, Main-owned current-Book route, private stdio service, SQLite, and Agent Data Root. Installation retains the unchanged native carrier and materializes the fixed `ai7.editorial-workspace-profile.authority` Revision 1 and Revision 2 definitions while remaining disabled; fresh enablement pins the Book directly to Revision 2. The Journey then closes the product, constructs a disposable exact schema-v12 predecessor with a canonical future-skewed enablement time, and proves production migration pins the existing enabled Book to Revision 1 until the same action explicitly appends Revision 2. Restart retains the append-only history while a later Book remains disabled and unpinned. Revision 2 adds eligibility only for the current Book's primary Manuscript Revision and Source Version; every Task, Plan, Run, Provider, credential, network, Effect, Enrollment, Apply, export, and release authority remains absent. The predecessor constructor drops every post-v12 table; a new schema layer must add its tables to that list.

Since #427 (S79a) J-15 also walks 知识库 › 审阅规范文件. Every launch names J-15's picker control, `--j15-picker-path`, with a runner-authored plain-text file of the house's own clauses — AI7's own words, never a manuscript. From 书库, `知识库` opens its seven classes as tabs in the specification's order — `审阅规范文件`, `评估方案`, `工序与规则`, `社级编辑记忆`, `范例`, `资料库`, `外部来源留存` — at 审阅规范文件: the seven guideline documents the review categories apply, each `第 1 版 · AI7 内置默认`, 文字规范条款 `用于：错别字与规范用语` with its four clauses `未被引用` and one version line, 「第 1 版 · AI7 内置默认 · 内置 · 4 条 · 还没有审阅用过」. 线索条款 and 事实核查契约 offer no `导入新版本…`: their categories read no clause of theirs, and each card says so — 「「情节逻辑与前后一致」把基线分析里的线索变成批注，不按这里的条款找问题。这是 AI7 的固定说明，不能导入新版本。」 and 「「事实核查」按 AI7 固定的事实核查契约执行，不读取这里的条款。……」. `社级编辑记忆` says 「尚未提供：社级编辑记忆还没有接通。」. `导入新版本…` on 文字规范条款 reads the picked file as the next version would read it, the preview's heading focused: 「将导入为《文字规范条款》第 2 版」, five clauses, and how they differ from version 1; `确认导入` records it, and the card reads `第 2 版 · 本社` with both versions listed and the status 「已导入《文字规范条款》第 2 版；之后的审阅按第 2 版。」, as `inspectReviewGuidelines()` answers. For J-14 the chosen tab takes focus and ArrowRight / ArrowLeft move between the classes; at 200% the tabs and cards reflow without sideways scroll, and under forced colours a card keeps its border and the chosen tab its heavier underline. A restart keeps version 2. Since S79d, `工序与规则` lists the nine review 工序 above the rules — seven `已启用` drawn solid, 书系一致性检查 and 跨交付物一致性检查 `尚未接通` drawn dashed with why said of the house (「书系知识还没有接通。」, 「生产文档之间的一致性核对还没有接通。」), none used by a review here — and 「本社方案 v2 · 已安装 · 已为 1 本书启用」, with no identifier among those words; its `查看技术详情`, closed, holds the carrier's identity, `1.0.0` and SHA-256, the 权限侧车's identity and digest, and the nine 工序 ids (LAYER-008). Since S79c the window relaunched after the guideline import names the admitted Public SampleBook `sample1` as its picker file, a book the editor collected: `资料库` opens empty with 「资料库里还没有资料。…」, `放入资料…` shows the file as it will arrive — 「放入资料库：sample1.docx」, its format and size, the title `sample1` from its name, no kind chosen and `放入资料库` disabled — and keeps nothing yet; the editor names it `样书一`, says it is a `图书`, and `放入资料库` keeps the original byte for byte under its digest in the Agent Data Root, the card reading `尚未定归属`, `尚未定` and 「定了归属与学习准入，任务才能把它列进「允许参考」。」. 待我处理 lists it in 等待你的决定 as 「资料库 · 图书「样书一」」 `资料库归属待定` under `尚未定归属`, its next step `定归属…`, and opening it returns to 资料库 with that button focused. `定归属…` offers both Books, `书系` disabled and `社级`, none chosen; the first Book recorded, focus moves to `定学习准入…`, whose five choices start unselected with 仅纳入 the Book marked `建议` and `纳入当前书系` disabled; choosing `纳入出版社经验` shows its consequence where it is chosen, and 仅纳入 the Book with a note records 「仅纳入《J15 空图书甲》（说明：…）」 and 「《J15 空图书甲》的任务可以把它列进「允许参考」。」, after which 待我处理 counts nothing. For J-14 the card, its facts and an open choice reflow at 200%, and under forced colours the card and the choice keep their borders. A review that applies an imported version as a new Task under its own contract, the clause citation counts with a finding found again counted once, the latest reviews named and the rest counted, the Books still on an older version, a Word file, each refusal, a fixed document's version never applied, and a category that applies a house version refused before any dispatch under developer-live are proven over the real store by `tests/service/review-guidelines.test.ts`; 资料库's chain of decisions — 稍后决定, a changed attribution setting the eligibility aside, the refusals — its format reading — a many-comment file in one pass, a Word file only by an entry under `word/` — its pages, its one-item read, its decisions counted past ten and its open-time sweep by `tests/service/library-materials.test.ts`; `更多资料…` and `更多图书…` are pressed in no Journey.

## J-03 provider-denied Task authorization

Reuses exact `sample1` and the supported setup paths to install and enable the carrier at sidecar Revision 2, create then remove one runtime-only synthetic credential, make an acknowledged Manuscript edit, and prepare one fixed `Task Input / 任务输入` checkpoint. The store keeps the canonical digest-linked Task Intent, Manuscript and Artifact pins, Run Source Scope, Provider Resolution Plan, Execution Plan, Plan Envelope, standard-direct Run Authorization, and terminal `recorded-not-dispatched` Run record; strict append-only validation rejects mutation at startup. The workbench exposes preparation (`准备任务`) only when the exact lineage, carrier and sidecar, and missing opaque credential prerequisites are present, followed — since #420, in the Task Drawer's authorization bar — by `开始任务`, which records the Run and never dispatches it (ADR 0055); the foreground execution boundary answers `前台执行已拒绝 · 未启动` for the recorded-only Run. Later edits, restart, and configuration changes cannot retarget or start the record. Since #418 the card names the plan in one line (`计划：固定任务 · 全书 · 任务输入修订版 r2 · 不发送任何内容`) beside `查看计划` — which reads `查看计划并开始` until the Task is recorded, since #420, when the recording left the card — and preparing opens the plan in the Task Drawer beside it with focus on the drawer's title: 精简 first — the goal sentence, the chips 书 · 位置 · 已选字数 · 任务输入修订版 · 工序, `已为任务保存修订版 r2，之后的编辑不影响这项任务。`, `修改` disabled with its reason and `← 任务` leading to the Book's 任务 panel, and the five rows 处理 / 发送 / 会得到 / 不会 / 中途 — then 完整's six sections, the two columns (`无` beside the three locked groups), `设为快速开始默认…` disabled with its reason, and in `查看技术详情` every identity the card's preview used to show, read by key exactly as the plan froze it: the readable scope, the source version as lineage evidence only, the carrier and sidecar digests, the provider binding with its adapter and configuration revisions, the opaque credential reference and its readiness, `未声明` for capabilities, fallback chain and effects, `development-ci · v1 · 拒绝 · 0 次实时传输`, the Plan Envelope digest and the six named non-effects; the footer reads `计划说明，不是运行授权` throughout. Since #420 (S74a) the footer is also the authorization bar: before the record it sums the plan up in one line (`《J-03 sample1 任务授权》 · 全书 · 主编辑角色 · 未设置任务预算上限 · 产出：一条运行记录（不派发） · 不改稿`), states `只是让 AI7 按这份计划做这一次；接受修改建议、批准受控动作、保存里程碑版本、设为发稿版本都仍由你另行决定` and `此任务只记录运行，不会派发`, and offers `开始任务` — with the Book's credential `missing`, because this Task's route sends nothing — beside `返回修改`, disabled with `这类任务的计划不能在这里修改` — a fixed task keeps no plan versions to edit (#419) — and `保存草稿`, the start on screen wherever the plan is scrolled (`drawer-bar-record-only`); `保存草稿` closes the drawer, records nothing and returns focus to `查看计划并开始` (`drawer-save-draft`); one activation of `开始任务` records the authorization and the Run with no second confirmation, after which the same region reads `已记录（不派发）` beside `查看运行记录`, which takes focus and opens 工作概览 with the drawer still beside it (`drawer-run-link`); and no button on the page or in the drawer carries 授权. Its drawer stages prove Escape closing it with focus back on `查看计划`, Enter reopening it, Tab over the two modes (Enter switches them) and `关闭`; a column the page makes room for at 1180 px and an overlay that leaves the page its width at 1000 px; at 200% no sideways scroll with every identity and the bar laid out; under forced colours the drawer's edge, the pill's outline, the chosen mode's own and the bar's rule; the drawer staying beside the manuscript of the same Book until `导航` opens and takes the one side slot, while the manuscript's `任务` entry stays disabled; the drawer reading `已记录 · 未派发` with the authorization and the Run record once its bar records it, holding nothing but `精简`, `完整`, `关闭` and the bar's `查看运行记录` enabled; still naming r2 after the post-authorization edit; opening in 完整 after the restart, the mode the editor chose; and every control in it disabled when the service stops. The runner pins the SQLite `user_version` and the stage names in `e2e/controller.mjs`.

## J-04 covered baseline manuscript analysis

Imports exact `sample1`, installs and enables Revision 2, creates and removes one runtime-only synthetic credential, then starts the baseline manuscript analysis on the clean imported Revision. It observes the derived Coverage Manifest and its unit accounting, starts the Run — since #420 from the Task Drawer's `开始任务`, one activation that records the authorization and the Run — through the real scheduler's governor, composed harness, Egress Gate, and the `ai7-local-deterministic` adapter bound only by the J-04 launch control `--j04-model-adapter` to the hand-written synthetic fixtures under `tests/fixtures/model/`, waits for the immutable Result Set Revision, verifies the four independently labeled axes, the one adapter-failure gap, the cross-unit conflicts, `回到稿件范围` navigation, restart persistence, and `stale` freshness after one acknowledged edit. It then drives the three Analysis Update Controls, `同步到当前稿件`, `重新分析所选范围`, and `重新分析全书`, asserting the reuse plan counts, the successor revisions (ordinals 2 to 4), lineage, predecessor immutability, the Revision History, read-only opening of an older revision, and restart persistence. Under Issue #48 it relaunches the product bound to the `sample1-baseline-transient-retry` fixture and proves the Plan Envelope as an authority-bearing boundary (ADR 0009): a `重新分析所选范围` Run over unit 5 whose first attempt answers a transient `PROVIDER_ERROR` 503 is retried once in-envelope and recorded as a `safe-retry` Plan Adaptation before the retry is dispatched (the record with its classified reason and first payload digest, the two spans of the unit, usage counting both attempts, the unchanged Execution Binding, the timeline entry `计划内调整 · 单元 5 安全重试 1 次 · <reason>`, and the Overview disclosure in the revision's provenance), while unit 2's non-retry-safe failure stays a first-attempt gap; the prepared Run's Plan Boundary Split reads in the Task Drawer since #418 — `运行中 AI7 可以自己调整` with exactly the safe retry in the editor's words, `这些一变就先停下来问你` with the three locked groups and the fifteen material fields in `查看技术详情`, `预计无需中途参与`, and `计划说明，不是运行授权`; a second prepared `重新分析所选范围` Task is prepared again with a different range so its version 1 is superseded by a pending Plan Revision — ②A saying so in one line since #420, its plan versions in a closed `查看技术详情` — whose diff, opened by the drawer bar's `查看计划修订`, names the selected range and the changed reuse-plan counts in the drawer as `计划的关键内容已变化` with `处理范围` and `重新分析与沿用的阅读范围` read by field key, each range as the paragraphs and characters it holds, 关键内容 and 随之变化 — refuses authorization of the stale version with `plan-revision-required` and no Run Record, takes the way back — preparing again at the range version 1 froze records a reverting Plan Revision that supersedes the pending one, restores `开始任务` on the version that was never replaced, and writes no second plan version (#281) — drifts once more, yields version 2 on the same Task Intent through the drawer bar's `重新确认计划` — whose range, since #418 and for #288, is the one the drawer's chips, its `查看技术详情` and ②A's one line name, with the character count the diff proposed, never the range the Task was first prepared with — and settles revision 6 under the renewed authorization bound to version 2; an acknowledged edit after the checkpoint then changes no plan version, revision, or binding. After the first baseline settles it asserts the Run Liveness Signal ended with the state that produced it — no progress line, no status toast still reading `已进入调度器`, and the Run Record's `上次状态更新` in local time with the exact instant beside it. Since #406 it reaches the analysis as its own destination — the manuscript's `资料与记录` group's `分析`, and `工作概览`'s one-line summary with `打开分析` — and asserts both layers of ②A (V2-UX-ANALYSIS-025, V2-UX-LAYER-008): the seven tabs in the specification's order with `梗概` selected and keyboard selection that survives a re-render, the four sentences under `覆盖范围`, `全书综合`, `与当前稿件` and `可信程度` with their exact readings and one next action each, the contradictions and open questions counted and pointed at 审阅's 情节逻辑与前后一致 with a working `打开审阅`, and — since #417 (V2-UX-REV-011) — no longer listed anywhere on ②A, `各章` naming the unread range — which, since #409, the manuscript's position rail also marks as one `分析未覆盖` band once the analysis with the gap has settled — each update mode's own button opening `先看计划` beside a quick start that stays disabled until a Default Execution Rule exists, and the Run Report opened closed-by-default from the Task Outcome and from every history entry — its decision layer in the editor's words, its technical layer carrying both digests, the instants and the usage per stage. It also asserts the destination's two persistent actions (`打开稿件`, `工作概览`) render enabled, in that order, inside the persistent `.workbench-actions` region and that the region is computed sticky, so the way out survives the longest surface the product renders (V2-UX-LAYER-005, #335). Since #417 it then reviews the manuscript it analysed, in ②B 审阅: relaunched with `--j04-model-adapter sample1-review-authored` (an authored fixture layered over the transient-retry fixture, answering the review categories over the manuscript as the Journey leaves it, both of its edits in the first block), it reaches 审阅 from the manuscript's `工作` group and finds every model-read category `未审阅` and 书系一致性 and 跨交付物一致性 `不可用` with their reasons; `新建审阅` opens the sheet with the nine categories and four scopes unselected, the four consequence lines `会读取 / 会发送 / 不会做 / 费用`, and `开始审阅` disabled until a quick-start default exists; 错别字与规范用语, 体例与格式 and 情节逻辑与前后一致 over `全书` prepare to one plan per category — the leads freeze no model plan — and ONE `开始任务` in the drawer's bar, after which each category settles on its own, the quotations Reference Integrity could not anchor stay out of the marks (2 and 1), and every other finding of a model-read category is a mark on the manuscript: `回到原文` opens the text at it with its card reading `AI7 · 审阅「错别字与规范用语」`. `接受并应用全部` lists every pending 错别字与规范用语 suggestion in one strip that states the write scope before `确认应用`, which writes them all as ONE Effect — the journal sequence moves by exactly one and every listed mark reads applied; `忽略并说明` on a 体例与格式 finding records its reason and the finding reads `已忽略` with its mark set aside; `生成报告` makes version 1 of the 审阅报告 with `概览表 / 必须处理的事项 / 各类别摘要 / 附录` and a live `导出…` for that version; and the coverage matrix now reads `需复审` for the two categories whose text the batch changed, offering `只审改动过的章`. Since #418 every baseline Task and the Review Run name their plan in one line beside `查看计划` — `查看计划并开始` until the Task is started, since #420 — on ②A and ②B, and each preparation opens that plan in the Task Drawer: the first baseline's in 精简 and then 完整, whose `查看技术详情` carries, exactly, the eight manifest units, the manifest digest, the fixture route and the policy reading the card used to list; each update's reuse plan unit by unit with where each predecessor unit goes, its goal and its predecessor, in 完整 after every relaunch because the mode is remembered; and the Review Run's one step per category — the leads reading the baseline — and `汇总`, with each model-read category's frozen Plan Envelope digest one step away. The drawer follows each Run to `已完成` and never holds an action of the surface beside it. Since #420 (S74a) its footer is the authorization bar, and every start in this Journey is the bar's: before the first Run it sums the plan up with `计划版本 1`, states what starting decides, and offers `开始任务` with the Book's credential `missing`, because the deterministic route sends nothing (`analysis-bar-ready`); once a Run settles the same region reads `已完成` beside `查看运行`, with nothing that starts it again and no status line still claiming the start (`analysis-bar-started`); a changed plan's bar offers `重新确认计划` and `查看计划修订` and no start at all (`plan-revision-bar`); the Review Run's bar offers its one approval (`review-bar-ready`); and no button on any page or in the drawer carries 授权. The Journey ends with zero loopback requests. The runner pins the SQLite `user_version` and the stage names in `e2e/controller.mjs`.

Since #419 (S73) J-04 edits the plan-revision Task's version 2 before it runs. `plan-edit-open` presses `返回修改`, which is enabled for a plan that takes edits: the drawer is in 完整 with focus on `核对与抽检`'s `×`. That `×` and the safe retry's — and, beside the safe retry's, `先问你` since #422 (S76d) — are named for what they do (`去掉这一步：核对与抽检`, `不允许：模型服务暂时出错时，同一个阅读范围安全地再试一次`, `改成先问你：…`) and drawn by the style sheet, so the column still reads as the adaptation's words alone. `逐章读取` and `汇总全书` offer nothing, and two notes say why and what the move means: 「这项分析的步骤由分析工序决定：可以去掉「核对与抽检」，不能改写、增加或调换顺序。」 and 「「先问你」：运行中遇到这种情况，AI7 先停下这一步来问你，其余阅读范围照常进行。」. `plan-edit-remove` leaves both out: each reads `你改的 · 不做` or `你改的 · 不允许` with `恢复`, the bar says `你改了 2 处` with `更新计划` where `开始任务` was, beside `撤销修改`, and `保存草稿` disabled with 「先更新计划或撤销修改」; nothing is recorded yet. `plan-edit-discard` lets them go with `撤销修改`. `j14-plan-edit-keyboard` makes both edits again without a pointer: Enter on `返回修改`, Enter on the `×`, with focus staying on the item as its `恢复`, Tab to ⑤'s `设置上限…` (#51, S16a), Tab to the next `×`, and Enter. `plan-edit-update` presses `更新计划`: version 3 on the same Task, with the material inputs as they were, and a `plan-edit` Plan Revision whose two lines are `edited`. The envelope leaves the retry out, the drawer records 「第 3 版由你修改（…）：步骤 · 核对与抽检：要做 → 不做；可以自己调整 · 安全地再试一次：允许 → 不允许」, and ②A's technical list reads `你改的 · 已更新计划`. `plan-revision-dispatch` then runs version 3: two recomputed units and the reduction's turn, no sample, and the revision says 「按你修改的计划，这次运行不做核对与抽检；保证抽样未发起。」. The Run makes no retry the plan withholds, which `tests/service/plan-edits.test.ts` proves under the transient-retry fixture.

Since #502 (S74b) J-04 also launches with `--j04-connectivity-path` and, after the plan-revision stages, takes the device offline through it. `connectivity-offline-bar` prepares 同步到当前稿件 and reads the drawer at `离线`: `联网后开始任务` beside `仅保存任务草稿`, neither focused nor preselected, no button carrying 授权. `connectivity-start-when-online` starts it that way and reads the Run waiting — `等待网络`, the same `standard-direct` authorization `开始任务` records, no attempt, no usage — with `取消` and `查看运行`. `connectivity-cancel` cancels it: `已取消 · 未发送任何内容`, the transitions ending in `cancelled`, no Task Outcome. `connectivity-wait-again` prepares the same mode again and waits again, and `connectivity-online-dispatch` brings the network back: the drawer's Reconnect Preflight admits the Run, whose transitions read `authorized · awaiting-connectivity · admitted · executing`, and it settles to a Result Set Revision with the bar at `已完成`. A missing credential while waiting, a plan that drifted while waiting, a waiting Run surviving a restart, and a Review Run offline are proven by `tests/service/connectivity-wait.test.ts`, `tests/unit/reconnect-preflight.test.ts` and `tests/unit/task-drawer-labels.test.ts`.

Since #500 (S64b part 2) every J-04 launch also carries `--j04-save-path`, the Save dialog's one answer for that launch, which only the 审阅报告's export consumes. After `生成报告`, `review-report-export` presses `导出…` — named for a screen reader `导出「审阅报告 · 第 N 次审阅 · 第 1 版」…` — and ④ 导出's own card opens in the report. Its review is the report's: the parts all `✓ 完整保留`, no 含批注, 含修改建议 or 含备注 to choose, and the sentence that the report is written from its recorded version. Choosing Markdown（备用格式） reviews again, and its 概览表 reads `△ 降级导出`. The destination comes from the launch control, with `（新建文件）`, and nothing is written before `按上述方式导出`, which ends at `已新建「审阅报告.md」。`. The runner reads the file back: the Book's title, the Run and version it reports, the 概览表 as a table with a row for each category the page lists, and the four parts in order. `完成` returns focus to `导出…`. DOCX and PDF of a report, and a report id that names no report of the Book refused, are proven by `tests/service/review-runs.test.ts` (a real report of another Book is refused by the same lookup, and is not proven on its own); each format's layout by `tests/unit/report-export.test.ts`.

Since #421 (S75) J-04 then sets and uses a 默认执行规则. `quick-start-set-rule` switches the drawer to 完整 on the settled 同步, opens `设为快速开始默认…`'s confirmation — closed until asked for, focus on `设为默认`, listing what the rule binds (模型服务 with its Credential Reference, 工序 with the pin, 预算上限, 发送内容类别, 会得到) — and sets it: the drawer names `开始同步 · 第 1 版（使用中，由这份计划设定）`, the action turns unavailable with its reason, and ②A's 同步 names the rule while it waits for an edit. `quick-start-fallback` makes one edit, goes offline and presses the quick start `开始同步`: the Task is prepared exactly as 先看计划 prepares it and stops at its plan, recording no authorization and no Run, the drawer saying why beside `联网后开始任务`. `quick-start-started` comes back online and presses it again: the Task the stop left prepared is prepared again unchanged and started, authorized with origin `default-execution-rule` naming the rule version, and runs `authorized · admitted · executing` to Result Set Revision 8, and the drawer says `已按默认执行规则「开始同步 · 第 1 版」快速开始 · 查看规则`. `quick-start-rules-page` follows `查看规则` to 知识库 › 工序与规则, reads the rule's card and turns it off: `已停用`, still on record. A busy slot, a stale or turned-off rule, a rule whose binding differs, developer-live, a first baseline, the migration from revision 30 and the ledger's immutability are proven by `tests/service/default-execution-rules.test.ts` and `tests/unit/default-execution-rules.test.ts`.

## J-05 Editorial Marks and AI7 Apply for a Change Suggestion

Composes a 40-block manuscript from exact `sample1`, imports it as a new Book through the launch control `--j05-picker-path`, and opens the manuscript. With nothing selected, and with a selection that spans two paragraphs, the selection menu names why the 编辑标记 entries cannot act; on a selection inside one paragraph it shows the three groups 文字处理 (剪切 / 复制 / 粘贴 / 粘贴为纯文本), 编辑标记 and AI7 任务 — the last disabled with `就选区发起的任务尚未接通`, and neither `加入任务范围` nor `在稿件中搜索` present — and answers the arrow, End and Escape keys, returning focus to the text. It then makes the four marks on exact text of one paragraph and asserts each: a 高亮 in the second personal colour with the paragraph's 3 px margin line and no card; a 批注 whose composer quotes the selection and refuses an empty body, whose Mark Card sits below the paragraph aligned to the text column with the source `你`, takes a reply, and leaves the margin line when 标记为已处理; a 备注 that says it is neither exported nor sent to a model and can be rewritten; and a 修改建议 whose creation changes no text, whose card holds the four regions 修改内容 / 修改理由 / 依据与核查 / 你的处理 with nothing preselected and `接受并应用` as its one primary action, and whose replacement shows in place labelled `预览 · 未应用` while the service still returns the original block text. 拒绝 records a Proposal Decision, removes the preview and offers the optional reason chips, of which one is recorded once; 撤回 returns the item to undecided; 修改后接受 with a filled 为什么这样改 (`接受修改后的版本并应用`) records the decision with that reason, asks no second time, and **writes the editor's wording**: the card reads `已应用`, the mark stands on the applied text, the preview is gone, `查看完整凭据` lists the Effect, the Proposal Decision, the Effect Approval, the dispatch, the Effect Receipt and the manuscript state before and after, the service returns the new text with the journal sequence advanced by exactly one, and 撤销 answers `没有可撤销的编辑` because a committed Apply is a history boundary. `接受并应用` on a second suggestion writes its text in one click with the same evidence; `准备撤销本次应用` states what it will write before `确认撤销本次应用`, which writes the original back as one new journal entry and leaves the suggestion open and undecided with its preview again; text typed into the words a suggestion would replace turns it `原文已变` with `接受并应用` and `修改后接受` disabled by name, and 撤销 of that typing makes it exact again. Since #409 the right edge's rail shows the three open system marks as one marker each in its own lane — 修改建议, 批注, 备注, never the highlight — each named with its count, beside the three entries 导航 / 分析 / 任务 (the last disabled until the task surface exists) with the 导航 panel closed and no unread band on a Book never analysed. In a second paragraph a highlight is recoloured and converted to a 备注, the 备注 to a 批注, the 批注 to a 修改建议 carrying the comment as its reason, and back to a 批注, each keeping the pinned text and naming its origin, and the last is deleted. Text typed in front of the four marks moves all four with their text before and after the journal acknowledges it; text typed inside the commented range turns that one mark `原文已变` at once and durably, its card quoting the text it was made on and refusing conversion, while its neighbours stay exact; 撤销 makes it exact again. 剪切 and 粘贴为纯文本 run through the window because the page's own clipboard read is refused. The card reflows at 200% and follows the pane; on that narrow window a menu opened at the text column's right edge stays open and inside the window and brings no scrollbar into the pane — the menu floats over the window, and the step fails on a build where it lives inside the pane. Under forced colours, the three system kinds differ by the line under the text and the highlight by an outline. The context-menu key opens a mark's menu on the caret, Enter opens its card, Escape returns to the text. After a restart — launched with `--j05-apply-control lose-first-acknowledgement` — the four marks, the applied suggestion with its receipt, reason and edited text, and the reply are found where they were; a new suggestion is then accepted, main commits the Apply and withholds its acknowledgement, and the surface, hearing nothing, asks the records by the same Effect identity, reports `写入结果已从记录确认`, shows `已应用`, and the journal sequence has advanced by exactly one.

Not proven here: the batch confirmation strip, which J-04 proves in 审阅's results since #417; reversing an Apply that deleted its text — the mark stays exact at the point its words left, drawn as a point in the text, and the reversal writes them back there, proven by `tests/service/manuscript-apply.test.ts` and `tests/unit/mark-anchor.test.ts`; a mark carried in from an imported file on screen, and `查看任务` pressed on an AI7 mark's card — the record family, its source attribution and the conversions of an AI7 mark are proven by `tests/service/editorial-marks.test.ts`, the lookup behind `查看任务` by `tests/service/review-runs.test.ts`, and a 审阅 mark on screen with its source by J-04, while the importer's marks are J-01's since Issue #411 — drawn with the file's author as source, and an imported insertion applied — and their mapping, reply and done handling, all-or-none creation and the insert kind's Apply and reversal are proven by `tests/service/imported-marks.test.ts`; a rail dense enough to bucket several marks into one marker, and its sparse disclosure past 400 chapters or 2,000 marks, which no test reaches yet — `tests/service/editorial-marks.test.ts` proves the read below both bounds.

## J-06 稿件冲突 of a single 修改建议

Composes a 30-block manuscript from exact `sample1` — few enough blocks that the whole manuscript is one window, so every paragraph the Journey works in stays on screen — imports it as a new Book through the launch control `--j06-picker-path`, and opens the manuscript; the renderer holds `inspectProposalConflict`, `saveProposalConflictDraft` and `resolveProposalConflict` and no execution-facing member. In three paragraphs whose first 60 code units are 60 graphemes it makes a 修改建议 over graphemes 26–32 through the selection menu. Typing one character inside the first suggestion's words drifts it: the card reads `待你处理 · 原文已变`, `接受并应用` is disabled with 原文已变 as its reason, and `解决冲突…` stands beside it as the primary action. 稿件冲突 replaces the manuscript view: `第 1 处，共 1 处未解决`, `需要解决冲突`, the three panes `提案基准` / `当前权威稿件` / `提议内容` aligned per unit with every cell carrying its pane's label, their words exactly the pinned words, the edited words and the proposal — the service's `inspectProposalConflict()` answers the same, one conflict unit and no draft — the context around them from the current paragraph, counted in graphemes, beside `前后文为当前稿件`, the four paths unselected and `基于当前稿件重新生成建议` disabled with `重新生成建议尚未接通`; `返回稿件` brings the card back and nothing was written. A character typed elsewhere in the second suggestion's paragraph leaves it exact with `接受并应用` available and 「本段后来改过别处，没有碰到这条建议的原文」 beside it (ADR 0085 §2), and the card projection says `changedElsewhere` with no conflict. In the Resolution Draft the one unit starts unresolved with `保存为新提案版本` unavailable and its reason in words; `采用当前内容` is saved at once and makes the draft the current words, which the save refuses by name (「解决结果与当前稿件相同，请选「保留当前稿件」」); `撤销` and `重做`, then Ctrl+Z, Ctrl+Shift+Z, Ctrl+Z and Ctrl+Y (Command on macOS), move the draft and each state is the one the service holds; `两者都保留` asks for the order and `提议后接当前` puts the proposal first; `编辑合并结果` opens the unit's field focused on those words, and text inserted as a keyboard does replaces them and is saved once typing rests. After a relaunch the paragraph digests to what it was, the conflict reads on the same basis, and the draft comes back exactly, `草稿已保存`. `保存为新提案版本` returns to the manuscript with the new version's card open — `待你处理`, `由冲突解决生成的新版本`, the edited words replaced by the merged ones, the old rationale, `接受并应用` available, no decision and nothing applied — while the old suggestion is gone and the paragraph unchanged; `接受并应用` then writes exactly that range. The third suggestion, drifted the same way, is kept: `确认保留当前稿件` returns with `已拒绝 · 保留当前稿件`, the reason `保留当前稿件` recorded as suggested, no `撤回`, and the paragraph unchanged. The second, drifted in turn, is deferred: `暂不处理，返回稿件` returns with its state `待你处理 · 原文已变 · 暂不处理 · M月D日 HH:mm`, `接受并应用` still blocked and `解决冲突…` still offered, and 稿件冲突 counts it in `共 1 处未解决`. Typing inside the words the applied new version wrote blocks `准备撤销本次应用`; 稿件冲突 names it `撤销这次应用时遇到冲突：应用后的文字又改过` with `提议内容 · 撤销后会恢复的原文`, and a draft that takes that content is saved as a Correction Proposal — `由撤销冲突生成的更正建议`, the Apply still in force and its conflict resolved by it — which `接受并应用` writes. For J-14, an arrow chooses `自行编辑解决草稿`, Tab reaches the draft and a quick action with visible focus, Enter acts, the draft's keys undo and redo, and Enter on `返回稿件` leaves; at 200% the panes stack, every cell keeps its label and nothing scrolls sideways; under forced colours a conflict unit keeps its heavier left border, the chosen path its heavier frame, and the classification and the draft their dashed lines. A restart leaves every record as it was — compared by a digest taken in the page and the facts it checks — and the Journey ends with zero loopback requests. Its fixed words are pinned to the surface's own by `tests/unit/proposal-conflict-labels.test.ts`. Since #424 (S78) `attention-conflict-row` opens the header's 待我处理 after `暂不处理` and reads the conflict under `异常与结果待确认`: `需要解决冲突 · 暂不处理`, blocking, `修改建议 · 稿件冲突`, the reason in 稿件冲突's own words and `安全的下一步：解决冲突…`; opening it lands on 稿件冲突 of exactly that suggestion, and `返回稿件` comes back to its card.

Not proven here: a conflict whose words were deleted outright, where `保存为新提案版本` is unavailable with its reason — `tests/service/proposal-conflicts.test.ts` proves the refusal and the other two paths; a draft of several units and `将全部无冲突更改加入解决草稿`, which one short suggestion never offers — `tests/unit/conflict-units.test.ts` proves the units and the bulk inclusion; a stale basis refused after a change elsewhere in the paragraph, an accepted-but-unapplied suggestion in conflict, digest-verified records refusing to show a changed row, the navigator across several conflicts, and the revision-25 migration, all proven by `tests/service/proposal-conflicts.test.ts`; a 审阅 finding followed into the new version its conflict saved, proven by `tests/service/review-runs.test.ts`; the mark menu's `解决冲突…` entry, and conflicts from a block split, merged or deleted, which are S63's; and `基于当前稿件重新生成建议`, which waits for the Task surface.

## J-07 发稿: Milestone Versions, 设为发稿版本, 导出 · DOCX and 交付 · 生产文档

Composes a 30-block manuscript from exact `sample1`, imports it as a new Book through the launch control `--j07-picker-path`, and opens the manuscript. On ⑥ 交付物 — reached from the manuscript's `工作` group, which since #429 reads 审阅 · 评估 · 交付物 — the 发稿 · 稿件 block lists no milestone and `设为发稿版本…` stands disabled with `先保存里程碑版本` beside it, and nothing on the page speaks of 生产文档 or 图书交付包. The milestone form in the 导航 panel asks for `标签`, `用途` and `说明（可选）`: the purpose is five unselected cards — 阶段留档 / 送审候选 / 交付候选 / 其他 / 自行输入, the last revealing a field for the editor's own words — and `保存里程碑版本` stays disabled, its reason beside it, until a label and a purpose are given. The first milestone (阶段留档, with a note) answers `已保存里程碑版本「一审稿」 · r1` and the form starts again unselected; after one acknowledged edit its 交付物 item reads `自「一审稿」后有修改`; a second milestone (送审候选) freezes the edit as r2. The list then holds both, newest first, each with its purpose, exact revision, actor and local time, note and later-change line, none marked final or current, every identity in a closed `查看技术详情`, and the service's `inspectDeliverables()` answers the same list. `设为发稿版本…` opens an inline form with both milestones as choices and none chosen, a summary of what will be recorded (图书 · 稿件 · 里程碑版本 · 与当前稿件 · 操作人 · 时间), `发稿范围` and `依据`, and the fixed sentence 「仅表示此版本可用于上述发稿范围；AI7 不会发布或发送」 beside a confirm that stays unavailable, its reason in words, until a milestone is chosen and both fields can be recorded: 81 characters of 发稿范围 are refused by name, while 80 characters outside the Basic Multilingual Plane — 160 UTF-16 units — are accepted, because the bound counts characters as the service does. Confirming with the sentence on screen designates 二审稿 for 纸质版首印 and answers `已设为发稿版本 · 「二审稿」 · r2 · 纸质版首印`: `发稿版本` marks that item and no other, the record lists it as 第 1 次 with its scope, basis and time, the pending line `录入定价与首印 · 随评估功能提供` appears with no action, and no change notice shows, because the manuscript is exactly r2. An identical repeat answers `已是当前发稿版本 · 「二审稿」 · r2 · 纸质版首印` and appends nothing; an acknowledged edit raises `自发稿版本后有修改` naming r2 while the designation stays where it was; designating the older 一审稿 for 电子版首发 — its relation `自「一审稿」后有修改` stated before the confirm — is a separate 第 2 次 record that becomes current and leaves the first one exactly as it was.

Since #413 the composed input also carries a header, a bold run in a paragraph no stage edits, and one comment and one tracked replacement by the file's author, every word of them `sample1`'s own, which the import makes two open marks under the author's name; the editor adds one 备注 through the selection menu. 交付物 offers `导出…` for the current revision and for each milestone, each named for a screen reader by its version. `导出…` of the current revision saves the unsaved edit as r3 — `未保存的修改已为导出保存为修订版 r3；这不是里程碑版本。`, and 交付物's manuscript line then reads r3 — and opens the card with focus on its first choice: DOCX checked of the three formats, PDF and Markdown（备用格式） offered beside it, 含批注 and 含修改建议（作为修订） on, 含备注 off, and the sentence that the export only writes where the editor chooses. The Export Fidelity Review lists six rows — 行内样式, 批注, 修改建议, 分节（含页面设置） and 页眉与页脚 `✓ 完整保留` one each, and 备注 `○ 本次不含` with its count — summarizes the six classes found nowhere on one line, says 27 paragraphs are restored from the original and 3 written anew, and degrades nothing; the service's own review of the same version carries the digest the card's technical layer names. `按上述方式导出` stays disabled with `先选择保存位置。` beside it until `选择保存位置…` takes the path from the Save dialog — answered by the launch control `--j07-save-path` — and shows it as returned with `（新建文件）`. Nothing is written before the approval, which ends at `已导出到所选位置` and `已新建「发稿旅程甲.docx」。` with `在文件夹中显示` focused; 交付物's `导出记录` lists it, the service's receipt names the file on disk by its size and SHA-256, and the chosen folder holds that one file and nothing half-written. The runner then reads the file itself: its paragraphs are the manuscript's, the two edits in the first; the untouched paragraph keeps its bold run and the header part is byte for byte the original's; the comment and the replacement, written as `w:del` and `w:ins`, carry the file author's name; and no 备注 was written. `完成` returns focus to the `导出…` 交付物 drew again after the export.

After a restart 交付物 answers byte for byte as before, its export record included, and 工作概览's one line reads `发稿 · 里程碑版本 2 个 · 发稿版本「一审稿」 · r1 · 电子版首发 · 自发稿版本后有修改` with `打开交付物` leading back to the destination. That launch answers the Save dialog once more: `导出…` of 一审稿 reviews r1 with nothing to save and takes the destination, and `取消` then closes the card with `已关闭导出，没有写入任何文件。` and focus on its opener, writing and recording nothing. No page and no record says 已发布, 已发送, 已交付 or 已确认送达, no control publishes or sends, the only controls that name 导出 are the `导出…` of each version, and the renderer holds 导出's four members and no member that publishes or sends. For J-14, Enter on `设为发稿版本…` opens the form on its first choice, an arrow chooses, Tab reaches 发稿范围, 依据, the confirm and 取消 with visible focus, and Enter on 取消 closes the form with focus back on the opener and nothing recorded; at 200% the lists, the open form and the persistent actions reflow without sideways scroll; under forced colours the 发稿版本 mark keeps its outline, its item a heavier border than the others, and the change notice and the fixed sentence their lines. The export card answers the same obligations: Enter on `导出…` opens it on DOCX, Tab reaches each option with visible focus, Space on 含备注 reviews again — the 备注 now `完整保留` — with focus kept, Tab reaches `选择保存位置…`, left alone because that launch's Save dialog has answered, and `取消`, and Enter on `取消` closes the card with focus back on the opener and nothing written; at 200% the card, its review, its actions and the export records reflow without sideways scroll; under forced colours the card and the export record keep their frames, a chosen option a heavier border than one left off, and every status its shape and words.

Since #500 (S64b) two more launches, each with a Save-dialog answer of its own, export the same revision r3 as a PDF and in the Markdown 备用格式. Choosing the format reviews again, and the review is the format's own: the format's line, the sentence that the file is laid out from the manuscript's words in place of the restoration count, 行内样式, 分节（含页面设置） and 页眉与页脚 `⊘ 无法导出`, 批注 and 修改建议 `△ 降级导出`, 备注 `○ 本次不含`, and each option's note in the format's words. The approval accepts those degradations for that export alone, and each export ends at `已新建「发稿旅程甲.pdf」。` or `已新建「发稿旅程甲.md」。` with its receipt. The PDF on disk begins `%PDF-` and is byte for byte the receipt's, and AI7's `export-staging` folder keeps nothing of the print. The runner reads the Markdown itself: its 30 paragraphs are the manuscript's words, the two edits in the first, escaped as the writer escapes them; the author's comment is footnote 1 after its words, `批注 · 示例作者`; the tracked replacement stands where it was as `{~~…~>…~~}` with footnote 2, `修改建议 · 示例作者`; and no 备注 is written. 交付物 then lists three export records. The Journey ends with zero loopback requests. The runner's fixed sentence, pending line and forbidden words are pinned to the service's own by `tests/unit/deliverables-labels.test.ts`, and the card's words by `tests/unit/manuscript-export-labels.test.ts`.

Since #415 (S66a) 交付物 also shows 交付 · 生产文档 beside 发稿: before any source material each of the house's five types (新闻稿, 宣传文章, 评论文章, 发布会材料, 营销要点) reads `尚未创建`, its `从来源材料创建…` unavailable with the reason in words beside it. A later launch whose picker answers with a three-paragraph draft composed from exact `sample1` imports it through `导入稿件 → 作为来源材料导入`. Back on 交付物 the five cards stand in the house's order, none made, each offering `从来源材料创建…` and `本书不做`, and nothing reads as progress. `从来源材料创建…` on 新闻稿 offers the one material with nothing preselected, and `创建文档` waits for the choice. The document then opens on the manuscript's own surface. It is named `新闻稿 · 版本 1` and `当前版本 版本 1` under `发稿旅程甲 · 生产文档`. Its `工作流程` column lists `版本与交付` with 版本 1 marked current, and `这份文档的材料` is empty. `返回交付物` is its only work destination and `导航` its only edge entry. It shows no 分析, no milestone form and no 里程碑, 签发 or 发稿 words of its own, and its paragraphs are the draft's. An edit is written to the document's journal, and `保存为版本` makes 版本 2, listed above 版本 1. The card then reads `版本 2 · 由「新闻稿初稿.docx」创建` with `打开`. `本书不做` on 宣传文章 is recorded with focus on `恢复`, and `恢复` puts it back with focus on `本书不做`; the service agrees on every type. After a restart 交付物 answers byte for byte as before, and the 新闻稿 opens at 版本 2 with its edit.

Since #547 the draft also carries one comment and one tracked replacement by the file's author, every word of them `sample1`'s own. The document's paragraphs are still the draft's, read with every revision rejected. Its `这份文档的材料` says `创建时，来源材料里的 2 处批注与修订没有带入：文字按全部修订被拒绝时的样子读出，批注不带入。` when the document opens, its card says the same beside `版本 2 · 由「新闻稿初稿.docx」创建`, and after the restart the reopened document still says it.

Since #415 (S66c) the 新闻稿's surface opens its 工作流程 column with the Deliverable Workflow: `基础书稿编辑流程 2.0.0 · 启用于 …`, `七个阶段都未开始`, `目前没有需要处理的事项`, and the seven phases, 接收与准备 to 维护, each offering `开始` and `跳过…`. `开始` and then `完成` of 起草 are said (`「起草」已开始`, `「起草」已完成`) with focus on the phase. `跳过…` of 来源建设 opens its reason form with nothing chosen: confirming without a reason says `请先选一个原因。` and moves nothing, and `这一阶段已在别处完成` skips it, the reason under `查看原因`. `重新打开…` of 起草 in the editor's own words reopens it, `共 3 次变动`. The column then reads `1 个阶段进行中 · 0 项等待处理` with `起草 · 已重新打开` under 下一项需要处理. Back on 交付物, the card shows the same summary, that 下一项 and a chip per phase in its state. The restart's byte-for-byte reading of 交付 · 生产文档 includes the workflow. Later, beside the first delivery, `开始` of 交付 reads `进行中`; after another edit, with no phase moved, the column reads 交付 `等待你处理 · 有修改尚未保存为版本`, first under 下一项需要处理.

Since #415 (S66b) the same launch records deliveries. The 新闻稿's card reads `尚未交付`, and `交付…` offers its two saved versions and the house's recipients, none preselected, beside the sentence that a delivery records and never sends. `交付` waits for a version and then a recipient. 版本 2 to 宣传部 with a note records `第 1 次交付 · 宣传部 · 版本 2`, and the export card opens on `导出 · 新闻稿 · 版本 2` by itself. Exported under the launch's Save-dialog answer, the file's paragraphs are the document's version, and the record reads `已导出到所选位置 · 新闻稿 · 版本 2.docx`; `完成` returns focus to the record's own `导出…`. The document's `工作流程` column lists the delivery. After another edit the card reads `交付后有修改` beside `有修改尚未保存为版本`. `再交付…` then offers `现在的文字（交付时先保存为版本 3）` before the saved versions, none preselected, and `自行输入` waits for its words. Delivering the current text to 出版社发行部 saves it as 版本 3 and records `第 2 次交付 · 出版社发行部 · 版本 3` newest first, and `交付后有修改` is gone; the export card opens on `导出 · 新闻稿 · 版本 3`, and cancelled, it writes nothing and leaves the record at `暂无导出记录`. The service agrees on every record. After the restart the three answers, 交付物's, 交付 · 生产文档's and 图书交付包's, read byte for byte as before, and the 新闻稿 opens at 版本 3 with both edits.

Since #416 (S67a) 交付物 ends with 图书交付包. Before any milestone it lists every condition and waits, naming what is missing. After the deliveries it states that a package is neither 发稿 nor 交付, and it lists the conditions: the 发稿版本 `发稿版本「一审稿」 · r1`, with `自发稿版本后有修改` beside it; the 新闻稿 at `第 2 次交付 · 版本 3`; the four other types `尚未创建`; and `暂无审阅记录`. `准备图书交付包` waits and names the four types. A row's route focuses that type's own `从来源材料创建…`. Once the four are marked 本书不做 on their cards, every condition holds. The preview then lists the 发稿版本 and `新闻稿 · 版本 3` as included, the four types and 备注 / 资料库原件 / 中间修订版 as left out, and the two limitations. `准备图书交付包` waits for a purpose. With it, v1 reads `v1 · 图书交付包已准备 · 暂无导出记录` with its purpose and summary, and no file is written. The same purpose again leaves v1 unchanged. A new purpose makes v2 above v1, naming it, and v1 reads as it did.

Since #416 (S67b) a version exports. `导出…` of v2 lists the three files it writes, each with what it holds, its file name and its format: the 发稿版本 `稿件 · 发稿版本「一审稿」 · r1` and `新闻稿 · 版本 3` as DOCX, and `交付包清单.md` in Markdown. Beside them stands the sentence that the files go only to the chosen folder, each with its own export record, and nothing is sent. `含批注` and `含修改建议（作为修订）` are on, and each file carries its own Export Fidelity Review, open exactly when the file is degraded, as the service's own review of v2 says; turning `含批注` off reviews the files again with focus on the switch, and it is turned on again. `按上述方式导出` stays disabled with `先选择位置。` beside it. `选择位置…` takes the run's empty folder from the folder dialog — answered by the launch control `--j07-folder-path` — and shows `导出到：` that folder; every file reads `已准备` and nothing is written yet. The approval writes the three files one after another and answers `已导出到所选位置 · 3 个文件`, each file `已导出到所选位置`, with `在文件夹中显示` focused. v2 then reads `已导出 1 次` with the export's line and folder, and v1 still reads `暂无导出记录`. The folder holds exactly the three files; each DOCX is a package; and the 交付包清单 is byte for byte the version's own words as the package and its Delivery Records answer them — its purpose, when it was prepared, its content digest, what it holds, both deliveries of the 新闻稿, what it leaves out and its limitations. 交付物's own export records do not list the package's files, and `完成` returns focus to v2's `导出…`. After the restart the package's answer, its export history included, reads byte for byte as before.

Since #426 (S68a) each 发稿版本 carries its 维护事项. `记录维护事项…` on the first designation opens a draft bound to it: `维护事项绑定到：第 1 次 · 「二审稿」 · r2 · 纸质版首印`, and the six classifications 更正, 勘误, 替代, 撤回, 再版 and 归档, unselected, each with what it does. `记录维护事项` waits with `先选择维护类型。` and then `请写明原因（1–500 个字）。` beside it. A 勘误 with its reason answers `维护事项已记录`. The case opens in place: `第 1 项维护事项 · 勘误`, bound to that designation, `状态：未解决 · 下一步：编写勘误`, and its timeline's first line, with focus on it. `编写勘误…` saves `勘误 · 第 1 版` as the timeline's second line. `记录维护事项结论…` with `已完成（AI7 内记录）` and a 结论 answers `维护事项结论已记录`; the case reads `已完成（AI7 内记录）` and offers no more steps. 撤回 of the current designation shows `仅在 AI7 内记录；不代表已撤稿、下架、召回、通知接收方或删除外部文件` in the draft, on the case and on the designation. 图书交付包's first condition then reads `发稿版本「一审稿」 · r1 · 已在 AI7 内撤回` and the package is not ready. The service agrees on both cases, no word of the page claims a correction, withdrawal or reissue outside AI7, and after the restart 交付物 answers byte for byte as before.

Since #427 (S79b), after the restart, 知识库's `范例` holds the Book: its title, 「作者：未填写 · 责编：未填写」, its latest designation by ordinal and time — 「第 N 次设为发稿版本于 … · 已在 AI7 内撤回；之后交付的文档，另设发稿版本后才归入」, since `maintenance-cases` withdrew it — and the 新闻稿 it delivered before that — the version its latest delivery named, 「学习准入：仅本社」, and the version delivered before it — with no further page, exactly as `inspectExemplars()` answers. A Book with no 发稿版本 brings nothing in; a document delivered before the designation comes in at it; one delivered after a 撤回 waits for another 发稿版本 and comes in at it; a Delivery Record rewritten behind the ledger is refused; the earlier versions are counted past ten; and the Books come twenty at a time by title — all proven by `tests/service/exemplars.test.ts` over the real store. `更多已出版的书…` is not pressed in any Journey.

Since #426 (S68b) a case still waiting on the editor comes to 待我处理. A 替代 on the first designation raises the header's count by one. 待我处理 lists it under 等待你的决定 as `维护事项 · 第 3 项 · 替代 · 第 1 次发稿版本`, `维护事项待处理 · 等待另设发稿版本`, with its reason and `安全的下一步：关联发稿版本`. The concluded 勘误 and the 撤回 never come there. Opening the item returns to 交付物 with the case open and focus on its heading. Five more 勘误 on that designation push the 替代 out of the five cases 交付物 lists; opened from 待我处理 again, it is drawn open below them with focus on its heading, and `还有 1 项更早的维护事项` stands for the oldest. An 归档 of that designation closes its maintenance, and the count falls back.

Not proven here: a milestone's export carried through to its file, a replace confirmed by the Save dialog, destinations refused inside AI7's data or without the format's extension, an approval refused because the payload or the destination drifted, a write that changed nothing (`未能导出`) or was interrupted (`结果待确认`, never retried), the ledger's immutability and the revision-28 migration, all proven by `tests/service/manuscript-export.test.ts`; a PDF approved before it was printed, or from a file that is no PDF, which records nothing, proven by `tests/service/manuscript-export-formats.test.ts`; the PDF page's layout and Markdown's headings, escaping and hard breaks, notes numbered in reading order and a reference that falls inside a change, proven by `tests/unit/docx-export.test.ts`; the degradations the review names for an edited paragraph, 批注 replies and 已处理, a merged text box, and a revision without a mapping written fresh, proven by `tests/unit/docx-export.test.ts`; a Save dialog the editor cancels and `在文件夹中显示` pressed, which the platform's own dialog and file manager answer and no runner drives; a mark on a document, the refusals of a second document of a type, of the Manuscript's own file and of a Book without a Manuscript, the ledgers' immutability and the revision-36 migration, proven by `tests/service/production-documents.test.ts`; a document made from a converted `.txt` or `.doc` material, which no layer proves yet; a document's workflow phases and gates, which are S66c and S66d's; a Delivery Record kept on its file after a re-export that wrote nothing, and an earlier version delivered again without `交付后有修改`, proven by `tests/service/production-documents.test.ts`; the 图书交付包's work records waiting for a report newer than a continued Review Run, proven by `tests/service/review-runs.test.ts`, and the same `交付后有修改` in its rows, proven by `tests/service/book-delivery-packages.test.ts`; a package export refused for a stale review, a folder inside AI7's data or one that already holds a file name, a file put in the folder after the preparation, or a 批注 made on a file's text, which refuses the set whole before anything is written, one put in the folder while the writes run, which stops the rest while the written ones stand, a refusal before anything was written that leaves the export prepared, no second write when approved again, a degraded file's review and the switches bound into every preparation, a 交付包清单 a later release words otherwise, read from its records, and the export ledgers' immutability and the revision-40 migration, all proven by `tests/service/book-delivery-package-exports.test.ts`; a failed or unconfirmed file of a package export, which `tests/unit/book-delivery-package-labels.test.ts` proves in words only; the package's archive as one `.zip`, which no Issue owns yet; 更正's 修改建议 and later designation linked, 替代 and 再版 waiting for a designation and refused 已完成 until one is linked, a designation's older cases read twenty at a time, 归档, a second 撤回 or 归档 of one designation refused, a stale or complete case refused a step, the same milestone designated again after 撤回 as a new 发稿版本, the ledgers' immutability and the revision-42 migration, all proven by `tests/service/maintenance-cases.test.ts`; `更早的维护事项…` pressed, which no Journey reaches with five cases or fewer on a designation; the ordering and count of `维护事项待处理` items, and a 替代 or 再版 read as waiting after an interim 仍未解决, proven by `tests/unit/global-attention.test.ts` over maintenance readings, and its words by `tests/unit/global-attention-labels.test.ts`; the ED-052 save menu opened from the persistence status, which no Issue owns yet — the milestone form stays in the 导航 panel; the exemplar archive of the Book's 审稿意见 into 范例, recorded with every designation for S79 and consumed by nothing yet, and the 定价与首印 entry the pending line waits for (S82) — both events are proven recorded by `tests/service/publication-versions.test.ts`; refusals of another Book's milestone, of a Book without a manuscript and of a designation whose record no longer digests to what was written, and the revision-24 migration, all proven by `tests/service/publication-versions.test.ts`; a milestone list or a designation history past its caps of 100 and 30, which no test reaches on screen — `tests/unit/publication-wording.test.ts` proves the widest answer fits one service frame.

## J-09 待我处理: the cross-Book attention view

Imports exact `sample1` twice through the launch control `--j09-picker-path` — the second time as a new Book of a distinct intended work — so the Journey holds two Books, 待我处理旅程甲 and 待我处理旅程乙; the renderer holds `inspectGlobalAttention` and no member named like execution. The shell's header carries `待我处理` with no number while nothing needs the editor. In a first launch with no executable route, 甲's baseline analysis is prepared and started from the Task Drawer's bar, and its Run is recorded and blocked before dispatch; on the next screen the header reads `待我处理，1 项需要你处理` with the number in its outlined badge, and a relaunch keeps it. A second launch binds the J-04 model adapter to `sample1-baseline-happy`: 乙's first baseline analysis runs to completion, and a `重新分析所选范围` Task prepared over unit 3 and then over unit 8 leaves a pending Plan Revision. 待我处理 then lists its four groups in their fixed order, as the service's own `inspectGlobalAttention()` answers item for item: `异常与结果待确认` holds 甲's Run — `《待我处理旅程甲》`, `基线分析 · 首次基线分析`, `派发前已阻止` with its diamond, the reason 「授权已记录，派发前阻止：当前启动没有可执行的路由。」, `安全的下一步：查看运行` and `记录于` a local time; `等待你的决定` holds 乙's `计划修订` with `安全的下一步：重新确认计划`; `运行中与已暂停` keeps its heading and says 「没有正在运行或中途停止的任务。」; `最近完成` holds 乙's completed analysis, `已形成第 1 份基线分析` and `完成于` a local time. Every identity waits in a closed `查看技术详情`, no item is called 已暂停, and nothing on the screen says 待审批 or 批准 or carries 授权 on a control. The count is the first two groups' and no other — `2`, with `1 项需要你处理` on each counted heading, `1 项` on 最近完成, and the header's entry reading `待我处理，2 项需要你处理`. Reading the view — the window regaining focus, the entry pressed again, the service asked three times at once — leaves every relation of the store byte for byte as it was, compared by a digest of every row taken through a separate read-only connection, and answers the same. Each item opens its own record in the window that asked: 甲's 分析 with the blocked Run, 乙's 分析 with the Task Drawer on the changed plan and `重新确认计划` offered there while the Plan Revision stays pending, and 乙's 分析 for the completion. For J-14, Enter on the header's entry opens the screen with its heading focused, Tab reaches the first item's way in with visible focus and Enter opens its record; at 200% the header, the four groups and every item reflow without sideways scroll; under forced colours blocked work keeps its heavier left rule, a state its pill's shape, the number its outline and each group its border. The Journey ends with zero loopback requests. Its words are pinned by `tests/unit/global-attention-labels.test.ts`, and its grouping, order, bounds and count by `tests/unit/global-attention.test.ts`.

Concurrent Book work (Issue #49, plan slice S14) follows in the second launch, which also names J-10's unit hold, `--j10-unit-hold-path`, admitted under J-09; its file does not exist until then, so nothing before is held. AI7 closes and opens again under the same controls — the picker's file serves one import per launch — and a third Book, 待我处理旅程丙, is imported the same way and its profile enabled. With the hold at one range, 甲's first baseline analysis is started again from its 分析 — the Run of the first launch never began — and 丙's first baseline from its own: both execute at once, each having read one range of its own Book and holding the next in flight, as `inspectGlobalAttention()` answers. 乙's Plan Revision is then reconfirmed from 待我处理 and its range update started from the drawer's bar while both places are taken: the bar reads `等待运行名额` with 「运行名额已满：正在运行的任务结束后，这项任务自动开始；在此之前什么都没有发送，也不产生用量」 beside `取消` and `查看运行`, ②A reads `等待运行名额`, and the Run is recorded `authorized` alone, with no progress and no attempt; under forced colours its pill keeps its hollow ring. 待我处理 then lists the three under `运行中与已暂停`, each by its own Book — 甲 and 丙 running, 乙 `等待运行名额` with 「运行名额已满：正在运行的任务结束后，这项任务自动开始；在此之前什么都没有发送。」 and `安全的下一步：查看运行` — counts none of them, and the header shows no number. `暂停` on 甲, from its own plan, reads `正在暂停`; the editor then opens 丙's manuscript with the caret in its text, and the hold moves to two ranges: 甲 finishes its range and pauses, and its place goes to 乙, whose Run starts in the background while the screen, the Book, the drawer and the focus stay exactly where the editor left them. `取消任务` on 丙, from its own plan, reads `正在取消`, and with the hold released 丙 stops after its range in flight while 乙 runs to its end beside it. Each Run's records stay its own Book's: 丙 is `已取消` with a partial revision of the three ranges it read, from its own Run and attempt; 乙's range update — admitted by the governor, 「已进入 AI7 调度器。」 — is its Book's second revision; 甲 is `已暂停` with the two ranges it read kept and no revision; and the three attempts are distinct. The governor's words are pinned by `tests/unit/task-drawer-labels.test.ts` and `tests/unit/global-attention-labels.test.ts`.

Not proven here: `运行中与已暂停`'s Review Run items — one being driven, and one a stopped service left mid-way with `继续审阅` as its step — and a failed or refused Review Run, an orphaned baseline Run, an import whose commit outcome cannot be proven, an abandonment whose cleanup is pending and a Recovery Attention State, all proven over the real store, with the read writing nothing, by `tests/service/global-attention.test.ts`; a group past its limit and the widest answer within one frame, proven by `tests/unit/global-attention.test.ts`; 稿件冲突 of a 修改建议 (ATTN-002's Manuscript Conflicts), which the view does not list yet; the material and knowledge-base items of ATTN-009, whose records do not exist yet and which `GLOBAL_ATTENTION_MATERIAL_GROUPS` only places; and the same view in several windows at once. The governor's queue order, a queued start 取消 before its turn, a queue AI7 closed on taken up again at the next start, a start a launch cannot admit blocked with the owner's reason, and a Controlled Apply Effect committed during a Run standing after that Run is cancelled are proven over the real store by `tests/service/concurrent-runs.test.ts`.

## J-10 暂停, 续行, 取消任务, 改计划重做, 先问你, the Run Budget Ceiling and the account limit on a Run under way

J-10's operations told apart by their consequence (Issue #422, plan slices S76a to S76d; Issue #51, S16a and S16b). Imports exact `sample1` through the launch control `--j10-picker-path` as 取消任务旅程, installs and enables the editorial workspace profile at Revision 2 for it, and saves and removes one Main Editorial Role connection's synthetic credential, as J-09 does; the renderer holds `cancelBaselineAnalysisRun`, `pauseBaselineAnalysisRun`, `resumeBaselineAnalysisRun` and `answerBaselineAnalysisClarification`, and no member that redoes, retries, replays or rewinds a Run — `改计划重做` prepares its new Task through `prepareBaselineAnalysis`, naming the Run it redoes. Every Journey launch binds the J-04 model adapter to `sample1-baseline-happy` and J-10's unit hold, `--j10-unit-hold-path`, whose file the Journey writes: two reading ranges may settle, and the third waits in flight once its turn is back. The first baseline analysis is started from the Task Drawer's bar, and the drawer follows the Run: `运行中`, and above the plan the activity card `运行动态` — `阶段` 正在逐个阅读范围分析, `当前` 第 3 个阅读范围（共 8 个）, `用时` this step's and the Run's, `尝试` 已派发, `上次更新` and `进展` 已读完 2 / 8 个阅读范围 · 已完成模型回合 2 次 — which says 「本步骤用时已超过通常水平」 once the held step outruns this Run's own. The bar offers `暂停`, `取消任务`, `改计划重做` disabled beside 「先暂停，再改计划重做」, and `查看运行`.

`暂停` is one click and asks nothing: the drawer reads `正在暂停` at once, its bar only `查看运行` beside 「已记下你的暂停；正在进行的这一步完成后停下，已完成的部分都会保存」, the activity card still on the range in flight, ②A's card `正在暂停`, and 待我处理's `运行中与已暂停` holds the Run as `analysis-pausing`. It stays `正在暂停` while that range is held. Released, the range finishes and is kept, and the Run waits at the boundary after it: `已暂停`, its bar `续行`, `取消任务`, `改计划重做` and `查看运行`, beside 「已读完 3 / 8 个阅读范围，结果都已保存；续行时从第 4 个接着读，不重复已读完的部分」, which the activity card repeats in place of what is in flight; ②A's card `已暂停`, 待我处理 `analysis-paused`, the record `authorized → admitted → executing → pausing → paused` with three unit turns, and no Task Outcome or revision. `续行` goes on in the same Run and the same attempt from the fourth range, reading none of the first three again; the Journey lets two more settle, and the drawer reads `运行中` with the sixth in flight and the running bar again.

`取消任务` then opens the inline `取消影响摘要` with its heading focused and records nothing: the range in flight finishes and the two after it, the reduction and the sample do not run; the ranges read and the one in flight are kept in a new Result Set Revision, the rest not attempted, and it becomes the Book's latest analysis; the analysis changes no manuscript and commits no Effect; and the answer in flight is never cut off — the same four lines `inspectTaskPlan()` answers. `继续运行` closes it with focus back on `取消任务`. By keyboard alone — Enter on `取消任务`, Tab to `确认取消任务` with visible focus, Enter — the cancellation is confirmed, and the drawer reads `正在取消` at once, its bar only `查看运行` beside 「已记下你的取消；正在进行的这一步完成后停止，此后不会再发送任何内容」, the activity card still on the range in flight, ②A's card `正在取消`, and 待我处理 holds the Run as `analysis-cancelling`. It stays `正在取消` while that range is held; under forced colours its pill keeps the half shape and the card its heavier edge. Released, the range finishes and the Run stops at the boundary after it: `已取消` in the drawer, its bar `改计划重做` and `查看运行`, and ②A's card `已取消`. The record is `authorized → admitted → executing → pausing → paused → admitted → executing → cancelling → cancelled` with one attempt holding six unit turns, ranges 1 to 6; the Task Outcome is `cancelled`, `任务结果：已取消`; the partial revision closes six ranges of eight and names ranges 7 and 8 `not-attempted`; and the Run Report shows the cross-unit reduction and the sample not run and the reflection 「运行已按你的要求取消，运行反思未发起。」. Nothing more is recorded after it stopped.

`改计划重做` on the cancelled Run asks nothing more: it prepares a new Task — its own Task Intent, plan and envelope, `同步到当前稿件` — that carries the six ranges the Run kept and reads the other two. Its `taskIntent.redoOf` names the Run and its Task, its reuse plan reuses six and recomputes two, and its predecessor is the Run's partial revision. The drawer opens it in its editing, `完整` with focus on 核对与抽检's `×`, and reads 「改计划重做：沿用已读完的 6 个阅读范围，接着读其余 2 个」 with the technical row `redo-of` naming the Run and its Task; ②A's card holds it `prepared`, and nothing runs. `×` leaves 核对与抽检 out and `更新计划` makes version 2, its Plan Revision `plan-edit`. `开始任务` runs it as a new Run — `authorized → admitted → executing → completed`, one turn each for ranges 7 and 8 — and its revision closes all eight with the sample not run; ②A's card and the drawer read it settled.

AI7 then closes and opens again, and a second Book, 续行旅程, is imported from exact `sample1` as a distinct intended work and its profile enabled. Its first baseline analysis is started with one range allowed to settle, and AI7 closes while the second range's turn is back and not yet settled. On the next launch — the hold released — 待我处理 holds the Run as `analysis-resumable`, and ②A's card reads `任务已中断 · 可续行`: the record `authorized → admitted → executing → resumable` with two unit turns — the turn AI7 closed under came back whole, so it was kept — and no Task Outcome. The plan's drawer reads `任务已中断 · 可续行`, with `续行`, `取消任务`, `改计划重做` and `查看运行`, beside 「已读完 2 / 8 个阅读范围，结果都已保存；续行时从第 3 个接着读，不重复已读完的部分」, and nothing is sent until the editor says `续行`. `续行` goes on in the same Run and attempt from the third range to the end: `completed`, one attempt with exactly one turn for each of the eight ranges, and a revision closing all eight.

A third Book, 改计划重做旅程, is imported the same way, from the library reached by ②A's `工作概览`. Its first baseline analysis is paused with the second range in flight; released, that range is kept, and the Run reads `已暂停` with two ranges read and the stopped bar. `改计划重做` opens the inline `改计划重做摘要` with its heading focused, and records nothing: the Run stops here and is cancelled, its two ranges kept in a new Result Set Revision and the rest not attempted; a new Task then carries those two and reads the other six, and can be changed before it starts; the analysis changes no manuscript and commits no Effect; and the new Task starts only when the editor starts it — the same four lines `inspectTaskPlan()` answers. `先不重做` closes it with focus back on `改计划重做`. By keyboard alone — Enter on `改计划重做`, Tab to `确认改计划重做` with visible focus, Enter — the redo is confirmed. The Run is cancelled through the slot into its partial revision, which closes ranges 1 and 2, names 3 to 8 `not-attempted`, and gives that Run, its Task and its attempt as its provenance. Once the Run reads `已取消`, the new Task is prepared — `同步到当前稿件`, reusing two and recomputing six, 「改计划重做：沿用已读完的 2 个阅读范围，接着读其余 6 个」 — and opens in its editing; a moment later it has still not run.

AI7 then opens over the transient-retry fixture — unit 2 fails for good, and unit 5's first attempt fails retry-safe — and a fourth Book, 澄清旅程, is imported the same way. Before its first baseline analysis starts, `返回修改` offers the safe retry `×` and `先问你` (`改成先问你：…`). `先问你` moves it into the right column, where it reads `你改的 · 先问你` with `恢复` and focus on it, and the bar says `你改了 1 处`. `更新计划` makes that version 2, whose envelope lists it under `askFirst` and none as adaptable, and whose plan says in `需要你参与的位置` 「模型服务暂时出错时，先问你要不要把这个阅读范围安全地再试一次；只有等你回答的这一步会停下。」. Started with five ranges allowed to settle, unit 5 asks instead of retrying: 6 is read and 7 is held in flight. The card above the plan — `需要你回答` — asks 「第 5 个阅读范围：模型服务暂时出错，这一次没有读成。要安全地再试一次吗？」 under 「该步骤等待说明 · 其他步骤仍在继续」, with `再试一次` marked `推荐` and `不重试，记为缺口`, neither chosen, and `提交回答` disabled until one is. The bar stays `运行中` with 「有 1 个问题等你回答」, and 待我处理 holds it in `等待你的决定` as `analysis-clarification`, not blocking. `暂不回答` sets the card aside as one quiet line, 「有 1 个问题等你回答」 with `回答`, focus on it, and records nothing. Released, ranges 7 and 8 are read and the Run stops at that boundary: `authorize → admitted → executing → awaiting-clarification`, eight turns, no revision; the drawer's pill `任务等待你的说明`, the bar `等你回答` beside 「已读完 7 / 8 个阅读范围；1 个问题等你回答，回答后接着做」 with `取消任务`, `改计划重做` and `查看运行`, ②A's card the same, and 待我处理's item now blocking. AI7 closes and opens again: the Run still waits, nothing was sent, and the card is back, under 「任务等待你的说明」. By keyboard alone — from the card's heading, Tab to `再试一次` and Space, Tab to `自行说明…` and Enter, the note written, Enter a new line in it that submits nothing, Tab to `提交回答` with visible focus, Enter — the answer is recorded with its note, and the Run goes on in the same attempt: `… → awaiting-clarification → admitted → executing → completed-with-gaps`, unit 5 retried as its second attempt and closed, unit 2 the one gap, the adaptation record naming the answer. The drawer's `问过你的问题` keeps the question and 「你已回答：再试一次 · 说明：服务刚才在维护（…）」.

AI7 then opens over the happy fixture again, whose turns report the tokens a ceiling counts, and a fifth Book, 预算上限旅程, is imported the same way. In `完整` the plan's ⑤ reads `未设置任务预算上限`, 「用量和费用仍受所选模型服务的账户控制与计费条款约束。」 and `设置上限…`. `设置上限…` opens one field, `预算上限（tokens）`, with focus in it. 「五千」 is refused in words — 「请填一个大于 0 的整数，单位是 tokens。」 — with nothing kept, and `5,000` with Enter becomes `你改的 · 预算上限 5,000 tokens`, focus back on `设置上限…`, and the bar says `你改了 1 处`. `更新计划` makes that version 2: the ceiling frozen into its Provider Resolution Plan and its material inputs, a `plan-edit` Plan Revision, and no drift — the plan can start. ⑤ now reads `任务运行预算上限：5,000 tokens`, its usage `达到 5,000 tokens 后不再发送新的请求（8 个阅读范围）` marked `上限，不是预测`, with `去掉上限` beside `设置上限…`, and the drawer records the change. Started, the Run spends 6,620 tokens on four ranges; the fifth would pass the ceiling, so it is never sent. The record is `authorized → admitted → executing → interrupted` with four unit turns. The Task Outcome is `interrupted`, `任务结果：任务运行预算已达上限 · 已保留部分结果`, and names the stop: the ceiling, the tokens used, and 4 of 8 ranges read. The partial revision closes four ranges and names 5 to 8 `not-attempted`. The drawer reads `已停止 · 预算已达上限`, its bar beside 「已读完 4 / 8 个阅读范围，结果都已保留；这次运行用了 6,620 tokens，达到了预算上限 5,000 tokens」 with `调整预算并重做` and `查看部分结果` and no `续行` or `重试`, and 待我处理 holds it in `异常` as `analysis-budget-reached`, its next step `调整预算并重做`. `查看部分结果` opens ②A, whose card reads `任务运行预算已达上限 · 已保留部分结果`. `调整预算并重做` prepares a new Task — `同步到当前稿件`, reusing four and recomputing four, 「改计划重做：沿用已读完的 4 个阅读范围，接着读其余 4 个」, its first version carrying the 5,000 tokens — and opens it in its editing with focus on `设置上限…`. By keyboard alone — Enter, the count typed, Enter — the ceiling becomes 20,000 tokens with focus back on `设置上限…` and visible, and Tab reaches `去掉上限`. `更新计划` makes version 2, whose Plan Revision line reads 5,000 → 20,000. Started, the redo reads ranges 5 to 8 to its end, `completed`, naming no stop, and 待我处理 no longer holds the Book.

AI7 then opens over the account-limit fixture — unit 4's first turn is refused on the model service's account limit, and its next is the happy fixture's result, the provider-side condition clearing — and a sixth Book, 账户限额旅程, is imported the same way. Started, its Run reads ranges 1 to 3, and the model service refuses the fourth. The Run stops at that boundary: `authorized → admitted → executing → resumable`, four unit turns — the refused one recorded and not kept — and no Task Outcome or revision. The drawer reads `模型服务账户限额`, never `任务已中断 · 可续行` (RUN-012). Its bar sits beside 「模型服务按账户限额拒绝了第 4 个阅读范围。已读完 3 / 8 个阅读范围，结果都已保存；处理好模型服务、限额解除后点「续行」从第 4 个接着读」 with `处理模型服务`, `续行`, `取消任务`, `改计划重做` and `查看运行`. ②A's card reads `模型服务账户限额`, and 待我处理 holds it in `异常` as `analysis-account-limit`, blocking, its next step `处理模型服务`. `处理模型服务` opens 设置 › 模型服务 and moves nothing about the Run. Back in ②A the plan reads the same, and `续行` goes on in the same Run and attempt from the fourth range, whose second turn is served: `… → resumable → admitted → executing → completed`, with turns for ranges 1, 2, 3, 4, 4, 5, 6, 7 and 8. 待我处理 no longer holds the Book. The Journey ends with zero loopback requests. Its words are pinned by `tests/unit/task-drawer-labels.test.ts`, `tests/unit/task-plan.test.ts`, `tests/unit/plan-edits.test.ts` and `tests/unit/global-attention-labels.test.ts`, and the ledger's behaviour by `tests/service/run-cancellation.test.ts`, `tests/service/run-continuation.test.ts`, `tests/service/run-redo.test.ts`, `tests/service/clarification.test.ts`, `tests/service/run-budget-ceiling.test.ts` and `tests/service/account-limit.test.ts`.

Not proven here: a Run cancelled before it began its units, which ends without provider work and without a revision; a stopped Run's cancellation waiting for the slot another Run holds; the startup reconciliation of a Run a stopped service left `executing`, `pausing` or `cancelling` (the Journey's close is a clean one, so the Run is already `resumable` when AI7 opens again); and `续行` refused while the plan moved, the slot is held or — on a route that reaches its model over the network — the device is offline, saying which, and never for a credential on a route that sends nothing — all deterministic only over the real store, in `tests/service/run-cancellation.test.ts` and `tests/service/run-continuation.test.ts`. `续行` refused, for a route that sends to a model service, while its credential is missing has its words pinned by `tests/unit/task-plan.test.ts`, and no provider-free layer can reach it. Not proven in any layer yet: a turn cut off mid-answer when AI7 closes, which is not kept and is read again by `续行`, and a cancellation or pause arriving during the cross-unit reduction or the sample, which stops once that step completes — neither can be timed deterministically without a hold inside the turn or those steps; the cancellation's words for them are pinned by `tests/unit/task-plan.test.ts`. `改计划重做` for a Run that kept nothing is proven only over the real store, in `tests/service/run-redo.test.ts`: it is redone as the Task it was — the first baseline again, or an update the same way over the same range, never a 同步 of the revision before it — and a Book such a first baseline left without a revision can be prepared again (`actions.canPrepare`), which ②A offers as `开始基线稿件分析`; that button is proven in no layer yet, since no Journey can end a Run that began without keeping a range. So is a redo refused when the Run it names is not the latest Task's cancelled Run, or when the request is anything but the one the drawer offers; and so is a stopped Run a later launch can no longer carry under the binding it persisted — another route, fixture, policy or AI7 version — whose `续行` is refused with 「这次运行授权时的执行绑定已经变化（模型服务、路由、策略或 AI7 版本不同），不能照原样续行；请改计划重做。」 and whose `取消影响摘要` and `改计划重做摘要` say that what it read cannot become a revision, so its redo reads from the beginning. A redo confirmed while another Run holds the slot, whose 正在取消 says 「已记下改计划重做：这次运行正在取消，取消完成后准备新任务」 until the Run has stopped, cannot be timed in this Journey; its words are pinned by `tests/unit/task-drawer-labels.test.ts`. An answer given while the Run still reads other ranges, found at its next unit boundary, and `不重试，记为缺口`, which settles the unit as a gap in the editor's words, are proven only over the real store in `tests/service/clarification.test.ts`; so are a Run cancelled while it waits, whose waiting unit becomes a gap and whose question stays on record unanswered; AI7 closing while a question is open, after which `续行` reads on and waits for the answer again; and a question answered while another Run holds the slot, whose Run goes on once the slot is free — or, if AI7 closes first, at the next launch. Not proven in any layer yet: a pause while a question is open. A question the model itself asks needs a new prompt contract and is not built. A ceiling reached by the last range, which stops the reduction and the sample and ends the Run the same way with every range kept, and a ceiling removed with `去掉上限`, are proven only over the real store, in `tests/service/run-budget-ceiling.test.ts`; so is a started plan's `设置上限…` refused with 「任务已经开始，计划不能再改」. A developer-live plan takes no ceiling of the editor's — its launch sets it, and the plan says 「这次启动的预算上限由开发者实时启动参数决定，不能在计划里设置。」 — which `tests/service/developer-live-analysis.test.ts` proves; and a plan carrying the editor's ceiling cannot become a quick-start rule yet, which `run-budget-ceiling.test.ts` proves. So is an answered `再试一次` that the ceiling, spent by the ranges read meanwhile, keeps from being sent: the range stays the gap its first attempt left, 「…；任务运行预算已达上限，没有再试一次」, and the Run ends at the ceiling. Not proven in any layer yet: a safe retry AI7 would make on its own that the spent ceiling keeps from being sent — the deterministic fixture reports no tokens for a failed turn, so no Run can reach the ceiling between an attempt and its retry. Also not here: 取消任务, 暂停, 改计划重做 and 先问你 for a Review Run, which its categories' drive loop does not offer yet; and J-10's other operations — `重试`, `回退运行方向` and `重放`, beside `改计划重做`, which is its `重做` as a newly authorized Run — and an ambiguous outcome (S16c). A Run stopped on the account limit and cancelled, whose partial revision closes the three ranges read and names the refused one `not-attempted`, is proven only over the real store in `tests/service/account-limit.test.ts`; a live route's limit — one transmission, then the stop, no outcome — in `tests/service/developer-live-analysis.test.ts`. Not proven in any layer yet: the account limit met by the reduction or a sampling turn, which stops the Run the same way with every range kept, and 续行 forms that step again — the fixture refuses a unit only; and a Review Run's category, which keeps no progress and still ends on it.

## J-11 A Book's 作者, 责编 and 相关人

J-11's first slice (Issue #431, plan slice S83): the people a Book is attributed to, whom the later feedback and learning records of S38, S26 and S27 will name. Two empty Books, 人员旅程甲 and 人员旅程乙, are made through `新建图书`, so the Journey reads no manuscript and composes no input, and every name in it is an authored stand-in. The renderer holds `updateBookPeople` and no member named like an account, a permission, a login or a grant. 甲's 工作概览 carries `人员`: 作者, 责编 and 相关人 each `未填写`, 「尚未填写作者、责编和相关人。」, and the note that the people label and find the Book and are what later feedback and learning records are attributed to — not accounts, and deciding nothing about who may do what. For J-14, Enter on `编辑人员…` opens the form at 作者, and Escape closes it with nothing saved and the focus back on `编辑人员…`. The form takes 作者 `周一、吴二` and 责编 `郑三` as names between 「、」, and a row added by `添加相关人` offers the house's roles — 校对, 美编, 译者, 外审专家, 作者经纪, 营销 and 其他 — behind an unselected `选择角色`; `保存人员` stays disabled with 「每位相关人都要选角色、填名字。」 until the row has both, 校对 and 王四. At 200% the section, its two fields, its 相关人 group and its row reflow without sideways scroll, and under forced colours the group keeps its rule, the card its edge and `保存人员` its outline. `保存人员` says `人员已保存`, shows the three lines and `第 1 次保存 · ` with a local time, and returns the focus to `编辑人员…`; the service's own overview agrees name for name, the 相关人 by its role's identity. The same set saved again says `人员没有变化` and records nothing. 书库's cards, in the list's title order, name 甲's people — `作者：周一、吴二 · 责编：郑三` and `相关人：校对 王四` — and nobody for 乙. `查找图书` finds 甲 alone by 作者 `吴二`, saying 「按作者查找「吴二」」 with the focus back in the words; 甲 alone by 责编 `郑三`, from the keyboard with Enter in the words; 乙 alone by 书名 `乙`; and nothing by 作者 `无此人`, saying 「没有找到符合的图书。」. `显示全部图书` lists both again. A restart moves nothing: `listBooks` answers exactly as before, the cards read the same, and 甲's 工作概览 shows the same people its overview answers. The Journey ends with zero loopback requests. Its words are pinned by `tests/unit/book-people-labels.test.ts`.

Since #429 (S81a) J-11 also walks ②C 评估. Every launch names J-11's picker control, `--j11-picker-path`, with the admitted Public SampleBook `sample1`, which the window relaunched at `restart-keeps-people` imports through the product's own flow as a third Book, 评估旅程丙. Its 工作概览 carries `评估` — 「还没有评估。」 and `打开评估`. The destination offers `开始评估`; version 1 binds `修订版 r1`, says 「AI7 初评尚未接通：这一版由你打分。」, lists the five items `… · 满分 20` unscored, reads 「总分 0 / 100 · 还有 5 项没有打分」, chooses no conclusion, and focuses its heading. The editor scores 18, 16.5, 15 and 17 — 卓越, 优秀, 优秀, 优秀 — and leaves 读者与市场潜力 `不评` with a reason, and the total follows as typed: 「总分 66.5 / 80 · 优秀（1 项不评）」. A `高` legal risk with a statement keeps `推荐出版` disabled with why until `已由人工复核` is ticked; the editor chooses `修改后再议`, lists one readiness line, and `保存评估` says `评估已保存。` and lists the version. `定稿` says 「第 1 版评估已定稿。」: the version reads as it was, every control disabled, with 「定稿 · 本机编辑 · 」 and a local time, and `重新评估` is offered. From 评估's own `打开稿件`, the manuscript's `工作` group reads 审阅 · 评估 · 交付物, and its `评估`, after the manuscript's leave step, opens the finalized version again. Version 2 begins from 定稿's scores — never its conclusion, and no risk counted as reviewed by a person — and says 「与第 1 版相比」; the comparison reads 「结论：修改后再议 → 结论未定」 from the start; one score moved to 19 and saved reads 「文学品质与作者声音：18 → 19」 and 「总分：66.5 / 80 → 67.5 / 80」. For J-14, at 200% the version, its five items, two risks and the conclusion reflow without sideways scroll, and under forced colours each keeps its border. 工作概览's line then names version 2, and 知识库 › `评估方案` shows 「审稿评估方案」 `第 1 版 · AI7 内置默认`, its five items, five bands, the two risk items and four conclusions, and 「已用于 1 本书的 2 版评估」, as `inspectEvaluationProfiles()` answers. The scale, 定稿's requirements, each refusal and a version rewritten by hand are proven over the real store by `tests/service/evaluation-records.test.ts`, and the arithmetic and words by `tests/unit/evaluation-labels.test.ts`.

Since #94 (S38) J-11 also walks ②A 分析反馈 on 评估旅程丙, and every launch also names `--j04-model-adapter sample1-baseline-happy`. From 知识库 the Journey returns to 书库, opens the Book's 工作概览 from its manuscript, installs the workspace profile and enables its Revision 2 for the Book, and creates and removes one runtime-only synthetic credential in `模型服务`, as J-16 does, owning its cleanup the same way. ②A's first baseline, prepared from the card and started from its plan's bar, settles on the adapter, and the drawer closes. Every item of the revision — the synopsis and each entry of the four lists, in the order `inspectAnalysisFeedback` answers — offers `反馈…` with no judgment, and the tally under the synopsis reads 「还没有给出判断。」 with what it is not: 「只统计你明确给出的判断：没有判断的条目不算认可；这是对分析结果的评价，不代表事实核实，也不改变 AI7 的做法。」. On 人物与名称's first entry `反馈…` opens the card focused at its first judgment, nothing chosen and `记录反馈` disabled; `不准确` offers 名字或称谓不对, 把不同人物当成一个 and 类别标错 with `其他 / 自行输入` beside them, none chosen, and the correction. One reason and a correction in the Journey's own words say `反馈已记录。` and read 「你的反馈：不准确 · 把不同人物当成一个 · 修正：…」 with a local time under the entry, the focus back on `改反馈…`, and the tally reads 「这本书判断了 1 条：准确 0、不准确 1、不完整 0」 over 「人物与名称：1 条，准确 0、不准确 1、不完整 0」. The synopsis judged `不完整` under `其他 / 自行输入` focuses the editor's words, offered beside 漏了关键情节 and 没有概括到结尾, and records them. `改反馈…` to `准确` asks for no reason and no correction and records a successor: the entry holds two signals, the latest naming the one it succeeds, and the tally counts it once, by its latest, over the lineage digest `inspectAnalysisFeedback` answers. The same judgment again is refused in the card with 「反馈没有变化。」. For J-14, Enter on an event's `反馈…` opens its card at the first judgment, and Escape closes it with nothing recorded and the focus back on `反馈…`; at 200% an open card, both its groups and its correction reflow without sideways scroll, and under forced colours the card and both groups keep their borders. Every item nobody judged reads no judgment, 待我处理 reads exactly as it did before any judgment, and a restart moves nothing: every judgment and the tally read as before. The ledger's refusals — another analysed Book's revision included — chain, metric, restart, migration and tamper checks are proven over the real store by `tests/service/analysis-feedback.test.ts`, the vocabulary and words by `tests/unit/analysis-feedback-labels.test.ts`, and ②A holding a follow-up draw while the editor is inside an open card by `tests/unit/analysis-follow.test.ts`. Not proven here: a card open while a Run moves, since no Run is in flight during these stages.

Not proven here: a save refused because the Book's people moved since the form read them, a name past its 40 characters or holding a separator, a repeated name, a list past its bound, a role outside the house's list, the search following only a Book's newest version, `全部` over 书名, 作者 and 责编 together, a found list paged as the whole one is, words across a line break refused, and a version altered by hand refused wherever it is read — all proven over the real store in `tests/service/book-people.test.ts`, with the frames in `tests/unit/request-frames.test.ts`. 书系 on the card and in the search waits for S28's Series. The people as the attribution of feedback and learning records are S38's, S26's and S27's, which extend this Journey.

## J-16 The 任务 panel: a Book's Tasks beside the manuscript

J-16's first slice (Issue #423, plan slice S77a). J-16's subject is the Interactive Editorial Dialogue; its dialogue Tasks, with `回答` and `打开对话`, come with S17 (#52), and this slice proves the panel they will live in. Imports exact `sample1` through the launch control `--j16-picker-path` as 任务面旅程, with the editorial workspace profile at Revision 2 and one Main Editorial Role connection whose synthetic credential is saved and removed, as J-10 does. Every launch binds the J-04 model adapter to `sample1-baseline-happy` and J-10's unit hold, which J-16 writes to hold a reading range in flight. The renderer holds `inspectBookTasks` and no member named like a dialogue, a chat, a conversation or a stream. The manuscript's right-edge `任务` opens the panel in the side slot, closing 导航: `任务`, `这本书 · 0 项`, 「这里只列这本书的任务；跨书的待办在「待我处理」。」, and `发起全书任务` offering 首次基线分析 with `准备任务` and `快速开始` disabled beside 「首次基线分析没有快速开始：先看计划再开始。」, above `等你处理` · `进行中` · `最近完成`, each empty with its own line. `准备任务` prepares the first baseline and opens its plan in the same slot; `← 任务` comes back to it in 等你处理 as `计划已准备 · 等你开始` with `查看计划并开始` — which 待我处理 does not list. Started from its plan, the Run is 进行中 on its card: `运行中`, 「正在逐个阅读范围分析 · 已完成 2/8 个阅读范围」, with `暂停`, `取消任务` and `查看计划`. `暂停` on the card reads `正在暂停` until the range in flight finishes, then `已暂停` with `续行`; `续行` goes on in the same Run to its end, which lands in 最近完成 as `已完成` — 「已形成第 1 份基线分析。」 — with `查看结果`. `发起全书任务` then offers the two whole-Book updates, 同步到当前稿件 disabled while the manuscript has not moved; `重新分析全书` is prepared and started from its plan, and the card's `取消任务` opens that plan with its Cancellation Impact Summary focused, where the keyboard alone confirms it: `已取消` stands first in 最近完成 — 「你取消了这项任务；读完的阅读范围已形成第 2 份基线分析。」 — with `查看结果`. `查看结果` on the completion opens a floating window as wide as the manuscript's text column and at the top of its pane, with its title focused: `任务结果`, the Task and `已完成`, its `任务` · `读取了` · `没有做` rows — 「全书 8 个阅读范围 · 稿件修订版 …」 and 「没有修改稿件：分析只读稿件。」 — one row per reading range with `跳到`, and `在分析中打开`. `跳到` the sixth range closes the window, moves the manuscript there and leaves `回到<位置>` in its header, titled 「回到跳转前的位置」; the chip stays through 导航 and through `返回图书工作概览` and `打开稿件`, until it is used and returns to where the editor was reading, saying 「已回到…。」. Words typed there a moment before the result window's `在分析中打开` are written before the manuscript is left: opened again from ②A, the paragraph holds them. ②A's `回到稿件范围` on the seventh range leaves the same way back. For J-14, Enter on `任务` opens the panel with its title focused, Tab moves into it with visible focus and Escape closes it back to `任务`; Escape in the result window brings back the panel at its title; at 200% the panel's groups and cards and the result window reflow without sideways scroll; under forced colours the window keeps its edge, a card its left rule and a count its outline. A restart moves nothing: `inspectBookTasks` answers exactly as before, and `查看结果` still opens the result. The Journey ends with zero loopback requests. Its words are pinned by `tests/unit/task-panel-labels.test.ts` and `tests/unit/global-attention-labels.test.ts`, and its grouping by `tests/unit/book-tasks.test.ts`.

Not proven here: a Review Run's cards — prepared, under way and on the manuscript — proven over the real store in `tests/service/book-tasks.test.ts` and by the unit suites, since J-16's launch runs the baseline fixture; its result's findings with `跳到` the mark, which no layer drives; a running 审阅's card, which offers no `暂停` or `取消任务` yet (TASK-044); a Task cancelled while it waited to start, standing in 最近完成 as 已取消, proven by `tests/service/connectivity-wait.test.ts`; 审阅's `回到原文` leaving the chip, which shares the jump ②A's does; 快速开始 from the panel under a 默认执行规则, which is ②A's own path and J-03's; a group longer than one answer and the plan-revision, blocked and waiting states of a card, proven by the unit suites; and the selection's `就这段发起任务…`, `就这段提问…` and the house's 常用工序, whose Tasks do not exist yet and which the menu says with 「就选区发起的任务尚未接通」.

## J-14 accessibility obligations

J-14 has no runner of its own. Its keyboard, IME, focus, 200% reflow, and forced-colors obligations are asserted inside the Journeys above where they apply; the Mark surface's are J-05's `j14-marks-*` and `marks-keyboard-menu-*` stages, 稿件冲突's are J-06's `j14-conflict-keyboard`, `j14-conflict-zoom-200-reflow` and `j14-conflict-forced-colors`, 交付物's are J-07's `j14-designate-keyboard`, `j14-deliverables-zoom-200-reflow` and `j14-deliverables-forced-colors`, with the export card's `j14-export-keyboard`, `j14-export-zoom-200-reflow` and `j14-export-forced-colors`, 待我处理's are J-09's `j14-attention-keyboard`, `j14-attention-zoom-200-reflow` and `j14-attention-forced-colors`, `等待运行名额`'s is J-09's `j14-queued-forced-colors`, 取消任务's are J-10's `j14-cancel-keyboard` and `j14-cancelling-forced-colors`, 改计划重做's is J-10's `j14-redo-keyboard`, the question card's is J-10's `j14-clarification-keyboard`, `设置上限…`'s is J-10's `j14-budget-keyboard`, the editable plan's is J-04's `j14-plan-edit-keyboard`, 人员's are J-11's `j14-people-keyboard`, `j14-people-zoom-200-reflow` and `j14-people-forced-colors`, 评估's is J-11's `j14-evaluation-reflow-forced-colors`, 分析反馈's are J-11's `j14-feedback-keyboard` and `j14-feedback-reflow-forced-colors`, 知识库's are J-15's `j14-knowledge-keyboard` and `j14-knowledge-reflow-forced-colors`, with 资料库's `j14-library-reflow-forced-colors`, the 任务 panel's are J-16's `j14-panel-keyboard`, `j14-panel-zoom-200-reflow` and `j14-panel-forced-colors`, and the Task Drawer's are J-03's `drawer-keyboard`, `drawer-push-overlay` and `drawer-reflow-forced-colors`.
