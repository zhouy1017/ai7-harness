# Current checkpoint

## What's done

- The covered-analysis path now updates and retains history (#93 / PR #269, `dev@c3e5def2d704079781d7cf8f20dc8e1736a56a0a`): on the exact `sample1` Book the three Analysis Update Controls — `同步到当前稿件`, `重新分析所选范围`, and `重新分析全书` — each start a newly user-initiated baseline-analysis Task through the same real execution path and append one immutable successor Result Set Revision with explicit reused / recomputed / invalidated / bypassed unit lineage, the predecessor link, and the four axes recomputed for the new exact Manuscript Pin; the Analysis Result Revision History keeps every revision reachable and opens older ones read-only. Reuse is decided by a content-exact, position-independent reuse plan stored as the eighth plan component; only recomputed units reach the harness and the Egress Gate; reused results are copied by lineage. The analysis ledger moved forward-only to revision 16 (two relations rebuilt in place with every existing row's canonical JSON and digest preserved), the service protocol to 18, the deterministic fixture resolver keys entries by unit ordinal and request digest, and J-04 gained fourteen stages after `acknowledged-edit-stale`.
- The first model-dependent product path exists (#92 / PR #266, `dev@8a205163`): from the exact `sample1` Book an editor starts one baseline manuscript analysis Task and the authorized Run executes through the real path — Task Intent, `Task Input / 任务输入` checkpoint and pins, Provider Preflight, Plan Envelope, standard-direct Run Authorization, Run Record, scheduler admission, one execution attempt with its immutable Execution Binding, the `PrimaryAgentHarness` composition over the pinned DSH loop, the final Provider Payload/Egress Gate before every model call, and the bound adapter — reaching one immutable Manuscript Analysis Result Set Revision with four independent axes and visible gaps/conflicts linked from the Task Outcome. The provider-free parts of the real Provider path landed with it: the DeepSeek OpenAI-compatible adapter revision 1, the Credential Broker with a read-only keyring resolver (value released only against a `transmit-remote` ticket, never under v1), and the Egress Gate that refuses every remote route under `development-ci` / Provider Processing v1. Hand-written synthetic fixtures under `tests/fixtures/model/` are the standing model-dependent test material; the J-04 journey binds the `ai7-local-deterministic` route only through the J-04-only `--j04-model-adapter` control.
- J-04 is the seventh executable admitted Journey (#264 / PR #265; no ADR needed because ADR 0055 and ADR 0056 removed every hard-coded count); the order is J-01 → J-02 → J-08 → J-12 → J-15 → J-03 → J-04. The J-03 foreground execution boundary (#91 / PR #193) answers `前台执行已拒绝 · 未启动` for the recorded-only Run. The Local Verification Ladder is complete and every layer is available (`doctor`, `bootstrap`, `check`, `test`, `test:service`, `build`, `e2e` / `e2e:all`, `e2e:debug`, `e2e:repeat`).

## What's next

- Owner direction of 2026-09-06: develop most of the designed functionality provider-free on synthetic deterministic fixtures before any recording; the human-attended exact-`sample1` recording under ADR 0044 is deferred until the analysis contracts stabilize, so that a later recording can serve as a fuller test basis. Recorded fixtures are keyed by prompt-contract digest, unit ordinal, and unit digest, so any contract or unit-splitting change silently invalidates them; recording early would only buy the real-path integration evidence for one contract version.
- Provider-free S-series order confirmed by the Owner (analysis mainline first): S13 (#48) → S15 (#50) → Clarification Requests (a new Issue the Commander opens after S15) → S16 (#51) → S38 (#94) → S17 (#52) → S14 (#49) → S18 / S19 / S20 / S21 (#53–#56) → S23 (#58), S22 (#57), S26 (#61), S27 (#62), S28 (#63), S29 (#64), S30 (#65), S31 (#66), S33 (#89), S34 (#90), S39 (#95); S24 (#59, the local file export Effect) and S25 (#60, publication versions) need a separate Owner confirmation when reached. Every Issue receives a current-format Change Brief on the then-current `dev` head before `ready-for-agent` (model: #93's revision 1); each old body is design-phase planning input only.
- #48 (S13) is narrowed to Plan Revision on material drift and one declared in-envelope adaptation class (`safe-retry`, one retry per unit on transient model-service errors) on the executing analysis Task in J-04; J-03's record-only Task keeps its single frozen plan, and its `post-authorization-edit` stage already proves that editing never retargets a Run. Clarification Requests follow S15 because they need a prompt-contract revision (which changes every fixture request digest) and per-unit incremental persistence that Resume also needs.
- Follow-up candidates without an Issue: from #93 — a reused unit's detail line shows the predecessor's usage without a `reused` qualifier; the update controls derive a working manifest and one reuse plan per range option on every inspect while the renderer polls every 250 ms during a Run; the active-run guard also blocks on `authorized`; a DML-level immutability test over rebuilt ledger tables; a fixture in which a successor closes a gap (unit 2 stays a gap in every J-04 revision); from #92 — the harness submitting every unit into one technical session, the broker's readiness check reading the keyring value inside the broker, the baseline-analysis executor owning the single-slot scheduler, the carrier and sidecar digest literals duplicated in three places, the unavailable-analysis note on every populated non-`sample1` Book; from earlier waves — the sibling J-03 actions re-enabling in `catch` without the `authorityInterrupted` guard, two J-03 stages sharing the location `foreground-boundary-check`, `serviceEnvironment` selecting `AI7_E2E_JOURNEY` by sequential `if`, a service-layer regression case for the foreground-boundary inspection, `awaitFixedOperation` deduplication, the controller's debug-capture child session bounded by `withTimeout` only, the active-set-v3 ordinary-production pin mismatch, the `docs/agents/incremental-development.md` sentence that still calls the later ladder layers staged, and the `docs/architecture-v2/HARNESS-INTEGRATION.md` non-decisions paragraph that still describes the code as having no executable Agent/Session/Provider path (an Owner-accepted design record, so its factual refresh is the Owner's call).
- Product integration remains serial.

## Key decisions

- Model-dependent testing uses hand-written synthetic deterministic fixtures by default; the ADR 0044 recorded fixture, if ever admitted, replays only through the same local deterministic adapter inside J-04 with a prompt-contract digest assertion. Recording authority is unchanged: no Provider call, credential value, recording, or fixture admission has occurred.
- Owner decisions of 2026-09-06 kept for the future recording Issue, none of them authorized as work yet: the recording binds `deepseek-v4-flash`, which needs an Owner ADR for a development-interval Main Editorial Role binding plus a T3 rebinding Issue because `deepseek-v4-pro` is pinned in the ledger CHECKs, protocol literals, adapter, Egress Gate, and three runners; the `fixture-recording` scope gets a new Provider Processing version (predecessor v2) allowing one Run per authorization with transmissions equal to the frozen Coverage Manifest unit count, plus active-policy-set and launch-policy pins, because v2's `maxCalls: 1` conflicts with `sample1`'s eight units; the harness uses one technical Session per unit; the Plan Envelope pins a Run Budget Ceiling of 500,000 total tokens (a Provider-side spend limit is only a second net). The recording Issue must also land the `fixture-recording` trusted-launch form, ceiling and transmission-count enforcement, recording-only raw capture into protected staging, and the gate's `transmit-remote` decision under the new policy.
- Reuse compatibility is content-exact and position-independent; a successor Result Set Revision never rewrites a predecessor; only the latest revision of a Result Set can be `current` (older revisions are `superseded`).
- The `ai7-local-deterministic` route is bound only through the J-04-only `--j04-model-adapter <fixture identity>` control, admitted with `AI7_E2E_JOURNEY=J-04` and mutually exclusive with the J-01/J-03/J-08 controls; without it an authorized analysis Run settles `blocked-before-dispatch`. No production-selectable surface can choose it.
- The Egress Gate returns exactly one of `transmit-local`, `transmit-remote`, or `refuse`; under `development-ci` / v1 it never returns `transmit-remote`, and the Credential Broker releases a value only against such a ticket for the same binding.
- A further Journey admission needs an explicit Owner routing decision plus a separate CI-governance record in `docs/agents/ci-test-boundaries.md`; no ADR is amended because none hard-codes a Journey count.
- Dispatch runs on either harness in any applicable launch mode; when a Claude Code Commander session lacks `SendMessage`, every attempt uses the `cli-session` mode. Bindings per harness are unchanged (Claude Code T1 `claude-sonnet-5 @ medium`, T2 `claude-opus-5 @ high`, T3 `claude-fable-5-1 @ xhigh`; Codex T1 `gpt-5.6-luna @ medium`, T2 `gpt-5.6-terra @ high`, T3 `gpt-5.6-sol @ xhigh`).
- A squash commit on `dev` carries only the stable product-level trailer of the attempt's harness; a trailer that names a model stays in branch history only.
- Product Provider, Model Role, DSH, Provider Processing/fallback, credential, Effect, export, publication, distribution, release, and `main` authority are unchanged.

## Unresolved matters or blockers

- A credential value was exposed in the Commander's chat on 2026-09-06; the Owner was asked to revoke it on the Provider side. Any future recording enrolls a fresh key entered only through the product's Protected Secret Store, never through a chat, prompt, Issue, log, or diagnostic.
- The active-set-v3 ordinary-production pin does not match the current v3 policy bytes and needs a separate successor/correction before any positive production-eligibility claim.
- Whether the `.agents/skills` vendoring on `temp/freeze-20260904-112022` enters `dev` remains a separate Owner decision; the self-hosted Gate question is shelved by the Owner on 2026-09-05.
- One local `LOCAL_COMPLETION/J-01/fail` of unknown cause recorded on #249 did not reproduce in six subsequent runs at the same head; one transient `EBUSY` on a freshly extracted `electron.exe` during `bootstrap` on #93 cleared on rerun.

## Safe Resume Prompt

```text
Commander: identify your harness (Codex or Claude Code), then query the Dispatch Register and current Issue receipts with your own session, agent, and background-process tools. Treat #91, #264, #92, and #93 as integrated: J-04 runs the covered baseline analysis and its three update modes end to end on synthetic deterministic fixtures through the real execution path, with the Egress Gate refusing every remote route under development-ci / Provider Processing v1. The Owner deferred the human-attended sample1 recording on 2026-09-06 until most designed functionality exists; its kept decisions are in PROGRESS.md; do not open a recording Issue, freeze a Provider Binding, touch credentials, or make or simulate a call. Continue the provider-free S-series in the Owner's confirmed order (S13 next, then S15, Clarification Requests, S16, S38, S17, S14, the J-04/J-05 chain, then the rest; S24 and S25 need Owner confirmation), giving each Issue a current-format Change Brief on the current dev head, one fresh Task Session per attempt in its own worktree, with schema-v4 receipts; keep product integration serial and require the complete Local Verification Ladder before Ready. Under ADR 0058 do not query, estimate, report, or consider Actions usage. Do not touch fixture admission, product Effect, export, publication, release, distribution, or main.
```
