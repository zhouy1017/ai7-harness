# Current handoff

Issue #45 / PR #105 is integrated at `dev@76e7ee36b281464d8d44938e57d36c52c4c0e10a`. Its consumed pre-merge root routing is preserved as one indexed historical snapshot. Issues #37/#41/#43/#45 are integrated, and PRD #28 plus Issues #38/#42/#88/#91 are refreshed.

## Current routing

- No open implementation Issue is currently ready for an agent. Issue #38 is the nearest Owner-decision gateway, not product dispatch authority: the Owner must select the exact native Workflow-definition carrier pinned by the created Workflow Instance while AI7 retains durable phases, gates, signoffs, and transitions.
- Issue #86's repository-current authority remains in force; v1/v2 policies and superseded ADR history remain preserved. Provider setup, credential configuration, import, artifact acquisition, installation, and enablement do not authorize a Provider call or Run. A new autonomous background Provider-backed manuscript-analysis dispatch requires a matching active Background Analysis Enrollment and an exact dispatch.
- Imported updates remain inert until explicit adoption or the narrow Artifact Update Rule. Every formal agent-originated Manuscript mutation still crosses one explicitly confirmed, drift-rechecked, single-use AI7 Apply bound to the exact Book, base pin, diff, and targets.

## Safe next action

The Owner resolves #38's native Workflow-definition carrier. The Commander then refreshes #38 against then-current `dev` before any later implementation dispatch. #42 Product Data Location/macOS mechanics, #88 artifact source/carrier/adapter, #91 production Provider binding, Provider/export/release work, and `main` remain outside this routing update.
