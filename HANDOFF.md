# Current handoff

Begin by identifying your harness, Codex or Claude Code, then query the [Dispatch Register](docs/agents/dispatch-register.md): read the current Issue bodies and Launch/Return Receipts, then correlate them with your own harness's live session state, with `ListAgents` in your own session for any subagent attempt, and with your own background-process tools for any `cli-session` attempt. Do not infer completion from `idle` or a process exit, and do not copy transient session status into this router.

Dispatch follows [ADR 0061](docs/adr/0061-route-repository-dispatch-by-commander-harness.md) and [ADR 0063](docs/adr/0063-allow-cross-harness-dispatch-through-cli-launched-task-sessions.md): you select each attempt's harness and launch mode. A same-harness attempt is a fresh Task Session in its own worktree launched as a hand-created top-level session, as a Claude Code subagent through your Agent tool, or as a `cli-session`; a cross-harness attempt is a `cli-session` launched from your shell with the exact class model and effort. A Claude Code Commander session without a `SendMessage` tool cannot complete the `subagent` receipt relay and uses `cli-session` for every attempt; `claude --worktree <name>` run from any worktree creates `.claude/worktrees/<name>` under the main root at the `origin/dev` head, and a Worker in that mode runs every shell command as one single invocation. Receipts are schema v4; the commit trailer follows the attempt's harness and never names a model.

Development verification follows [ADR 0062](docs/adr/0062-adopt-a-local-verification-ladder-with-ci-as-delivery-gate.md) and the [CI and test boundary](docs/agents/ci-test-boundaries.md): the complete Local Verification Ladder (`doctor`, `bootstrap`, `check`, `test`, `test:service`, `build`, `e2e` / `e2e:all` with seven Journeys, `e2e:debug` / `e2e:repeat`) is required at the exact head before Ready, and the Hosted E2E Functional Gate stays the per-Ready-pull-request delivery gate; a red Gate returns to Draft for local, then CI-parity, reproduction. Shared product changes must also update the hidden cross-Journey pins the runners carry (J-01's exact renderer API inventory, the `user_version` literals in J-03 and J-12, J-15's v12-predecessor drop list), each named as a synchronized delta.

The model-dependent product path now exists: #92 / PR #266 (`dev@8a20516397a5aa5fb6f99822949dc9877f786de1`) runs J-04's covered baseline analysis end to end through the real execution path on hand-written synthetic fixtures, with the Egress Gate refusing every remote route under `development-ci` / Provider Processing v1 and the Credential Broker releasing no value. The next step on that path — the human-attended exact-`sample1` recording under [ADR 0044](docs/adr/0044-use-sample1-as-compatibility-and-recording-baseline.md), Provider Processing v2, and the [manual recording runbook](docs/development/manual-model-fixture-recording.md) — is outside this route until the Owner authorizes a separate recording Issue and is present for the single call; the Commander's step-by-step recording instruction is recorded on Issue #92. Do not open that Issue, freeze a Provider Binding, enroll or read a credential, select the `fixture-recording` scope, or make or simulate a call on this router's authority.

Provider-free product Issues (#93, #51, #48, #50, and the remaining S-series) are dispatchable only after their Change Briefs are refreshed to the current `dev` head under Owner sequencing. Keep product integration serial and require the ladder attestation before Ready. Fixture admission, product Effect, export, publication, release, distribution, and `main` remain outside this route. Under ADR 0058, repository development does not query, estimate, report, or consider Actions usage.

## Safe Resume Prompt

```text
Commander: identify your harness (Codex or Claude Code), then query the Dispatch Register and current Issue receipts with your own session, agent, and background-process tools. Treat #91, #264, and #92 as integrated: J-04 runs end to end on synthetic deterministic fixtures through the real execution path with the Egress Gate refusing every remote route under development-ci / Provider Processing v1. The next step on the model-dependent path is the human-attended sample1 recording under ADR 0044, which needs a separately authorized recording Issue and the Owner's presence; do not open it, freeze a Provider Binding, touch credentials, or make or simulate a call without that authorization. Dispatch only provider-free product Issues after refreshing each Change Brief to the current dev head, one fresh Task Session per attempt in its own worktree, with schema-v4 receipts; keep product integration serial and require the complete Local Verification Ladder before Ready. Under ADR 0058 do not query, estimate, report, or consider Actions usage. Do not touch fixture admission, product Effect, export, publication, release, distribution, or main.
```
