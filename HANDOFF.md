# Current handoff

GitHub Issue #45, **S10 / J-08 interrupted Manuscript Recovery**, is in hosted PR #105 from `feat/45-recovery-workspace` to `dev`. The isolated Worker tree `C:\Users\Chooo\Playground\ai7-harness-worktrees\issue-45-recovery-workspace` is at committed diagnostic/signature repair `HEAD 4b04f8941088b85f743c0b15c6a000c3f0998b21` on exact `origin/dev@3f73bab57c53eb469d704cf65ff208f1e752ab50` and now contains an uncommitted five-path loopback-sentinel plus combined J-02 timeout-diagnostic repair. The intended pull-request target remains `dev`, never `main`.

On 2026-08-28 the Commander completed and accepted the bounded static integration inspection, reverified that the full Issue #45 Change Brief is unchanged, and confirmed exact `HEAD`/`origin/dev` base `3f73bab57c53eb469d704cf65ff208f1e752ab50`, the intended 18-path implementation scope, clean diff and bundled-Node syntax diagnostics, and no structural or authority drift.

## Current routing

- The existing SQLite authority now advances v5→v6→v7 without inventing prior-lifetime evidence for a migrated v6 database. V7 adds explicit service lifetime and per-branch high-water facts, command-evidence-derived working-digest links, recovery snapshot metadata, Recovery Attention/decision/restoration lineage, hidden persistent bounded restore staging, a restoration history frontier, and persistent recovered-state review status.
- The existing Agent Data Root owner now contains one internal versioned `recovery-objects/v1` area. Bounded streaming line decoding, hidden-partial formation, separate byte and manifest hashes, file sync, independent verification, no-clobber promotion, SQLite visibility last, and per-entry recognized orphan cleanup preserve fail-closed behavior without a second store or authority. A newly damaged/missing applicable object cannot hide an older verified snapshot.
- Startup classifies only explicit unclean prior lifetimes with acknowledged journal writes newer than the last checkpoint. Before Recovery Attention becomes visible, the service reconstructs the candidate from that exact checkpoint through retained immutable command evidence in bounded pages and block-local replay, then compares it symmetrically with persisted working blocks. Manuscript recovery precedes import recovery; clean shutdown is recorded only after the serialized `shutdown` request is accepted.
- Recovery compares journal reconstruction, the relevant checkpoint/Milestone, and an applicable independently verified snapshot without preselection; discloses exact revision/digest/snapshot identities, Last Durable Edit Boundary, coverage, uncertainty, and the selected descendant/history consequence; provides bounded permanent-readonly `仅查看`, explicit `稍后处理`, and deterministic `恢复为新版本`; and service-blocks normal mutation on an unresolved affected branch while typed return context keeps the attention reachable through lower-priority import and unrelated-work screens. Snapshot none/unavailable is a separate disclosure, never a fake selectable third column.
- Restore forms one all-or-none descendant revision, records competing sources and the pre-restore boundary, preserves prior revisions/journal/commands/Milestones/snapshots, does not introduce or move a Manuscript Pin or manufacture pin proof, supersedes the old undo/redo frontier, creates no automatic Milestone or Task/Run placeholder, and retains `当前为恢复的工作状态` until a later successful explicit Milestone.
- Protocol v5, service/main/preload private IPC, renderer recovery-first presentation, README/current source-checkout truth, and the existing logical E2E gate now include J-08 alongside retained J-01/J-02. `e2e/run-j08.mjs` uses only runtime-generated clearly public synthetic DOCX material under a disposable external root and includes two/three-candidate states, older verified snapshot fallback, typed defer/import return routing, selection-specific restoration retry behavior, controller-owned loopback-sentinel proof of renderer cancellation, and applicable direct J-14 behavior.

All v1/v2 policies, superseded ADR history, Provider non-authorization, protected-material restrictions, exact single-use AI7 Apply behavior, the single standing E2E Functional Gate, release restrictions, and the prohibition on `main` changes remain unchanged. This Worker performed no Provider/model/Harness execution, credential access, dependency or plugin change, export/copy Effect, protected manuscript input, Task/Run/Proposal placeholder, external mutation, commit, push, merge, release, or `dev`/`main` write; the Commander alone created hosted PR #105.

The two earlier strictly read-only T3 diagnostics and the Commander-accepted findings from three fresh independent non-author read-only T3 reviews remain advisory candidate-rulebook inputs only, not accepted authority or a re-review gate. The three reviews were requested as Claude Code `claude-opus-5`/high; the Claude surface was unavailable; actual review execution was OpenAI Codex `gpt-5.6-sol`/xhigh as a same-provider fallback. They performed no edits, spawn/delegation, external action, or gate execution.

## Validation status

- Commander-accepted bounded static inspection: tracked `git diff --check` and untracked no-index diff checks pass across the exact 18-path scope.
- Non-gate parser diagnostic only: bundled `C:\Users\Chooo\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe` reports v24.19.0; its built-in TypeScript stripping parser accepts all 9 changed/untracked `.ts` sources, and `node --check` accepts all 3 changed/untracked `.mjs` sources.
- Hosted PR #105 ran the exact Node 24.18.1 / TypeScript 6.0.3 build on Windows and macOS; both hosts failed before E2E. Commander read-only diagnostics isolated one real new-source error at the snapshot fallback accumulator in `src/service/store.ts`. The minimal fix now narrows assignment to `verified.state === 'unavailable'`, preserving the eligible early return and defensive `none` continuation. The scoped diff and bundled-Node parser diagnostics pass after this repair; the Commander independently reran local TypeScript 6.0.3 semantic diagnostics with bundled Node declarations and found zero non-environment diagnostics across the changed recovery/main/store/protocol/renderer sources.
- Commander committed/pushed the last signature proof and J-02 stage diagnostic as `4b04f89`. Hosted run `33119538475` proves macOS build/J-01/J-02 pass but J-08 still fails only at `J-08/renderer-cdp-event-timeout`, establishing that Electron emits no usable Network failure event for this product-session cancellation. The current test opens one in-process HTTP sentinel on an OS-assigned `127.0.0.1` port before controller Node denial, keeps Page CSP bypass scoped to the probe, requires renderer fetch rejection plus a healthy zero-request sentinel, returns only a fixed neutral response if reached, and closes immediately/idempotently. Removing product `installChromiumDenial` therefore makes fetch resolve and the request count nonzero. No external network, Provider, dependency, process, gate, artifact, payload, or production authority is added.
- Windows job `98670916537` first proved `FileHandle.writev` did not collapse the fixed batch below the unchanged ten-minute service bound. Commit `9489679` switched to one contiguous bounded Buffer/write and deterministic proven-equivalent code-unit key order, but hosted run `33117802836` Windows J-02 ran 21:23:43Z–21:36:27Z and again failed `J-02/milestone-r2`; those changes are disproven as sufficient and production snapshot semantics are not changed again here. Commit `4b04f89` made the existing timeout stream only names/types from exact disposable `recovery-objects/v1`. Completed run `33119538475` proves Windows build/J-01 pass and J-02 ran `2026-08-27T21:46:20Z`–`21:59:25Z` before failing `J-02/milestone-r2-recovery-object-absent`; J-08 was not reached. That rules out a partial/promoted object at timeout but remains ambiguous between pre-formation pending and completed failure plus cleanup. The current timeout-only refinement now combines the object category with a fixed IPC category derived solely from `operation`/`phase`/`ordinal` in the existing before/after observation snapshots: no flush invoke, flush pending/error/result without save invoke, save pending/error, save result without renderer r2, or diagnostic unavailable. It reads/logs no inputs, outputs, errors, payload, manuscript text, database bytes, or paths and changes no production semantics, timeout, cleanup, or gate.
- For the current five-path repair, `git diff --check` and bundled-Node syntax checks for `e2e/run-j02.mjs` and `e2e/run-j08.mjs` pass; a loopback-only diagnostic confirms OS-assigned `127.0.0.1` bind, fixed neutral response, exact request counting, and clean close. Hosted J-08 and combined Windows J-02 diagnostic evidence are pending. This Worker was not authorized to install/change dependencies, commit, push, or mutate the pull request.

## Safe next action

Commander should review and commit the five-path loopback-sentinel plus combined J-02 timeout-diagnostic repair, push it to existing PR #105 against `dev`, and rerun the hosted existing E2E Functional Gate on both hosts. The pinned build and retained J-01 must remain green, any recurring Windows J-02 timeout must yield the fixed combined category, and J-08 must pass. Only exact new hosted evidence should return to implementation.

## Resume prompt

```text
Commander: review the uncommitted five-path loopback-sentinel plus combined J-02 timeout-diagnostic repair in C:\Users\Chooo\Playground\ai7-harness-worktrees\issue-45-recovery-workspace on feat/45-recovery-workspace at `4b04f8941088b85f743c0b15c6a000c3f0998b21`, commit and push it to existing PR #105 against dev, then rerun the hosted existing E2E Functional Gate. Require the pinned build and retained J-01 to remain green, preserve any fixed combined Windows J-02 timeout category as the next diagnostic evidence, and require J-08 to pass on Windows and macOS. Never target main or broaden the unchanged Change Brief.
```
