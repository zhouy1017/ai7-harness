# Current handoff

Issue #170 is the active agent-instruction task from exact `origin/dev@244117bac73756f4269563a594f49f531b2d168f`. It adds one implementation-lifecycle owner rule and one Change Brief prompt: every implementation records either named existing Gate coverage checked unchanged or the exact affected scenario/local/Hosted projections updated atomically in the same Issue and pull request. Only documentation/design-only work may use the `N/A` route. Required projection work outside the Structural Budget stops for a revised brief; unchanged invocation does not justify mechanical workflow YAML edits.

The exact current controller/Hosted projection is already consistent at J-01/J-02/J-08/J-12/J-15, so Issue #170 changes no code or workflow configuration. Its exact four-path documentation diff passes local validation and independent read-only review is CLEAN. Live workflow state is active with zero September and queued/in-progress runs at the latest check. Keep its PR Draft until a fresh pre-Ready usage/state read; the only authorized Hosted activity is the normal Markdown route occurrence after Ready. After integration and lifecycle closure, return to the separate Owner decision in Issue #165; Issue #47 remains blocked.

## Safe Resume Prompt

```text
Commit Issue #170's exact four-path CLEAN documentation unit and open a Draft PR. Before Ready, query live workflow/run/account usage; if readable and available, allow only the normal Markdown route occurrence and inspect its actual job usage after completion. Never dispatch, probe, rerun, modify workflow configuration, fix out-of-scope README drift, or touch Issue #165/#47 implementation authority. Run the scoped lifecycle sweep after merge.
```
