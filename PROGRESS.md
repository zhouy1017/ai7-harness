# Progress

## What's done

- Verified the sole writable worktree/branch and exact base before editing: `C:\Users\Chooo\.codex\worktrees\issue24\ai7-harness`, `feat/24-j01-new-book-tracer`, `b70a0f014829fd92f898d55beb75f8595bfad996`. Read Issue #24, Commander amendment 1, and every routed authority; no predecessor implementation or unrelated archive was admitted.
- Bootstrapped the one deep root package in [`.node-version`](.node-version), [`.npmrc`](.npmrc), [`package.json`](package.json), [`pnpm-workspace.yaml`](pnpm-workspace.yaml), [`pnpm-lock.yaml`](pnpm-lock.yaml), and [`config/dependency-artifacts.json`](config/dependency-artifacts.json). Exact versions/SRI, strict engines/peers/store integrity, exact DSH convergence overrides, build allowlist, Node/Electron artifact hashes, and frozen installation are recorded.
- Added the closed-input host/toolchain boundary in [`tools/doctor.mjs`](tools/doctor.mjs) and [`tools/bootstrap.mjs`](tools/bootstrap.mjs). Acquisition children receive a deterministic PATH, exact registry/user-config/cache locations, no ambient proxy/CA/credential propagation, and no ambient npm/pnpm/Electron/esbuild selector; the independently hashed Electron archive is reverified before extraction and Electron Node mode is probed for Node `24.18.1`, ABI `148`, SQLite `3.53.1`, and FTS5.
- Implemented the private, maximum-512-KiB framed contract and pathless named renderer API in [`src/shared/protocol.ts`](src/shared/protocol.ts), synchronous fail-closed Node network denial plus default/named/call/construct self-proof in [`src/shared/network-denial.ts`](src/shared/network-denial.ts), and canonical non-overlapping Agent Data Root creation in [`src/shared/data-root.ts`](src/shared/data-root.ts).
- Implemented safe bounded DOCX staging in [`src/service/docx.ts`](src/service/docx.ts): streaming fflate/saxes bounds; active/embedded-content rejection; stable ordered plain-text blocks; truthful eight-category fidelity; only an empty terminal single-section baseline accepted as clean; nonempty run properties and actual section breaks rejected rather than falsely preserved; round-trip remains explicitly unsupported and non-blocking.
- Implemented the SQLite authority boundary in [`src/service/store.ts`](src/service/store.ts): content-addressed non-authoritative staging; digest-bound Review Before Import; one atomic exact Book/dimension/source/fidelity/primary-Manuscript/branch/r1/block/workflow/import-record graph; maximum-32-block signed windows; and an independent committed journal transaction with exact idempotent client acknowledgement. The accepted Q8 dimension set and seven-phase workflow are version/digest bound. Rollback failure poisons the whole store and becomes fatal instead of returning an ordinary response.
- Implemented the exact dormant six-service provider-free DSH composition in [`src/service/runtime.ts`](src/service/runtime.ts) and supervised service executable in [`src/service/index.ts`](src/service/index.ts). Readiness follows SQLite plus awaited Cordis lifecycle and proves six owners with zero agents, Sessions, providers, tools, prompt, or runtime context; excluded services remain absent. Exact Electron Node mode, 512-KiB framing, serial dispatch, parent PID/stdin lease, signals, and explicit shutdown all fail closed without payload logging.
- Implemented the Electron owner boundary in [`src/main/index.ts`](src/main/index.ts), [`src/main/application.ts`](src/main/application.ts), [`src/main/service-client.ts`](src/main/service-client.ts), and [`src/main/preload.ts`](src/main/preload.ts). Main owns native selection, one-use J-01 injection, service supervision, Chromium network/permission/download denial, IPC sender validation, exact commit idempotency binding, and teardown. The renderer uses `contextIsolation:true`, `nodeIntegration:false`, explicit sandboxing, a five-method bridge, no devtools/menu/navigation/window/webview authority, and never receives a raw path, DOCX bytes, database path, or whole manuscript.
- Implemented the Chinese no-preselection Review Before Import and bounded ProseMirror editor in [`src/renderer/index.html`](src/renderer/index.html), [`src/renderer/styles.css`](src/renderer/styles.css), [`src/renderer/index.ts`](src/renderer/index.ts), [`src/renderer/editor.ts`](src/renderer/editor.ts), and [`src/renderer/global.d.ts`](src/renderer/global.d.ts). It displays all eight fidelity categories, exact records/non-effects, and neutral dimension/workflow semantics; creates only on the final action; opens 32 of 51 synthetic blocks; rejects structural/reordered/multi-block/unbounded edits; and shows `已写入修订日志` only after the independent durable acknowledgement. Ctrl+S/Command+S is IME-safe.
- Added the single production-shaped build/start carrier in [`tools/build.mjs`](tools/build.mjs) and [`tools/start-built.mjs`](tools/start-built.mjs): built CJS main/preload, ESM service, local HTML/CSS/browser renderer, no source maps, no second package/launcher/server, and post-build guard/surface inspection.
- Added the one admitted UI journey in [`e2e/run-j01.mjs`](e2e/run-j01.mjs). It generates a 51-block public-synthetic Chinese DOCX only under an external runtime temp root, uses the same built subject and main-owned picker result hook, proves sandboxed renderer/loopback denial, no target preselection, exact fidelity/records/non-effects, atomic import, 32-of-51 projection, one bounded stable-block edit, platform save shortcut, busy state, and durable sequence-1 journal acknowledgement. It emits no screenshot, trace, video, artifact, DOM/manuscript text, or database assertion.
- Commander amendment 1 is implemented exactly: `pnpm run doctor`, `pnpm run bootstrap`, `pnpm run build`, `pnpm run start-built -- --data-root <absolute-path>`, and `pnpm run e2e -- --journey J-01`; both pnpm literal separators are normalized once and every other CLI shape is rejected.
- Local Windows 11 Pro `10.0.26200` x64 evidence is green with exact Node `24.18.1` and pnpm `11.24.0`: doctor, closed frozen bootstrap, build/typecheck, and the full J-01 E2E. Additional sanitized probes verified clean/default-section acceptance, styled/real-section rejection, lone-surrogate rejection with continued readiness, and both-direction canonical code/data-root exclusion.

## What's next

- Add the one dual-platform functional workflow, quickstart, dependency/source/artifact provenance, third-party notices, and current HANDOFF; then run the dependency/license/archive audit and the required independent standards/spec review.

## Unresolved matters or blockers

- No Issue #24 stop condition is active.
- macOS 15 arm64, Windows Server 2025 CI, and fresh hosted-runner acquisition/E2E remain unproven locally; no claim is made for them.
- The fatal rollback-failure branch is implemented and code-inspected but was not forced by runtime fault injection; ordinary rollback/continued-service behavior is covered by rejected-request probes.
- Otherwise-required DSH packages contain unused replay/fork, fixture, and Code Mode symbols, but no excluded package/service is installed, mounted, invoked, or renderer-facing. Provenance must state this API-surface nuance without claiming byte-level absence.

## Key decisions made

- The only harmless section baseline is one empty terminal body `sectPr`; page layout, inline styling, extra sections, and DOCX round-trip are not modelled by the plain-text tracer and therefore cannot be labelled preserved.
- The exact built checkout root, not `process.cwd()`, binds the data-root exclusion. Canonical overlap is rejected in both directions before and after creation, while a future external sibling data root remains possible when the code-root binding is adjusted by its carrier.
- Main installs the Node guard before dynamically importing Electron owners; the service installs it before DSH imports. The preload is explicitly sandboxed and therefore keeps only Electron’s narrow preload polyfills rather than gaining full Node solely for monkey-patching.
- Harness readiness is infrastructure evidence only. Import completion comes from the atomic authority transaction, and edit completion comes only from the separately committed journal acknowledgement.
- The E2E control hook supplies only the main-owned picker result. It cannot give the renderer a path or bypass staging, review, commit, projection, editor, or journal flow.

## Resume Prompt

Resume Issue #24 by adding the exact dual-platform E2E workflow plus quickstart/provenance/notices/HANDOFF, then run the final audits and required standards/spec review over the green built J-01 tracer.
