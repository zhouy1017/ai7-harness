# Current checkpoint

## What's done

- Reverified the authorized-resume entry at clean `feat/43-bounded-editor@cf0181670d064faa678a1df02f4f8aeb5bf837f1`, parent `d036fd3b7de79dbd8985bc4033efde6ed5cb2c02`, and local target `dev@6b4ef18d2c4b2a212ec34a24ec3a25b3bc3be5b5`. Scheme 1 alone is authorized by the Commander-supplied Issue #43 authority delta verified at `2026-08-27T10:27:57Z`.
- Completed repair A in the existing Store/bounded-manuscript owner. Branch-local schema v7 adds the single authorized private STRICT `working_offset_nodes` Fenwick structure. Fresh/v2, candidate v3-v5, and v6-to-v7 paths atomically create/reconstruct/admit/validate it. Ordinary length edits and exact prefix/inverse reads are O(log total blocks); all working window, character/proportion, outline, search global-position, replacement/history, and milestone consumers preserve exact absolute-offset truth without successor scans.
- Applied the Commander correction before commit: v7 now retires both mutable `working_blocks.start_offset` and `manuscript_outline.start_offset` plus `working_blocks_global_offset`. Candidate/v6 migration drops them transactionally after reconstructing the private index; fresh/v2 creates only the final column shape. Store import inserts and derived/index refresh paths no longer write either mutable offset column. `start_offset` remains only on exact staged/import and immutable revision rows, where streamed validation continues to prove it.
- Applied the bounded pre-commit replacement exactness correction. `commitReplacement` now captures the exact pre-edit segmentation, immediately re-segments the joined `afterText`, and uses that single post-join grapheme count for the block bound, Fenwick delta, and persisted `working_blocks.grapheme_length`; the independent UTF-16 code-unit bound remains intact. This handles grapheme clusters that merge across a replacement boundary without stale offset truth.
- Completed repair B in existing runtime owners. Cursor-only navigation retains exact off-window anchor/head/direction/scroll continuity and restores it only when all points return; explicit navigation and real user edits clear it. Renderer exclusions are hard-bounded to 1,000 and retain at least one included result. Search display fields are bounded by encoded JSON/UTF-8 cost and every 24-result projection is asserted below 512 KiB without changing identity/range truth.
- Strengthened the existing single J-02 journey: sustained cooperative edits now occur at exact 0.000%/the first stable block; a backward selection and scroll anchor are proven absent from the next non-overlap projection and exact after return; 1,001 runtime-only marker matches prove real paging, exactly 1,000 exclusions, retain-one behavior, and truthful preview/dismissal; search display fields and match IDs are checked. The exact runtime synthetic input remains 10,000,000 characters and the renderer remains at most 32 blocks.
- Completed coherent integrity repair C. Candidate v3 without a Signoff table may advance only with zero Milestones; v3-v5/v6/v7 admission requires reverse Milestone-to-Signoff coverage, while exact schema uniqueness guarantees one record at most. No evidence is fabricated.
- Staged drafts now receive a SQL byte preflight and streamed recomputation of every block digest plus complete parser-time content/structure identities before candidate repair, current reopen, and commit. Candidate/v2 derived offsets and draft-scoped IDs may still be repaired only after immutable content/structure truth passes; current derived identities fail closed. Commit validation runs inside the existing import transaction before any Book mutation.
- Every command-history group now receives SQL byte preflight and 128-row streamed verification of before/after text against immutable block kind/level digests during migration/reopen. Undo/redo revalidates the selected whole group before its first write in the same transaction, preventing partial mutation from tampered history.
- The two advisory T3 reviews remain disclosed as **same-provider review — independence reduced**.

## Validation

- PASS final `git diff --check` after repairs A-C and the Commander mutable-offset correction.
- PASS corrected target-shape Windows SQLite 50,000-row Fenwick probe: block-1 length change updated exactly 16 nodes, exact total became `10,000,003`, the prefix before block 50,000 became `9,999,803`, target `working_blocks` xinfo had no `start_offset`, the obsolete index was absent, STRICT remained enabled, and FKs were clean.
- PASS corrected SQLite v6-to-v7 retirement rollback/retry probe: induced failure restored v6, both legacy columns, the legacy index, all data, and no offset table; retry committed v7 with both columns/index absent, two exact nodes, preserved data, and clean FKs.
- PASS actual candidate-v5 repair and direct-v2 target-shape probes: corrupt mutable candidate offsets were retired rather than admitted, exact block text was preserved, grapheme/node values rebuilt to `3,4` / `3,7`, and both paths ended at the same no-mutable-offset v7 xinfo with clean FKs.
- PASS actual SQLite Signoff probe: unsigned candidate v3 stopped and rolled back without creating a Signoff table or changing version/data; exact reverse coverage then passed only after one real record.
- PASS actual SQLite staged probe across 300 rows (> migration batch): valid full identities passed; a same-length row-299 text tamper with stale digest failed, rolled back candidate migration DDL/version, remained unlaundered, and allowed no commit-side Book row.
- PASS actual SQLite history probe across 129 rows (> history batch): a terminal-batch same-length text tamper was rejected before replay; working text, command status, and sequence remained unchanged.
- PASS runtime-only synthetic-shape probe: exactly `10,000,000` graphemes, 25 normal search matches, 1,001 exclusion-cap matches, and one deterministic non-overlapping overlap match; no file was created.
- PASS 15 final no-install source assertions for target/current mutable-offset absence, exact retirement ordering, logarithmic maintenance, bounded reopen preflights, non-overlap continuity, exclusion/frame bounds, Signoff/staged/history truth, and near-start J-02 coverage. The diff remains exactly six modified existing paths with no added or untracked file.
- PASS no-install Unicode replacement-boundary probe: replacing the second grapheme in `ab` with combining acute retained two pre-join pieces but joined to `á` and re-segmented to exactly one grapheme (two UTF-16 code units). A scoped source assertion proves immediate re-segmentation and proves the grapheme bound, Fenwick delta, and persisted length all use `after.length`, with no stale pre-join `text.length` use in `commitReplacement`.
- Exact Node `24.18.1` is unavailable, the only pnpm carrier is fallback `11.19.0` rather than pinned `11.24.0`, and `node_modules` is absent. Therefore dependency-free Node syntax, filtered TypeScript, doctor/build, and official J-02 cannot run; none is claimed and no install/bypass occurred.

## What's next

- Resume in a sandbox that can write this worktree's existing shared Git metadata, stage exactly the six modified paths, create the authorized local commit with `Refs #43` and the required co-author trailer, then verify its parent is `cf0181670d064faa678a1df02f4f8aeb5bf837f1` and status is clean. Official doctor/build/J-02 must wait for the exact pinned carriers; later semantic integration remains ordered `#37 -> #41 -> #43`.

## Key decisions

- Mutable working/outline `start_offset` truth does not exist in v7. Exact absolute working positions come only from `working_offset_nodes`; staged/import and immutable revision `start_offset` columns remain exact and validated.
- Candidate derived offsets and draft-scoped staged IDs are repairable; persisted parser content/structure digests, Signoff evidence, and command text digests are authority and are compared, never rewritten or invented.
- Exact DOM selection cannot exist while its block is deliberately unrendered, so cursor navigation stores exact logical continuity privately without substituting an approximate visible position.
- Search truncation affects display fields only. Match identity, exact ranges, global positions, digests, inclusion, replacement, and navigation remain unchanged.
- Scheme 2, another Store/process/owner/table beyond the one authorized offset structure, a dependency, public interface, or gate remains forbidden. Branch-local schema numbering stays noncanonical; Commander later replays semantics in order `#37 -> #41 -> #43`.

## Unresolved matters or blockers

- No implementation or authority blocker remains within the authorized #43 module.
- Local commit is operationally blocked in the current sandbox: `git add` cannot create `C:/Users/Chooo/Playground/ai7-harness/.git/worktrees/issue-43-bounded-editor/index.lock` (`Permission denied`). No path was staged and no lock file remains; HEAD is still `cf0181670d064faa678a1df02f4f8aeb5bf837f1`.
- Provider/artifact inertness, one SQLite/service owner, the 32-block renderer ceiling, runtime-only synthetic input, v1/v2 policy, historical unsigned-Milestone stop semantics, separate Signoff truth, single-use exact AI7 Apply, and all earlier Issue #43 repairs must remain unchanged.
- Official pinned validation carriers remain unavailable. Branch-local schema v7 is noncanonical for integration: preserve #37 identity/review-v4 behavior, then #41 per-draft commitBindings/attempt/ack semantics, and replay #43 as the next forward schema rather than mechanically reusing this number.

## Resume Prompt

Resume only to complete the authorized local commit in an environment that can write the existing shared Git metadata. Verify branch `feat/43-bounded-editor`, HEAD `cf0181670d064faa678a1df02f4f8aeb5bf837f1`, local `dev@6b4ef18d`, and exactly six dirty paths, including the post-join replacement re-segmentation correction; rerun `git diff --check`, stage those six paths, commit with `Refs #43` plus `Co-authored-by: gpt-5.6-sol <noreply@openai.com>`, and verify exact parent/body/clean status. Do not reopen implementation, enter `docs/archive/`, use network, install, call Providers, add manuscript artifacts, create a gate/process/public API, push, open a PR, merge/rebase, release, or touch `main`.
