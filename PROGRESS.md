# Progress

## What's done

- Restored this workspace as a detached worktree at exact `dev@2f8471f0d80ffa79f3cbdf1d79b0f0491697ca63`.
- Routed the Owner-requested two-requirement addition through the Ask Matt design flow and inspected the current `dev` authority owners for the Book-anchored UI, DSH composition, Task Skills, Workflow Profiles, capability profiles, and third-party DSH plugins.
- Confirmed that the requested optional agent-primary Book surface and shipped DSH ecosystem/marketplace compatibility materially revise the accepted UI/UX baseline and ADR 0042 rather than merely adding implementation detail.
- Drafted Owner answer A as `Book-bound DSH-composed Agent Workspace / 图书绑定的 DSH 组合式智能体工作区` in `docs/ui-ux-v2/CONTEXT.md` and its package-local index in `docs/ui-ux-v2/GLOSSARY.md`. These remain unintegrated candidate changes on the Issue #86 handoff branch; the authorized normalization must explicitly adopt, revise or discard them before they become target-qualified authority.
- Recorded Owner answer A for central presentation: explicit entry gives the Agent Workspace the primary central slot while preserving the prior Active Work Object, visible Book/safety state, and direct return route.
- Added a required later decision branch to assess whether AI7 Task Skills and Workflow Profiles should remain separate from, project into, or converge with DSH-native plugins.
- Recorded Owner answer C: the unimplemented AI7 Task Skill and Workflow Profile definition/package models are to migrate to DSH-native plugin/composition artifacts rather than survive as parallel long-term runtime models.
- Audited 96 current non-archived design files on exact `dev`, including all 44 root ADRs and 14 UI ADRs; 52 files contain migration-relevant object references.
- Classified additional migration candidates: DSH-native definition/composition carriers for Agent Behavior Assets, Harness-side capability/tool adapters, capability-profile compositions, and extension/catalog UI; bridge projections for Reusable Procedure Capture, Model Roles/provider configuration, Execution Bindings, and version/history views; AI7-retained product authority for Books, manuscripts, durable Workflow Instances/Gates/Signoffs, Policies, Plan/Run, grants, Effects, ledgers, sources, factual review, and learning records.
- Recorded Owner answer A for Workflow runtime ownership: DSH-native plugins own versioned Workflow definitions and technical execution logic, while AI7 remains the sole owner of durable Workflow Instances, Phase/Gate state, Signoff, scheduling, and deterministic transition commands.
- Recorded Owner answer A for Capability execution: DSH-native plugins may fully own self-contained technical, presentation, in-memory, or plugin-local behavior, but any operation touching AI7 business state, unpublished content, credentials, outbound data, or external Effects must cross the AI7 Capability Facade into an AI7 Capability Executor.
- Recorded Owner answer A for Task Skill migration: a DSH Skill is the canonical semantic unit, with DSH Plugin/Bundle packaging for distribution, registration, versioning, and optional code; AI7-specific requirements become namespaced metadata rather than a second Skill package format.
- Audited the current non-archived long-manuscript retrieval, indexing, Exact Fetch, context-assembly, provider-policy, and text-analysis design owners on exact `dev` and compared them with the current `src/`, runtime probe, dependencies, and J-01 E2E implementation.
- Confirmed that V2 did not prohibit or remove embeddings: Manuscript Revision remains the sole authority, while lexical indexes, retrieval chunks, rankings, and embeddings are disposable local projections; lexical/vector/hybrid selection remains a later authorized implementation choice.
- Confirmed that "local mode" is not a canonical product mode. Local editing, indexing/search, Exact Fetch, history/recovery, task preparation, and local export may operate without a Provider, while every model-driven analysis still crosses the Primary Agent Harness and AI7 payload/egress authority. Active Provider Processing v2 currently denies ordinary manuscript processing and admits only exact `sample1` future manual-recording eligibility.
- Confirmed the current code implements only the bounded provider-free J-01 tracer: SQLite authority/block storage, an r1 working state, a first 32-block service window, and durable journal acknowledgement. It does not implement the accepted long-manuscript scale tiers, continuous multi-window UI, the three disk-backed indexes, retrieval chunks, embedding/vector search, Exact Fetch, text analysis, context assembly, or executable Agent/Provider paths.
- Evaluated a potential implementation sequence: first complete checkpointed immutable revisions and disk-backed block/outline/CJK lexical projections; then candidate search, pinned-revision Exact Fetch, bounded context assembly, and a separate coverage-guaranteed hierarchical whole-manuscript analysis path; add local embeddings/hybrid ranking only if measured semantic-recall gaps justify them.
- Recorded the Owner's correction that current Provider Processing v2's default-deny/sample1-only rule is a development and test anti-leakage baseline, not the intended production product policy. In production, user-configured LLM services processing authorized manuscript content are a foundational ordinary AI7 path and remain distinct from cloud-drive upload, Internet publication, External Export, and Public Release.
- Recorded the Owner's correction that long-manuscript retrieval exists primarily to overcome provider/model context-window and attention limits so AI7 can extract accurate, comprehensive information from the manuscript. Exact Fetch remains essential for revision-correct wording and quotation, but it does not establish retrieval recall, analysis coverage, or synthesis completeness.
- Verified that the Provider correction restores an already accepted principle in ADR 0016: sending manuscript content to a configured model provider is controlled processing and a basic AI7 feature, while storage, publication and redistribution remain separately prohibited or governed. Current Provider Processing v2 conflicts with that product principle by elevating a development recording boundary into the active product policy.
- Audited the long-manuscript gap: current ADR 0026 defines ranked approximate candidates, projection freshness and Exact Fetch, but no deterministic whole-revision coverage, structured per-range partials, coverage-aware synthesis, failed/gap disclosure, or durable coverage status.
- Revised the grilling estimate from 17 to 23 questions. The two Owner corrections add six dependent decisions covering ordinary production Provider authorization, development/product policy separation, model-derived background processing, targeted versus whole-revision analysis, coverage-aware synthesis/assurance, and coverage-sensitive acceptance before the remaining DSH compatibility/import branches.
- Recorded Owner answer A for production Provider authorization: a user-initiated Task or an exact user-approved Default Execution Rule, together with an eligible configured Provider, frozen Plan/Run Authorization, Provider Binding, Run Source Scope and declared outbound categories, is sufficient authority for required manuscript metadata, excerpts or full content. No additional per-Book, first-use, per-call or per-chunk privacy confirmation is required; unmatched and out-of-scope payloads still fail closed.
- Recorded Owner answer A for policy-scope separation: development/CI provider-free execution, exact human-attended fixture recording, and ordinary production Provider Processing use separate immutable policy selections chosen by trusted build/launch authority. They are not ordinary settings, environment-variable or plugin switches; Provider Processing v1/v2 remain immutable history and production receives a new superseding policy version.
- Recorded Owner answer A for inherited production processing: every model-driven operation class declared inside one frozen Plan Envelope—including generation, remote embedding, reranking, subagents, multi-pass coverage analysis and background continuation of that Run—shares its Run Authorization without per-call confirmation. Adding an undeclared operation class or expanding Provider, data or source scope requires Plan Revision and renewed authorization; DSH Session or Plugin membership alone grants nothing.
- Recorded the Owner's cross-cutting authorization ergonomics principle: ordinary LLM/Agent task initiation must minimize user burden and offer durable authorization options. Reading, retrieval, analysis, Provider processing and proposal generation should normally execute through a one-action path or a matching long-term rule; an exact formal manuscript mutation remains an explicit editor confirmation boundary.
- Recorded Owner answer A for long-term authorization scope: a revocable Default Execution Rule may apply by Book, Series or all Books and let a matching user-initiated non-mutating Task directly read, retrieve, process with Providers, analyze, research, draft and create Proposals while AI7 creates exact per-Run records behind the simple start interaction. `Non-mutating` describes formal Manuscript state, not unrestricted network activity: research queries, sources and outbound data categories remain declared Run scope, while external writes, messages, publication, purchases and comparable actions retain separate Effect authority. Material boundary drift returns to a compact confirmation/update path; applying any Proposal to the formal Manuscript always requires an exact explicit editor confirmation.
- Recorded Owner answer A for long-manuscript execution contracts: ordinary questions and passage-finding use targeted lexical/vector/hybrid retrieval, while any Task claiming whole-manuscript, comprehensive or complete review must deterministically cover the exact pinned revision, persist unit-level results, perform coverage-aware hierarchical synthesis and disclose every failed, skipped or uncovered range. Goal semantics and the DSH Skill contract select the mode automatically; users do not choose a technical RAG mode.
- Recorded Owner answer A for exhaustive analysis-unit formation: AI7 deterministically derives a complete Coverage Manifest from the pinned Manuscript Revision, prefers structural boundaries such as chapters, scenes, headings and Manuscript Blocks, splits structures that exceed the attention budget, adds bounded boundary overlap, and records exact ranges and digests. A DSH Skill may declare budget, context and result-schema needs but may not omit revision ranges.
- Traced the fixed 32-block value to the provider-free J-01 tracer (`MAX_WINDOW_BLOCKS = 32`, introduced by `69face7` and integrated by `4ef62ac`). It bounds one service/renderer manuscript-window projection and its signed paging cursors; the E2E proves a 32-block first window from a larger sample. The accepted design requires only a bounded editor window and explicitly allows retrieval chunks to span or subdivide editor units, so 32 is neither a RAG chunk size nor an exhaustive-analysis unit.
- Recorded Owner answer A for coverage-aware synthesis: each DSH Skill declares a task-specific structured partial-result schema and compatible reducer stages, while AI7 preserves the generic coverage/provenance envelope. Unit results remain bound to exact ranges and evidence; chapter/part/book reducers preserve source-unit lineage, unresolved conflicts and confidence, and a cross-unit contradiction/continuity pass precedes final prose. Free-text recursive summaries do not replace the structured evidence graph or Coverage Manifest.
- Recorded the Owner's added persistence requirement: whole-Book information analyses and comparable expensive model-derived datasets must persist locally so compatible later work can reuse them without repeated LLM calls. Users must be able to request both local-scope and whole-Book updates.
- Audited the current persistence seam. `Task Outcome` is a durable per-task result and `Editorial Artifact` is a separately governed versioned workflow record, while retrieval projections are disposable/rebuildable; none currently supplies a stable reusable whole-Book analysis identity. Current code has no analysis, Coverage Manifest, unit-result, synthesis, cache-hit, invalidation, local-refresh or global-refresh API/storage/E2E behavior. Exact Edit Journal ranges and the accepted revision/range-overlap invalidation rule are usable future inputs, but no analysis consumer exists.
- Confirmed a compatible precedent in the accepted factual-verification UX: selection/chapter/whole-manuscript scopes, exact revision/range and last-check bindings, preservation of old-revision results after manuscript drift, and promotion that executes only missing/stale work while reusing current evidence. This supports immutable analysis generations plus incremental dependency closure, but factual findings are too narrow to own general character, event, chronology, theme or relationship datasets.
- Recorded Owner answer A for durable analysis lifecycle: AI7 owns a Book-bound `Manuscript Analysis Result Set / 稿件分析结果集` with immutable result-set revisions. Each revision binds an exact Manuscript Pin, Coverage Manifest, exact DSH Skill/Plugin Analysis Contract and schema/reducer digests, unit results, synthesis/conflict/gap state, producing execution provenance, reuse lineage and cost. A Task Outcome links the produced revision; only an explicit later promotion creates a formal Editorial Artifact.
- Separated three update commands accepted with that lifecycle: `同步到当前稿件` reuses dependency-compatible unchanged units and recomputes the invalidation closure; `重新分析所选范围` bypasses cached model results for the selected/local dependency closure; `重新分析全书` bypasses all prior model results and covers the complete manifest. Every operation appends a result-set revision, preserves history and may claim current completeness only after coverage and dependency closure are closed.
- Recorded Owner answer A for analysis-update triggering: every new Manuscript Checkpoint performs only local deterministic invalidation and dependency-closure marking by default. A user-started task that requires current analysis may include incremental synchronization inside the same simple task start and Run Authorization; a matching active Background Analysis Enrollment, rather than a user-initiated Default Execution Rule, authorizes scoped post-checkpoint, idle or scheduled background synchronization by Book/Series/all Books, analysis kind, Provider and budget. Changes coalesce at checkpoints rather than triggering LLM work per edit. No matching Task, Default Execution Rule or Background Analysis Enrollment means no Provider call or cost.
- Recorded Owner answer A for analysis completion semantics: no single generic `complete` flag is sufficient. Coverage, reducer/synthesis closure, exact-revision freshness and semantic/evidence assurance are independent states. A result may claim complete whole-revision coverage only when every manifest unit and required reducer/reconciliation node closes; it may claim current whole-Book analysis only when that completed revision also matches the current manuscript. Failures, skips, cancellation and invalid outputs persist exact gaps and support missing-only continuation without repeating successful LLM work; semantic correctness remains a separate qualified assurance claim.
- Recorded the Owner's default-background-analysis addition: Baseline analysis is the default selection within an active Background Analysis Enrollment. Successful import of a new Manuscript queues whole-Book baseline analysis and missing-unit/reducer completion only when a matching Enrollment exists; otherwise import succeeds with analysis pending. No matching Task, Default Execution Rule or Background Analysis Enrollment means no Provider call or cost. Background results remain non-mutating persisted result-set revisions.
- Identified and resolved the authority-language conflict in the accepted replacement design: the current UI `Default Execution Rule` and `Default-executed Run` correctly require a future user-initiated Task and exclude background automation, while background analysis instances run through the same DSH agent loop under a distinct active, disclosed Background Analysis Enrollment. The current UI authority text still requires explicit supersession to name that separate contract; it must not imply that the old rule already authorizes background work.
- Audited candidate default-analysis scope and found no existing enumerable whole-Book analysis Skill catalog on `dev`. The eight Editorial Dimensions are task-selectable rather than an all-at-once default, Series Knowledge categories require separate human promotion, and Workflow Profiles are deliverable lifecycles rather than analysis catalogs. A conservative candidate baseline is therefore a new exact versioned contract covering structure/Coverage Manifest, structural-unit synopsis, entities/aliases/terms, events and chronology, relationships, internal-setting claims, exact ranges, confidence, conflicts and unresolved items. Interpretive editorial review, external factual research, correction, Series promotion and learning remain specialized work.
- Confirmed that installed, catalog-visible, recommended or enabled DSH plugins cannot automatically join the import baseline: existing plugin/catalog authority rules grant neither automatic selection nor Run authority. A separate explicit analysis enrollment would be required for any plugin-provided analysis kind. Import commit and background analysis must also remain separate outcomes so an offline, Provider, budget or analysis failure leaves a successful import intact and visible as analysis pending.
- Recorded Owner answer A for automatic analysis scope: a product-built, exact-versioned Baseline Manuscript Analysis Contract automatically maintains structure/Coverage Manifest, structural-unit synopsis, entities/aliases/terms, events and chronology, relationships, internal-setting claims, exact ranges, confidence, conflicts and unresolved items. It describes manuscript-internal information and does not itself perform external truth adjudication, interpretive editorial review, mutation, Series Knowledge promotion or learning.
- Added the Owner's required optional breadth: all eight built-in Editorial Dimensions—Literary Quality and Authorial Voice; Theme, Values and Social-cultural Context; Structure, Narrative Logic and Coherence; Chinese Language, Style and Expression; Factual Accuracy and Source Integrity; Target Readership and Market Positioning; Legal, Rights, Ethical and Publication-policy Risk; and Production Readiness and Cross-deliverable Consistency—must be offered for individual or one-action enrollment into default background analysis. They are not forced into the built-in baseline, and plugin installation/enablement alone still enrolls nothing.
- Recorded Owner agreement with a distinct versioned `Background Analysis Enrollment / 后台分析登记`: its user-facing surface is simple automatic-analysis configuration scoped by Book, Series or all Books. Baseline analysis is the default selection within an active, disclosed Enrollment established through the low-burden onboarding/provider configuration path; manuscript import or Provider setup alone does not create ambient authority. The eight built-in dimensions and plugin/user-defined analysis kinds are independently selectable, with an all-eight shortcut. Selected kinds may share one authorized background batch Run but retain independent Result Sets, coverage/freshness/failure state and updates. Enrollment freezes scope, kinds, Provider/data categories, budget, prospective/backfill choice, pause/revoke and restart semantics; each dispatch still creates exact per-Run plan/provider/authorization/cost provenance. Without a matching active Enrollment, import succeeds and analysis remains pending without a Provider call, and no result may mutate the formal Manuscript.
- Recorded the Owner's analysis-quality requirement: persisted Baseline and optional analysis Result Set revisions must accept user feedback and participate in quality evaluation. Feedback needs exact attribution to the Book, Manuscript Pin, result-set/version, analysis kind/dimension, DSH Skill/Plugin and effective execution/model provenance rather than becoming an unbound rating.
- Recorded Owner answer A for analysis feedback and quality: feedback is an immutable, exact-result-version-bound Quality Signal targeting a whole set, analysis kind/dimension, synthesis node, item or omitted range. It may record typed dispositions and free text but never overwrites historical model output, counts silence as approval, establishes factual truth, automatically enters learning or changes a Skill/Plugin. An explicit structured user correction creates a provenance-marked successor Result Set Revision and recomputes affected synthesis; requested local/full reanalysis creates a new authorized Run and revision.
- Added a distinct versioned `Analysis Quality Metric Snapshot` candidate rather than misusing proposal-centric Delivery Quality Metrics. It aggregates reviewed usefulness/correction/rejection, user-discovered omissions, entity merge/split, relationship/chronology and range/evidence corrections, schema/coverage/reducer/freshness closure, conflict/gap preservation, repair-after-rerun, reuse, cost and latency by exact analysis/contract/model/provider and permitted Book/editor/house scope. Factual-review dispositions remain separate evidence-governed decisions, and metric aggregation grants no cross-Book retrieval authority.
- Rechecked the live official DSH ecosystem against the repository's `0.1.0-rc.6` pin. Official DSH profiles are directories whose `package.json` holds plugin dependencies plus an ordered `dsh.profile.bundles` manifest; bundle packages declare a `dsh.bundle.patch`, composition layers patches with user overrides, and `dsh plugin --profile ...` forwards package operations to pnpm. Official documentation still labels DSH a rapidly changing developer preview and directs discovery through the `dsh-plugin` GitHub topic rather than naming one canonical official marketplace; several independent community marketplaces currently wrap the native install flow.
- Confirmed that root ADR 0042's current model—development-only need-based discovery, a small-or-zero admitted plugin set, immutable release-time local pins, no shipped runtime discovery and no automatic upstream update—directly conflicts with the newly requested complete DSH plugin/marketplace product compatibility. Preserving AI7 authority gates remains compatible, but the discovery/install/update lifecycle and native-artifact posture require explicit supersession rather than incremental wording.
- Recorded Owner answer A for complete DSH ecosystem compatibility: DSH Plugin, Bundle, Profile, Agent Preset and Skill artifacts retain their native identity, manifest, dependencies and versions, while AI7 adds a non-destructive sidecar overlay for source/digest pins, Book or user scope, capability/data/Provider/Effect authority, compatibility status, audit and rollback. AI7's DSH ecosystem surface supports pluggable catalog sources plus native browse/search/install/update/disable/remove/rollback operations; DSH UI plugins may render inside the Book-bound Agent Workspace but may not replace the AI7 shell, Book binding, safety state or explicit manuscript-Apply boundary.
- Completed a live official cross-host Skill-format audit. Current Codex and Claude documentation both describe filesystem Agent Skills built around a `SKILL.md` with `name` and `description` plus optional references, assets and scripts; Codex explicitly builds on the open Agent Skills standard and scans `.agents/skills`, while Claude Code follows the same standard but adds host-specific invocation, permission, subagent, hook, variable-substitution and dynamic-context features. Current official DSH likewise scans `.agents/skills`, accepts directory `SKILL.md` or flat Markdown entries, and normalizes model/user invocation controls. Therefore foreign-skill intake must first preserve a portable core when it is already DSH-readable and reserve semantic rewriting for host extensions, tool/path/runtime assumptions, permissions and packaging dependencies rather than rewriting every file by default.
- Recorded Owner answer A for foreign Agent Skill import representation. AI7 preserves an immutable `Source Skill Snapshot` containing the original files, upstream identity, source format/version and digest; derives a versioned DSH-native `Imported Skill Working Revision` with its own local identity/digest that preserves already-portable content as exactly as possible and minimally rewrites only host-specific semantics; and attaches a separately versioned AI7 authority/compatibility sidecar. The converted working revision never masquerades as the upstream artifact, and the sidecar describes compatibility and authority requirements rather than acting as a live grant. Every conversion emits a reviewable report classifying preserved, deterministically mapped, review-required and unsupported elements. Unsupported tool, permission, path, hook, subagent, runtime or packaging semantics remain blocked rather than guessed, and importing never by itself installs, enables or authorizes the resulting Skill.
- Recorded Owner answer A for imported-Skill updates. Every upstream revision becomes a new immutable Source Skill Snapshot; AI7 reconciles the prior source snapshot, current locally editable DSH working revision and new source snapshot into an inert provenance-bearing update candidate. Deterministic conflict-free changes may merge while preserving local edits; conflicts require an explicit source/local/generated review. New scripts, dependencies, tools, network use, Provider/data scope, permissions, host-specific behavior, analysis contract/schema/reducer, budget or schedule are expansive changes and never inherit authorization silently. Default update checking accesses only configured-source metadata and no Book content. The executable working revision changes only through explicit adoption or a distinct revocable Artifact Update Rule—not a Default Execution Rule or Background Analysis Enrollment—limited to a trusted source and conflict-free, compatibility-valid, semantically non-expansive changes. Every adoption creates working and sidecar revisions; rollback restores artifact revisions but never restores revoked grants, rules or enrollments.
- Recorded Owner answer A for staged artifact admission and activation, completing the 23-question design interview. Catalog discovery, acquisition/pinning, compatibility/conversion validation, scoped enablement, exact Task/Run authority and formal-Manuscript Apply are distinct internal boundaries even when a compatible artifact receives a one-action `install and enable` user path. Acquisition and validation are non-executing. AI7 reports `native ready`, `converted ready`, `restricted`, `needs adapter` or `incompatible`; any lifecycle hook, dependency script or native code that requires execution makes the artifact at least `restricted` and stops the one-action path until separate executable-admission and sandbox authority is satisfied. Installation or enablement grants no manuscript, Provider, network, credential, external-Effect or background-analysis authority: foreground Provider processing requires a matching user-initiated Task/default rule, background processing requires an active Background Analysis Enrollment, and both issue an exact frozen Run. Every agent/plugin/background/imported-Skill formal mutation routes through the one AI7 Apply command, which is single-use and bound to an exact Book, base pin, diff and targets with drift reconciliation and explicit editor confirmation; no install, enablement, update rule, enrollment or Run may contain or inherit Apply. Direct human typing remains the separate human edit action.
- Completed an independent final consistency audit and repaired stale or ambiguous checkpoint wording. The accepted model is coherent around four non-negotiable boundaries: Provider setup/artifact install never creates ambient manuscript authority; foreground and background Provider work both require exact frozen Runs but originate from different standing contracts; imported-artifact update candidates remain inert until explicit adoption or an Artifact Update Rule; and formal agent-originated Manuscript mutation can occur only through the one exact, single-use AI7 Apply command. The one intentional pre-existing conflict is Policy-revision activation between ADR 0018 and ADR 0004/Learning Eligibility; the stricter developer-review rule governs until explicit supersession.
- Completed a read-only domain-object closure. The normalization target retires the parallel Task Skill Manifest/Package/Candidate/Installed Version/Harness Skill Projection and monolithic Admission State as canonical runtime owners, distributing their responsibilities across native DSH artifacts, immutable acquisition/source pins, a versioned AI7 authority/compatibility sidecar, compatibility disposition, scoped enablement and exact per-Run activation. New decided objects include Source Skill Snapshot, Imported Skill Working Revision, Coverage Manifest, Analysis Unit, DSH Analysis Contract, Baseline Manuscript Analysis Contract, Manuscript Analysis Result Set/Revision and Background Analysis Enrollment; exact names/schemas remain open for the sidecar, conversion report, update-reconciliation record, catalog source, scoped enablement, per-Run activation and Analysis Quality Metric Snapshot.
- Completed a current, non-archive document-impact audit. The proposed bounded successor set is root ADRs 0045–0048 for native DSH artifacts/sidecars, operational-scope Provider Processing, targeted retrieval versus covered analysis, and enrolled/evaluated background analysis; plus UI ADRs 0015–0017 for the Book-bound Agent Workspace, staged artifact lifecycle and versioned coverage-aware analysis. Root ADRs 0010 and 0042 require full supersession; ADRs 0008, 0017, 0018 and 0041 require partial supersession; ADRs 0019 and 0026 need successor links or amendments without losing their current semantics. `docs/domain/execution/CONTEXT.md`, `docs/domain/editorial/CONTEXT.md`, all six `docs/architecture-v2/` design files, the main UI/UX package files and their routing documents require normalization. Provider Processing v1/v2 and active-policy-set v1/v2 remain immutable; ordinary production requires a new policy/active-set version after its exact trusted selection and serialization contract is authorized.
- Created GitHub Issue [#86](https://github.com/zhouy1017/ai7-harness/issues/86) with the full T3 documentation/design Change Brief, explicit target-qualified authority, supersession map, structural budget, non-goals and stop conditions; labeled it `enhancement` and `ready-for-agent` for one remote successor.
- Created and published branch `codex/docs/86-design-handoff` from exact `dev@2f8471f0d80ffa79f3cbdf1d79b0f0491697ca63` after confirming current `origin/dev` is the same commit, and replaced root `HANDOFF.md` with a concise one-time cold-start router, suggested skills and an exact resume prompt. Issue #86 records the exact remote handoff head; no pull request was created.

## What's next

- The 23-question interview, closure audits, Issue #86 Change Brief and published handoff branch are complete. One remote successor may now continue the bounded documentation normalization on this exact branch.
- Before promoting or extending the current UI working definition, the successor must recheck current `origin/dev` and target-qualified authority under Issue #86. Product implementation, dependency changes and implementation Issue decomposition remain separately unauthorized.

## Key decisions made

- `dev`, not `main` or the frozen `design-doc` line, is the intended integration baseline.
- No product code, dependency, plugin discovery/install, or implementation issue decomposition is authorized by this requirements interview.
- The optional agent-primary interface is a Book-bound DSH-composed surface inside the AI7-owned shell; AI7 retains Book/object bindings, product truth, capabilities, and authority while admitted DSH composition may provide the inner experience and compatible admitted DSH UI plugins.
- Explicit user entry makes that Agent Workspace the central presentation; it does not replace the prior Active Work Object or change Book, Run, or authority state.
- Task Skill and Workflow Profile definitions/packages will converge on DSH-native plugin/composition artifacts, and the native-carrier/AI7-authority-overlay boundary is decided. Exact Profile/Bundle mappings and sidecar schemas remain unresolved.
- Workflow Profile migration changes the definition/composition carrier only: DSH does not become the authority for the Deliverable-owned Workflow Instance or its business state.
- Capability migration is authority-based rather than blanket: native DSH execution is permitted only while it does not cross an AI7 product-state, sensitive-data, egress, credential, or Effect boundary.
- The former Task Skill maps to a native DSH Skill; Plugin/Bundle is its distribution and optional-code carrier, while Agent Preset/Profile remains a broader composition object.
- `RAG` remains an informal description rather than a canonical architecture object. The owned design vocabulary is `Projection and Retrieval`, `Manuscript Retrieval Chunk`, and `Exact Fetch`.
- Embeddings are optional, governed, revision-stamped manuscript derivatives, not authority and not a mandatory first implementation. A CJK-aware local lexical baseline is the current implementation recommendation, not yet an accepted technology selection.
- Query-oriented retrieval and exhaustive whole-manuscript analysis are different execution shapes: top-K candidate retrieval is appropriate for finding passages, while coverage-sensitive analysis must enumerate the pinned revision and hierarchically reduce provenance-bound findings.
- Development/CI provider-free execution, one exact human-attended fixture recording, and ordinary production Provider Processing are separate operational scopes. Provider Processing v2 must remain immutable history and be superseded for production rather than edited in place.
- Configured Provider processing is controlled model processing, not external storage, Internet publication, or Public Release. Ordinary product authorization is fixed at an exact Run issued by a user-initiated Task/default rule for foreground work or an active Background Analysis Enrollment for background work, rather than an additional per-Book, first-use, per-call or per-chunk privacy-consent layer.
- Long-manuscript analysis has an explicit coverage contract in addition to candidate relevance and Exact Fetch. Processing every declared range can be evidenced; semantic completeness and model attention cannot be falsely guaranteed, so coverage, reducer closure, freshness and semantic/evidence assurance remain separate visible axes.
- Product Provider permission uses exact Run authority: foreground processing requires an eligible configured Provider plus a matching user-initiated/default-rule Task, while background processing requires a matching active Background Analysis Enrollment. Both freeze Plan/Run Authorization, Provider Binding, Run Source Scope and declared data categories without per-call or per-chunk privacy prompts; Provider setup alone grants none, and unmatched/out-of-scope payloads still fail closed. This is the accepted future production baseline; until a superseding Policy is authorized and integrated, active Provider Processing v2 continues to prohibit ordinary manuscript Provider calls.
- Owner accepted that normal task-authorized processing recommendation. Provider setup alone grants no ambient Book access, but no second privacy-consent layer sits inside an already authorized exact Run.
- Owner accepted separate, non-user-toggleable policy selections for development/CI, fixture recording and production. No runtime mode predicate inside one combined policy and no procedure-only development safeguard replaces that separation.
- Owner accepted inheritance of production Run authority by all declared model-driven suboperations; authority follows the Plan Envelope, not individual calls, DSH Sessions or Plugins.
- Authorization UX follows minimum necessary ceremony. Default Execution Rules and equivalent durable choices must be first-class paths for recurring non-mutating work, while creating a Proposal is not formal manuscript mutation and applying it is.
- Owner accepted automatic authorization/dispatch after the user starts a matching non-mutating Task and rejected both repeated Plan-start ceremony and automatic formal manuscript Apply. The reusable Default Execution Rule issues exact per-Run authority rather than becoming ambient Agent or Session authority; only Background Analysis Enrollment authorizes work without a newly user-started Task.
- Long-manuscript execution uses a dual contract: targeted retrieval for ordinary questions, and deterministic whole-revision coverage with durable structured partials, hierarchical synthesis and explicit gaps for Tasks claiming whole-manuscript or comprehensive analysis.
- Owner accepted that dual contract. Retrieval relevance never substitutes for coverage, and comprehensive mode cannot omit a range merely because it ranked below a top-K threshold.
- Owner accepted structure-aware, completely enumerated, bounded-overlap Analysis Units backed by a deterministic Coverage Manifest. Unit size is an execution budget rather than a fixed block count, and DSH Skill customization cannot weaken full-revision coverage.
- The tracer's 32-block constant is an implementation guardrail for bounded editor/IPC projection, not a domain invariant. Editor Window, Retrieval Chunk, exhaustive Analysis Unit and Exact Fetch Range are separate projections/contracts with different sizing rules; only the boundedness requirement should survive as architecture authority.
- Owner accepted typed, provenance-preserving hierarchical reduction rather than recursive prose summarization. DSH owns the task-specific analysis schema/reducer contract; AI7 owns completeness, revision/range lineage, persistence, conflict/gap status and the rule governing any claim of whole-revision coverage.
- Persisted whole-Book analysis is an accepted product requirement, not an optional transient Run optimization. Reuse never conceals staleness or implies that a model evaluated changed text; incremental synchronization, selected-scope forced reanalysis and whole-Book forced reanalysis each append immutable result-set revisions with explicit cache-reuse or bypass lineage.
- Owner accepted immutable write-on-update result sets and the three-way distinction between incremental synchronization, local forced reanalysis and whole-Book forced reanalysis. Reuse is exact and provenance-bearing; old revisions remain valid history for their original Manuscript Pin and are never rebound to the latest manuscript.
- Owner accepted local-first invalidation plus on-demand or durably authorized incremental synchronization. Default Execution Rules are reused rather than inventing ambient Agent/Session authority; each automatic synchronization still creates an exact authorized Run and result-set revision.
- Owner accepted the four-axis completeness model. Mechanical coverage and synthesis closure are evidence-backed; currentness is revision-relative; assurance and unresolved conflicts remain visible and are never collapsed into an absolute semantic-completeness claim.
- New-Manuscript baseline analysis and missing-result completion are the default selections inside an active Background Analysis Enrollment. Import queues them only when a matching Enrollment exists; otherwise import succeeds with analysis pending. No matching Task, Default Execution Rule or Background Analysis Enrollment means no Provider call or cost.
- Owner accepted the conservative manuscript-information baseline and required every built-in Editorial Dimension to remain available as an optional default-background addition, individually and through an all-eight shortcut. Baseline extraction and optional editorial judgment remain separately identifiable analysis contracts/result sets.
- Owner accepted a separate Background Analysis Enrollment rather than stretching user-initiated Default Execution Rules. One batch may reduce ceremony and dispatch overhead, while each analysis kind keeps independent persistence, currentness, completion, feedback and update semantics.
- User feedback and quality evaluation are required for every persisted analysis family. Existing Quality Signal semantics are a starting owner, but proposal-centric Delivery Quality Metrics do not yet measure analysis omissions, item correctness, grounding or relationship/entity errors.
- Owner accepted exact immutable analysis Quality Signals, successor revisions for corrections/reanalysis and a separate Analysis Quality Metric family. User editorial feedback is quality evidence for usefulness/judgment, not factual proof; automatic learning or behavior mutation remains prohibited.
- Owner accepted native DSH artifacts plus an AI7 sidecar overlay as the ecosystem compatibility model. Marketplace/catalog UI, install/update/rollback and native DSH presentation are product capabilities; native identity is preserved, while AI7 authority gates remain mandatory at every AI7 state, unpublished-text, credential, egress and Effect boundary.
- Claude/Codex Skill compatibility is not inherently a full format migration: portable Agent Skills retain their core `SKILL.md` and resources where possible, while host-specific extensions use the accepted immutable-source, derived-working-revision and provenance-aware three-way conversion/update model.
- Owner accepted immutable-source plus derived-working-revision import semantics. The original foreign artifact remains exact provenance, the versioned DSH working revision is the current authorable/executable Skill inside AI7, and the AI7 sidecar owns declared authority requirements and compatibility status without creating another Skill instruction format or live grant. Live authority remains in exact AI7 grants, rules, enrollments and Runs. Conversion is minimal, reviewable and fail-closed for uncertain host semantics.
- Owner accepted provenance-aware three-way imported-Skill updates. Automatic checking is source-metadata-only; reconciliation produces an inert candidate, and automatic adoption requires a distinct exact Artifact Update Rule limited to conflict-free, validator-clean, semantically and operationally non-expansive changes from the selected trusted source. Local edits and historical artifact revisions survive every update, capability expansion always returns to review rather than borrowing old authority, and rollback never resurrects revoked grants, rules or enrollments.
- Owner accepted a staged admission/activation state model with a compact user path. Discovery, acquisition and validation execute no artifact code; lifecycle hooks, dependency scripts or native code force a `restricted` disposition until separate executable-admission and sandbox authority is satisfied. Install and scoped enablement may otherwise be one visible action for a compatible artifact, but neither is Run authority; AI7 still governs exact unpublished-text, Provider, credential, network and Effect access, Background Analysis Enrollment remains separate, and every formal agent-originated Manuscript mutation uses one single-use, exact-pin/diff/target AI7 Apply command with drift reconciliation and explicit editor confirmation.
- No ADR or Policy has been rewritten, and no canonical change has been integrated. The two UI context/index worktree diffs are explicitly unintegrated interview working changes rather than target-qualified authority. ADR 0010 and 0042 require supersession; ADR 0041 and 0008 require at least partial supersession through an authorized normalization change.

## Unresolved matters or blockers

- GitHub Issue #86 and its full Change Brief now own the bounded documentation normalization; no pull request exists, no canonical change is integrated, and the branch remains accepted-but-unintegrated candidate state.
- Native-artifact plus sidecar ownership, the high-level DSH UI-plugin/marketplace lifecycle, imported Claude/Codex-style Skill representation, provenance-aware update reconciliation and staged admission/activation boundaries are decided. Exact catalog adapter schemas, trust tiers, install sandboxing, compatibility tests, state serialization and compact UI remain unresolved implementation-facing design details.
- Because official DSH has a native Profile/Bundle/plugin management contract but no single canonical marketplace authority, the exact supported catalog-source set and registry adapter contract remain unresolved; marketplace UI must not silently make one community directory authoritative.
- Lexical/vector/hybrid selection, retrieval-chunk boundaries, index generation/storage schema, context-budget policy, exact exhaustive-analysis schemas/storage, local embedding/provider choice, and the implementation of ordinary manuscript Provider Processing remain undecided and unimplemented. The exhaustive Analysis Result Set/Manifest/unit/reducer lifecycle itself is decided.
- The exact production Provider Processing policy serialization/rules and trusted build/launch selection mechanism remain unresolved; its separation from development/CI and fixture-recording selections is decided, and additional Book-, first-use-, per-call- and per-chunk consent has been rejected.
- The detailed long-term rule fields, budget defaults and treatment of non-manuscript external Effects remain to be specified; its Book/Series/all-Books applicability, user-initiated non-mutating scope, revocability and exclusion of formal manuscript mutation are decided.
- The dual targeted/whole-revision contract, structure-aware complete unit decomposition, typed hierarchical reduction, mandatory persistence/disclosure of every gap and four-axis closure model are decided; exact Coverage Manifest/result schemas, enum names, acceptance evidence and UI remain unresolved.
- The stable `Manuscript Analysis Result Set` identity, immutable revision lifecycle, Task Outcome link, optional Editorial Artifact promotion, incremental/local/global update meanings and reuse lineage are decided. Exact schemas, compatibility-key fields, retention/storage lifecycle and user-facing browsing remain unresolved.
- Cross-Run reuse cannot live in DSH Run scratch/cache because accepted isolation prohibits sharing that state across Runs and Books. The accepted owner is the AI7 Book-bound, durable, non-authoritative `Manuscript Analysis Result Set`; its concrete storage schema remains unresolved.
- Analysis-update trigger policy is decided. Exact rule fields, checkpoint coalescing/debounce behavior, scheduling, notification and Provider-failure recovery remain implementation/design details.
- The multi-axis status model is decided; exact enum/schema names, progress aggregation and user presentation remain unresolved.
- Automatic Baseline Book Analysis scope is decided, as is optional availability of all eight built-in Editorial Dimensions, the separate Background Analysis Enrollment requirement and non-enrollment by plugin installation alone. Exact DSH-native composition/version selectors, per-dimension contract boundaries, plugin/user analysis registration and enrollment serialization remain unresolved.
- The current user-initiated-only Default Execution Rule definition must be superseded only to acknowledge that background work has a separate owner; the accepted replacement authority is an active, disclosed Background Analysis Enrollment, not an expansion of Default Execution Rule.
- Background Analysis Enrollment resolves the owner, default-selection and high-level authority shape. Exact onboarding/provider-setup presentation, creation/activation UX, pause/disable mechanics, budget/provider binding, restart continuation, failure recovery, per-Run serialization and storage schema remain unresolved.
- Exact canonical names/schemas remain unresolved for the AI7 artifact sidecar, conversion report, update-reconciliation record, catalog-source record, scoped enablement, per-Run activation and Analysis Quality Metric Snapshot. Exact DSH carrier/pin mapping also remains open for Workflow definitions and any retained AI7-facing `Workflow Profile` projection label.
- Analysis feedback lifecycle, successor behavior, metric ownership, Learning Eligibility and factual boundary are decided. Exact signal/metric schemas, retention, aggregation windows, minimum sample disclosure and UI remain unresolved.
- The production editor must replace the tracer's first-window-only behavior and fixed OFFSET paging with a calibrated, continuous bounded-window experience; the exact display budget and paging/index strategy remain implementation decisions and must not leak into retrieval or exhaustive-analysis semantics.
- The current tracer's `r1`-only schema, bounded import limits, OFFSET windowing, whole-working-digest recomputation, missing checkpoint lifecycle, and synchronous service work must be deepened before it can meet the accepted 500K/1M/10M long-manuscript behavior.
- The audit also exposed a pre-existing Policy activation conflict: ADR 0018 says Policy revisions always require developer review, while ADR 0004 and the Learning Eligibility policy allow bounded non-expansive activation. Until explicit supersession resolves it, the stricter developer-review rule governs; no DSH/plugin admission, Artifact Update Rule, Default Execution Rule or Background Analysis Enrollment may activate a Policy revision.

## Resume Prompt

Continue Issue #86 on `codex/docs/86-design-handoff` using root `HANDOFF.md` and its exact Resume Prompt; first recheck branch HEAD, current `origin/dev` and target-qualified authority, then normalize documentation only within the Change Brief without entering `docs/archive/` or starting product implementation/dependency work.
