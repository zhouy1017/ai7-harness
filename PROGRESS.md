# Current checkpoint

## What's done

- The first model-dependent product path exists on `dev` (#92 / PR #266, `dev@8a20516397a5aa5fb6f99822949dc9877f786de1`): from the exact `sample1` Book, an editor starts one baseline manuscript analysis Task, the Run executes through the real path — Task Intent, `Task Input / 任务输入` checkpoint and pins, Provider Preflight, Plan Envelope, standard-direct Run Authorization, Run Record, scheduler admission, one execution attempt with its immutable Execution Binding, the `PrimaryAgentHarness` composition over the pinned DSH loop, the final Provider Payload/Egress Gate before every model call, and the bound adapter — and one immutable Manuscript Analysis Result Set Revision with four independent axes and visible gaps/conflicts is linked from the Task Outcome. The task ledger moved additively to schema 15; the service protocol to 17.
- The provider-free parts of the real Provider path landed with it: the AI7-owned DeepSeek OpenAI-compatible adapter revision 1 (deterministic request assembly, prompt-contract digest, transmit only against a `transmit-remote` gate ticket), the Credential Broker with a read-only keyring resolver (value released only against that ticket, never under v1), and the Egress Gate that refuses every remote route under `development-ci` / Provider Processing v1. Five hand-written synthetic fixtures under `tests/fixtures/model/` cover the happy `sample1` run, one failed unit, quota-exceeded, usage-ceiling, and interrupted shapes; the J-04 journey binds the `ai7-local-deterministic` route only through the J-04-only `--j04-model-adapter` control.
- J-04 is the seventh executable admitted Journey: its admission was recorded under the Owner's 2026-09-05 routing decision (#264 / PR #265, no ADR needed because ADR 0055 and ADR 0056 removed every hard-coded count) and cut over atomically with the runner (`e2e/run-j04.mjs`, `ADMITTED_JOURNEYS`, `JOURNEY_MODULES`, `JOURNEY_LOCATIONS`, `e2e:all`, the workflow job). The order is J-01 → J-02 → J-08 → J-12 → J-15 → J-03 → J-04.
- The J-03 foreground execution boundary is integrated (#91 / PR #193, `dev@7fc6022b`): the exact recorded-only Run answers `前台执行已拒绝 · 未启动` after revalidation, restart-safe, with a J-03-only interruption variation proving the fail-closed renderer.
- The Local Verification Ladder is complete and every layer is available (`doctor`, `bootstrap`, `check`, `test`, `test:service`, `build`, `e2e` / `e2e:all`, `e2e:debug`, `e2e:repeat`); the 2026-09-04 test-integration wave and its follow-ups are integrated.

## What's next

- The next step on the model-dependent path is the human-attended exact-`sample1` recording under ADR 0044 and Provider Processing v2, which is not repository-development work: it requires a separately authorized recording Issue/Change Brief (the `fixture-recording` trusted-launch form, `maxCalls: 1`, a non-`unset` Run Budget Ceiling, the human-confirmed public recording instruction, and the recording-only raw capture into protected local staging), the Owner's confirmation of every deferred binding, and the Owner's presence for the single call. The Commander's recording instruction is recorded on Issue #92; no recording Issue exists and none is authorized by this router.
- Provider-free product work that remains dispatchable after Owner sequencing: S37 (#93, Result Set update modes), S16 (#51, budget ceiling / account limit / ambiguous outcomes as product behavior), S13 (#48, Plan Adaptation and Clarification), S15 (#50), and the remaining S-series Issues; each needs its Change Brief refreshed to the current `dev` head before `ready-for-agent`.
- Follow-up candidates without an Issue: the sibling J-03 actions (`prepare`, `authorize-no-dispatch`, `cancel`) re-enable in `catch` without the `authorityInterrupted` guard the foreground-boundary action has; two J-03 stages share the closed location `foreground-boundary-check`; `serviceEnvironment` selects `AI7_E2E_JOURNEY` by sequential `if`; the unavailable-analysis note appears on every populated non-`sample1` Book; a service-layer regression case for the foreground-boundary inspection; `awaitFixedOperation` deduplication; the controller's debug-capture child session bounded by `withTimeout` only; the active-set-v3 ordinary-production pin mismatch; the `docs/agents/incremental-development.md` sentence that still calls the type-check, unit, service-integration, debug, and repeat ladder layers staged; the `docs/architecture-v2/HARNESS-INTEGRATION.md` non-decisions paragraph that still describes the code as having no executable Agent/Session/Provider path (an Owner-accepted design record, so its factual refresh is the Owner's call); the baseline-analysis executor owning the single-slot scheduler; the carrier and sidecar digest literals duplicated in three places; the harness submitting every unit into one technical session (a design decision before any recording); the broker's readiness check reading the keyring value inside the broker.
- Product integration remains serial.

## Key decisions

- Model-dependent testing uses hand-written synthetic deterministic fixtures by default; the ADR 0044 recorded fixture, if ever admitted, replays only through the same local deterministic adapter inside J-04 with a prompt-contract digest assertion. Recording authority is unchanged: no Provider call, credential value, recording, or fixture admission has occurred.
- The `ai7-local-deterministic` route is bound only through the J-04-only `--j04-model-adapter <fixture identity>` control, admitted with `AI7_E2E_JOURNEY=J-04` and mutually exclusive with the J-01/J-03/J-08 controls; without it an authorized analysis Run settles `blocked-before-dispatch`. No production-selectable surface can choose it.
- The Egress Gate returns exactly one of `transmit-local`, `transmit-remote`, or `refuse`; under `development-ci` / v1 it never returns `transmit-remote`, and the Credential Broker releases a value only against such a ticket for the same binding.
- A further Journey admission needs an explicit Owner routing decision plus a separate CI-governance record in `docs/agents/ci-test-boundaries.md`; no ADR is amended because none hard-codes a Journey count.
- Dispatch runs on either harness in any applicable launch mode; when a Claude Code Commander session lacks `SendMessage`, every attempt uses the `cli-session` mode. Bindings per harness are unchanged (Claude Code T1 `claude-sonnet-5 @ medium`, T2 `claude-opus-5 @ high`, T3 `claude-fable-5-1 @ xhigh`; Codex T1 `gpt-5.6-luna @ medium`, T2 `gpt-5.6-terra @ high`, T3 `gpt-5.6-sol @ xhigh`).
- A squash commit on `dev` carries only the stable product-level trailer of the attempt's harness; a pre-receipt trailer that names a model stays in branch history only.
- Product Provider, Model Role, DSH, Provider Processing/fallback, credential, Effect, export, publication, distribution, release, and `main` authority are unchanged.

## Unresolved matters or blockers

- The active-set-v3 ordinary-production pin does not match the current v3 policy bytes and needs a separate successor/correction before any positive production-eligibility claim.
- Whether the `.agents/skills` vendoring on `temp/freeze-20260904-112022` enters `dev` remains a separate Owner decision; the self-hosted Gate question is shelved by the Owner on 2026-09-05.
- One local `LOCAL_COMPLETION/J-01/fail` of unknown cause recorded on #249 did not reproduce in six subsequent runs at the same head.

## Safe Resume Prompt

```text
Commander: identify your harness (Codex or Claude Code), then query the Dispatch Register and current Issue receipts with your own session, agent, and background-process tools. Treat #91, #264, and #92 as integrated: J-04 runs end to end on synthetic deterministic fixtures through the real execution path with the Egress Gate refusing every remote route under development-ci / Provider Processing v1. The next step on the model-dependent path is the human-attended sample1 recording under ADR 0044, which needs a separately authorized recording Issue and the Owner's presence; do not open it, freeze a Provider Binding, touch credentials, or make or simulate a call without that authorization. Dispatch only provider-free product Issues after refreshing each Change Brief to the current dev head, one fresh Task Session per attempt in its own worktree, with schema-v4 receipts; keep product integration serial and require the complete Local Verification Ladder before Ready. Under ADR 0058 do not query, estimate, report, or consider Actions usage. Do not touch fixture admission, product Effect, export, publication, release, distribution, or main.
```
