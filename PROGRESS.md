# Current checkpoint

## What's done

- Issue #42 remains isolated on `feat/42-book-workbench-routes` from exact `dev@9b77b26c32da9a06018d943d47abaa8ccfc96076`; no commit, push, PR/Issue mutation, Actions operation, or other external action has occurred.
- Existing protocol/service/store owners resolve exact Book/Revision Workbench routes. A historical request supplies only `revisionId`; the service derives Book, Manuscript, Branch, source-version, digest, and Revision identity, validates the complete Book/source/Manuscript/Branch/parent/block ownership chain with canonical joins, and reconstructs at most 32 blocks. Historical text and structure come only from immutable `manuscript_block_versions`; `working_blocks` and editor state are excluded.
- Electron main owns one private exact-Book window registry plus one application-level arrival-order queue for route transitions and effectful/reconciling operations. Per-window route requests reserve a sequence immediately so newest-route-wins and stale reads fail before presentation, while route generation changes only when a queued route actually applies. Bounded global same-owner/unowned claims prevent another renderer from reacquiring staged drafts, recovery attentions, or import completions; exact per-window Manuscript/Branch/job/search/preview capabilities guard all editor operations. At most one Workbench exists per exact Book, distinct Books coexist, and only explicit editor actions create/reveal/focus.
- Existing-Book first-manuscript, source-import, reimport, and recovery restoration reserve/focus the exact Book before durable mutation and report consequence-first typed rejection when another Workbench owns it. Committed/recovered overviews bind silently before renderer presentation; noncommitted startup/abandonment/defer states release prior routes. Recovered lower-priority drafts are captured for their exact sender before continuation.
- The renderer provides exact Book/Revision startup/routing, a bounded immutable historical surface with no editor/mutation commands, explicit return to current editable state, exact open-another-Book behavior, and `设置 > 数据与存储` actual platform/source-checkout/root/bounded-128-entry-footprint/secrets-separation/reveal projection.
- `e2e/run.mjs` dispatches real `e2e/run-j12.mjs`. Strengthened J-12 proves deterministic typed stale editable-read rejection when an exact historical Revision wins; foreign `getImportStartup` and direct draft reacquisition rejection; effect-before-route arrival ordering with a classified durable journal success, final exact Revision route, and current-state persistence; Book-A rejection of Book-B Manuscript/Branch/job/search/preview capabilities; immutable/current Revision separation; duplicate source-import preflight; offline sentinel; restart persistence; Data & Storage/reveal; and applicable J-14 obligations.
- The earlier J-08 async assertion false-positive was diagnosed at the real seam and fixed without weakening its `restored-r2` assertion; J-01's analogous network-denial assertion is also Promise-aware. J-08's three direct editor calls establish an exact current Book route first. The first Commander sequence then exposed a focus-order race: recovery DOM readiness could precede the renderer's scheduled heading focus, letting a controller `Tab` occur too early. J-08 now waits for the recovery `h2` to become `document.activeElement` before pressing `Tab`, while preserving the existing first-radio plus `:focus-visible` assertion unchanged.
- ADR 0052 owns the selected macOS V1 profile: macOS 15+ arm64, bundle `io.github.zhouy1017.ai7`, direct-download Developer ID-signed/hardened/notarized DMG, manual app replacement preserving product data, and OS-resolved per-user Application Support Agent Data Root with no silent fallback. Current ADR/context/architecture/UI/development projections reflect it without package/release or credential work; ADR 0023 remains the Windows owner and points to ADR 0052 for macOS.
- Repository-declared Node 24.18.1 and pnpm 11.24.0 are selected locally. Final-source TypeScript, build, and `git diff --check` pass. After the focus synchronization, `node --check e2e/run-j08.mjs`, scoped `git diff --check`, and targeted J-08 pass (~5m48s). The Commander's uninterrupted Windows sequence `doctor -> bootstrap -> build -> J-01 -> J-02 -> J-08 -> J-12` is green on the repo-pinned runtime. The existing build owner then canonicalized, deleted, and regenerated only this worktree's exact `dist`; the clean `build -> J-01 -> J-02 -> J-08 -> J-12` rerun is also green. A temporary payload-free J-12 diagnostic proved the stale-read root cause and was removed.
- The first Commander J-01 attempt exposed only restart replay of an already acknowledged manuscript-reimport commit: the restarted landing renderer legitimately had no pending draft capability, so main rejected before the service's idempotent replay path. A private, read-only exact-proof service preflight now admits only the same acknowledged commit tuple, reserves its exact Book and bounded claims, never primes the full-result replay cache, and rolls fresh authority back on deterministic failure while retaining it across ambiguous outcomes. J-01 permanently proves a wrong tuple is rejected without route/cache acquisition and the following valid replay remains queued. The temporary callsite-only diagnostic was removed; the diagnostic rerun passed, and final-source TypeScript plus `git diff --check` pass.
- Final pre-commit authority resolution found exact local `HEAD` and remote `dev` both at `9b77b26c32da9a06018d943d47abaa8ccfc96076`; Issue #42 remains open with `ready-for-agent`; all and only the exact 34 authorized paths are changed. Workflow `342459594` remains `disabled_manually` with zero queued and zero in-progress runs. PR #110 is historical merged state (`MERGED`, not Draft) and is not an active dependency.
- No schema/migration, package/lock/dependency, workflow, URL/file association, custom/network root, credential/Provider/Keychain operation, Detached Manuscript Window/Active Binding, dual editor, unbounded inventory, signing, notarization, upload, release, or `main` surface changed.

## What's next

- Commander integration remains: complete the final scoped status/document sweep, commit the exact Issue #42 unit, obtain the mandatory two-axis fresh-context read-only advisory review, address any real finding, then create and integrate its Draft PR only while the target and disabled/no-run waiver conditions remain true.

## Key decisions

- Existing deep owners remain authoritative: service-derived identities and canonical immutable ownership/content tables for historical reads; Electron main for window lifecycle/routing/native selection/reveal and sender capabilities; renderer for projections; authenticated cursors and the standing 32-block limit for bounded content.
- Book Workbench Window and Detached Manuscript Window remain distinct. Issue #42 implements only full exact-Book Workbench routing; it does not implement manuscript-surface transfer or Active Manuscript Surface Binding.
- Product Data Location is a truthful projection of one main-owned canonical Agent Data Root shared by the application/service/windows. Protected Secret Store is separate; bounded footprint observation creates no inventory. Source checkout reports its actual runtime form and never claims packaged/notarized DMG status.
- The sender-owned native picker participates in the same application arrival-order queue. Its modal selection/staging action therefore completes or cancels before a later route/effect is applied; this is acceptable for Issue #42's explicit native-selection ownership and does not let background state create, reveal, or focus a window.
- Import commit claims are reserved before direct/source/reimport service mutation. A newly acquired claim is released on a classified deterministic service rejection and retained only across timeout/stop/write/protocol/response ambiguity for idempotent recovery; the staged-draft global capacity is checked before the native picker/service staging call.

## Unresolved matters or blockers

- No current implementation or local-validation blocker. Both prescribed Windows evidence sequences are green. Final target/workflow authority checks, commit/review, and Draft-PR integration remain.
- No real macOS 15 arm64 host is available in this Windows task. No macOS build, journey, packaging, signing, or notarization evidence will be claimed.
- Workflow `342459594` remains outside Worker operation and must remain `disabled_manually`; hosted paired-platform evidence remains temporarily waived under ADR 0050 and is not claimed.

## Safe Resume Prompt

```text
Commander: continue only in the exact Issue #42 worktree/branch. Complete the scoped status/document sweep, re-resolve exact dev and workflow 342459594 as disabled_manually with no queued or active run, commit the exact unit, run the mandatory fresh-context Standards and Spec reviews, fix only real in-budget findings, then create and integrate a Draft PR under ADR 0050 without enabling, dispatching, or running Actions.
```
