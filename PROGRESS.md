# Current checkpoint

## What's done

- Completed the cohesive provider-free Issue #45 S10/J-08 Manuscript Recovery unit and the Commander-accepted repair sub-task in the sole writable isolated tree `C:\Users\Chooo\Playground\ai7-harness-worktrees\issue-45-recovery-workspace` on `feat/45-recovery-workspace`. The current committed branch `HEAD` is cross-platform repair commit `9489679a04fc5716ee8336db4cf6cdf25e8279c1` on exact `origin/dev@3f73bab57c53eb469d704cf65ff208f1e752ab50`; the intended target is `dev`, never `main`.
- Kept the exact v6 contract frozen and the direct v5→v6→v7 migration. The additive v7 authority in `src/service/bounded-manuscript.ts` remains the same SQLite database and now includes hidden persistent recovery-restore staging, exact lifecycle/high-water facts, Recovery Attention/decisions/restoration lineage, history frontier, snapshot metadata, and recovered-state review status. An existing v6 database still baselines without invented prior-lifetime evidence.
- Removed active whole-manuscript TEMP recovery materialization. Checkpoint→journal proof now pages retained journal/command evidence and reconstructs one bounded checkpoint block at a time; checkpoint restoration uses transactional `INSERT … SELECT`; snapshot restoration streams verified batches into same-authority hidden stage rows and exposes them only inside the final revalidating all-or-none descendant transaction. No second database, store, ledger, process, or filesystem authority was added.
- Changed new v7 edit/replacement/undo/redo working-digest links to derive from retained immutable command-group/edit evidence, independent of prunable replacement previews. Recovery actually recomputes post-lifetime-boundary links and block results; exact v6 migration/history remains preserved.
- Hardened `src/service/recovery-objects.ts`: line decoding is byte-bounded before accumulation while accepting worst-case valid JSON escaping; object-byte and manifest digests remain separate; only a final newly promoted by the current call is removed after post-promotion failure; EEXIST objects are not removed by that call; and startup orphan checks query SQLite one streamed directory entry at a time instead of materializing every key.
- Snapshot comparison in `src/service/store.ts` now verifies applicable records newest-first in bounded indexed order, so a corrupt/missing newest object cannot hide an older verified snapshot. View performs one complete verified scan while retaining only the bounded window; restore retains the exact selected snapshot identity through hidden staging and lineage. Whole-object startup/comparison/view/restore calls use the existing long service timeout in `src/main/service-client.ts`.
- Repaired `src/main/application.ts` restoration binding so the exact same selection fingerprint keeps a stable restoration ID, while a failed selection-specific attempt does not block a different candidate in the same renderer session.
- Repaired `src/renderer/index.ts` and `src/renderer/styles.css`: each eligible card visibly exposes exact revision ID/digest and snapshot ID when applicable; snapshot none/unavailable is a separate disclosure with exactly two radios, not a fake third candidate; selection visibly states the new-descendant/preserved-history/no-auto-Milestone consequence before restore enables; and typed Recovery Return Context survives defer through import recovery, target/title, review, commit, completion, and the imported/unrelated editor while the affected branch remains service-readonly.
- Completed `e2e/run-j08.mjs` inside the existing `e2e/run.mjs`/single Windows+macOS E2E Functional Gate while retaining J-01/J-02. Its runtime-generated public synthetic cases now encode clean reopen, acknowledged interruption and recovery-first priority, exact candidate identities/unselected/readonly behavior, typed defer/import return routing, guarded affected branch plus editable unrelated work, newest-corrupt/newest-missing fallback, mismatch/incomplete/missing/none and invisible orphan behavior, exactly two choices without a verified snapshot, selection-specific failed restore followed by another choice, descendant/history floor, persistent marker/later Milestone clearing, direct J-14, and a CDP-correlated product-cancellation signature for the exact fixed probe request. All J-08 failure tokens are neutral and never interpolate a synthetic title or manuscript text.
- Updated current truth only in `README.md`, `docs/development/source-checkout.md`, `HANDOFF.md`, and this checkpoint. The broader already-complete unit still extends only the existing shared protocol/private IPC, service/store, main/preload/client bridge, renderer startup/editor/presentation surfaces, current commands/workflow, and one logical gate.
- On 2026-08-28 the Commander completed and accepted the bounded static integration inspection, reverified that the full Issue #45 Change Brief is unchanged, and confirmed exact `HEAD`/`origin/dev` base `3f73bab57c53eb469d704cf65ff208f1e752ab50`, the intended 18-path implementation scope, clean diff/parser diagnostics, and no structural or authority drift.
- Hosted PR #105 first reached the repository-pinned Node 24.18.1 / TypeScript 6.0.3 build on both supported hosts and failed before E2E. The suppressed build diagnostics were isolated read-only by the Commander to one new-source union-narrowing error in `src/service/store.ts`; first-repair commit `3c16660` now assigns only when `verified.state === 'unavailable'`, preserving the eligible early return and defensive `none` continuation.
- Hosted rerun `33114944932` then built successfully on both hosts and exposed two exact Issue #45-caused gate defects. macOS job `98667085994` timed out at `J-08/renderer-cdp-event-timeout` because CSP `connect-src 'none'` rejected the probe before the existing product-session request interceptor. The first probe repair bypassed CSP only for that request; run `33116057742` proved exact `errorText` was not portable, and commit `9489679` correlated the URL/request ID but required `canceled === true`. Hosted run `33117802836` proves macOS build/J-01/J-02 pass yet its J-08 still times out because that boolean is also not portable. J-08 now waits for any `Network.loadingFailed` correlated to the fixed URL/request ID, then accepts only an explicit product-cancellation signature: `canceled === true`, `net::ERR_BLOCKED_BY_CLIENT`, or macOS Electron `net::ERR_FAILED`. It explicitly rejects `net::ERR_NAME_NOT_RESOLVED` and all other errors, so reserved-TLD DNS or ordinary connection failure cannot pass; CSP enforcement is restored immediately afterward.
- Windows job `98667085968` first timed out at retained `J-02/milestone-r2`; Issue #45 made that 50,000-block Milestone form and independently verify a recovery object, but formation awaited one filesystem write per block while J-02 retained its former 180-second bound. Commit `836905f` changed that to `FileHandle.writev` per fixed 256-block batch and aligned J-02 with the existing ten-minute Milestone/recovery-snapshot service bound. Hosted Windows job `98670916537` still failed, so commit `9489679` changed formation to one contiguous bounded Buffer/write per batch plus proven-equivalent code-unit ordering. Hosted run `33117802836` Windows J-02 then ran from 21:23:43Z to 21:36:27Z and again failed at the same token, disproving those write/canonical changes as sufficient. Production snapshot semantics remain untouched in this sub-task. On the existing timeout only, J-02 now streams entry names/types from the exact disposable `recovery-objects/v1` directory without reading bytes or the database and emits one fixed payload-free category: partial object present, promoted snapshot present, or neither/directory absent. Existing cleanup and the ten-minute bound are unchanged.
- Materially applied the two earlier strictly read-only T3 diagnostics: lifecycle/readonly/startup/history-marker/J-14 guidance and v6→v7/high-water/object/lineage/schema guidance. Also applied the Commander-accepted findings from three fresh independent non-author read-only T3 reviews. Those reviews were requested as Claude Code `claude-opus-5`/high; Claude execution was unavailable; actual review execution was OpenAI Codex `gpt-5.6-sol`/xhigh as a same-provider fallback. They remain advisory candidate-rulebook inputs only, not accepted authority or a re-review gate, and performed no edits, spawn/delegation, external action, or gate execution.
- Actual bounded checks before PR submission: exact HEAD/branch/origin-dev assertions passed; `git diff --check` passed for tracked changes and `git diff --no-index --check` passed for both then-untracked sources; stale TEMP recovery/readline/whole snapshot-reference-set/old staging APIs were absent; recovery schema/FK/index/protocol/timeout/runner/workflow references were cross-checked; no Task/Run/Proposal schema or recovery path/byte IPC was introduced. Diagnostic-only bundled Node `C:\Users\Chooo\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe` v24.19.0 accepted all changed TypeScript and `.mjs` syntax. Commit `9489679` received hosted green macOS build/J-01/J-02 evidence but Windows disproved its performance changes as sufficient. For the current product-cancellation-signature and failure-localization repair, `git diff --check` passes and the parser accepts `e2e/run-j01.mjs`, `e2e/run-j02.mjs`, `e2e/run-j08.mjs`, and `e2e/run.mjs`; authoritative hosted J-02 localization and J-08 remain pending.
- Worker binding: role T3 Worker; requested Claude Code claude-opus-5/high; observed unavailable Claude execution surface on 2026-08-28 Asia/Hong_Kong; actual OpenAI Codex gpt-5.6-sol/xhigh; same-class fallback; exact reason no Claude execution surface is available in this Commander environment.

## What's next

- Commander reviews and commits this five-path product-cancellation-signature plus J-02 localization repair, pushes it to existing PR #105 against `dev`, and reruns the existing E2E Functional Gate on Windows and macOS. The exact pinned build and retained J-01 must remain green; Windows J-02 must either pass or emit the fixed recovery-object stage category; J-08 must complete on both hosts.

## Key decisions

- SQLite remains the sole authority. Hidden restore stage rows are persistent only to bound process memory and are cascade-cleaned/invisible; snapshot bytes remain digest-bound subordinate evidence under the existing Agent Data Root.
- Recovery Attention requires explicit unclean lifetime facts plus exact same-transaction acknowledged journal high-water evidence newer than the checkpoint. WAL state, timestamps, working-block drift, generic close/finally, and migrated-v6 absence never imply attention.
- Every applicable snapshot is checked in deterministic newest-first order; a verified older candidate is exposed when newer evidence is unavailable. The exact exposed/selected snapshot ID is preserved through view and restoration lineage.
- The mutable editor is never mounted for `仅查看`; deferred/unresolved affected-branch mutations remain service-blocked. Restore preserves all history and pins, creates only a new descendant, establishes a history floor, creates no automatic Milestone, and clears the service-owned recovered marker only in a later successful explicit Milestone.
- No unacknowledged renderer-buffer recovery, broad Book Workspace, salvage export/copy Effect, Provider/Harness work, dependency/plugin change, protected material, second gate, or fake Task/Run/Proposal/Pin proof was introduced.

## Unresolved matters or blockers

- At `9489679`, macOS build/J-01/J-02 are green but J-08 proves correlated `canceled === true` alone is not portable; Windows J-02 proves contiguous batch writes and code-unit key ordering are still insufficient. The current explicit product-cancellation-signature repair and exact J-02 object-stage diagnostic have not yet received hosted evidence. Local product E2E remains unavailable because this tree has no `node_modules`; ambient `node` is unavailable and ambient pnpm is 11.19.0, while bundled Node v24.19.0 diagnostics are not the required pinned Node 24.18.1 gate evidence. No Change Brief expansion or unrelated/pre-existing blocker was found.

## Safe Resume Prompt

```text
Commander: review the uncommitted five-path product-cancellation-signature plus J-02 localization repair in C:\Users\Chooo\Playground\ai7-harness-worktrees\issue-45-recovery-workspace on feat/45-recovery-workspace at `9489679a04fc5716ee8336db4cf6cdf25e8279c1`, commit and push it to existing PR #105 against dev, then rerun the hosted existing E2E Functional Gate. Require the pinned build and J-01 to remain green; if Windows J-02 still fails, use only its fixed partial/promoted/absent recovery-object category to authorize the next bounded diagnosis; require J-08 to pass on Windows and macOS. Never target main or broaden the unchanged Change Brief.
```
