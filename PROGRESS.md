# Current checkpoint

**Scoped review repair (2026-09-27, #574 / Issue #61, S26a).** Decision-reason reads stream every immutable feedback entry while retaining only the predecessor, latest revised reason, dismissal flag and count. Recording reuses that bounded standing instead of reading lifetime history twice. Tests retain stale-count refusal, first-reason/dismissal precedence and restart behavior through sixty-six feedback entries, and reject corruption of superseded reasons. No protocol, schema or user-flow changes. This branch remains Draft on its parent pending scoped checks, parent integration/restack and exact final-head full guest/hosted validation.

This is the single Commander-owned status and routing file under [ADR 0064](docs/adr/0064-reweight-repository-development-toward-value-first-delivery.md). The delivery order lives in the [development plan](docs/development/development-plan.md); what each Journey proves lives in [E2E journeys](docs/development/e2e-journeys.md); what every editor-facing surface must become lives in the [editor-facing surface specification](docs/ui-ux-v2/editor-surfaces.md) ([ADR 0077](docs/adr/0077-adopt-the-editor-facing-surface-specification-as-the-execution-standard.md)). The Commander updates this file inside the integrating pull request or through its own documentation pull request. This version was written on `dev@28cc1dd800cf55b17947cceb64a3bce4d81ccfef` (PR #404) by the Owner's UI design session under the Owner's instruction of 2026-09-10 ([ADR 0078](docs/adr/0078-align-the-remaining-references-the-plan-and-the-tracker-to-the-editor-facing-specification.md)) and updated in the pull request that accepted ADR 0074 (PR #391), then refreshed at `dev@d3c04b3` (PR #472) by the agent-readiness review of 2026-09-17 at `dev@50e7a6b` (PR #480) by the completion run of 2026-09-19 and 2026-09-20, at `dev@f169695` (PR #488) by the landing of 2026-09-22, at `dev@80554ad` by its continuation on 2026-09-23, at `dev@10af99e` and `dev@09749ff` on 2026-09-24, at `dev@da94971` by the landing session of 2026-09-24, at `dev@34fa3d3` by the landing session's continuation on 2026-09-24, and at `dev@6a043cb` by the landing session of 2026-09-25; the previous version, with the wave-by-wave record of 2026-09-07 to 2026-09-09, is archived at [docs/archive/editor-surfaces-standard-2026-09-10](docs/archive/editor-surfaces-standard-2026-09-10/INDEX.md).

## What exists

- Twelve executable Journeys on `dev` — J-01 import, J-02 long-manuscript editing, J-08 recovery, J-12 workbench and Model Service setup, J-15 native artifact lifecycle, J-03 provider-denied Task authorization, J-04 covered baseline analysis, J-05 marks on the manuscript, J-06 稿件冲突, J-07 delivery and 发稿, J-09 待我处理, J-10 controls on a Run under way (admitted by S76a, PR #515) — on the real execution path (Task Intent, `Task Input / 任务输入` checkpoint, Provider Preflight, Plan Envelope, Run Authorization, single-slot scheduler, immutable Execution Binding, `PrimaryAgentHarness`, Egress Gate, bound adapter). The Egress Gate refuses every remote route under `development-ci`; the only live calls in this product's history are the nine test items under `developer-live` (`S40/first-baseline/1` to `/8` of 2026-09-07, `S40/reanalyze-range/1` of 2026-09-08), all cached and never to be re-transmitted (ADR 0067).
- The Local Verification Ladder is complete (`doctor`, `bootstrap`, `check`, `test`, `test:service`, `build`, `e2e` / `e2e:all` / `e2e:gate`, `e2e:debug`, `e2e:repeat`); the hosted Gate is a three-minute Windows pull-request lane over `GATE_JOURNEYS` plus a nightly Windows and macOS full set (ADR 0075, PR #401). **The nightly was red from 2026-09-12 to 2026-09-19 for two defects PR #470 introduced, and both are fixed (PR #479, #474 and #475).** First, `navigate()` held its re-entrancy guard across the entry-position write, so a paging command that arrived while the disk was busy was dropped — J-01's and J-02's paired paging commands on hosted Windows; slowing that write by 400 ms on a background test guest reproduces both failures at the stages the nightly named, and the fix passes under the same delay. Second, since the restart lands mid-manuscript, an action taken before the arrival's centring frame captured the pane un-centred, restoring it scrolled the pane back to its top, and that `scroll` event — arriving a frame after the action's guard lapsed — was read as the editor reaching the top edge: the surface paged to the previous window and paused editing, and J-02's IME step composed into a read-only editor. Frame against IPC timing decides that race, which is why hosted macOS lost it about three nights in four and Windows never did. It was found by dispatching the full Gate against temporary refs whose extra stage names said what state the editor was in (`moved-and-deferred`, three of three), and the fixed head passed hosted macOS four of four and hosted Windows twenty-nine of twenty-nine. J-02's last stage now reports each step and each part of the close by name, its import reports its three waits, and the IME guard names the precondition it lacked.
- **The Phase 1 analysis chain is complete service-side.** S13 plan revisions and `safe-retry` (PR #280); S40 the `developer-live` scope with Provider Processing v4, the trusted launch form, the enrollment helper, the Provider Test Ledger and Result Cache (PR #285); the unit contract surviving the bound model with `structuredOutput: json-object` on `opencode-go/deepseek-v4-flash` (PR #330); S41 fixture generation from the cache (PR #376); S42a the Baseline Cross-Unit Reduction Contract v1 (PR #393); S18a the `factual-review` kind with the Factual Review Contract v1, deterministic Reference Integrity and every finding at `未外部复核` (PR #395); S43 the Assurance Sampling Contract v1 (PR #397); S44a the durable Run Report inside every Task Outcome (PR #399). Schema revision is 39 (S67a, PR #531) and `SERVICE_PROTOCOL_VERSION` is 60 (PR #545). All three declared suboperations — the cross-unit reduction, assurance sampling and the Run Report reflection — are named `true` by Provider Processing v5 as of PR #440, so the `developer-live` policy gate no longer blocks them; the first v5 live Run is a separate named test item under ADR 0070.
- Model capability profiles keyed by route and model (PR #320), four request shapes (`openai-chat-completions`, `anthropic-messages`, `openai-responses`, `google-generate-content`) and the explicit provider support list of #321 declared as inert profiles with their evidence (PRs #340, #348, #359, #364, #366; [provider support](docs/development/provider-support.md)). Adding a provider endpoint still needs a credential slot: that is 1c.10 (S55a (#435)) under ADR 0073, accepted on 2026-09-10.
- Multi-format intake under ADR 0072: DOCX, `.doc`, `.txt` and `.md` identified by content and read through their own routes, PDF source-only, the original always the Source Version (PRs #357, #370, #374, #378, #382); every admitted Public SampleBook imports.
- The display rules of ADR 0071 (Decision Layer and Technical Identity Layer, the Run Liveness Signal) are applied across all forty-four surveyed surfaces (PRs #341, #347, #355, #380, #383, #386, #392); the Owner then judged the result still an operator's console, which is what the editor-facing specification answers.
- Test inputs come from the admitted SampleBooks wherever one can serve; under [ADR 0079](docs/adr/0079-record-the-owner-s-decisions-of-2026-09-10-on-storage-policies-export-egress-providers-and-samplebooks.md) §5 only exact `sample1` stays admitted to the repository, and S88 (#438, PR #446) removed the other five files from the tree: `.gitignore` refuses them and anything else under `SampleBooks/`, J-08 composes its three inputs from `sample1`, J-01's `.txt` intake excerpts `sample1`, the service builders read `sample1`, and J-01's `.doc` intake is local-only — it runs where a developer keeps the legacy document (`AI7_LOCAL_SAMPLEBOOKS` or the ignored `SampleBooks/`; each background test guest holds it under ADR 0084) and otherwise prints a disclosed skip that the pull-request lane and the nightly repeat beside the Journey's result (J-02 and J-12 stay synthetic by subject).
- The Policy Documents ADR 0079 §2 and §3 decide are integrated under one **active-policy-set v5** (PR #440): Provider Processing v5 for `developer-live` (the three suboperations allowed, a Run Budget Ceiling that defaults to 30,000 tokens per frozen Coverage Manifest unit, the house-people redaction rule, no web-search allowance), Provider Processing v6 as the `ordinary-production` successor of v3 over the unchanged v3 binding, and External Export Policy v2 (five eligible target kinds over the Editorial Deliverable Revision rather than the manuscript alone; 批注, 修改建议 and the retained DOCX content included by default and excludable item by item; 备注 off by default and included only when the editor says so; a 图书交付包 export covering the members the editor chose). The Owner reviewed each set's bytes and amended ADR 0079 §3 in that review; `development-ci` v1 and `fixture-recording` v2 are unchanged, and since S64 (#413, PR #501) every export is bound to External Export Policy v2, and is refused when this launch's check of that policy fails.
- **The manuscript is the entry (S57, #405, PR #470).** Opening a Book that has a primary Manuscript enters the manuscript where the editor left it, with one floating notice that goes by itself; 工作概览 is now a destination reached from the manuscript's 资料与记录 group and is the entry only for a Book with no Manuscript. The position is a durable record per Book and Manuscript Revision at schema revision 21 (S57-s, #467, PR #469), recorded on arriving at a window, on paging and on leaving — never per keystroke — and a position whose Revision has been superseded resolves to the nearest surviving block and says so. This is the first Phase 2 outcome an editor can see.
- **The analysis is its own destination (S71, #406, PR #480).** From the manuscript's `资料与记录` group an editor opens `分析` and reads the baseline as ②A: seven tabs (`梗概 · 人物与名称 · 事件 · 关系 · 设定 · 各章 · 历史与更新`), four sentences in editorial Chinese under `覆盖范围`, `全书综合`, `与当前稿件` and `可信程度`, events, relationships and settings listed with their provenance, `各章` naming what is still unread, each update mode saying what it keeps and what it reads again, and the Run Report of every Run opened from the Task Outcome and from each history entry (the history entry carries its Run's report; protocol 30). Units, digests, reducer stages, reuse counts and usage sit in `查看技术详情`; the contradictions and open questions are counted and pointed at 审阅 and stay exact in the closed technical layer until S69. `工作概览` names the analysis in one line with `打开分析`. The quick start on each update mode is shown disabled with its reason until S75 gives it a Default Execution Rule — the Owner's answer of 2026-09-20.
- **Marks on the manuscript (S58, #407, PR #482).** Select text, right-click, and mark it: 批注, 备注, 高亮 in three personal colours, and 修改建议 — the editor-facing Proposal Change Item, previewed in place while the manuscript stays the original. Marks follow their words through edits and turn `原文已变` when those words change. J-05 is admitted.
- **One-click 接受并应用 (S59, #408, PR #483).** A 修改建议 is written into the manuscript as one exact Apply Effect with its receipt, and reversed as a new Reverse Apply Effect; an applied deletion keeps an exact point where its words left, so reversing it writes them back there.
- **The right-edge column (S60, #409, PR #484).** The Whole-manuscript Position Rail with chapter ticks, mark lanes and unread ranges beside the text, and 导航 opened on demand. Tab into the text no longer pages the window back (#485, PR #486).
- **审阅 (S69, #417, PR #487).** A Review Run over any of the nine built-in categories, authorized once, turns its findings into marks on the manuscript, with the batch strip, 忽略并说明 and its Quality Signal, versioned reports and the coverage matrix.
- **发稿 (S65, #414, PR #488).** A milestone carries one of the four frozen purposes; 交付物 lists the milestones, and 设为发稿版本 designates the exact version for a scope with its basis, kept apart from its internal release permission. J-07 is admitted. S65 was taken ahead of S64 because S64 waited on S61's class table and S65 depends on nothing.
- **One Task Drawer for every Task (S72, #418, PR #499).** Any Task — the fixed task on 工作概览, a baseline analysis, a 审阅 run — opens the same drawer and reads the same plan, in 精简 or 完整 mode, with the technical layer folded away behind 查看技术详情. It shares the shell’s 导航 slot and adds no schema.
- **开始任务, in one click (S74a, #420, PR #503).** The drawer’s foot carries the authorization bar: the summary, the authorization statement, and a start that works for all three kinds — the fixed task records its run and says it will not be dispatched (ADR 0055), the analysis authorizes and dispatches in one step, a 审阅 run records the one approval and then runs category by category. After a start the bar becomes the Run’s state with a link to it; when the plan changed it offers 重新确认计划 and 查看计划修订 instead; when the plan’s route needs a model and the credential is absent it reads 模型未连接 and offers 去设置连接; and when the one slot is busy the start is refused before anything is recorded. The cards lost their authorization buttons and now offer 查看计划并开始. 联网后开始任务, the Reconnect Preflight and the Connectivity Wait are S74b (#502).
- **稿件冲突 (S22, #57, PR #491).** When a 修改建议's words changed under it, the conflict is decided by the words it replaces (ADR 0085): the editor sees the words it was written against, the current manuscript and the suggestion side by side, and resolves it into a new version of the suggestion, keeps the current manuscript (保留当前稿件), or defers it (暂不处理), each recorded. J-06 is admitted.
- **待我处理 (S78, #424, PR #513).** One read-only view across every Book in four fixed groups — 异常与结果待确认, 等待你的决定, 运行中与已暂停 and 最近完成 — reached from every window's header, which counts the first two; a 稿件冲突 opens where it can be resolved. J-09 is admitted.
- **The import review's ten classes (S61, #410, PR #492).** What a Word file holds besides its words is shown class by class under ADR 0086: inline styles, tables, images, sections with page setup, headers and footers are kept with the original file (`完整保留（随文件保留）`), and each manuscript block records the source paragraph it came from. Text boxes are kept (`保留为文本框`) or folded into the text (`并入正文`), so a file with a text box imports. Footnotes and fields are the only `降级导入`. A manuscript like sample1 imports without the degradation decision.
- **A Word file's comments and tracked changes arrive as marks (S62, #411, PR #498).** The manuscript is the text before the author's revisions. Each tracked change becomes a 修改建议 from the file's author, with the new change kind `insert` for an insertion. Each comment becomes a 批注, with its replies and done state. A revision the text cannot express (a whole paragraph or table row, a split, a join, a move) becomes a descriptive 批注. The landing review fixed a row inserted by one author and deleted by another, which had swallowed the text after it.
- **Export the manuscript to DOCX (S64, #413, PR #501).** 交付物's `导出…` writes the current revision or a milestone as a Word file rebuilt from the original: unedited paragraphs come back exactly, and marks travel as Word comments and tracked changes (备注 only when chosen, and never an AI7 mark's basis). The write is atomic and never replaces a file the editor did not choose: `create` takes the name through a hard link or refuses, and `replace` binds the file the Save dialog resolved by size and SHA-256. J-07 exports and reads the file back.
- **联网后开始任务 (S74b, #502, PR #512).** While the device has no network the plan's route needs, the drawer's bar reads `离线` and offers `联网后开始任务`. That records the Run Authorization now, and the Run waits in Connectivity Wait, sending nothing, until Reconnect Preflight admits it, or until the editor cancels it: it then reads `已取消 · 未发送任何内容`. The drawer's bar and 待我处理 say what the Run waits for (等待网络, 需要处理模型连接 or 等待运行名额), and ②A reads 等待网络 · 未启动. A 审阅 run does not wait: offline, its 开始任务 is unavailable. A Run whose plan moved while it waited is blocked before dispatch; #540 (below) gives it the 需要重新确认计划 state.
- **快速开始 under a 默认执行规则 (S75, #421, PR #514).** After reading an analysis update's plan once, the editor can make it the Book's quick-start default with `设为快速开始默认…`, which lists exactly what the rule binds. From then on `快速开始` starts that update in one click. It falls back to the plan, with the reason, whenever anything differs from what was approved. That includes text edited since the plan was prepared: a quick start never reads stale text. A 审阅 has no quick start.
- **A Run under way: 取消任务, 暂停 and 续行, 改计划重做 and 先问你 (S76a to S76d, #422, PRs #515, #516, #520 and #522).** While a baseline analysis runs, the drawer shows the activity card `运行动态` above the plan: the step, the reading range in flight, the time on it and since the start, and what is done. `取消任务` first shows the 取消影响摘要, then reads `正在取消` while the range in flight finishes and `已取消` once it has; the ranges read are kept in a partial revision that becomes the Book's latest analysis. `暂停` lets the range in flight finish and stops there, and `续行` goes on in the same Run without reading a kept range again; a Run AI7 closed under reads `任务已中断 · 可续行` at the next launch. `改计划重做` stops a Run and prepares a new Task that carries every finished range and whose plan can be edited before it starts. A safe retry moved into `先问你` makes the Run ask — `再试一次` or `不重试，记为缺口` — while it reads on, and it then waits holding nothing until the editor answers. A Review Run offers none of these yet. J-10 is admitted.
- **Edit a plan before it starts (S73, #419, PR #519).** In a baseline analysis's plan, `返回修改` lets the editor leave out 核对与抽检 or withdraw the automatic safe retry; each edit reads `你改的` with `恢复`, and `更新计划` records it as the plan's next version on the same Task, which the Run honours. What cannot change says why in words, and a fixed task or a Review Run takes no edits.
- **The Run Budget Ceiling and the model service's account limit (S16a and S16b, #51, PRs #523 and #524).** `设置上限…` in the plan caps a Task's spend in tokens. A Run that spends it stops keeping what it read, as `已停止 · 预算已达上限`, and `调整预算并重做` carries that into a new Task whose cap the editor raises or removes. When the model service refuses a request on the account's limit, the Run stops rather than ends, as `模型服务账户限额`, keeping what it read; `处理模型服务` opens 设置 › 模型服务, and `续行` goes on in the same Run once the limit clears. Under `developer-live` the launch sets the ceiling, and the account limit still ends the Run (ADR 0067 §Quota). S16c, an ambiguous turn's own ending, still waits (Next, item 4).
- **PDF, the Markdown 备用格式 and the 审阅报告 export (S64b, #500, PRs #526 and #527).** `导出…` also writes a manuscript version as a PDF, laid out on A4 and printed by AI7's own Chromium in a hidden window denied the network, or, under the `备用格式` disclosure, as Markdown. Each has its own review before anything is written: nothing beyond the words is carried, and 批注 and 修改建议 are written but can no longer be replied to or accepted (`△ 降级导出`). A Review Run's 审阅报告 exports the version on show in the same three formats, its four parts in 审阅's words, and 交付物's `导出记录` lists it.
- **Chapter-level reimport (S63, #412, PR #528).** A revised file's changes show as rows between paragraphs that match exactly, which are carried by themselves; each row is settled by one verb its shape admits — 拆分, 改写与新增, 删除 or 并入 — never preselected. After the commit, a mark follows its words where they stand exactly once in the row and is otherwise set aside and listed, the new file's comments and tracked changes arrive as marks, and the editor lands back in the manuscript with a notice of the result.
- **Production Documents and 交付 (S66a and S66b, #415, PRs #529 and #530).** 交付物's `交付 · 生产文档` has one card per house type — 新闻稿, 宣传文章, 评论文章, 发布会材料 and 营销要点. `从来源材料创建…` makes the type's document from a source material; it is edited on the manuscript's own surface with the same marks, `保存为版本` saves 版本 2, 版本 3 and so on, and `本书不做` marks a type the Book will not have. `交付…` records which version went to whom as a Delivery Record and opens the export of that exact version; AI7 sends nothing, and the card reads `交付后有修改` once the text is edited after a delivery. S66c, the workflow lens, and S66d, the gates, follow.
- **The 图书交付包's conditions and frozen versions (S67a, #416, PR #531).** 交付物 ends with the 图书交付包: a condition row for the 发稿版本, for each house type and for 工作记录, each `✓ 已满足` or `○ 未满足` with a route to where it is met; the Manifest Preview's 包含, 不包含 and 限制; and `准备图书交付包`, which, once every condition holds and a purpose is given, freezes exactly what the page showed as v1, v2 and so on. Preparing it changes no record and writes no file. S67b, the export of a package version, follows.
- **The five follow-ups of the landing reviews (PRs #540, #542, #544, #545 and #546, closing #536, #541, #543, #539 and #537).** A waiting Run whose plan moved reads `需要重新确认计划` with what moved, in the drawer, ②A and 待我处理, and `重新准备` prepares the same goal and range as a new Task to read and start. A Run continued after a stop reports all it spent in its Run Report, and under `developer-live` 待我处理 sends a Run the launch's ceiling stopped to `改计划重做`, as the drawer does. A production document's column and toolbar follow its edits, and every way back to a document draws it. 待我处理 reads the keyring only while a Run waits, and ②A stays on the revision the editor left. A DOCX export never carries half a bookmark, and a file chosen for replacement that cannot be read is named as unreadable, not as changed.
- Factual review and assurance have no editor surface of their own yet; evaluation and Task-produced proposals do not exist, and the knowledge base exists only as its one class 知识库 › 工序与规则, where S75 (PR #514) lists every Book's Default Execution Rules and turns them off.

## The design standard and what it re-cut

- On 2026-09-08 and 2026-09-09 the Owner settled every editor-facing screen with the UI design session: [ADR 0076](docs/adr/0076-align-editor-facing-surfaces-to-the-owner-s-september-decisions.md) (PR #403: manuscript-first entry, marks, review, evaluation, the knowledge base, the findings center, external evidence retention) and [ADR 0077](docs/adr/0077-adopt-the-editor-facing-surface-specification-as-the-execution-standard.md) (PR #404: task plan and authorization, import, reimport and export, navigation, the knowledge base classes, settings, 发稿 / 交付 / 图书交付包) with the execution standard [`docs/ui-ux-v2/editor-surfaces.md`](docs/ui-ux-v2/editor-surfaces.md): per screen the final form, states, wording, data and backend needs; §10 the vocabulary; §11 the thirty backend alignment rows B1 to B30; §12 the design revisions A to X. There is no handoff document and none is coming.
- On 2026-09-10 the Owner merged PR #404 and directed that every existing document and Issue be aligned to it. [ADR 0078](docs/adr/0078-align-the-remaining-references-the-plan-and-the-tracker-to-the-editor-facing-specification.md) names the remaining clauses that are re-read or rewritten, re-cuts the development plan from §11 and records the tracker re-cut: superseded Issues closed as outdated with a named successor — #50 → S76 (#422), #53 → S69 (#417) and S70 (#425), #54 → S58 (#407), S59 (#408) and S70 (#425), #55 → S58 (#407), #56 → S59 (#408), #58 → S66 (#415), #59 → S64 (#413), #60 → S65 (#414) and S68 (#426), #277 → S75 (#421), #322 → S55a (#435); completed Issues closed — #276 (S44a; S44b → S71 (#406)), #304, #307, #321; new slice Issues S55a and S57 to S86; every kept slice Issue carries an alignment block.
- The two proposed ADRs were evaluated against the standard on 2026-09-10 and both are accepted: ADR 0073 (PR #377, merged by the Owner as compatible; its status line was set to `accepted` in PR #391) and ADR 0074 (PR #391, amended in four clauses per the evaluation — the origin is the 事实核查 category of a Review Run, SRC-013 retention happens within the authorizing Task, the curated hosts bind `developer-live` only with the `ordinary-production` search egress named as a successor decision, and the evidence surfaces are `editor-surfaces.md` §4 — then merged on the Owner's direction).
- The five decisions the re-cut left open were answered by the Owner the same day and recorded verbatim in [ADR 0079](docs/adr/0079-record-the-owner-s-decisions-of-2026-09-10-on-storage-policies-export-egress-providers-and-samplebooks.md): the storage decision (data version as a compatibility contract, nothing frozen before the first release, backup-only rollback, one package format, merge semantics, no encryption, database export through External Export Policy v2), Provider Processing v5 for `developer-live` and the production successor of v3 (three suboperations, 30,000 × units, house-people redaction, no web search yet), External Export Policy v2's target kinds and attached content, the three-tier egress model with search as the model's tool and retention fetches by AI7, the narrowing of the repository SampleBooks to `sample1` (S88 #438) and the provider assignment design (S87 #437).

## In flight

**The landing of 2026-09-25.** Twenty pull requests landed, one squash commit each, in this order. #548 (#533, `dev@aa38049`), merged by the nightly queue of 2026-09-24: an author's imported insertion whose place an edit took can be applied — its 稿件冲突, which offered only 保留当前稿件 and 暂不处理, now offers 保存为新提案版本, and wherever its right place is no longer known the editor places it among the words now there. #549 (#532, `dev@79c06f1`): a Book an earlier parser read is told at the choice why its unchanged file cannot be taken again, and to import it as a new Book, instead of a refusal at the commit that blamed the file; 发现相同内容 still finds such a Book; and the import summary says 并入正文 when the editor merges a text box. #550 (#518, `dev@e700abb`): a J-01 launch that misses its readiness budget says where the product's startup stopped. #555 (#551, `dev@56de47d`): 重新准备 works however often the plan moves while a Task waits for the network, 重新确认计划 works on a redo's Task whose plan drifted with no manuscript edit, ②A says when it cannot draw a follow-up answer, and after 恢复为新版本 a screen whose restored window and fallback both fail to draw is no longer stuck with every button disabled. #553 (#552, `dev@87f7e17`): a PDF, RTF, ODT, TXT, Markdown, legacy `.doc` or unrecognised file kept as source material no longer stops AI7 from opening the store again, and keeping a TXT, Markdown or `.doc` that way now completes. #571 (#569, `dev@7ed5cd5`): J-01's reimport tamper control always alters staged text, so the launch it tampers fails closed every time instead of starting by chance. #587 (#580, `dev@4e2626d`): the reimport's parser guard is pinned, and its refusal says 新建图书. #586 (refs #579, `dev@4789ac7`): J-02 presses ⌘F only once the editor reports the composition closed, the window is focused and focus is in the text, and a failure names what was missing or what the press did. #588 (#581, `dev@971d9f8`): the readiness trace's time filter and J-01's target mark are pinned. #589 (#582, `dev@f471682`): when a document's restore meets that double failure, its retry reads 打开交付物 and opens 交付物, and the recovery's choices close once the restore stands. #590 (#584, `dev@5199a9c`): a service case proves the tamper control is refused by the startup validator, not by the control itself. #594 (refs #583, `dev@655b0cf`): a store whose source records claim a format they were never read as is refused at open with the reason, and every store AI7 has written keeps opening. #598 (#593, `dev@7726278`): once a restore stands and nothing it restored could open, the recovery screen says 已恢复 instead of asking for a choice. #599 (refs #591, `dev@c99d438`): J-02's hosted marker says whether its ⌘F keydown reached the page and with the platform's modifier. #600 (#592, `dev@4c3e0ee`): J-01's one caller of `attachRendererTarget` is pinned. #605 (#596, `dev@1889509`): an upgrade that stopped before its version stamp — a power loss, a forced restart or a killed service — finishes on the next open, where every later launch used to close without a message and no Book could be opened. #607 (#603, `dev@dd9b75e`): once a restore stands, the recovery screen's lede no longer describes choices that are gone. #608 (#601, `dev@555cb4d`): the same content gives the same archive bytes on every computer, whatever its time zone, for a converted working representation and an exported DOCX alike. #606 (#597, `dev@a98be5d`): a reimport from a TXT, Markdown or `.doc` file commits as its review promised, and the commit proves that the kept original is still the file the review read. #610 (#604, `dev@6a043cb`): J-02's hosted marker says what the focus found when the Tab into the text revealed nothing. The integrated head at this checkpoint is `dev@6a043cb` (PR #610).

**How it landed.** The Owner's rules for this landing: P0 to P2 findings are fixed before a merge and P3s become follow-up Issues (#583), and only the Commander marks a pull request Ready, once its review is posted and its evidence is complete, since the 19:00 UTC nightly queue merges any open, non-draft pull request that passes; the peer's pull requests opened as Ready went back to draft (comments on #571, #572 and #586 to #590). The queue had merged #548 before anyone re-reviewed its fix commit, and the Commander's post-merge re-review found a P2 — an undo, or the renderer's flush, whose words begin with the grapheme after a drifted insertion's point leaves the insertion bare at the wrong place — filed as #568, whose fix is draft #572. Each of the other nineteen was squash-merged after a Commander review posted on it, the complete ladder at its exact landing tree on ai7-testbed-01 or ai7-testbed-02 (for #553, #571, #586, #588 to #590, #594, #598 to #600, #605 to #608 and #610 run by the Commander's local ladder sessions and recorded as a comment on the pull request), and the full Gate green on Windows and macOS at that tree. Force-pushing was still unavailable, so a branch took `dev` by a merge commit where it needed one, and every landed tree equals its attested tree. The reviews' P3s are follow-ups #580 to #584, #591 to #593, #602, #603, #609 and #611; #583 and #591 stay open for what #594 and #599 left. The reviews also found three defects on `dev` itself, filed as #596, #597 and #601, and #605, #606 and #608 fixed them in this landing. #606's own review found that its commit did not prove the kept original was still the file the review read, which the Commander fixed before it landed, and #608's review found one more gap on `dev`, recorded in #611 (Open at this checkpoint). Three known flakes were each re-run once and passed: macOS J-02 `j14-keyboard-search-focus` (#579) failed the first macOS attempt at the landing trees of #549, #550 and #586 — at #586's as `-no-focus-move`, which rules out the composition guard; macOS J-02 `j14-keyboard-focus-keeps-window-no-reveal` failed once at #598's, a recurrence of #493 filed as #604; and J-01 `renderer-ready` (#518) failed once on Windows in the nightly over `dev@aa38049`.

**Open at this checkpoint**, every one a draft. **The author's stack**, twenty-one pull requests in this order: #556 (S66c, #415) → #557 (S67b, #416) → #558 (#547) → #559 → #560 (S68a and S68b, #426) → #561 (S83, #431; admits J-11) → #562 (S77a, #423; admits J-16) → #563 (S14, #49) → #564 → #565 → #566 → #567 (S79a, S79b, S79d's first piece and S79c, #427) → #570 (S81a, #429) → #573 (S38, #94) → #574 → #575 → #576 (S26a to S26c, #61) → #577 (S82, #430) → #578 (S28a, #63; admits J-13) → #585 (S28b, #63) → #595 (S85a, #433). Each has a Commander review posted: #558, #574 and #595 are approved; #562, #564 and #575 have a P1 — the 任务 panel's and its result window's exits skip the manuscript's leave step, so the reading position and words not yet flushed can be lost (#562); a house guideline version imported for 事实核查 or 情节逻辑 is shown as applied though those categories never read clauses (#564); and the key pattern of a learning-eligibility decision refuses every 审阅 material (#575); every other one has P2s, and #563's review includes an Owner question on ADR 0034. The stack waits for the author's fixes, and its restack on `dev` must give each of its schema revisions 40 to 54 the one line and the case that #605 gave each revision from 21 to 39 that adds relations. **#572** (#568) waits for the author's fix of its review's two P2s. **#612** (#602), the target-mark pin that #600's review asked for, is approved and lands next. **Open follow-up Issues:** #547 (fixed within the stack by #558), #568, #579 (with #591, which keeps the `-search-disabled` split), #583 (the Owner's label reading and #594's P3s), #602 (#612), #609 and #611. **A gap the reviews found on `dev`:** #611, reselecting a staged TXT, Markdown or `.doc` draft never converts it again, so a draft whose working representation can no longer be reproduced can only be abandoned (放弃); nothing committed is lost.

**The landing of 2026-09-24.** On the Owner's goal of 2026-09-23 (review, test with the gates, fix and merge every open and upcoming pull request, drafts included) and the Owner's answers in that session, the Commander merged ADR 0085 (#489, `dev@eba5e52`) and ADR 0086 (#490, `dev@fe0fc44`) on the Owner's word; both are accepted, and #538 set their status lines to say so. It then landed, in order: S61 (#492); S62 (#498), with its review's fix; the J-05 keyboard-step fix (#535, Issue #534), for the context-menu key that was pressed before the editor had read the caret on slow hosted runners; S64 (#501); S74b (#512); S75 (#514); S76a (#515); S76b (#516); S73 (#519); S76c (#520); S76d (#522); S16a (#523); S16b (#524); S64b in two parts (#526, #527); S63 (#528); S66a (#529); S66b (#530); S67a (#531); and the five follow-ups #540 (#536), #542 (#541), #544 (#543), #545 (#539) and #546 (#537). It ended at `dev@34fa3d3` (PR #546).

Each was squash-merged after a Commander review posted on the pull request (from #501 on, with a re-review of its fixes), the complete ladder on ai7-testbed-02 at its exact landing tree, and the full Gate on both platforms at that tree. Force-pushing is not available in that session, so each stacked branch takes `dev` by a merge commit whose tree is exactly the clean rebase of its own commits. Among the defects the reviews and ladders caught before a merge: ②A kept reading `正在取消` after the Run had ended, which J-10's `cancel-settled` caught at landing (#515); a spent Run Budget Ceiling left a Run waiting for an answer it could never honour (#523); a reimport whose marked words moved into the next paragraph could never commit, and a mark one reimport set aside came back with the next (#528, over its review and three re-review rounds); a document reached other than from 交付物 was drawn with the manuscript's 审阅, 分析 and 发稿 (#529); a failed re-export replaced the delivered file on its Delivery Record (#530); and the 图书交付包 counted a report its Review Run had moved past (#531). #524's review kept the `developer-live` account limit terminal, as ADR 0067 §Quota says, and S75's review fix moved J-04's pin to the new 审阅 quick-start reason (`REVIEW_QUICK_START_REASON`).

Before the stack's landing began on 2026-09-24, `dev@10af99e` (PR #513) was the integrated head. Under the Owner's standing merge word of 2026-09-22 — 「auto merge PRs you believe good to merge. This applies in this session for all PRs」 — the Commander integrated the pull requests the previous checkpoint lists, then #505 and #508 (two repairs of the nightly merge queue: a call site that withheld a scope its candidates ask for, and a listing read before GitHub had computed `mergeable`), #511 (J-01's readiness wait aligned with its siblings), #491 (S22, once ADR 0085, #489, was merged on the Owner's word) and #513 (S78). Each had the complete ladder green on a background test guest at its exact tree and the full Gate green on both platforms.

**The dispatch hold of [ADR 0082](docs/adr/0082-hold-the-dispatch-rules-for-implementation-work.md) is active (2026-09-17, PR #472).** The Owner held the dispatch rules for implementation work: each slice is built directly in a session the Owner runs, on its Issue branch, one pull request per slice with the Issue linked and the provenance line `Authored under the ADR 0082 hold: <harness>, <model> @ <effort>` in the Change closure; no Brief, Task Session, receipt, or class binding; the complete ladder at the exact head before Ready, the Gate and the nightly queue unchanged. The hold ends only on the Owner's word, by the lift pull request ADR 0082 §5 describes.

**The parallel wave of 2026-09-12.** On the Owner's instruction the Commander dispatched four file-disjoint attempts at once with the Journey rung withheld — Workers ran the ladder through `build`, opened draft pull requests and returned `ready-for-e2e`; the Commander ran `e2e:all` on every branch serially, fixed what it found, and marked each Ready only then, so ADR 0062's precondition still held at every integrated head. Width is bounded by three hub files — `src/renderer/index.ts`, `src/service/store.ts`, `src/shared/protocol.ts` — of which one attempt per wave may own each; the single-E2E-slot rule of ADR 0069 stops binding once no Worker runs a Journey. Outcome: S87-f1 (#464, PR #468) and S87-f2 (#465, PR #466) integrated, S57 (#405) and S13-f3 (#287) returned `needs-commander` having written nothing, and both returns were worth their cost. #405's measured that the entry position could not be stored at terminal schema version 20, which produced S57-s (#467, PR #469) and then S57 itself on a second attempt. #287's proved its own Issue's premise false: no admitted state can hold a second value for either durable-state drift field — the sidecar pin is immutable and bounded to revisions 1 and 2, and four of five Provider Binding columns are CHECK-pinned to single literals — so the derivation is sound and only the stimulus is unreachable; the Issue is parked until ADR 0080's configurable binding or a sidecar Revision 3 exists. Three Briefs in that wave each named an outcome their Allowed-to-change list could not reach; the lesson is recorded under Process rules.

Before the wave: ADR 0081 is accepted and the nightly is a serial merge queue, proven end to end on 2026-09-11 over one passing and one deliberately failing candidate after four hosted-runner fixes (PRs #460 to #463: the runner's missing git identity, its shallow checkout, the three-way squash's identity, and per-candidate result artifacts). ADR 0081 is accepted and the nightly is a serial merge queue from its first occurrence on; before that, ADR 0080 — the provider assignment design S87 (#437) — is accepted, with the Commander's amendments recording the Owner's decisions of 2026-09-11 (search is model-initiated and AI7 may execute the model's call through the policy-named public service as AI7 platform tools; the anonymous search service is allowed; the Run Budget Ceiling is the only budget; no provider, route, endpoint or model identifier is a source constant — #452 lists today's literals). On 2026-09-11 the Owner ran a model comparison — #328, #281 and #438 each implemented twice on the same base, once under the class binding and once under `deepseek-v4-flash` inside Claude Code — and the Commander reviewed all six on the full ladder: PR #444 (the deepseek arm of #328), PR #445 (the opus arm of #281) and PR #446 (the opus arm of #438) were selected and integrated in that order; #443, #447 and #448 were closed with their tips recorded. The flash tier executed each brief literally and did well where the acceptance test was objective; it stopped at the letter where the intent needed one more step (one revert route of #281 missing; the composed-fixture builder's cases gated away in #438 instead of moved to `sample1`). #387 (replay the model's real analysis of `sample1` in J-04 from the Provider Result Cache) is a Commander unit, unblocked by ADR 0079 §5.

**ADR 0080 §7.8 steps 1 and 2 are done.** The capability profile set declares `toolCalling` and `webSearchTool` per route and model with their evidence, inert (PR #468); Provider Processing v7 exists as reviewed bytes naming AI7's two platform tools on the `developer-live` eligible-only rule, **selected by no active policy set** (PR #466) — active-set v5 is byte-unchanged, so v5 stays the pin and a Run that asks for a platform tool is still refused. The Owner reviewed those bytes on 2026-09-12 and settled both Commander proposals: the search service is Parallel, and the per-frozen-unit Run Budget Ceiling default for the search-enabled rule is 90,000 tokens rather than the 60,000 proposed. Step 3 — the Egress Gate narrowings, the network-denial allowance set, the `tool` payload source, and the two tools with their ledger and cache — is S87-f3 (#473), a T3 slice with nothing left blocking it; it carries the active-set v6 that finally selects v7, which is the Owner's byte review at Ready and a change the nightly queue never merges (ADR 0081). #437 (S87) is closed as completed by ADR 0080.

## Next

In order, from the [development plan](docs/development/development-plan.md), which is the only place delivery order lives:

1. **Gate health: three known intermittent failures, each re-run once per occurrence.** macOS J-02 at `j14-keyboard-search-focus` (#579): #586 made the step wait for its precondition and name what its one ⌘F press did, and its landing ruled out the composition guard; #599 split the hosted marker by whether the key arrived and with its modifier, and #591 keeps the `-search-disabled` split. macOS J-02 at `j14-keyboard-focus-keeps-window-no-reveal`, a recurrence of #493, once on 2026-09-25: #610 (#604) splits that hosted marker by what the focus found. J-01 at `renderer-ready`: since #550 (#518) a miss says where the product's startup stopped, and J-01's 60 s budget stays until a trace shows a slow service rather than a stall (#550's reading 1). PR #479 closed #474 and #475; J-01's `review` remains a sticky location worth splitting when it next matters, and the way to see a hosted-only failure is a `workflow_dispatch` of `e2e-nightly.yml` against a temporary ref (Process rules).
2. **Land the open drafts** (In flight) under the rules of the landing of 2026-09-25: #612 (#602), then the author's stack bottom-up, each pull request once its review's fixes are in, then #572 (#568) and the follow-ups, among them #611.
3. **Phase 2 — the manuscript surface, review, files and delivery.** S88 (#438), 2.1 S57, 2.2 S71, 2.3 S58, 2.4 S59, 2.5 S60, 2.6 S69, 2.12 S65 and S22 (#57) are integrated. S61, S62 and S64 are integrated (PRs #492, #498, #501), and so are S64b (#500, PRs #526 and #527), S63 (#412, PR #528), S66a and S66b (#415, PRs #529 and #530) and S67a (#416, PR #531). S66c (the workflow lens) and S67b (the export of a package version) are built at the bottom of the stack (#556, #557). S66d, the gates, is not built: the only workflow profile defines no gates, and #556 asks the Owner whether to define one.
4. **Phase 3 — one Task Drawer and run governance**: 3.1 S72 (#418), 3.2 S73 (#419, PR #519), 3.3 S74a (#420), 3.3b S74b (#502), 3.4 S75 (#421), 3.5 S76a to S76d (#422, PRs #515, #516, #520 and #522) and 3.9 S78 (#424) are integrated, and so are S16a and S16b (#51, PRs #523 and #524). #51's third part, S16c — an ambiguous turn's own ending, `结果待确认` — waits: no deterministic route can produce an ambiguous turn, so no layer could prove it yet. Built in the stack: S77a (#423, #562), S14 (#49, #563), and S68a and S68b (#426, #559 and #560). Next beyond the stack: the rest of S77 (#423), #95, S70 (#425) (after S87-f3 #473, which lands the platform tools and the active-set v6 that selects the reviewed v7 bytes).
5. **Phase 4 — the knowledge base, evaluation and learning**: built in the stack: S79a, S79b, S79c and S79d's first piece (#427; #564 to #567), S83 (#431, #561), S81a (#429, #570), S38 (#94, #573), S26a to S26c (#61; #574 to #576), S82 (#430, #577), and S28a and S28b (#63; #578, #585). Still to build: S80 (#428), the rest of S79 (#427) and S81 (#429), #62 and #64.
6. **Phase 5 — ecosystem, dialogue and writing**: #52, #90, #65, #66, #89, S84 (#432).
7. **The provider line** stays behind the value chain: S87-f3 (#473) is its next unit (dispatchable now; the Owner's byte review of active-set v6 at Ready); S55a (#435) (1c.10) follows with the ADR 0080 §6 document set; 1c.11 deferred; #452's literals retire with those two and a schema T3 of their own.
8. **Owner decisions pending** — each is one line from the Owner:
   1. **Readings raised by the landing reviews of 2026-09-24**, each listed in its pull request, none blocking a merge:
      - whether a `developer-live` account-limit stop should also be resumable, which would amend ADR 0067 §Quota (#524 is to keep it terminal until the Owner decides);
      - that a Default Execution Rule binds the Provider binding, the outbound category and the ceiling but not the execution route or the Provider Processing pin, which a rule meant to hold across launches would bind too (#514);
      - that a Run cancelled after AI7 closed under it reports the units it submitted but counts no usage for them, since none was recorded, rather than estimating it (#515);
      - that a cancel waiting for another Task's Run to free the slot reads `正在取消` with the note of a Run stopping at its next step, 「正在进行的这一步完成后停止」 (#516);
      - whether a Default Execution Rule should carry a plan's edits, so that an edited plan can become the quick-start default, which today it cannot (#519);
      - whether the Run Budget Ceiling should keep a Run strictly under its value by holding back a request whose cost could pass it, rather than stop new requests once the value is reached, as now (#523);
      - whether `developer-live` should let the plan raise a launch's ceiling for a redo, rather than a relaunch (#523);
      - whether 从来源材料创建… should carry a material's comments and tracked changes into the document as marks, as the Manuscript's import does, rather than say they stayed behind (#529);
      - whether, after an earlier version is delivered again, the card should say that the text differs from what went out last (#530);
      - whether a Run blocked because its plan moved belongs in 待我处理's 等待你的决定, where #536 put it, or under 异常与结果待确认, where ATTN-002 and the information architecture put material drift (#540);
      - that 重新准备 prepares an ordinary new Task, carrying neither the blocked Task's plan edits nor a redo's lineage (#540).
   2. **Raised on 2026-09-25:**
      - #548's readings 1 and 2: where two authors' points stand at one place, AI7 asks where the second goes rather than assume Word's usual order or record each point's order at a schema revision; and a recovery that changed a pending insertion's paragraph shows the whole paragraph for the editor to place it. #568 adds to reading 1 that 撤销 of the insertion's Apply leaves the deletion's reversal at `target-deleted` for good;
      - the label a TXT, Markdown or `.doc` kept as source material records, which names DOCX (#583; #594 leaves it open);
      - ADR 0034 (#563's review): at launch the service admits queued starts before the editor acts, which ADR 0034 allows only for 联网后开始 after an unchanged Reconnect Preflight; the review asks for a code fix or the Owner's amendment of ADR 0034;
      - house guideline text under `developer-live` (#564's review): imported clauses would reach the model with a review, so the review asks for a refusal before dispatch until the Owner admits such text in a policy revision;
      - whether to delete the 84 temporary `land/*` branches the landing sessions left on `origin`.
   3. **#286's promise** — its criterion compares the two attempts' payload digests, which legitimately differ on the single-Session route; the finding and the recommended reading (compare per-attempt unit-message digests) are on the Issue.
   4. **The active-policy-set v6 bytes** when S87-f3 (#473) is Ready — the selection of the already reviewed Provider Processing v7; the queue never merges a policy-byte change, so the Owner merges.
   5. **The Factual Verification Policy v1 bytes and the egress document** when S70 (#425) is Ready (ADR 0074 §2, ADR 0079 §4) — the one policy document ADR 0079 still owes.
   6. **S80's local dependency** (#428: an embedding, recognition or vector-store dependency beyond the ADR 0070 §3 pre-authorization) when Phase 4 is reached.
   7. **Whether the `.agents/skills` vendoring on `temp/freeze-20260904-112022` enters `dev`**; the self-hosted Gate stays shelved.
   8. **Five readings taken in the built slices, each one line to confirm or overturn**, recorded in the pull request that took them: whether 保留当前稿件 ends a Proposal or only records the outcome (S22, #491); the words for a split or joined paragraph on reimport (S62, #498); whether an editor may leave the retained file-level content out of an export (S64, #501); whether 去设置连接's reason should name the ADR 0067 enrollment rather than 设置 (S74a, merged as #503); and the eleven refusal messages still in the old vocabulary (S72, merged as #499). None blocks a merge; each is the Owner's word on wording or scope. The pull requests #492, #512 to #516, #519, #520, #522 to #524, #526 to #531, #540 and #542 each list their own readings the same way, under 「Readings taken, for the Owner」, and #549, #550 and #555 list theirs under their readings.
   9. **Re-measure `SampleBooks/README.md`'s history table** under parser `/2` (S61) and `/3` (S62): it covers local-only files, which agents never read.

   Decided and no longer pending: **S61's class table and S22's two readings** (2026-09-22, recorded as ADR 0086 and ADR 0085; merged on the Owner's word on 2026-09-24 and 2026-09-23, and their status lines read accepted); **Journey admission** (2026-09-20, the Owner: "admit as you need" — J-05, J-06 and J-07, and J-09, J-10, J-11, J-13 and J-16 when their phases are reached, each admitted by its first slice's own pull request, which supplies the real runner and cuts it over atomically into `ADMITTED_JOURNEYS`, `JOURNEY_MODULES`, `JOURNEY_LOCATIONS`, `e2e/run-all.mjs` and the nightly's full set with `GATE_JOURNEYS` unchanged; recorded on #407, #57 and #413, whose `ready-for-human` labels are removed); **the quick start on ②A** (2026-09-20: "disabled for now", until S75); **the marker step and the hosted dispatches of #474** (2026-09-19 and 2026-09-20, taken under the Owner's direction to implement and then to fix and merge); the Provider Processing v7 bytes (2026-09-12: Parallel, 90,000 tokens per frozen unit); #387's echo bound (ADR 0079 §5); the storage decision (ADR 0079 §1); the External Export Policy v2 bytes (PR #440); ADR 0081 (PR #456).

## Decisions still routing work

- ADR 0064 (value-first order; this file is the only root router; one-page Briefs; the frozen references change only through an ADR naming the clause); ADR 0065, 0067 and 0070 (`developer-live`: admitted SampleBooks only, the live-once ledger and cache, unattended under Commander control, the per-unit ceiling of 30,000 tokens times the frozen unit count); ADR 0068 and 0069 (bindings — Commander `claude-opus-5 @ high`, T1 `claude-sonnet-5 @ high`, T2 and T3 `claude-opus-5 @ high`, Reviewer `claude-fable-5-1 @ xhigh` and optional; file-disjoint parallel attempts; one Electron Journey at a time, amended by ADR 0083 to one ladder run per background test guest); ADR 0082 (the dispatch hold: implementation work built directly in a session the Owner runs, without Brief, Task Session, receipt, or class binding, until the Owner lifts it); ADR 0083 (agents run every Electron Journey, and the ladder a closure attests, on the background test guests `ai7-testbed-01` and `ai7-testbed-02` through the host runner, never on the host desktop; one run per guest replaces the single E2E slot); ADR 0084 (the one legacy `.doc` is admitted to the guests, outside the checkout and placed only as an Owner action, so J-01's `.doc` scenario runs there; agents never copy it, extract it from history, or open a failing `doc-manuscript` stage's artifacts); ADR 0071 (Decision Layer and Technical Identity Layer, the Run Liveness Signal); ADR 0072 (multi-format intake); ADR 0075 (the Gate split); ADR 0076 and 0077 (the design standard); ADR 0078 (this alignment); ADR 0079 (the storage, policy, export, egress, provider-assignment and SampleBooks decisions of 2026-09-10).
- The Owner's directions of 2026-09-08 stand: report progress as **what the editor can newly do**; order development by business value; take E2E inputs from admitted SampleBooks wherever one can serve; the product must read as a Chinese editor's workbench — now specified screen by screen.
- Workers implementing any editor-facing slice read the specification's screen section first and cite its clause IDs in the Brief; the prototypes are consulted for form, never copied as authority (ADR 0077 Consequences).
- Process rules that stand, with their derivations in the archived checkpoint: a Brief settles the decisions and dispatches only the checkable remainder; a widening after the Launch Receipt is a Brief revision published before Ready and re-hashed for T3; `e2e:all` runs on a background test guest through the host runner (ADR 0083), invoked as a foreground command and waited on, never a background task; every head SHA is read from the worktree; a repository record cites the pull request, never a branch commit; merges are serial by the #324 sequence and verified with `git ls-remote`; pull requests open directly as ready, except in a wave where the Journey rung is withheld, when they open as drafts the nightly queue skips and the Commander marks Ready after the Journeys pass; for the landing of 2026-09-25 the Owner ruled that only the Commander marks a pull request Ready (In flight).
- **A durable write never sits inside a re-entrancy guard, a fixed frame wait is not a settle signal, and a surface's own scroll is never the reader's.** PR #470 put an awaited write between a window load and the two frames that release `edgeNavigation`, and scheduled an arrival's centring for a later frame than the one an action could capture the pane in; nothing failed on a fast host and both hosted platforms lost Journeys. When a slice adds an `await` inside a guarded section, or scrolls a pane whose edges mean something, the guard's scope and the scroll's origin are part of the review. Reproduce a timing failure by slowing the awaited call in a throwaway commit on a guest, not by loading the machine.
- **A hosted-only failure is diagnosed on the hosted runner, by name.** `gh workflow run e2e-nightly.yml --ref dev -f ref=refs/heads/<temporary-branch>` runs the full Gate on both platforms against any pushed ref in about seven minutes for macOS; one commit pushed to several temporary branches gives parallel samples of an intermittent failure; and because hosted output is stage names only, the question is asked in stage names — a failing step evaluates what it found and sets a more specific admitted `at()` before it throws, the pattern `cooperative-position-*` established. The macOS half of #474 took nine such questions; each answered in one round. Read a finished job's markers with `gh api repos/<repo>/actions/jobs/<id>/logs` (it works while the run is still in progress), delete every temporary ref afterwards, and keep in the runner only the names that state a precondition.
- **A Journey the pull-request lane cannot run is proven only by `e2e:all` at the exact head, and the closure says so.** PR #470 merged with its `e2e:all` rung withheld and its closure asking the Commander to run it; the lane's five Journeys stayed green and the nightly's J-02 has been red since. Under the hold as before it, a pull request whose change reaches J-01 or J-02 records the `e2e:all` result at its exact head in the Change closure before Ready, and a wave that withholds the rung records the Commander's run in the closure before the merge, never after.
- **Before writing a slice's Allowed-to-change list, grep for the pins its change class will hit.** Three Briefs of 2026-09-12 each named an outcome their list could not reach. A new durable record or schema change: `user_version` across `src`, `e2e` and `tests`, *and* every predecessor reconstruction — J-15 fakes a revision-12 store by dropping newer relations, and `tests/service/*` carry downgrade helpers. A new renderer-facing method: `e2e/run-j01.mjs` pins the exact sorted `window.ai7` member list, and the surface spans `protocol.ts`, `preload.ts`, `application.ts`, `service/index.ts` and `request-frames.ts`. A change to where a route lands: sweep the **clicks**, not the assertions after them — the nine stages S57 moved were found in two passes because the first swept for waits naming the overview and five of them name a card that only lives there. Put the findings in the Brief as named `file:line` entries: a Worker told which line pins what moves it correctly; one that has to discover it stops.

## Unresolved

- **ADR 0073 and ADR 0074 are accepted (2026-09-10).** The evaluations that led there are in this file's history (PR #436); what remains open from them is the `ordinary-production` search-egress policy that S70 (#425) needs, under Next; External Export Policy v2 is integrated (PR #440), and S64 (#413) landed on it (PR #501).
- Of the four policy documents of ADR 0079, three — Provider Processing v5, its production successor v6 and External Export Policy v2 — are integrated under active-policy-set v5 (PR #440). The fourth, the egress document beside the Factual Verification Policy v1 of ADR 0074, is still pending as bytes, which the Owner reviews at Ready, bundled into as few active-set versions as possible.
- The OpenCode Go key is enrolled in the Protected Secret Store and has been used for nine items; no agent has read, printed or copied the key file; the DeepSeek credential pasted into a chat on 2026-09-06 was revoked on 2026-09-07.
- The two `developer-live` transport questions from S40 remain unproven: whether the captured native `fetch` plus the single-host allowance actually transmits under denial, and the gateway's real limit-response shape.
- The nightly's two failures of 2026-09-12 to 2026-09-19 are fixed (PR #479; What exists). One hosted-runner timing remains on record: on 2026-09-19 a Windows runner spent more than 240 s staging J-02's ten-million-character file, where the whole Journey usually takes three to five minutes; the bound is now 8 min and the wait has its own stage name, so a repeat is attributable. Three intermittent failures remain known (Next, item 1): macOS J-02 at `j14-keyboard-search-focus` (#579) and at `j14-keyboard-focus-keeps-window-no-reveal` (a recurrence of #493, filed as #604 and closed by #610, which splits its marker), and J-01 at `renderer-ready`, which since #550 says where the product's startup stopped.
- Open technical follow-ups, none blocking: #286 and #288 (the plan envelope; #286 is dispatchable now that #281 is integrated in PR #445), #287 (parked 2026-09-12 until a second admitted binding or a sidecar Revision 3 exists), #387 (a Commander unit, unblocked, not yet run), #452 (routed to S55a, S87-f3 and a schema T3). #301 closed on 2026-09-07 and #328 in PR #444.
- Host and tracker hygiene: an earlier session of this same review left branch `claude/agent-readiness-review-cea172` at `dev@d3c04b3` with no commits (worktree `pr-gate-test-optimization-b8a55c`); the merged branch `docs/adr-0082-dispatch-hold` still exists on `origin` with its worktree and awaits the #324 retirement sequence; and the landing sessions of 2026-09-24 and 2026-09-25 left 84 temporary `land/*` branches on `origin` (Owner decisions pending).
- Whether the `.agents/skills` vendoring on `temp/freeze-20260904-112022` enters `dev` remains an Owner decision; the self-hosted Gate is shelved.

## Safe Resume Prompt

```text
Commander: identify your harness (Codex or Claude Code), then read AGENTS.md, this file, docs/development/development-plan.md, docs/ui-ux-v2/editor-surfaces.md (§0, the screen you are about to dispatch, §10 and §11) and the Dispatch Register. The integrated head at this checkpoint is dev@6a043cb (PR #610); ADR 0073, 0074, 0076 to 0086 are accepted, ADR 0079 §3 amended on 2026-09-10 by the Owner's byte review; every open pull request is a draft (In flight): #612 (#602), approved and landing next; the author's stack of twenty-one, #556 to #595; and #572 (#568). The nightly's two failures of 2026-09-12 to 2026-09-19 are fixed by PR #479 (#474, #475): navigate() held its guard across the S57 position write and dropped paging commands, and the pane's own scroll restoration after an action taken in the arrival's first frame was read as the editor reaching the top edge, which paged away and paused editing (hosted macOS). Read the next scheduled nightly; J-02 now names its step and its missing precondition. Three intermittent failures are known and re-run once per occurrence: macOS J-02 at j14-keyboard-search-focus (#579, whose remaining split is #591) and at j14-keyboard-focus-keeps-window-no-reveal (a recurrence of #493, filed as #604 and closed by #610, whose marker says what the focus found), and J-01 at renderer-ready, which since #550 says where the product's startup stopped. A hosted-only failure is diagnosed by dispatching e2e-nightly.yml against a temporary ref and asking the question in stage names (Process rules). S71 (#406) is integrated as PR #480: the analysis is its own destination under 资料与记录 › 分析. The Phase 1 analysis chain is complete service-side; every remaining editor-facing unit is specified by editor-surfaces.md and ordered by the plan; there is no handoff document and none is coming.

Land under the Owner's rules of 2026-09-25: a Commander review posted on the pull request; every P0 to P2 finding fixed before the merge and every P3 filed as a follow-up Issue; the complete ladder at the exact landing tree on ai7-testbed-01 or ai7-testbed-02, run by the Commander's local ladder sessions and recorded as a comment on the pull request; the full Gate green on Windows and macOS at that tree; then a squash merge whose tree equals the attested tree. Force-pushing is unavailable, so a stacked branch takes dev by a merge commit whose tree is the attested tree. Only the Commander marks a pull request Ready: the nightly queue merges any open, non-draft pull request that passes, so one opened as Ready before its review and its landing-tree ladder goes back to draft. Land #612, then the author's stack bottom-up once each review's fixes are in, then #572 and the follow-ups, among them #611. When the stack restacks on dev, each of its schema revisions 40 to 54 must take the one line and the case that #605 gave each revision from 21 to 39 that adds relations.

The dispatch hold of ADR 0082 is active (2026-09-17): build each implementation slice directly in this session on the slice's Issue branch (<type>/<issue>-<slug> from current dev), one pull request per slice with the Issue linked and the provenance line in the Change closure; write no Brief, launch no Task Session, post no receipt, apply no class binding; run the complete ladder at the exact head before Ready, then merge by hand or leave the merge to the nightly queue. The hold ends only when the Owner says so, by the lift pull request of ADR 0082 §5; do not ask whether it continues.

Provider Processing v5 for developer-live, v6 as the production successor of v3, and External Export Policy v2 are integrated under one active-policy-set v5 (PR #440), each reviewed byte by byte by the Owner; the egress document beside the Factual Verification Policy v1 is the one policy document ADR 0079 still owes. S88 (#438) is integrated (PR #446): the repository keeps only sample1, the five other SampleBooks are local-only test material, and J-01's .doc scenario discloses a skip where they are absent. #281 (PR #445) and #328 (PR #444) are integrated too, so #286 is dispatchable; #287 is parked until a second admitted binding or a sidecar Revision 3 exists. S87 (#437) is accepted as ADR 0080 (PR #449) and closed; the v7 bytes are written and reviewed (PR #466; Parallel, 90,000 per frozen unit) but selected by no active set; the platform-tools slice is S87-f3 (#473), the provider line's next unit, and its active-set v6 is the Owner's byte review at Ready; #452's literals retire with S55a, #473 and a schema T3. ADR 0081 is in force (PR #456): every night at 19:00 UTC the queue tests each open, non-draft, mergeable pull request against dev as the tree that would land and merges the ones that pass, one at a time, then runs the full Gate over dev; a proposed ADR or a policy-byte change is tested and left to the Owner; the Commander still integrates by hand when it chooses and remains the one who decides what is open and Ready. Then build, under the hold and in the plan's order, the next units the stack does not carry (Next). On 2026-09-20 the Owner answered the Journey routing question with "admit as you need": a slice whose Journey has no runner — S58 (J-05), #57 (J-06), S64 (J-07), and later J-09, J-10, J-11, J-13 and J-16 — supplies the real runner in its own pull request and cuts it over atomically into ADMITTED_JOURNEYS, JOURNEY_MODULES, JOURNEY_LOCATIONS, e2e/run-all.mjs and the nightly's full set, GATE_JOURNEYS unchanged. #286 first needs the Owner's reading of its promise; read the specification's screen section and cite its clause IDs in the pull request; the plan's class still says how much care a slice needs; parallel sessions only on file-disjoint sets; every Electron Journey on a background test guest through the host runner, one run per guest, never on this host's desktop (ADR 0083). A pull request that reaches J-01 or J-02 records e2e:all at its exact head in the closure before Ready. #387 is your own unit and is unblocked (sample1 is exempt from the echo rule). S55a (#435) is dispatchable under ADR 0073 and ADR 0080 §6 behind the value chain; S70 (#425) waits for #473; S85a (#433) is built in the stack (#595); S85b and S86 (#434) are dispatchable against ADR 0079 §1 when reached. Report every status as what the editor can newly do.

While the hold is active no class binding applies to implementation work; the session runs at the model and effort the Owner chose and records it in the pull request. At the lift, bindings under ADR 0068 as amended by ADR 0069 resume: Commander claude-opus-5 @ high; T1 Worker claude-sonnet-5 @ high; T2 and T3 claude-opus-5 @ high; Reviewer claude-fable-5-1 @ xhigh and optional. A Claude Code Commander without SendMessage uses cli-session for every attempt. There is no fallback across models, efforts, harnesses, or modes.

Live Provider calls are authorized unattended under ADR 0070, bounded by the transmittable-set and lineage refusals, the enforced ceiling, and the live-once ledger; every live call answers a stated question the Result Cache cannot answer, named as a test item on the Issue before transmission. Never read, print, or copy the key file at the main checkout root; the enrolment helper is its only reader. Only admitted Public SampleBooks may be transmitted. Do not re-transmit S40/first-baseline/1 through /8 or S40/reanalyze-range/1.

Do not touch export, publication, release, distribution, or main. Under ADR 0058 do not query or consider Actions usage.
```
