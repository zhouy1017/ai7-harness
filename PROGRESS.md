# Current checkpoint

This is the single Commander-owned status and routing file under [ADR 0064](docs/adr/0064-reweight-repository-development-toward-value-first-delivery.md). The delivery order lives in the [development plan](docs/development/development-plan.md); what each Journey proves lives in [E2E journeys](docs/development/e2e-journeys.md). The Commander updates this file inside the integrating pull request or through its own documentation pull request.

## What exists

- Seven executable Journeys on `dev`: J-01 import, J-02 long-manuscript editing, J-08 recovery, J-12 workbench and Model Service setup, J-15 native artifact lifecycle, J-03 provider-denied Task authorization with the foreground execution boundary, and J-04 covered baseline analysis with its three update modes and Revision History (#92 / PR #266, #93 / PR #269). J-04 runs through the real execution path (Task Intent, `Task Input / 任务输入` checkpoint, Provider Preflight, Plan Envelope, Run Authorization, single-slot scheduler, immutable Execution Binding, `PrimaryAgentHarness`, Egress Gate, bound adapter) on hand-written synthetic fixtures; the Egress Gate refuses every remote route under `development-ci` / Provider Processing v1; the only live calls in this product's history are the nine test items under `developer-live` recorded below.
- The Local Verification Ladder is complete: `doctor`, `bootstrap`, `check`, `test`, `test:service`, `build`, `e2e` / `e2e:all`, `e2e:debug`, `e2e:repeat`.
- S13 (#48 / PR #280, `dev@f417e50`): the Plan Envelope is an authority-bearing boundary on the executing analysis Task. Plans are versioned (analysis ledger 16 → 17), a material change among fifteen fields between Plan Preview and Run Authorization suspends authorization with `查看计划修订` and its field-level diff, `重新确认计划` yields the next version, a stale version is refused with `plan-revision-required`, the Plan Boundary Split is part of the canonical envelope, and the one declared adaptation class `safe-retry` repeats a unit's unchanged request at most once on `RATE_LIMIT` / `TRANSPORT_FAILED` (any status) or `PROVIDER_ERROR` (5xx) with a durable Plan Adaptation written before dispatch. J-04 gained ten stages; protocol is 19.
- S40 (#272 / PR #285, `dev@3c3d820`): the `developer-live` scope. It has been used exactly once, on 2026-09-07 — see the live Run below. Provider Processing Policy v4 and active-policy-set v4 are the sole active set for all four scopes, correcting the stale `ordinary-production` v3 pin; three trusted launch arguments (`--trusted-operational-scope`, `--run-budget-ceiling`, `--provider-cache-root`) bind the scope from the launch form only, never from an environment variable or product setting; route `opencode-go` sits beside the unchanged production route behind a route profile; `tools/enroll-dev-credential.mjs` is the only reader of the Owner's key file; the Provider Test Ledger and Result Cache make every live call a named test item `S40/<purpose>/<n>` that replays on an identical request and is refused on repeat; one technical Session per Analysis Unit; the Run Budget Ceiling refuses `unset` under v4 and is enforced before every dispatch; a limit response ends the Run as a Provider Account Limit. Protocol is 20. Protocol is 20 after #310.
- The first live Provider Run in this product's history happened on 2026-09-07, human-attended: `S40/smoke/1`, exact `sample1`, eight units through `opencode-go` / `deepseek-v4-flash`, 113,767 tokens under a bound 200,000 ceiling, outcome `completed-with-gaps` with five of eight units closed. Every enforcement guarantee held. Its record, the seven problems it exposed, and the designed fix for each are [#307](https://github.com/zhouy1017/ai7-harness/issues/307). **Its eight cached responses are still on this host** at `C:\Users\Chooo\Playground\ai7-harness-provider-cache\`, outside every checkout, beside the ninth item recorded below; they were the evidence base for #306, now closed.
- Model capability profiles (#310 / PR #320, `dev@881467c`): a route profile carries how to reach a model, a model profile keyed `${route}/${model}` carries how to speak to it in six declared capabilities, and one normalizer returns `answer`, `empty-answer`, or `malformed` so nothing above the provider layer branches on a vendor. `structuredOutput` was `none` on every profile until #306, because nothing had observed any gateway accept a format constraint, and assuming otherwise is what produced the first Run's three failures. Replaying all eight cached responses through it classifies the three failed units as `empty-answer` with a populated reasoning channel, not as malformed JSON.
- **The unit contract survives contact with the bound development model** (#306 / PR #330, `dev@37dd88243acf3e5e516d64c23032e4337b5b8396`). `structuredOutput: 'json-object'` is a capability the assembler implements as exactly one body field, `response_format: {"type":"json_object"}`, declared only by `opencode-go/deepseek-v4-flash` on the evidence of one live test item: `S40/reanalyze-range/1` of 2026-09-08, run by the Commander through the product's own broker, gate, adapter, and ledger under ADR 0070 with the question stated on the Issue first — HTTP 200, 1,900 tokens in and 17,486 out, the very unit that had returned an empty answer on the first Run closed with a parsed JSON result, and a 1,000-token ceiling stopped the Run after that single transmission. An empty answer now reads `模型完成了推理，但没有给出答案：答案通道为空，推理通道有内容。这不是稿件或契约的问题；重新分析本单元通常会得到结果。`, and a non-JSON answer is measured in characters and never quoted. The production route's request bytes are proven unmoved; every other profile stays `none`. The Phase 1 exit criterion's transport half and its "usable unit results" precondition are met; the result half still waits on S41 to S44.
- The provider-denied Task kind derives its scope statements from the bound launch (#316 / PR #331, `dev@dd1ce674ef6375cc9945d1b1c4b84199113342c4`): one pin table keyed by trusted scope, four readings branching on the pin's decision, three value builders shared by the write and read sides so they cannot drift, every stored row validated against the pin it carries rather than the launch reading it now, and three `protocol.ts` literals widened to their general shapes with the wire unchanged under `development-ci`. The guard still admits only `development-ci`; the envelope's `denied` / `dispatchAllowed: false` and the plan's production binding stay literal until a slice admits a second scope.
- [ADR 0071](docs/adr/0071-state-the-decision-layer-and-run-liveness-rules.md) states the two display rules the first live Run showed missing, in the frozen `docs/ui-ux-v2/` references (PR #329, `dev@59eaf51`): V2-UX-LAYER-001 to 008 separate a Decision Layer from a disclosure-closed Technical Identity Layer with nothing authority-bearing demoted, glossary vocabulary, local time, primary actions reachable without scrolling, and provenance as counts; V2-UX-LIVE-001 to 006 define the Run Liveness Signal from recorded facts, sampled within 2 s, with a measured stale case and state-bound indicator lifetimes. Rule first, survey second, per-surface units third.
- **The Run Liveness Signal exists** (#305 / PR #341, `dev@4254345`): while a baseline analysis Run executes, the card shows which unit is in flight and since when, what the Provider attempt is doing (`已派发` / `等待模型响应` / `安全重试中`), when the Run last changed state in local time, and how many model turns are done, refreshed by the card's existing 250 ms loop, with the stale case `本步骤用时已超过通常水平` measured against the Run's own longest step or 3 minutes before any; the success-path status toast that outlived `已进入调度器` is gone and the state-bound header pill carries the state. `SERVICE_PROTOCOL_VERSION` is 21. A held-transport service case proves the mid-flight facts provider-free; J-04 asserts after settlement that the signal ended with its state and that the Run Record's `上次状态更新` row shows local time with the exact instant beside it. The DOM-free helpers (`localInstantLabel`, `elapsedLabel`, `attemptStateLabel`, `runStepIsStale`) live in `src/renderer/plan-preview-labels.ts` for the survey's units to reuse.
- **The OpenCode Go plan's chat-completions models are declared** (#338 / PR #340, `dev@0794a71`): eight inert profiles on the existing `opencode-go` route (four GLM, three Kimi, DeepSeek V4 Flash Vision Exp) built by one module-private factory from one `vendor-documentation` record read on 2026-09-08, every read-side capability `unverified`, `answerChannel: 'none'` so none can be accidentally functional, and a proof case that refuses each at the read boundary and assembles each with no branch. Six products the Go page names without a documented id, and MiniMax with its path disagreement between the two pages, are excluded with their reasons in the table.
- **The Technical Identity Layer exists product-wide** (#332 / PR #347, `dev@e21acad`): the `technical-identity` class, which had 92 renderer sites and no stylesheet rule, is subdued to the label's rank and gathered per sub-surface behind one closed `查看技术详情` disclosure built by one shared helper; every instant an editor reads is absolute local date and time with the exact ISO instant beside it; three values moved up to the decision layer under V2-UX-LAYER-002 (`可读范围`, the lineage-evidence scope sentence, `实际位置`). Three sub-surfaces keep a technical row at decision rank with the demoting style only, each with a stated cause — two the survey judged conforming, and the manuscript-recovery identity card because a focusable `<summary>` between the heading and the recovery radiogroup broke J-08's one-Tab-to-the-choice guarantee, found by bisection and fixed in the product. J-15's `facts()` helper reads both layers; J-04 asserts the timeline's local time and exact instant.
- **The second request shape exists** (#342 / PR #348, `dev@fcbaf3a`): `anthropic-messages` is assembled from the same payload with a top-level `system` and a `max_tokens` the route declares (`maxOutputTokens`, `null` on the chat-completions routes so their bytes cannot move), and read by the normalizer from `content[]` text blocks, `type: 'thinking'` presence, and `input_tokens` / `output_tokens` into the same three canonical outcomes. A second Go route, `opencode-go-messages`, reuses the `opencode-go` credential slot, states its credential header provenance (every route now must, through `credentialHeaderEvidence`), and carries the plan's documented `/messages` models — three MiniMax on a recorded page disagreement, two Qwen — as inert profiles. `ExecutionRoute` stays narrow so no Run can bind an inert route; `SERVICE_PROTOCOL_VERSION` and `CredentialSlotId` are unchanged; both frozen digests are unmoved.
- Every admitted Public SampleBook has an import verdict (#297 / PR #327, `dev@eacffb9`, in `SampleBooks/README.md`): only exact `sample1` imports; three of the four DOCX files refuse at one `sample1`-shaped condition (`docx.ts:316`) with a second gate behind it (`docx.ts:661`); `.doc` and `.pdf` refuse on the extension before a byte is read. #313 designs against that table.
- Branch retirement works and is verified (#324 / PR #326, `dev@b073905`): `gh` deletes the local branch before the remote one and stops on the first error, so a branch held by a linked worktree survived every merge; the documented five-step sequence ends in `git ls-remote --heads origin <branch>` and was exercised on every merge of this wave.
- The delivery Gate binds to the pull request, not to a habit (#318 / PR #319, `dev@fa765d1`). Before that fix, a pull request **created directly as ready fired `opened`, matched no trigger, and ran no Gate at all** while reporting `mergeStateStatus: CLEAN`. Every pull request had happened to be opened as a draft first; that convention, not the workflow, had been enforcing ADR 0062.
- Cross-unit reduction is deterministic string matching only; factual review, proposals, Apply, and export do not exist yet.

## In flight

- **Wave 4 of 2026-09-08 is closed; `dev@dd1ce674ef6375cc9945d1b1c4b84199113342c4` is the integrated head and no worktree holds uncommitted work.** Its attempts, all `cli-session` under ADR 0069's bindings: #324 (T1, sonnet), #297 (T2, opus), #306 (T3, opus, two-phase with the Commander's one live item between the passes), #316 (T2, opus), and #304 A1 (T2, opus, a report-only survey whose deliverable is a comment on the Issue and no commit). Six pull requests merged: #326, #327, #329 (the Commander's ADR 0071), #330, #331, and this status pull request. Earlier: the T1 closeout wave (#289, #290, #291), the second wave (#301, #303, #308), and the third (#310, #318) on 2026-09-07 and 2026-09-08.
- **Wave 5 of the same day closed two more:** #338 (S54a, PR #340, `dev@0794a71`) and #305 (PR #341, `dev@4254345593cb614de9585d1457d83cdb565919f2`), both `cli-session` at `claude-opus-5 @ high`, both rebased once onto the then-current `dev` and run through `e2e:all` in the foreground before Ready. `dev@4254345` is the integrated head.
- **Wave 6 closed two more, and nothing is dispatched at this checkpoint.** #332 (the survey's family 0, PR #347, `dev@e21acad`) and #342 (S54b, PR #348, `dev@fcbaf3a6c944fde6d3ebe9bd39e68ddb4a40925e`), both `cli-session` at `claude-opus-5 @ high`. **`dev@fcbaf3a` is the integrated head; `git worktree list` shows only the main checkout and the Commander's session worktree, and no worktree holds uncommitted work.** Eleven pull requests merged on 2026-09-08 across three waves, seven of them from Workers. ADR 0072 (multi-format manuscript intake) was accepted by the Owner later the same day and merged as PR #343 (`dev@6e7f157a`); wave 7 below is built on it.
- **Wave 7 closed three more and split #313 once more.** #335 (the survey's family 4, PR #355, `dev@3105b208`), #352 (S51a, the classified DOCX import, PR #357, `dev@1c5168fc`), and #354 (S54c, `openai-responses`, PR #359, `dev@2bab1589`), all `cli-session` at `claude-opus-5 @ high`, ran in parallel on disjoint file sets and were integrated serially by the #324 sequence. **`dev@2bab158936c7f5b2dfeac0a626051a6b2639985c` is the integrated head; `git worktree list` shows only the main checkout and the Commander's session worktree, and no worktree holds uncommitted work.** Opened rather than folded in: #353 (direct unit cases for the DOCX hostile-input bounds, T1), #356 (S51b-2), and #350 re-scoped to S51b-1. Fifteen pull requests merged on 2026-09-08 across four waves, ten of them from Workers.
- **Wave 8 ran three Workers at once on disjoint file sets** — #350 (S51b-1, T3), #311 (S50, T2), #358 (S54e, T1 at `claude-sonnet-5 @ high`) — with #362 (S54d, T3) taking the slot #358 freed. Integrated: #358 (PR #364, `dev@c990b72f`), #311 (PR #365, `dev@55299e40`), and #362 (S54d, `google-generate-content` against test-local profiles, PR #366, `dev@3f2fbbb3`), which completes #321's five units. Closing the wave: #346 (the ladder's runner parse, T1 at `claude-sonnet-5 @ high`, PR #368, `dev@b8986b3d`), #360 (J-08 composed inputs, PR #369, `dev@2117689f`), and #350 (S51b-1: format identification from content, source-only retention of any recognised file, schema revision 18, protocol 22, PR #370, `dev@4d8a9723`). **`dev@4d8a972302281744282e4bcba06ec0a5578a202b` is the integrated head; no attempt is open, and no worktree holds uncommitted work.** Opened rather than folded in: #360 (compose J-08's inputs), #363 (the intake rename), #362 (S54d).

## Next

In order, from the [development plan](docs/development/development-plan.md), which is the only place delivery order lives:

1. **#313's tail, in order**: #363 (the intake rename S51b-1 could not carry — nine files, three of them #311's — T1, dispatchable now that #350 and #311 are integrated); S51b-2 (#356, `.txt` / `.md` to a DOCX working representation kept beside the original, T3); S51c (#351, the `.doc` converter under ADR 0070 §3, T3). #350 is in flight on Brief revision 2 (the rename moved to #363; schema 18 declared in four modules).
2. **Beside #363, file-disjoint**: #345 (J-02's sequenced-run observation, T2 — it holds the Electron slot for its whole investigation, so it runs while the others are in their work stages) and #353 (direct unit cases for every reachable DOCX bound, T1). J-02 stays generated because its subject is scale and J-12 because its subject is routing; J-08 now composes from admitted content (#360).
3. **The survey's remaining families** (1c.1), serial with #356 where the renderer overlaps: #333, #334, #336 (T1 — the Commander lists each heading's glossary form in the Brief first), #337.
4. **1c.10 (#322 tier 1)** once #321's five units are all integrated (S54d #362 landed as PR #366): the first format-selected configurations are Claude, Qwen, HY, and OpenAI official and Gemini's route, each needing a credential slot the provider layer does not yet have.
5. Then Phase 1 resumes at S41 #273, and a second `developer-live` Run on `sample1` becomes justified when S41 needs real unit results to generate fixtures from — with new item identifiers and its own stated question.

**#322 is settled and scheduled.** The Owner decided on 2026-09-08 that it is repository tooling, not an in-product capability, and added a tier the Issue lacked: a provider whose compatibility format AI7 already implements is configured by **selecting that format** — the CC Switch pattern — with no agent at all. That is slot 1c.10; agent-driven discovery for anything matching no format is 1c.11 and probably need never ship.

## Decisions of 2026-09-06

- [ADR 0064](docs/adr/0064-reweight-repository-development-toward-value-first-delivery.md): value-first order; `PROGRESS.md` is the only root router; schema-v5 receipts; Reviewer optional; T3-only body hash; base-drift continuation; one-page Brief; frozen design references; implementation status only here and in E2E journeys; PRD moved to `docs/prd/ai7-v2-prd.md`.
- [ADR 0065](docs/adr/0065-admit-a-developer-live-provider-processing-scope.md): the human-attended `developer-live` scope (Provider Processing v4) for prompt tuning on developer hosts, with `deepseek-v4-flash` as the development-interval Main Editorial binding, one Session per unit, a required ceiling of 500,000 tokens by default, admitted Public SampleBooks only, and no fixture or capture by itself.
- [ADR 0066](docs/adr/0066-add-model-driven-cross-unit-reduction-and-assurance-sampling.md): the Baseline Cross-Unit Reduction Contract, assurance sampling into the assurance axis, the Run Report, the factual finding record shape, and the per-Run research budget.
- [ADR 0067](docs/adr/0067-authorize-the-opencode-go-development-credential-with-live-once-testing.md): the Owner's OpenCode Go subscription key (untracked `opencodego.key.txt` at the main checkout root, `*.key.txt` ignored) is authorized for development Provider testing while its quota lasts; the `developer-live` scope binds `deepseek-v4-flash` through the `opencode-go` route; every live call is a named test item recorded in the Provider Test Ledger and replayed from the Provider Result Cache, so the same or a similar test runs live at most once; limit responses end the Run as a Provider Account Limit.
- The human-attended `sample1` recording under ADR 0044 is scheduled after the Phase 1 exit criterion; its kept decisions (policy v2 successor with transmissions equal to the unit count, per-unit Sessions, the ceiling) are in ADR 0065 and the plan. No recording Issue is open.
- Dispatch bindings were unchanged on this date; they were changed on 2026-09-07 by ADR 0068 (see below). A Claude Code Commander without `SendMessage` uses `cli-session` for every attempt.

## Decisions of 2026-09-07

- [ADR 0068](docs/adr/0068-rebind-the-t3-worker-and-reserve-fable-for-the-commander-and-reviewer.md): on the `claude-code` route the T3 Worker binds `claude-opus-5 @ high` and every Reviewer binds `claude-fable-5-1 @ xhigh` whatever the reviewed class; the Commander stays `claude-fable-5-1 @ xhigh` and T1 and T2 are unchanged. The Codex route is untouched. The Commander, every T3 Worker, and every Reviewer had been drawing one Fable budget, and on 2026-09-06 the Commander and S40 Worker A2 stopped at the limit within twenty minutes of each other. No fallback exists and a resumed session keeps its model, so an open attempt cannot adopt the new binding: close it `superseded` and relaunch. Every open T3 Issue needs `claude-opus-5 @ high` in its body, a Brief revision, and a recomputed T3 hash before its next launch. On `claude-code` a Reviewer receipt records `class_match` as `fixed-reviewer-binding`.

- [ADR 0069](docs/adr/0069-bind-the-parallel-closeout-wave.md): the Owner resumed development the same day in a parallel shape. On `claude-code` the Commander binds `claude-opus-5 @ high` and the T1 Worker `claude-sonnet-5 @ high`; T2 and T3 keep `claude-opus-5 @ high` and the Reviewer keeps `claude-fable-5-1 @ xhigh`, still optional. The Commander reviews what it integrates instead of dispatching a Reviewer for T1 work. Work is reduced to the T1 bar before dispatch — the Commander settles the decision in the Brief and dispatches only the mechanically checkable remainder — never by lowering a class to fit a model. Parallel attempts must be file-disjoint, and because only one Electron Journey runs on this host at a time, a parallel Worker stops at `ready-for-e2e` after `build` and the Commander releases the `e2e:all` rung to one attempt at a time.
- [ADR 0070](docs/adr/0070-run-developer-live-unattended-and-size-the-ceiling-per-unit.md): three Owner decisions taken after attending `S40/smoke/1`. **`developer-live` may run unattended under Commander control** — the guarantees attendance stood for are now carried by the transmittable-set and lineage refusals, the enforced ceiling, and the live-once ledger, and what replaces attendance as an obligation is that every live call answers a stated question that could not be answered from the cache. **The Run Budget Ceiling default becomes 30,000 tokens times the frozen unit count** rather than a flat 500,000, derived from the largest observed unit (about 26,100) with margin; recorded beside it, this model counts reasoning tokens inside its output, so a ceiling reasoned about as result size is wrong by roughly four times. **A conversion dependency is pre-authorized for #313** if it is pure JavaScript or WebAssembly and permissively licensed, reported in the pull request that adds it — nothing wider.
## Decisions of 2026-09-08

- **The Owner named an explicit provider support list**: Google Gemini, the OpenCode Go subscription API **including every model in its plan**, DeepSeek official, OpenAI, Claude, Qwen, and HY. Recorded as [#321](https://github.com/zhouy1017/ai7-harness/issues/321). It spans at least three request shapes — OpenAI chat-completions, Anthropic messages, and Google generateContent, plus `/responses` for part of the Go plan — so it implements what #310 named and refused rather than adding data.
- **Anything outside that list is to be handled by an agent-driven discovery adapter**, recorded as [#322](https://github.com/zhouy1017/ai7-harness/issues/322) and deliberately left T0 and undispatchable. The Commander's position, in the Issue: build it as repository tooling that emits a reviewed profile proposal, not as an in-product capability, until the Owner decides otherwise — because an agent deciding unreviewed what gets sent to an unknown endpoint is a new kind of authority in this product, and every one of its six open questions answers differently under each form.
- **The delivery Gate had a hole and it is closed.** A pull request created directly as ready ran no Gate while reporting `CLEAN`. Fixed in #318; the convention of opening drafts had been doing the work the workflow was supposed to do.

- **#322 is settled: repository tooling, in two tiers.** The Owner decided against an in-product capability, which answers the five remaining authority questions at once — probing lives outside the Run machinery, so it needs no Egress Gate purpose, no Credential Broker widening, and no place in the live-once ledger. The Owner also added the tier the Issue was missing: most unlisted providers do not need discovering because they **announce** a compatibility format, so configuring one is selecting that format and filling in endpoint, credential slot, and model ids — the CC Switch pattern. #310 already made a profile pure data; this makes that data authorable. The rule that does not bend: a declared format is a claim about the **request shape only**, and `structuredOutput`, the reasoning channel, and usage attribution stay `none` until observed.
- **A branch retirement rule that did not work.** PR #295 made retirement automatic at merge via `--delete-branch`, and six merged branches survived it anyway; they were deleted by hand on 2026-09-08. The cause, confirmed from the `gh` source and a local reproduction: the flag deletes the local branch first and stops on the first error, and `git branch -D` refuses a branch held by a linked worktree. #324 states the sequence that works and how to verify it; every merge of wave 4 used it.
- [ADR 0071](docs/adr/0071-state-the-decision-layer-and-run-liveness-rules.md): the decision-layer / technical-identity rule and the Run Liveness Signal, written by the Commander as the rule-first half of #304 and #305 and merged as PR #329. It names the clauses it changes in the frozen `docs/ui-ux-v2/` references; the sampling interval (2 s / 5 s) and the stale factor (twice the longest completed step, or 3 minutes before any) are the first numbers the design states for liveness, derived from one Run and revisited through an ADR, not tuned in code.
- **The first format-constraint observation on any gateway.** Test item `S40/reanalyze-range/1`, stated on #306 before transmission and run through the product from a Commander-owned probe worktree with the profile flipped locally: the `opencode-go` gateway accepted `response_format: {"type":"json_object"}` for `deepseek-v4-flash`, and the unit that had produced only reasoning produced a parseable answer. One item establishes that the field is accepted; it does not establish that the constraint is honoured on every response, and the profile's doc comment says so. The empty-answer and non-JSON readings therefore stay in place.
- **Three of the four admitted DOCX SampleBooks refuse, not two.** #297's probe found the third had never been attempted. All three stop at the body-level section-properties allowlist, which admits a zero-attribute container or `sample1`'s exact shape, and a second gate in the fidelity plan admits only the all-zero projection or `sample1`'s 266/1 shape. Neither is a boundary any requirement or ADR chose; V2-UX-IMP-005 makes an explicit degradation decision the design's answer. The widening is #313's to decide.
- **#321 is split into five units** on the Issue, from the OpenCode Go documentation read on 2026-09-08: the plan serves DeepSeek, GLM, Kimi, LongCat, Hy, Omen, and MiMo models on `chat/completions`, Grok, GPT 5.6 Luna, and Muse Spark on `/responses`, and MiniMax and Qwen on `/messages` (the Zen page disagrees on MiniMax; both readings are recorded with the date). HY is reachable twice — through Go and through its official endpoint — as two routes.
- **The #304 survey is posted, and it is smaller than the rule feared.** A report-only T2 attempt judged all forty-four surfaces the seven Journeys drive at `dev@59eaf51`: fifteen conform (the editor window is the product's own worked example of the rule — a real disclosure holding no digests, sticky action regions; so are the gap list, the four state axes, the import-recovery sentences, and the task outcome's safe next action), twenty-nine do not, and twenty-four of those reduce to two absent mechanisms rather than to twenty-four designs. It also corrected a seed: the workbench's primary actions sit below both large cards but above the `精确记录` section, not last; the reimport review is where actions genuinely come last. Six family Issues, #332 to #337, carry the routing; #305 owns the Run surfaces and the floating `已进入调度器` pill, which the survey traced to `setStatus` on the authorization success path writing into the fixed `.status-bar` with nothing to end it, while the card's own heading pill is state-bound and conforms.
- **ADR 0072 was proposed and accepted the same day** (PR #343, `dev@6e7f157a`): multi-format manuscript intake with the original always kept as the Source Version, conversion loss classified through the existing three labels and an explicit Import Degradation Decision, PDF source-only by default, converters under ADR 0070 §3, and the two `sample1`-shaped parser gates turned from refusal into classification while every hostile-input bound stays. It records the Owner's direction of 2026-09-07 and #297's finding; the Owner accepted it as written, §5's converter policy being the one decision that was the Owner's alone, and #313 split on it into S51a (#352), S51b-1 (#350), S51b-2 (#356), and S51c (#351).
- **Two ladder findings from wave 6, recorded as Issues rather than fixed in place.** #345: J-02 failed 2 of 5 sequenced `e2e:all` runs on the developer host and passed 3 of 3 standalone plus the final sequenced run, not tied to the diff under test; `run-all.mjs` discards the child's output, so the failing runs left no stage name. #346: `pnpm run check` does not parse `e2e/*.mjs`, so a runner syntax error (a backtick inside a template-literal comment) rode the whole ladder green and surfaced only when Electron imported the runner; `node --check` catches it in a second.
- **Three process facts from wave 4.** A Worker that starts `e2e:all` as a background task and returns leaves an orphan nobody observes; the rung is not evidence and is run again in the foreground, and every Brief now says "never a background task" in those words. A Worker's report abbreviated a head SHA to 39 characters; the Commander reads every SHA from the worktree, never from a report. A Commander live check under ADR 0070 runs from a **Commander-owned detached worktree at the Worker's head with the local edit uncommitted**, against a **copy** of a data root, so neither the attempt's worktree nor the Owner's record is touched; the product's credential-readiness literal in the plan projection is `ready`.
- **All four admitted DOCX SampleBooks import** once S51a (#352) turned the two `sample1`-shaped gates into classification: the body-level terminal `sectPr` counts once in `sections`, and `deriveImportFidelityPlan` plans any well-formed report by reconstruction. `sample1` is unchanged at 266 / 1; the second verdict column in `SampleBooks/README.md` records the other three with their counts. Two things it surfaced: `蟠虺.docx` carries 124 comment and revision marks that are dropped content accepted in the same Import Degradation Decision — the first admitted file to exercise that copy — and `duplicate terminal section properties` is unreachable (recorded on #353). The DOCX hostile-input bounds have no direct unit cases; #353 owns that.
- **Source-only retention of a non-DOCX file is a schema change, not a routing change.** `source_versions.format` is `CHECK(format = 'DOCX')` and its parse digests are `NOT NULL`, so a retained PDF needs a rebuilt table (schema revision 18), a widened wire protocol, and a picker contract change before any conversion exists. That is why S51b split: S51b-1 (#350) routes and retains, S51b-2 (#356) converts.
- **S54c read the Responses shape from the vendor's SDK, pinned to a commit**, because the platform reference page refuses unauthenticated fetches; the evidence record names `openai-node` `src/resources/responses/responses.ts@eecbebe2`. Only `grok-4.6` and `gpt-5.6-luna` are admitted on the Go `/responses` path — Muse Spark is excluded because the Go page and the Zen table print different ids, not because either omits it. OpenAI official is deferred to 1c.10 as Claude official was. Gemini has no Go route at all, so S54d ships its shape against test-local profiles and its route waits for the same credential-slot change.
- **Two process facts from wave 7.** A T3 Brief whose allowed change turns out incomplete at preflight gets a revision 2 published before the Launch Receipt, and the same attempt continues (#354, on the #305 precedent). A repository record never cites a branch commit as its measurement head, because a squash merge leaves that commit unreachable — it cites the pull request (#352's README column).
- **The intake rename is its own unit.** ADR 0072 §1 names `stageSelectedManuscript`; the #350 preflight measured the rename at fifteen sites in nine files beyond the intake path, three of them suites #311 owned in the same wave and two of them runners that call the preload key at runtime. S51b-1 ships the behaviour under the old names and #363 renames afterwards; a Brief that widens across a live attempt's files is a Brief revision, not a receipt note.
- **Schema revision 18 is declared in four modules.** The `user_version` counter is one global walked across `store.ts`, `task-authorization.ts` (which owns the version constant, the accepted set, and the revision-15-to-17 migrations), `editorial-workspace-profile.ts`, and `bounded-manuscript.ts`; a store bumped by `store.ts` alone fails `SCHEMA_UNSUPPORTED` at the next initializer. #350's Brief revision 2 names all four.
- **What the admitted SampleBooks actually contain, measured by #311's builder.** No admitted DOCX carries a title block; only `2听漏（定稿368544字）.docx` carries headings, to level 3; `蟠虺.docx` (100 blocks) is the small source content-bearing tests compose from. A search query is derived from the composed text at test time and every assertion is a count or a boolean, so no failure diff can print prose. J-02 stays generated (its subject is scale: 50,000 blocks no admitted file approaches), J-12 stays generated (its subject is routing), J-08 converts under #360.
- **The provider support document keeps three senses apart** — declared, live-verified, bindable — and writes every gap as "not established"; it is derived from one head and re-derived by the next provider-layer unit.
- **Gemini's shape ships routeless.** S54d (#362) implements `google-generate-content` — the model id in the URL, `assistant` mapped to `model` in that arm alone, `systemInstruction` top-level, `candidates: []` read as `candidate-absent` because that is a blocked prompt, not an empty answer — against test-local profiles, because Gemini is on no OpenCode Go path and its official endpoint needs a credential slot and the `x-goog-api-key` header, both 1c.10's. The source of record is Google's GenAI SDK types at a pinned commit.
- **A turn that ends does not report.** #350's Worker backgrounded `e2e:all`, citing a tool time cap, and returned; the run was killed with the turn and left a J-01 root mid-scenario, which the Commander removed before rerunning the rung in the foreground. A Journey started in the background is not evidence; `e2e:all` completes in about four minutes on this host, under any cap, and the rule now says so in the slot message itself.
- **S51b-1 landed with three forced widenings and one reviewed loosening.** The protocol pin in `service-client.ts` (22), the two terminal-version test pins, and the DDL validators in `bounded-manuscript.ts` accepting both the revision-17 and revision-18 shapes of `source_versions`, `source_provenance`, and `import_drafts` regardless of `user_version` — a loosening to revisit once every store is past 17. Decision 1's `.docx` display-name rule became conditional on the router having identified the format, which kept #311's suite untouched.
- **J-08 runs on admitted material and `check` parses the runners.** `e2e/composed-docx.mjs` is the plain-JS twin of the vitest builder (#360); `tools/check.mjs` parses every tracked `e2e/*.mjs` and `tools/*.mjs` with `node --check` after `tsc`, failing closed on enumeration (#346).
## Unresolved

- The DeepSeek credential pasted into a Commander chat on 2026-09-06 was revoked by the Owner on 2026-09-07. Any live run under ADR 0065 still uses a key entered only through the product's Protected Secret Store, and no agent reads, prints, or copies a key file.
- Corrected by S40: active-set v4 carries the right `ordinary-production` v3 pin. The 32 ADRs that carried no `status:` frontmatter were stamped `accepted` on 2026-09-07; all 68 now declare a status.
- Follow-up Issue #281 (S13-f1, Phase 3) carries the advisory review's two P2 findings on #48. The remaining #48 candidates are now Issues #286 (retried unit's payload digest on the adaptation), #287 (durable-state drift proven beyond unit tests), and #288 (the Task Intent range versus later plan versions), all still open and none dispatched.
- The S40 follow-ups are closed: #289 (policy-schema validation now runs from the installed closure and `pnpm test` covers it — the nine validations S40 integrated without are proven at every head from now on), #290 (the `sample1` transmit-lineage refusal is asserted from the service suite; a change removing it fails a test), #291 (the J-03 Plan Preview derives its five literals from the frozen plan). `e2e/run-j03.mjs` needed no edit and was correctly left unchanged; the Brief's Journey disposition had over-listed it.
- Open from the earlier waves, none blocking: #301 — the Plan Preview label helpers still live in the renderer entry point, so their test stubs `MutationObserver`, `document`, and `window.ai7`; extracting them would have meant a Worker widening its own allowed change. #297 closed in wave 4 with its verdict table. New from wave 4: #328 — `EditorialStore.open` leaks its `DatabaseSync` handle when ledger validation fails, found by #316's Worker and worked around in the suite rather than fixed out of scope.
- Two process facts worth carrying: `pnpm run bootstrap` needed retries under three parallel Workers on one host, plausibly registry contention on transient `bootstrap failed: terminated` aborts; and a single-Journey capture must be invoked as `node e2e/run.mjs --journey <id>`, because `pnpm run e2e --` writes pnpm's own command banner to stderr and it lands in the capture as about 43 bytes.
- Correction to the audit of 2026-09-07: the receipt-gap finding was mostly a false positive. It counted the HTML markers `ai7-dispatch-*-receipt`, which schema v1 receipts do not carry, so #223, #225, and #227 were reported as missing receipts when their pairs exist. A re-audit by receipt heading over all 36 Issues carrying receipts found exactly one unpaired attempt, #239 A2, and one `launch-unavailable` record, #221 A4 — both of them attempts that ended *before* a Launch Receipt existed. That is a shape the schema did not cover; `kick-in/27-repository-development-dispatch.md` now says such an attempt is closed by a Return Receipt alone with `launch_receipt: none`, which settles both retroactively. Issue #292 is closed by that rule.
- The PowerShell tool returned exit 66 for every command in one interrupted `cli-session` on 2026-09-06 and recovered after a reset. Host observation only; no Issue.
- The OpenCode Go key is enrolled in the Protected Secret Store and has been used twice: `S40/smoke/1` on 2026-09-07 (eight items) and `S40/reanalyze-range/1` on 2026-09-08 (one item). No agent has read, printed, or copied the key file; the enrollment helper remains its only reader.
- **What #307's seven problems became, as of wave 4.** P1 (the contract failure) closed by #306. P2 (statements contradicting the Run) closed by #303, and its second family in the provider-denied Task kind by #316. P5 (`--check` silence) closed by #308. P3, P4, and P7 have their rule in ADR 0071; P3's survey is #304 A1's report and its per-surface units follow it; P4 and P7's implementation is #305's unit, next. P6 (the unmeasured ceiling) is settled in ADR 0070, with a ninth measurement added by #306's item.
- **The live-call question is answered for one gateway and one model.** `opencode-go` accepts a `json-object` constraint for `deepseek-v4-flash` (item `S40/reanalyze-range/1`); every other route and model still declares `none` until its own item or vendor documentation is recorded beside it, which is what #321's five units do.
- The ledger now holds nine items. Under ADR 0067 `S40/first-baseline/1` … `/8` and `S40/reanalyze-range/1` must not be transmitted again; the next live Run needs new identifiers and its own stated question. The data-root copy and the probe worktree the Commander used for the item were removed after the wave; the Owner's smoke data root at `C:\Users\Chooo\Playground\ai7-smoke-2026-09-07` is untouched.
- **The two `developer-live` transport questions from S40 remain unproven**: whether the captured native `fetch` plus the single-host allowance actually transmits under denial, and the gateway's real limit-response shape. The first Run exercised the transport and it worked; neither has been isolated and asserted.
- The `developer-live` ceiling has its measurements, which ADR 0065's 500,000 default never had: 8 units, 113,767 tokens, **output 8× input**, per-unit output ranging 4,374 to 24,225 on the first Run; and on the ninth item, unit 3 under the constraint produced 17,486 output tokens where it had produced 4,374 of reasoning alone. ADR 0070's per-unit default of 30,000 stands; the spread matters more than the mean.
- The Provider Result Cache holds all nine responses; every finding above is diagnosable from it without another live call.
- Whether the `.agents/skills` vendoring on `temp/freeze-20260904-112022` enters `dev` remains an Owner decision; the self-hosted Gate is shelved.
- One unreproduced `LOCAL_COMPLETION/J-01/fail` of unknown cause was recorded on #249; a transient `EBUSY` on a freshly extracted `electron.exe` cleared on rerun.
- Wave hygiene, end of wave 4: every attempt worktree of the wave was retired by the #324 sequence and `git worktree list` shows only the main checkout and the Commander's session worktree; a `git worktree remove` that fails with `Directory not empty` over ignored `node_modules` is finished with `--force` after `git worktree prune`, never over uncommitted work.
- The pre-reorganization follow-up candidate list is preserved in [the archive node](docs/archive/reorg-value-first-2026-09-06/INDEX.md).

## Safe Resume Prompt

```text
Commander: identify your harness (Codex or Claude Code), then read AGENTS.md, this file, docs/development/development-plan.md, and the Dispatch Register. The integrated head is dev@4d8a972302281744282e4bcba06ec0a5578a202b; no attempt is open and no worktree holds uncommitted work. ADR 0072 is accepted (PR #343).

Take #363 (the intake rename, T1) first, with #345 (T2, it holds the Electron slot for its five sequenced runs) and #353 (T1) beside it, then S51b-2 (#356, T3) and #360 (T2, file-disjoint from #356), then S51c (#351). Then #333, #334, #336 (T1, after you list each heading's glossary form in the Brief), #337. Once #321's five units are all integrated (they are, as of PR #366), 1c.10 (#322 tier 1) is next on the provider line: format-selected configurations for Claude, Qwen, HY, OpenAI official, and Gemini's route, each needing a credential slot.

Bindings under ADR 0068 as amended by ADR 0069: Commander claude-opus-5 @ high; T1 Worker claude-sonnet-5 @ high; T2 and T3 claude-opus-5 @ high; Reviewer claude-fable-5-1 @ xhigh and optional. A Claude Code Commander without SendMessage uses cli-session for every attempt. There is no fallback across models, efforts, harnesses, or modes.

Then Phase 1 at S41 #273. #322 is settled as repository tooling in two tiers and waits behind #321; Claude, Qwen, HY, and OpenAI official endpoints are its first configurations because each needs a credential slot.

How wave 4 was run, and what it taught: reduce each unit to its class bar in the Brief — settle the decisions yourself and dispatch only the checkable remainder, never lower a class to fit a model. Run only file-disjoint attempts in parallel; an attempt that would touch a file another live attempt owns waits for the integration. One Electron Journey at a time on this host: a Worker stops at ready-for-e2e after build, you verify no electron.exe and no %TEMP%\ai7-j0*-e2e-* root before releasing the slot, and every slot message says "foreground, waited on inside your turn, never a background task" in those words, because a backgrounded e2e:all becomes an orphan nobody observes and is not evidence. Read every head SHA from the worktree, not from a report. Review every diff yourself at the exact head and record in the Return Receipt what you verified. Merge serially by the #324 sequence and verify with git ls-remote. Open pull requests directly as ready. A T3 Brief whose allowed change proves incomplete at preflight gets a revision 2 published before the Launch Receipt and the same attempt continues. A repository record cites the pull request, never a branch commit, as its measurement head. A Journey a Worker starts in the background is not evidence — a turn that ends does not report — so the rung is run again in the foreground.

A two-phase attempt is how a Worker's change and a Commander's live check compose: the Worker lands the mechanism with the profile unchanged, reports ready-for-e2e, you run the item from a Commander-owned detached worktree at the Worker's head with the local flip uncommitted against a copy of a data root, and you resume the same session with the verdict for the profile commit. Live Provider calls are authorized unattended under ADR 0070, bounded by the transmittable-set and lineage refusals, the enforced ceiling, and the live-once ledger. Every live call answers a stated question that could not be answered from the Result Cache, named as a test item with its purpose recorded on the Issue before transmission, and a ceiling below one unit's usage bounds a check to one transmission. Curiosity is not a question. Never read, print, or copy the key file at the main checkout root; the enrolment helper is its only reader. Only admitted Public SampleBooks may be transmitted. Do not re-transmit S40/first-baseline/1 through /8 or S40/reanalyze-range/1.

Do not touch export, publication, release, distribution, or main. Under ADR 0058 do not query or consider Actions usage.
```
