# Current checkpoint

This is the single Commander-owned status and routing file under [ADR 0064](docs/adr/0064-reweight-repository-development-toward-value-first-delivery.md). The delivery order lives in the [development plan](docs/development/development-plan.md); what each Journey proves lives in [E2E journeys](docs/development/e2e-journeys.md); what every editor-facing surface must become lives in the [editor-facing surface specification](docs/ui-ux-v2/editor-surfaces.md) ([ADR 0077](docs/adr/0077-adopt-the-editor-facing-surface-specification-as-the-execution-standard.md)). The Commander updates this file inside the integrating pull request or through its own documentation pull request. This version was written on `dev@28cc1dd800cf55b17947cceb64a3bce4d81ccfef` (PR #404) by the Owner's UI design session under the Owner's instruction of 2026-09-10 ([ADR 0078](docs/adr/0078-align-the-remaining-references-the-plan-and-the-tracker-to-the-editor-facing-specification.md)) and updated in the pull request that accepted ADR 0074 (PR #391), then refreshed at `dev@d3c04b3` (PR #472) by the agent-readiness review of 2026-09-17 and at `dev@901aa87` (PR #478) by the completion run of 2026-09-19; the previous version, with the wave-by-wave record of 2026-09-07 to 2026-09-09, is archived at [docs/archive/editor-surfaces-standard-2026-09-10](docs/archive/editor-surfaces-standard-2026-09-10/INDEX.md).

## What exists

- Seven executable Journeys on `dev` — J-01 import, J-02 long-manuscript editing, J-08 recovery, J-12 workbench and Model Service setup, J-15 native artifact lifecycle, J-03 provider-denied Task authorization, J-04 covered baseline analysis — on the real execution path (Task Intent, `Task Input / 任务输入` checkpoint, Provider Preflight, Plan Envelope, Run Authorization, single-slot scheduler, immutable Execution Binding, `PrimaryAgentHarness`, Egress Gate, bound adapter). The Egress Gate refuses every remote route under `development-ci`; the only live calls in this product's history are the nine test items under `developer-live` (`S40/first-baseline/1` to `/8` of 2026-09-07, `S40/reanalyze-range/1` of 2026-09-08), all cached and never to be re-transmitted (ADR 0067).
- The Local Verification Ladder is complete (`doctor`, `bootstrap`, `check`, `test`, `test:service`, `build`, `e2e` / `e2e:all` / `e2e:gate`, `e2e:debug`, `e2e:repeat`); the hosted Gate is a three-minute Windows pull-request lane over `GATE_JOURNEYS` plus a nightly Windows and macOS full set (ADR 0075, PR #401). **The nightly has not been green since 2026-09-11.** J-02 fails at `j14-behavior` on five of the six hosted platform runs that reached it since PR #470 (#474; the same head passes on the developer host, four clean runs of four, one of them under full CPU load), and J-01 fails on hosted Windows on three of five nights at `review` or `completion-reimport-paged-editor-scan` (#475, the successor of the closed #345). The pull-request lane never runs J-01 or J-02, so it stays green while the full set is red, and under ADR 0081 the queue merges nothing while `dev` is red. **The cause of the hosted J-01 failures and of the Windows J-02 failure is found and its fix is open as PR #479**: since PR #470 `navigate()` held its re-entrancy guard across the entry-position write, so a paging command that arrived while the disk was busy was dropped, and both runners issue such pairs two frames apart. Slowing that write by 400 ms on a background test guest fails J-02 at `forward-window-resolved` and J-01 at exactly `completion-reimport-paged-editor-scan`; PR #479 passes both under the same delay. The macOS J-02 failures are immediate rather than a 60-second wait and are not yet explained; PR #479 gives each step of that stage and each part of the close its own stage name, so the next hosted failure names its step. The nightly of 2026-09-18 passed on both platforms, which is the intermittency and not a fix.
- **The Phase 1 analysis chain is complete service-side.** S13 plan revisions and `safe-retry` (PR #280); S40 the `developer-live` scope with Provider Processing v4, the trusted launch form, the enrollment helper, the Provider Test Ledger and Result Cache (PR #285); the unit contract surviving the bound model with `structuredOutput: json-object` on `opencode-go/deepseek-v4-flash` (PR #330); S41 fixture generation from the cache (PR #376); S42a the Baseline Cross-Unit Reduction Contract v1 (PR #393); S18a the `factual-review` kind with the Factual Review Contract v1, deterministic Reference Integrity and every finding at `未外部复核` (PR #395); S43 the Assurance Sampling Contract v1 (PR #397); S44a the durable Run Report inside every Task Outcome (PR #399). Schema revision is 21 (S57-s, PR #469) and `SERVICE_PROTOCOL_VERSION` is 29 (S57, PR #470). All three declared suboperations — the cross-unit reduction, assurance sampling and the Run Report reflection — are named `true` by Provider Processing v5 as of PR #440, so the `developer-live` policy gate no longer blocks them; the first v5 live Run is a separate named test item under ADR 0070.
- Model capability profiles keyed by route and model (PR #320), four request shapes (`openai-chat-completions`, `anthropic-messages`, `openai-responses`, `google-generate-content`) and the explicit provider support list of #321 declared as inert profiles with their evidence (PRs #340, #348, #359, #364, #366; [provider support](docs/development/provider-support.md)). Adding a provider endpoint still needs a credential slot: that is 1c.10 (S55a (#435)) under ADR 0073, accepted on 2026-09-10.
- Multi-format intake under ADR 0072: DOCX, `.doc`, `.txt` and `.md` identified by content and read through their own routes, PDF source-only, the original always the Source Version (PRs #357, #370, #374, #378, #382); every admitted Public SampleBook imports.
- The display rules of ADR 0071 (Decision Layer and Technical Identity Layer, the Run Liveness Signal) are applied across all forty-four surveyed surfaces (PRs #341, #347, #355, #380, #383, #386, #392); the Owner then judged the result still an operator's console, which is what the editor-facing specification answers.
- Test inputs come from the admitted SampleBooks wherever one can serve; under [ADR 0079](docs/adr/0079-record-the-owner-s-decisions-of-2026-09-10-on-storage-policies-export-egress-providers-and-samplebooks.md) §5 only exact `sample1` stays admitted to the repository, and S88 (#438, PR #446) removed the other five files from the tree: `.gitignore` refuses them and anything else under `SampleBooks/`, J-08 composes its three inputs from `sample1`, J-01's `.txt` intake excerpts `sample1`, the service builders read `sample1`, and J-01's `.doc` intake is local-only — it runs where a developer keeps the legacy document (`AI7_LOCAL_SAMPLEBOOKS` or the ignored `SampleBooks/`; each background test guest holds it under ADR 0084) and otherwise prints a disclosed skip that the pull-request lane and the nightly repeat beside the Journey's result (J-02 and J-12 stay synthetic by subject).
- The Policy Documents ADR 0079 §2 and §3 decide are integrated under one **active-policy-set v5** (PR #440): Provider Processing v5 for `developer-live` (the three suboperations allowed, a Run Budget Ceiling that defaults to 30,000 tokens per frozen Coverage Manifest unit, the house-people redaction rule, no web-search allowance), Provider Processing v6 as the `ordinary-production` successor of v3 over the unchanged v3 binding, and External Export Policy v2 (five eligible target kinds over the Editorial Deliverable Revision rather than the manuscript alone; 批注, 修改建议 and the retained DOCX content included by default and excludable item by item; 备注 off by default and included only when the editor says so; a 图书交付包 export covering the members the editor chose). The Owner reviewed each set's bytes and amended ADR 0079 §3 in that review; `development-ci` v1 and `fixture-recording` v2 are unchanged, and no v2 export rule is implemented — S64 (#413) and S86 (#434) do that.
- **The manuscript is the entry (S57, #405, PR #470).** Opening a Book that has a primary Manuscript enters the manuscript where the editor left it, with one floating notice that goes by itself; 工作概览 is now a destination reached from the manuscript's 资料与记录 group and is the entry only for a Book with no Manuscript. The position is a durable record per Book and Manuscript Revision at schema revision 21 (S57-s, #467, PR #469), recorded on arriving at a window, on paging and on leaving — never per keystroke — and a position whose Revision has been superseded resolves to the nearest surviving block and says so. This is the first Phase 2 outcome an editor can see.
- Cross-unit reduction, factual review, assurance and the Run Report have no editor surface yet; marks, review, evaluation, proposals, Apply, export, production documents and the knowledge base do not exist.

## The design standard and what it re-cut

- On 2026-09-08 and 2026-09-09 the Owner settled every editor-facing screen with the UI design session: [ADR 0076](docs/adr/0076-align-editor-facing-surfaces-to-the-owner-s-september-decisions.md) (PR #403: manuscript-first entry, marks, review, evaluation, the knowledge base, the findings center, external evidence retention) and [ADR 0077](docs/adr/0077-adopt-the-editor-facing-surface-specification-as-the-execution-standard.md) (PR #404: task plan and authorization, import, reimport and export, navigation, the knowledge base classes, settings, 发稿 / 交付 / 图书交付包) with the execution standard [`docs/ui-ux-v2/editor-surfaces.md`](docs/ui-ux-v2/editor-surfaces.md): per screen the final form, states, wording, data and backend needs; §10 the vocabulary; §11 the thirty backend alignment rows B1 to B30; §12 the design revisions A to X. There is no handoff document and none is coming.
- On 2026-09-10 the Owner merged PR #404 and directed that every existing document and Issue be aligned to it. [ADR 0078](docs/adr/0078-align-the-remaining-references-the-plan-and-the-tracker-to-the-editor-facing-specification.md) names the remaining clauses that are re-read or rewritten, re-cuts the development plan from §11 and records the tracker re-cut: superseded Issues closed as outdated with a named successor — #50 → S76 (#422), #53 → S69 (#417) and S70 (#425), #54 → S58 (#407), S59 (#408) and S70 (#425), #55 → S58 (#407), #56 → S59 (#408), #58 → S66 (#415), #59 → S64 (#413), #60 → S65 (#414) and S68 (#426), #277 → S75 (#421), #322 → S55a (#435); completed Issues closed — #276 (S44a; S44b → S71 (#406)), #304, #307, #321; new slice Issues S55a and S57 to S86; every kept slice Issue carries an alignment block.
- The two proposed ADRs were evaluated against the standard on 2026-09-10 and both are accepted: ADR 0073 (PR #377, merged by the Owner as compatible; its status line was set to `accepted` in PR #391) and ADR 0074 (PR #391, amended in four clauses per the evaluation — the origin is the 事实核查 category of a Review Run, SRC-013 retention happens within the authorizing Task, the curated hosts bind `developer-live` only with the `ordinary-production` search egress named as a successor decision, and the evidence surfaces are `editor-surfaces.md` §4 — then merged on the Owner's direction).
- The five decisions the re-cut left open were answered by the Owner the same day and recorded verbatim in [ADR 0079](docs/adr/0079-record-the-owner-s-decisions-of-2026-09-10-on-storage-policies-export-egress-providers-and-samplebooks.md): the storage decision (data version as a compatibility contract, nothing frozen before the first release, backup-only rollback, one package format, merge semantics, no encryption, database export through External Export Policy v2), Provider Processing v5 for `developer-live` and the production successor of v3 (three suboperations, 30,000 × units, house-people redaction, no web search yet), External Export Policy v2's target kinds and attached content, the three-tier egress model with search as the model's tool and retention fetches by AI7, the narrowing of the repository SampleBooks to `sample1` (S88 #438) and the provider assignment design (S87 #437).

## In flight

`dev@901aa87` (PR #478) is the integrated head at this checkpoint. On 2026-09-19 the Owner directed that every open Issue be implemented under the ADR 0082 hold with the background test guests. Two pull requests are open from that run, each with the complete ladder green on a guest at its exact head and the pull-request lane green: **PR #479** (#474 and #475: the paging guard, and J-02's per-step stage names) and **PR #480** (S71, #406: the analysis as ②A under `资料与记录 › 分析`, with the Run Report readable from the Task Outcome and from every history entry). The session that built them cannot merge — its own permission guard refuses `gh pr merge` — so each lands by the Owner's hand or by the ADR 0081 queue. Until they land, work that depends on them cannot stack: a slice branches from `dev`. The provider line's next unit, the platform-tools slice of ADR 0080 §7.8 step 3, is #473.

**The dispatch hold of [ADR 0082](docs/adr/0082-hold-the-dispatch-rules-for-implementation-work.md) is active (2026-09-17, PR #472).** The Owner held the dispatch rules for implementation work: each slice is built directly in a session the Owner runs, on its Issue branch, one pull request per slice with the Issue linked and the provenance line `Authored under the ADR 0082 hold: <harness>, <model> @ <effort>` in the Change closure; no Brief, Task Session, receipt, or class binding; the complete ladder at the exact head before Ready, the Gate and the nightly queue unchanged. The hold ends only on the Owner's word, by the lift pull request ADR 0082 §5 describes.

**The parallel wave of 2026-09-12.** On the Owner's instruction the Commander dispatched four file-disjoint attempts at once with the Journey rung withheld — Workers ran the ladder through `build`, opened draft pull requests and returned `ready-for-e2e`; the Commander ran `e2e:all` on every branch serially, fixed what it found, and marked each Ready only then, so ADR 0062's precondition still held at every integrated head. Width is bounded by three hub files — `src/renderer/index.ts`, `src/service/store.ts`, `src/shared/protocol.ts` — of which one attempt per wave may own each; the single-E2E-slot rule of ADR 0069 stops binding once no Worker runs a Journey. Outcome: S87-f1 (#464, PR #468) and S87-f2 (#465, PR #466) integrated, S57 (#405) and S13-f3 (#287) returned `needs-commander` having written nothing, and both returns were worth their cost. #405's measured that the entry position could not be stored at terminal schema version 20, which produced S57-s (#467, PR #469) and then S57 itself on a second attempt. #287's proved its own Issue's premise false: no admitted state can hold a second value for either durable-state drift field — the sidecar pin is immutable and bounded to revisions 1 and 2, and four of five Provider Binding columns are CHECK-pinned to single literals — so the derivation is sound and only the stimulus is unreachable; the Issue is parked until ADR 0080's configurable binding or a sidecar Revision 3 exists. Three Briefs in that wave each named an outcome their Allowed-to-change list could not reach; the lesson is recorded under Process rules.

Before the wave: ADR 0081 is accepted and the nightly is a serial merge queue, proven end to end on 2026-09-11 over one passing and one deliberately failing candidate after four hosted-runner fixes (PRs #460 to #463: the runner's missing git identity, its shallow checkout, the three-way squash's identity, and per-candidate result artifacts). ADR 0081 is accepted and the nightly is a serial merge queue from its first occurrence on; before that, ADR 0080 — the provider assignment design S87 (#437) — is accepted, with the Commander's amendments recording the Owner's decisions of 2026-09-11 (search is model-initiated and AI7 may execute the model's call through the policy-named public service as AI7 platform tools; the anonymous search service is allowed; the Run Budget Ceiling is the only budget; no provider, route, endpoint or model identifier is a source constant — #452 lists today's literals). On 2026-09-11 the Owner ran a model comparison — #328, #281 and #438 each implemented twice on the same base, once under the class binding and once under `deepseek-v4-flash` inside Claude Code — and the Commander reviewed all six on the full ladder: PR #444 (the deepseek arm of #328), PR #445 (the opus arm of #281) and PR #446 (the opus arm of #438) were selected and integrated in that order; #443, #447 and #448 were closed with their tips recorded. The flash tier executed each brief literally and did well where the acceptance test was objective; it stopped at the letter where the intent needed one more step (one revert route of #281 missing; the composed-fixture builder's cases gated away in #438 instead of moved to `sample1`). #387 (replay the model's real analysis of `sample1` in J-04 from the Provider Result Cache) is a Commander unit, unblocked by ADR 0079 §5.

**ADR 0080 §7.8 steps 1 and 2 are done.** The capability profile set declares `toolCalling` and `webSearchTool` per route and model with their evidence, inert (PR #468); Provider Processing v7 exists as reviewed bytes naming AI7's two platform tools on the `developer-live` eligible-only rule, **selected by no active policy set** (PR #466) — active-set v5 is byte-unchanged, so v5 stays the pin and a Run that asks for a platform tool is still refused. The Owner reviewed those bytes on 2026-09-12 and settled both Commander proposals: the search service is Parallel, and the per-frozen-unit Run Budget Ceiling default for the search-enabled rule is 90,000 tokens rather than the 60,000 proposed. Step 3 — the Egress Gate narrowings, the network-denial allowance set, the `tool` payload source, and the two tools with their ledger and cache — is S87-f3 (#473), a T3 slice with nothing left blocking it; it carries the active-set v6 that finally selects v7, which is the Owner's byte review at Ready and a change the nightly queue never merges (ADR 0081). #437 (S87) is closed as completed by ADR 0080.

## Next

In order, from the [development plan](docs/development/development-plan.md), which is the only place delivery order lives:

1. **The red nightly first.** PR #479 is the fix for #475 and for the Windows half of #474; merge it, then read the next nightly over `dev`. If J-02 still fails on macOS the marker now names the step (`j14-composition-focus` to `j14-forced-colors`, or `completion-browser-close`, `completion-fixture-survived`, `completion-cleanup`), and the fix continues under #474; both Issues close when the nightly is green on both platforms. What has been ruled out is recorded on #474: the six-second entry notice (the whole restart and `j14` sequence runs in about 0.4 s, so the notice is always present), and closing the product with a position write in flight (on Windows the close waits and the run root is removed cleanly). The rest of [ADR 0079](docs/adr/0079-record-the-owner-s-decisions-of-2026-09-10-on-storage-policies-export-egress-providers-and-samplebooks.md)'s decisions are landed: S88 (#438, PR #446), Provider Processing v5 and v6 and External Export Policy v2 under active-policy-set v5 (PR #440), the storage decision (§1, S85 and S86 dispatchable), and the provider assignment (ADR 0080); the egress document beside the Factual Verification Policy v1 is the one policy document still owed, written in S70.
2. **Phase 2 — the manuscript surface, review, files and delivery**, S88 (#438) integrated, and 2.1 S57 (#405) is integrated (PR #470) with its storage half S57-s (#467, PR #469); 2.2 S71 (#406) is open as PR #480, so from 2.3: S58 (#407) (marks), S59 (#408) (一键接受并应用), S60 (#409) (rail lanes), S69 (#417) (审阅), #57 (S22 conflicts), S61 (#410), S62 (#411) and S63 (#412) (import retention, imported marks, chapter-level reimport), S64 (#413) (export; the v2 byte review is the Owner's confirmation), S65 (#414) (发稿), S66 (#415) (生产文档与交付), S67 (#416) (图书交付包). S58, #57 and S64 each open a Journey with no runner (J-05, J-06, J-07) and wait for the Owner's routing decision under Owner decisions pending.
3. **Phase 3 — one Task Drawer and run governance**: S72 (#418) to S78 (#424), then #51, #49, #95, S70 (#425) (after S87-f3 #473, which lands the platform tools and the active-set v6 that selects the reviewed v7 bytes), S68 (#426).
4. **Phase 4 — the knowledge base, evaluation and learning**: S79 (#427) to S83 (#431), then #94, #61, #62, #63, #64.
5. **Phase 5 — ecosystem, dialogue and writing**: #52, #90, #65, #66, #89, S84 (#432).
6. **The provider line** stays behind the value chain: S87-f3 (#473) is its next unit (dispatchable now; the Owner's byte review of active-set v6 at Ready); S55a (#435) (1c.10) follows with the ADR 0080 §6 document set; 1c.11 deferred; #452's literals retire with those two and a schema T3 of their own.
7. **Owner decisions pending** — each is one line from the Owner; none blocks 2.2 S71, and the first two matter soonest:
   1. **Admit J-05, J-06 and J-07 as executable Journeys** at their first slices' cutover — S58 (#407), S22 (#57) and S64 (#413), each labelled `ready-for-human` with the exact question on the Issue — into `ADMITTED_JOURNEYS` and the nightly's full set with `GATE_JOURNEYS` unchanged (the recommended answer), or name another disposition. Phases 3 to 5 meet the same question at J-09, J-10, J-11, J-13 and J-16 and it can be answered in the same line.
   2. **The marker step of #474 was taken in PR #479** after five guest runs of J-02 did not reproduce and no macOS host can exist: it is #474's own ladder step 2, taken under the Owner's direction of 2026-09-19 to implement every open Issue. It changes no assertion, wait bound or predicate; if the Owner would rather not have it, the runner half of that pull request is separable from the product fix. A Windows Server 2025 host is no longer needed for #475, whose cause reproduces on a guest under a slowed write.
   3. **S61's class table** (#410), before that slice is built. The specification says 八类一张表 and IMP-055 names headers and footers, page setup, style sheets, text boxes and images as `完整保留（随文件保留）`; the ④ prototype's table has nine rows — it adds 文本框 (with the `保留 / 并入` choice) and 目录域 (the one `降级导入`), and turns the round-trip row into a summary card — and marks 内联样式 and 表格 `完整保留 · 原样恢复`, which a text-only block model cannot honour once a paragraph is edited. The decision is whether the class set grows to the prototype's nine (a change to `FidelityCategoryKey`, the `position BETWEEN 1 AND 8` checks in both stores, every persisted review's immutability, and J-01's literal rows), and whether inline styles and tables read `完整保留` only for blocks the editor has not changed. The seams are mapped; the original file is already kept whole with the Source Version, so retention needs a mapping record, not a new blob.
   4. **#286's promise** — its criterion compares the two attempts' payload digests, which legitimately differ on the single-Session route; the finding and the recommended reading (compare per-attempt unit-message digests) are on the Issue.
   5. **The quick start on ②A** (PR #480): `开始同步` is shown disabled with its reason until a Default Execution Rule exists (S75, #421), rather than absent; the Owner may prefer it absent.
   6. **The active-policy-set v6 bytes** when S87-f3 (#473) is Ready — the selection of the already reviewed Provider Processing v7; the queue never merges a policy-byte change, so the Owner merges.
   7. **The Factual Verification Policy v1 bytes and the egress document** when S70 (#425) is Ready (ADR 0074 §2, ADR 0079 §4) — the one policy document ADR 0079 still owes.
   8. **S80's local dependency** (#428: an embedding, recognition or vector-store dependency beyond the ADR 0070 §3 pre-authorization) when Phase 4 is reached.
   9. **Whether the `.agents/skills` vendoring on `temp/freeze-20260904-112022` enters `dev`**; the self-hosted Gate stays shelved.

   Decided and no longer pending: the Provider Processing v7 bytes (2026-09-12: Parallel, 90,000 tokens per frozen unit); #387's echo bound (ADR 0079 §5); the storage decision (ADR 0079 §1); the External Export Policy v2 bytes (PR #440); ADR 0081 (PR #456).

## Decisions still routing work

- ADR 0064 (value-first order; this file is the only root router; one-page Briefs; the frozen references change only through an ADR naming the clause); ADR 0065, 0067 and 0070 (`developer-live`: admitted SampleBooks only, the live-once ledger and cache, unattended under Commander control, the per-unit ceiling of 30,000 tokens times the frozen unit count); ADR 0068 and 0069 (bindings — Commander `claude-opus-5 @ high`, T1 `claude-sonnet-5 @ high`, T2 and T3 `claude-opus-5 @ high`, Reviewer `claude-fable-5-1 @ xhigh` and optional; file-disjoint parallel attempts; one Electron Journey at a time, amended by ADR 0083 to one ladder run per background test guest); ADR 0082 (the dispatch hold: implementation work built directly in a session the Owner runs, without Brief, Task Session, receipt, or class binding, until the Owner lifts it); ADR 0083 (agents run every Electron Journey, and the ladder a closure attests, on the background test guests `ai7-testbed-01` and `ai7-testbed-02` through the host runner, never on the host desktop; one run per guest replaces the single E2E slot); ADR 0084 (the one legacy `.doc` is admitted to the guests, outside the checkout and placed only as an Owner action, so J-01's `.doc` scenario runs there; agents never copy it, extract it from history, or open a failing `doc-manuscript` stage's artifacts); ADR 0071 (Decision Layer and Technical Identity Layer, the Run Liveness Signal); ADR 0072 (multi-format intake); ADR 0075 (the Gate split); ADR 0076 and 0077 (the design standard); ADR 0078 (this alignment); ADR 0079 (the storage, policy, export, egress, provider-assignment and SampleBooks decisions of 2026-09-10).
- The Owner's directions of 2026-09-08 stand: report progress as **what the editor can newly do**; order development by business value; take E2E inputs from admitted SampleBooks wherever one can serve; the product must read as a Chinese editor's workbench — now specified screen by screen.
- Workers implementing any editor-facing slice read the specification's screen section first and cite its clause IDs in the Brief; the prototypes are consulted for form, never copied as authority (ADR 0077 Consequences).
- Process rules that stand, with their derivations in the archived checkpoint: a Brief settles the decisions and dispatches only the checkable remainder; a widening after the Launch Receipt is a Brief revision published before Ready and re-hashed for T3; `e2e:all` runs on a background test guest through the host runner (ADR 0083), invoked as a foreground command and waited on, never a background task; every head SHA is read from the worktree; a repository record cites the pull request, never a branch commit; merges are serial by the #324 sequence and verified with `git ls-remote`; pull requests open directly as ready, except in a wave where the Journey rung is withheld, when they open as drafts the nightly queue skips and the Commander marks Ready after the Journeys pass.
- **A durable write never sits inside a re-entrancy guard, and a fixed frame wait is not a settle signal.** PR #470 put an awaited write between a window load and the two frames that release `edgeNavigation`; nothing failed on a fast disk, and every hosted runner dropped paging commands. When a slice adds an `await` inside a guarded section, the guard's scope is part of the review, and a Journey that follows one navigation with another two frames later is the test that finds it — reproduce such a failure on a guest by slowing the awaited call in a throwaway commit, not by loading the machine.
- **A Journey the pull-request lane cannot run is proven only by `e2e:all` at the exact head, and the closure says so.** PR #470 merged with its `e2e:all` rung withheld and its closure asking the Commander to run it; the lane's five Journeys stayed green and the nightly's J-02 has been red since. Under the hold as before it, a pull request whose change reaches J-01 or J-02 records the `e2e:all` result at its exact head in the Change closure before Ready, and a wave that withholds the rung records the Commander's run in the closure before the merge, never after.
- **Before writing a slice's Allowed-to-change list, grep for the pins its change class will hit.** Three Briefs of 2026-09-12 each named an outcome their list could not reach. A new durable record or schema change: `user_version` across `src`, `e2e` and `tests`, *and* every predecessor reconstruction — J-15 fakes a revision-12 store by dropping newer relations, and `tests/service/*` carry downgrade helpers. A new renderer-facing method: `e2e/run-j01.mjs` pins the exact sorted `window.ai7` member list, and the surface spans `protocol.ts`, `preload.ts`, `application.ts`, `service/index.ts` and `request-frames.ts`. A change to where a route lands: sweep the **clicks**, not the assertions after them — the nine stages S57 moved were found in two passes because the first swept for waits naming the overview and five of them name a card that only lives there. Put the findings in the Brief as named `file:line` entries: a Worker told which line pins what moves it correctly; one that has to discover it stops.

## Unresolved

- **ADR 0073 and ADR 0074 are accepted (2026-09-10).** The evaluations that led there are in this file's history (PR #436); what remains open from them is the `ordinary-production` search-egress policy that S70 (#425) needs and External Export Policy v2 that S64 (#413) needs, both under Next.
- The four policy documents of ADR 0079 are pending as bytes, not as decisions: Provider Processing v5 and the production successor of v3, External Export Policy v2, and the egress document beside the Factual Verification Policy v1 of ADR 0074; the Owner reviews each pull request's bytes at Ready, bundled into as few active-set versions as possible.
- The OpenCode Go key is enrolled in the Protected Secret Store and has been used for nine items; no agent has read, printed or copied the key file; the DeepSeek credential pasted into a chat on 2026-09-06 was revoked on 2026-09-07.
- The two `developer-live` transport questions from S40 remain unproven: whether the captured native `fetch` plus the single-host allowance actually transmits under denial, and the gateway's real limit-response shape.
- **The nightly is red** (#474, #475; What exists). #345 is closed and its `renderer-ready` family is folded into #475. On the developer host on 2026-09-17, J-02 passed four clean runs at `dev@d3c04b3` — `e2e:debug` in 49 s, a repeat in 47 s, one run under full CPU load in 48 s — and one repeat run failed at `renderer-ready` after 325 s while a second Journey and CPU burners were running on the same host by the review's own mistake, so that run is void, not evidence. The hosted Windows runner is about five times slower than the developer host on J-02.
- Open technical follow-ups, none blocking: #286 and #288 (the plan envelope; #286 is dispatchable now that #281 is integrated in PR #445), #287 (parked 2026-09-12 until a second admitted binding or a sidecar Revision 3 exists), #387 (a Commander unit, unblocked, not yet run), #452 (routed to S55a, S87-f3 and a schema T3). #301 closed on 2026-09-07 and #328 in PR #444.
- Host and tracker hygiene: an earlier session of this same review left branch `claude/agent-readiness-review-cea172` at `dev@d3c04b3` with no commits (worktree `pr-gate-test-optimization-b8a55c`); the merged branch `docs/adr-0082-dispatch-hold` still exists on `origin` with its worktree and awaits the #324 retirement sequence.
- Whether the `.agents/skills` vendoring on `temp/freeze-20260904-112022` enters `dev` remains an Owner decision; the self-hosted Gate is shelved.

## Safe Resume Prompt

```text
Commander: identify your harness (Codex or Claude Code), then read AGENTS.md, this file, docs/development/development-plan.md, docs/ui-ux-v2/editor-surfaces.md (§0, the screen you are about to dispatch, §10 and §11) and the Dispatch Register. The integrated head at this checkpoint is dev@901aa87 (PR #478); ADR 0073, 0074, 0076 to 0084 are accepted, ADR 0079 §3 amended on 2026-09-10 by the Owner's byte review. Two pull requests are open and green on the ladder and the lane: PR #479 (the fix for #475 and the Windows half of #474 — navigate() held its guard across the S57 position write and dropped paging commands — plus per-step stage names for J-02's last stage) and PR #480 (S71 #406, the analysis as ②A). Check whether they have landed first: a session whose permission guard refuses gh pr merge leaves merges to the Owner or the ADR 0081 queue, and nothing may stack on an unmerged pull request. After PR #479 lands read the next nightly: a macOS J-02 failure now names its step, and that is where #474 continues. The Phase 1 analysis chain is complete service-side; every remaining editor-facing unit is specified by editor-surfaces.md and ordered by the plan; there is no handoff document and none is coming.

The dispatch hold of ADR 0082 is active (2026-09-17): build each implementation slice directly in this session on the slice's Issue branch (<type>/<issue>-<slug> from current dev), one pull request per slice with the Issue linked and the provenance line in the Change closure; write no Brief, launch no Task Session, post no receipt, apply no class binding; run the complete ladder at the exact head before Ready, then merge by hand or leave the merge to the nightly queue. The hold ends only when the Owner says so, by the lift pull request of ADR 0082 §5; do not ask whether it continues.

Provider Processing v5 for developer-live, v6 as the production successor of v3, and External Export Policy v2 are integrated under one active-policy-set v5 (PR #440), each reviewed byte by byte by the Owner; the egress document beside the Factual Verification Policy v1 is the one policy document ADR 0079 still owes. S88 (#438) is integrated (PR #446): the repository keeps only sample1, the five other SampleBooks are local-only test material, and J-01's .doc scenario discloses a skip where they are absent. #281 (PR #445) and #328 (PR #444) are integrated too, so #286 and #287 are dispatchable. S87 (#437) is accepted as ADR 0080 (PR #449) and closed; the v7 bytes are written and reviewed (PR #466; Parallel, 90,000 per frozen unit) but selected by no active set; the platform-tools slice is S87-f3 (#473), the provider line's next unit, and its active-set v6 is the Owner's byte review at Ready; #452's literals retire with S55a, #473 and a schema T3. ADR 0081 is in force (PR #456): every night at 19:00 UTC the queue tests each open, non-draft, mergeable pull request against dev as the tree that would land and merges the ones that pass, one at a time, then runs the full Gate over dev; a proposed ADR or a policy-byte change is tested and left to the Owner; the Commander still integrates by hand when it chooses and remains the one who decides what is open and Ready. Then build Phase 2 from 2.3 in the plan's order under the hold — 2.1 (S57 #405 and its storage half #467) is integrated and 2.2 (S71 #406) is PR #480; the slices that need no Owner decision and no unmerged pull request are S85 (#433) then S86 (#434), S55a (#435), #473 and #288 — S61 (#410) first needs the class-table answer under Owner decisions pending, and #286 the reading of its promise; read the specification's screen section and cite its clause IDs in the pull request; the plan's class still says how much care a slice needs; parallel sessions only on file-disjoint sets; every Electron Journey on a background test guest through the host runner, one run per guest, never on this host's desktop (ADR 0083). A slice whose Journey has no runner — S58 (#407, J-05), #57 (J-06), S64 (#413, J-07) — waits for the Owner's routing decision recorded on the Issue; write no runner before it. A pull request that reaches J-01 or J-02 records e2e:all at its exact head in the closure before Ready. #387 is your own unit and is unblocked (sample1 is exempt from the echo rule). S55a (#435) is dispatchable under ADR 0073 and ADR 0080 §6 behind the value chain; S70 (#425) waits for #473; S64 (#413) has its v2 bytes and waits on S58, S61 and the J-07 admission decision; S85 (#433) and S86 (#434) are dispatchable against ADR 0079 §1 when reached. Report every status as what the editor can newly do.

While the hold is active no class binding applies to implementation work; the session runs at the model and effort the Owner chose and records it in the pull request. At the lift, bindings under ADR 0068 as amended by ADR 0069 resume: Commander claude-opus-5 @ high; T1 Worker claude-sonnet-5 @ high; T2 and T3 claude-opus-5 @ high; Reviewer claude-fable-5-1 @ xhigh and optional. A Claude Code Commander without SendMessage uses cli-session for every attempt. There is no fallback across models, efforts, harnesses, or modes.

Live Provider calls are authorized unattended under ADR 0070, bounded by the transmittable-set and lineage refusals, the enforced ceiling, and the live-once ledger; every live call answers a stated question the Result Cache cannot answer, named as a test item on the Issue before transmission. Never read, print, or copy the key file at the main checkout root; the enrolment helper is its only reader. Only admitted Public SampleBooks may be transmitted. Do not re-transmit S40/first-baseline/1 through /8 or S40/reanalyze-range/1.

Do not touch export, publication, release, distribution, or main. Under ADR 0058 do not query or consider Actions usage.
```
