# Progress

## What's done

- Verified the sole writable worktree/branch and exact base before editing: `C:\Users\Chooo\.codex\worktrees\issue24\ai7-harness`, `feat/24-j01-new-book-tracer`, `b70a0f014829fd92f898d55beb75f8595bfad996`. Read Issue #24, Commander amendment 1, and every routed authority; no predecessor implementation or unrelated archive was admitted.
- Bootstrapped the one deep root package in [`.node-version`](.node-version), [`.npmrc`](.npmrc), [`package.json`](package.json), [`pnpm-workspace.yaml`](pnpm-workspace.yaml), [`pnpm-lock.yaml`](pnpm-lock.yaml), and [`config/dependency-artifacts.json`](config/dependency-artifacts.json). Exact versions/SRI, strict engines/peers/store integrity, exact DSH convergence overrides, build allowlist, Node/Electron artifact hashes, and frozen installation are recorded.
- Added the closed-input host/toolchain boundary in [`tools/doctor.mjs`](tools/doctor.mjs), [`tools/bootstrap.mjs`](tools/bootstrap.mjs), and [`tools/electron-runtime.mjs`](tools/electron-runtime.mjs). Acquisition children receive a deterministic PATH, exact official registry plus fresh checkout-local pnpm/npm config/auth/cache roots, and no ambient credential or identity/output selector. Parent-process Node proxy/CA selectors that could affect Electron acquisition are rejected; unrelated proxy variables are not enabled for Node fetch or forwarded to acquisition children. Bootstrap first removes only a canonical exact checkout-local `node_modules`, installs the frozen closure, re-verifies the SHA-bound Electron secondary zip, and uses canonical `System32\\tar.exe` or `/usr/bin/ditto` to stage a native-npm-free runtime. Electron `43.4.1`, embedded Node `24.18.1`, ABI `148`, SQLite `3.53.1`/FTS5, `LICENSE`, and `LICENSES.chromium.html` are proven before activation.
- Implemented the private, maximum-512-KiB framed contract and pathless named renderer API in [`src/shared/protocol.ts`](src/shared/protocol.ts), synchronous fail-closed Node network denial plus default/named/call/construct self-proof in [`src/shared/network-denial.ts`](src/shared/network-denial.ts), and canonical non-overlapping Agent Data Root creation in [`src/shared/data-root.ts`](src/shared/data-root.ts).
- Implemented safe bounded DOCX staging in [`src/service/docx.ts`](src/service/docx.ts): streaming fflate/saxes bounds; active/embedded-content rejection; stable ordered plain-text blocks; truthful eight-category fidelity; only an empty terminal single-section baseline accepted as clean; nonempty run properties and actual section breaks rejected rather than falsely preserved; round-trip remains explicitly unsupported and non-blocking.
- Implemented the SQLite authority boundary in [`src/service/store.ts`](src/service/store.ts): content-addressed non-authoritative staging; digest-bound Review Before Import; one atomic exact Book/dimension/source/fidelity/primary-Manuscript/branch/r1/block/workflow/import-record graph; maximum-32-block signed windows; and an independent committed journal transaction with exact idempotent client acknowledgement. The accepted Q8 dimension set and seven-phase workflow are version/digest bound. Rollback failure poisons the whole store and becomes fatal instead of returning an ordinary response.
- Implemented the exact dormant six-service provider-free DSH composition in [`src/service/runtime.ts`](src/service/runtime.ts) and supervised service executable in [`src/service/index.ts`](src/service/index.ts). Readiness follows SQLite plus awaited Cordis lifecycle and proves six owners with zero agents, Sessions, providers, tools, prompt, or runtime context; excluded services remain absent. Exact Electron Node mode, 512-KiB framing, serial dispatch, parent PID/stdin lease, signals, and explicit shutdown all fail closed without payload logging.
- Implemented the Electron owner boundary in [`src/main/index.ts`](src/main/index.ts), [`src/main/application.ts`](src/main/application.ts), [`src/main/service-client.ts`](src/main/service-client.ts), [`src/main/preload.ts`](src/main/preload.ts), and narrow ambient declarations in [`src/shared/electron.d.ts`](src/shared/electron.d.ts). Main owns native selection, one-use J-01 injection, exact launcher-parent lease, single-instance lock, service supervision, Chromium network/permission/download denial, IPC sender validation, exact commit idempotency binding, payload-free readiness after first paint, and teardown. A post-readiness service loss leaves the window and unsaved editor text visible while freezing authority; dirty/saving/retry/interrupted-at-risk state blocks ordinary window/app close until a durable acknowledgement clears it. The renderer uses `contextIsolation:true`, `nodeIntegration:false`, explicit sandboxing, a five-method pathless bridge, no devtools/menu/navigation/window/webview authority, and never receives a raw path, DOCX bytes, database path, or whole manuscript.
- Implemented the Chinese no-preselection Review Before Import and bounded ProseMirror editor in [`src/renderer/index.html`](src/renderer/index.html), [`src/renderer/styles.css`](src/renderer/styles.css), [`src/renderer/index.ts`](src/renderer/index.ts), [`src/renderer/editor.ts`](src/renderer/editor.ts), and [`src/renderer/global.d.ts`](src/renderer/global.d.ts). It displays all eight fidelity categories, exact records/non-effects, and neutral dimension/workflow semantics; creates only on the final action; opens 32 of 51 synthetic blocks; rejects structural/reordered/multi-block/unbounded edits; and shows `已写入修订日志` only after the independent durable acknowledgement. Ctrl+S/Command+S is IME-safe.
- Added the single production-shaped build/start carrier in [`tools/build.mjs`](tools/build.mjs) and [`tools/start-built.mjs`](tools/start-built.mjs): built CJS main/preload, ESM service and shared network guard, local HTML/CSS/browser renderer, no source maps, no second package/launcher/server, fixed readiness forwarding, and post-build guard/surface inspection. Generated `dist`, cache, store, runtime, and dependency roots are checked against canonical checkout-local owners before destructive or acquisition use.
- Added the one admitted UI journey in [`e2e/run-j01.mjs`](e2e/run-j01.mjs). It generates a 51-block public-synthetic Chinese DOCX only after proving an external runtime temp root, installs the same built Node egress guard before dynamically importing Playwright, and drives the exact Electron executable over Chromium's inherited `--remote-debugging-pipe` rather than a TCP debugger. The main-owned picker result hook cannot bypass staging/domain flow; boolean-only checks prove sandboxed renderer/loopback denial, no target preselection, exact fidelity/records/non-effects, atomic import, 32-of-51 projection, one bounded stable-block edit, platform save shortcut, busy state, and durable sequence-1 journal acknowledgement. It emits no screenshot, trace, video, artifact, DOM/manuscript text, or database assertion.
- Commander amendments 1 and 2 are implemented. Amendment 1 fixes the exact five `pnpm run ...` spellings and literal-separator normalization. Amendment 2 corrects the rooted ADR list and adds canonical ADR 0028; it does not change scope, topology, commands, or stop conditions.
- Added the root quickstart and cold-start routing in [`README.md`](README.md) and [`HANDOFF.md`](HANDOFF.md), the concrete buildability contract in [`docs/development/source-checkout.md`](docs/development/source-checkout.md), and the exact package/artifact/lifecycle/license ledger in [`docs/development/dependency-provenance.md`](docs/development/dependency-provenance.md) plus [`THIRD_PARTY_NOTICES.md`](THIRD_PARTY_NOTICES.md). The maintained carrier includes exact MIT notices immediately above the verbatim grant and both complete saxes ISC notice blocks; every build carries it together with the verified current-host Electron `LICENSE` and `LICENSES.chromium.html` files.
- Added exactly one [`.github/workflows/e2e.yml`](.github/workflows/e2e.yml) for the same J-01 semantics on truthfully labelled `windows-2025` x64 and `macos-15` arm64. It uses Commander-approved full-SHA v6 checkout/setup-node actions, `contents: read`, no persisted checkout credential or cache action, exact Node/pnpm selection, and the same doctor/bootstrap/build/E2E commands without a CI-only subject or uploaded artifact.
- Final local Windows 11 Pro `10.0.26200` x64 evidence is green with exact Node `24.18.1` and pnpm `11.24.0`: `pnpm run doctor`, closed frozen `pnpm run bootstrap` from a reconstructed dependency tree, `pnpm run build`, and full raw-pipe `pnpm run e2e -- --journey J-01`. The final bootstrap proved Electron `43.4.1`, embedded Node `24.18.1`, ABI `148`, SQLite `3.53.1`, and FTS5; a poisoned `NODE_USE_ENV_PROXY` diagnostic failed closed without printing a value. Additional sanitized probes verified clean/default-section acceptance, styled/real-section rejection, lone-surrogate rejection with continued readiness, both-direction canonical code/data-root exclusion, and fixed readiness while the launcher remains attached.
- The final closure/artifact/notices sweep found 64 lock package identities and 54 snapshots, no Electron/native-extractor npm carrier, zero installed `.node` files, no tracked DOCX/database, exactly one workflow, and no uploaded screenshot/trace/video/artifact carrier. Tracked notices exactly match the built copy, both built Electron notice carriers match the verified generated runtime, local Markdown links resolve, and `git diff --check` is clean. The document-lifecycle sweep found no consumed working record to archive and no new archive authority.

## What's next

- Commit the final runtime, source-checkout, and CI task units with the required why bodies/model trailer, then obtain the required read-only standards/spec review over the clean final SHA.

## Unresolved matters or blockers

- No Issue #24 stop condition is active.
- macOS 15 arm64, Windows Server 2025 CI, and the committed workflow remain unproven locally; no hosted-runner claim is made before Commander-run external PR CI.
- The fatal rollback-failure branch is implemented and code-inspected but was not forced by runtime fault injection; ordinary rollback/continued-service behavior is covered by rejected-request probes.
- Ordinary dirty-buffer close blocking and post-readiness service interruption are implemented and build-inspected but were not added as a second scenario; the one admitted J-01 journey finishes cleanly after durable acknowledgement.
- Otherwise-required DSH packages contain unused replay/fork, fixture, and Code Mode symbols, but no excluded package/service is installed, mounted, invoked, or renderer-facing. Provenance must state this API-surface nuance without claiming byte-level absence.

## Key decisions made

- The only harmless section baseline is one empty terminal body `sectPr`; page layout, inline styling, extra sections, and DOCX round-trip are not modelled by the plain-text tracer and therefore cannot be labelled preserved.
- The exact built checkout root, not `process.cwd()`, binds the data-root exclusion. Canonical overlap is rejected in both directions before and after creation, while a future external sibling data root remains possible when the code-root binding is adjusted by its carrier.
- Main installs the Node guard before dynamically importing Electron owners; the service installs it before DSH imports. The preload is explicitly sandboxed and therefore keeps only Electron’s narrow preload polyfills rather than gaining full Node solely for monkey-patching.
- Electron is a verified secondary runtime artifact, not an npm package. Windows extraction uses only fixed `System32\\tar.exe`; macOS extraction uses only fixed `/usr/bin/ditto`. A staged carrier is activated only after runtime and license probes, so the prohibited native npm extractor is absent from the lock and installed tree.
- Harness readiness is infrastructure evidence only. Import completion comes from the atomic authority transaction, and edit completion comes only from the separately committed journal acknowledgement.
- The E2E control hook supplies only the main-owned picker result. It cannot give the renderer a path or bypass staging, review, commit, projection, editor, or journal flow.
- Playwright control uses inherited Chromium file-descriptor pipes with no debugging port, inspector, WebSocket endpoint, TCP listener, or dev server; the controller itself receives the built Node network-denial guard before Playwright loads.

## Resume Prompt

Resume Issue #24 under Commander amendments 1–2 by committing the fully green and audited no-native raw-pipe J-01 tracer, then obtain standards/spec review over its clean final SHA without claiming unrun macOS or hosted CI evidence.
