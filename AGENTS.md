# AI7 Harness Project

## Standing authority and phase

`AGENTS.md` is the mandatory short entry point for every agent and human working in this repository. Root `CLAUDE.md` remains exactly `@AGENTS.md`; shared rules never live there.

On 2026-08-25 the owner explicitly superseded the former monolithic root-instruction layout with this thin-router shape. `AGENTS.md` remains the canonical shared instruction entry point, while each exact linked runbook owns its routed detail. Edit the owning linked file for that shared detail and update this router when routing changes; never move shared rules into `CLAUDE.md`.

During development, `dev` is the long-lived integration line and the current implementation-facing design baseline. Task branches and ordinary pull requests start from and target `dev`. `main` is the protected stable/release-promotion line and may be advanced only by a separate exact Owner authorization. Frozen `design-doc@6895f02d2983865516d267809d8cdda77026f62c` is the provenance source for the accepted V2 allowlist, never a mechanically mergeable development line. See [development lines](docs/agents/development-lines.md).

Every claim of “canonical” or “accepted” is qualified by the exact intended integration target. Resolve an authority owner as `<target-commit>:<path>`, not merely by its path in the current worktree. A record present only on `design-doc`, another branch, a prototype, or an archive remains source-qualified candidate/evidence unless an exact Owner decision and Commander integration promote it to the intended target.

The design interview is complete. The Owner has separately authorized the exact V2 baseline normalization, minimum Provider Processing and External Export policy baselines, implementation planning, the bounded provider-free J-01 new-Book happy-path tracer, and the exact `sample1` compatibility / future manual recording policy baseline, in that sequence on `dev`. The tracer is not full J-01, and the recording baseline performs no current Provider call or fixture generation. An accepted decision still does not by itself authorize adjacent issue decomposition, dependency installation, predecessor source copying, Plugin discovery, implementation, push, merge, publication, release, or promotion to `main`. Follow [design authority and action authorization](docs/agents/design-authority.md).

Repository-development dispatch is Claude Code-only and Issue-bound under [ADR 0060](docs/adr/0060-dispatch-repository-work-through-issue-bound-claude-code-sessions.md). T0 remains with the Commander; every T1–T3 Worker and every Reviewer starts in a fresh top-level Claude Code Task Session in its own worktree and verifies its finalized Launch Receipt before controlled-file work. The fixed bindings, attempt identity, two-stage launch, Return Receipt, parallelism, and retention rules live in [Repository Development Dispatch](kick-in/27-repository-development-dispatch.md); live project status is queried through the [Dispatch Register](docs/agents/dispatch-register.md).

Never place a manuscript or manuscript derivative in any repository, working tree, hosted CI input, log, artifact, fixture, corpus, or distribution, except for the narrow **Public SampleBooks** test-input exception accepted by [ADR 0043](docs/adr/0043-allow-public-samplebooks-in-repository-and-ci.md) and exact `sample1`'s separately governed reviewed-fixture exception in [ADR 0044](docs/adr/0044-use-sample1-as-compatibility-and-recording-baseline.md). Public SampleBook admission applies only to material in exact root `SampleBooks/` that the Owner designates through an authorized Issue and pull request; placement alone grants nothing. It permits repository tracking and provider-free local/hosted-CI test input. ADR 0044 additionally makes exact `sample1` the standing compatibility baseline and permits only a future local, human-attended, no-fallback recording under Provider Processing v2; raw material remains outside repositories, and only a normalized, sanitized, rights-reviewed, human-reviewed fixture may enter through a separate authorized Issue and pull request. Neither exception grants payload logging or uploaded artifacts, distribution, learning, export, publication, or public-release authority. All other manuscripts and private sample Books remain prohibited. Never place credentials or secret values in repository files, prompts, generic environments, tool results, or diagnostics. The detailed standing product constraints live in [AI7 project constraints](docs/agents/project-constraints.md); read only the sections implicated by the active Change Brief.

## Session entry

1. Inspect the exact `HEAD`, branch or detached state, worktree status, and intended integration target.
2. Resolve the repository role. The Commander reads the compact current [`PROGRESS.md`](PROGRESS.md), [`HANDOFF.md`](HANDOFF.md), and [Dispatch Register](docs/agents/dispatch-register.md). In a no-write bootstrap before a Launch Receipt exists, a Worker or Reviewer reads `AGENTS.md`, the active GitHub Issue, and its immutable launch expectations only; after the Commander sends the finalized Receipt permalink, it fetches and verifies that Receipt. Root routers or another task transcript are read only when the Issue names them.
3. For a Worker or Reviewer, recompute the Issue-body hash from the exact GitHub API body string and verify the branch or detached state, actual worktree, role-specific start commit (Worker exact base; Reviewer immutable `reviewed_head`), exact base/target, role/class/binding, stop conditions, and—once available—the Receipt before any controlled-file edit. Stop `needs-commander` on a mismatch.
4. Use the [agent-document router](docs/agents/README.md) to load only the applicable context definitions, ADRs, constraints, and runbooks.
5. Treat `docs/archive/` as excluded from ordinary search and reading. Enter it only when the current authority record or Change Brief names an exact archived artifact and explains why current material is insufficient.

## Incremental development

Every authorized code, design, dependency, schema, configuration, or workflow change follows the [incremental development lifecycle](docs/agents/incremental-development.md).

Commander clarification, Issue shaping, dispatch, acceptance, integration, and external actions are T0 responsibilities. The Commander does not perform T1–T3 controlled-file work inline. One fresh Issue-bound Claude Code Task Session executes each Worker or Reviewer attempt under the exact fixed binding in the dispatch owner.

- Start from the current implementation and accepted design. Reuse, extend, or locally deepen the existing owner before adding another owner.
- Product work keeps one user-visible outcome or observed defect; design, documentation, dependency, and repository work keeps one exact decision, consumer, or operational outcome. Each has a closed structural budget, explicit non-goals, and named stop conditions. File counts and lines of code are not scope measures.
- Do not create parallel implementations, opportunistic cleanup, speculative abstractions, new frameworks, new processes, new stores, new public interfaces, or broader authority because they seem cleaner or easier to generate.
- A Worker stops when its brief is wrong or the structural budget must expand. It never silently revises its own authority.
- Initial bootstrap is the narrow exception: after separate authorization, it delivers the first thinnest accepted runnable end-to-end outcome and creates only the owners that outcome actually traverses. Empty packages, horizontal architecture skeletons, and launch/plumbing work that is not itself an accepted outcome are not bootstrap outcomes. As soon as a responsibility lands, later work is extension-first.

## Work, Git, and validation

GitHub Issues are the canonical work-item tracker. One Issue owns one branch, one pull request, and one writable Worker. Follow [issue tracker rules](docs/agents/issue-tracker.md), [Git conventions](docs/agents/git-conventions.md), [Repository Development Dispatch](kick-in/27-repository-development-dispatch.md), and the [Dispatch Register](docs/agents/dispatch-register.md). Only the Commander integrates or takes external actions; independent review is optional and advisory.

The only standing automated engineering test surface is the provider-free [E2E Functional Gate](docs/agents/ci-test-boundaries.md), executed with the same applicable supported journey IDs on Windows and macOS. Do not create additional standing test, proof, review, coverage, provider, performance, package, signing, release, or exact-head gates unless the owner explicitly reverses ADR 0027.

The three verification states—Local diagnostic, Local completion, and Hosted Gate evidence—are distinct; hosted-workflow restoration history follows [ADR 0057](docs/adr/0057-restore-hosted-gate-under-observed-actions-usage.md), current no-usage-observation operation follows [ADR 0058](docs/adr/0058-remove-actions-usage-observation-from-development-gating.md), and the bounded CI-degraded path follows the [CI and test boundary](docs/agents/ci-test-boundaries.md). Before the exact Initial v1.0.0 Development Milestone Boundary, fresh exact-head Windows completion remains required before Ready; while the restored workflow is active, the paired Windows/macOS Gate must then succeed. [ADR 0054](docs/adr/0054-defer-macos-evidence-until-after-initial-v1-0-0-development-milestone.md)'s Windows-only route and truthful macOS deferral apply only during an exact degraded interval and remain historical truth for already integrated work. A local result never becomes Hosted Gate evidence or bypasses a required product, bootstrap, build, or Journey failure or unknown cause.

For domain work, start from the intended target's [`CONTEXT-MAP.md`](CONTEXT-MAP.md), then read the applicable target-qualified `CONTEXT.md`, root ADRs, and Policy Documents. `GLOSSARY.md` is a bilingual index and collision guide, not a definition owner. Follow [domain documentation rules](docs/agents/domain.md).

## Checkpoints and document lifecycle

Root `PROGRESS.md` and `HANDOFF.md` are Commander-owned integration-line routers, not per-attempt ledgers and not universal Worker/Reviewer context. The Commander replaces them when integration routing changes; Task progress and terminal outcomes live in the Task Session plus Issue Launch/Return Receipts. Parallel Workers do not edit root routing files unless one exact Change Brief explicitly makes a root-router transition part of its atomic outcome.

Run a documentation archive sweep at the lifecycle nodes defined in [document lifecycle and archiving](docs/agents/document-lifecycle.md). Root `PROGRESS.md` and `HANDOFF.md` contain current routing only. Stable authority owners remain at stable paths; consumed working documents move to an indexed archive; disposable scratch and diagnostics are deleted.

Architecture and design forks additionally follow [multi-session design workflow](docs/agents/multi-session-design-workflow.md). A design branch, prototype, review, freeze handoff, or archive never grants implementation authority.
